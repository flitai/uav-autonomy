# G5-T08 生命周期与恢复验收

日期：2026-09-23，Asia/Shanghai。**G5-T08 已完成；G5-T09 可执行。** 本卡复用已合格的覆盖页面候选 `g5-coverage-build-20260922-140926-685`、正式 G4 网关、UxAS／AMASE 和 T04 三实体真实地形副本。没有修改页面、后端或正式指针。新增独立编排与 Edge 取证入口，验证现有只读状态机在故障和删除下的行为。

## 入口与来源

```powershell
$pythonExe = Join-Path $env:LOCALAPPDATA 'Python/pythoncore-3.14-64/python.exe'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\g5-lifecycle.tests.ps1 `
  -PythonExecutable $pythonExe -BuildRunId g5-coverage-build-20260922-140926-685
```

入口会重新检查 G3 基线；本次为减少重复核查，最终运行显式复用本轮已通过的 `g3-t01-check-20260923-214731-692`。编排见 [runtime.py](../scripts/g5_lifecycle/runtime.py)，Edge 取证见 [browser.py](../tests/g5_lifecycle/browser.py)。来源清单和合格页面候选摘要写入本次 [acceptance.json](../out/runs/g5-t08-test-20260923-223641-852/acceptance.json)；发布阶段资格仍为 `false`。

功能复验 `g5-t08-test-20260923-221137-946` 的四份收据均为 passed，验收 SHA256 为 `a6959ac27e3138bb246f7390ac7bb7ac732e54852f7ac002d310c8d13626d13b`。来源复核发现 PowerShell 验收入口缺少哈希绑定，已补入清单；首次重跑 `g5-t08-test-20260923-223021-982` 揭示管理入口成功时仍保留初始化的 `lifecycleQualified=false`，与运行及验收收据不一致，已修复。最终 `g5-t08-test-20260923-223641-852` 的 `result.json`、`entry-result.json`、`runtime-result.json`、`acceptance.json` 均为 passed，`lifecycleQualified=true`、`stageQualified=false`，验收 SHA256 为 `073adcdb383b56c2c300bde23ab99fe58fb7b95d62688e02f4e31bcf1bce82d7`。两模式各四类恢复、正常退出并释放端口。运行记录和原始浏览器状态在被忽略的 `out/runs`，不进入 Git。两模式分别拥有独立后端 `run_id` 和累计覆盖线程，不复用前一模式的对象或事件库。

## 实际矩阵

| 检查 | Headless | Gui |
| --- | --- | --- |
| AMASE 观察断开、UxAS 观察断开、双路断开 | 网关快照 503、浏览器两层覆盖／实体／任务清空、时间标过期并冻结；恢复为同运行新流 | 同左 |
| 网关进程重启 | 两个网关实例均正常退出；同运行新 `stream_id`，浏览器重新完整快照 | 同左 |
| 晚加入、刷新、慢客户端 | 新页面绑定本次运行／流；刷新建立新页面和快照；只读慢客户端不影响普通页面 | 同左 |
| 完成保留、独立快照 | 点 3001／区域 3002 正确身份完成后对象仍存在；暂停页面全量状态与网关快照一致 | 同左 |
| 影像／资源服务故障 | 在线影像不可用回到本地底图；本地地图服务正常停止期间页面清空并标过期，重启后恢复同运行快照；后端仍在 | 同左 |
| 合法删除 | 删除已完成任务 3001 和实体 500 后，网关／页面对象、当前相机与累计覆盖均不再含对应身份 | 同左 |
| 收尾 | Edge 退出 0、累计线程正常退出、AMASE／UxAS 退出 0、所属端口释放、无强制终止 | 同左 |

两模式的三次观察断开均记录 WebSocket `1013`，浏览器旧时间在断流期间不推进。浏览器与网关的暂停状态比较使用实际完整对象，而非仅比较数量；删除后再次比较完整状态。覆盖线程继续从只读持久事件重建，关闭浏览器不关闭后端或线程；线程退出收据均为 passed。浏览器使用绝对 profile、`Browser.close` 和正常退出检查；截图失败后的关闭路径沿用已有 T05／T07 回归。完整水道线任务在本卡暂停时仍执行中，T09 将验证原两实体完整任务，本卡不借 T04 历史结果声称 T09 已通过。

## 失败记录与修正

首次 `g5-t08-test-20260923-214731-480` 在刷新后错误要求新页面快照计数大于旧页面；真实刷新会重建连接并从 1 计数。改为核对 `performance.timeOrigin`、新页面快照和运行／流身份。第二次 `g5-t08-test-20260923-215254-082` 已观察三任务完成，但编排把暂停阶段命名为上游保留的 `amase-paused`，命中了初始化截止时间；改用独立阶段名，并按本卡范围在点／区域完成后暂停。第三次 `g5-t08-test-20260923-220702-788` 的 Headless 用例 passed；GUI 取证文件被并发读取时发生 Windows `WinError 5`，浏览器正常退出但整组 failed。原子替换加入有限重试，控制器在取证进程退出时立即报错。上述失败收据保留，未改写为成功。

## 边界与下一步

此验收使用真实三实体地形后端和生产覆盖候选，但仅是 T08 生命周期矩阵；没有新增业务写接口、控制按钮或正式发布。线任务完整执行、两实体原例统计归 T09；20 实体、三客户端与 30 分钟资源采样归 T10；最终人工页面确认及指针切换归 T11。用户先前提出的累计覆盖显示反馈仍需在后续页面人工确认中关闭，不以自动矩阵代替其视觉判断。
