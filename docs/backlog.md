# 实施任务清单

更新日期：2026-09-20。总约束见[总体实施计划](../04.项目总体实施计划与阶段验收.md)，当前状态见 [status.md](status.md)，历史基线见 [g0-baseline.md](g0-baseline.md)，当前阶段方案见 [G4 实施方案](g4-message-gateway-plan.md)，G3 历史方案见 [原文档](g3-system-integration-plan.md)，G2 历史方案见 [原文档](g2-windows-uxas-plan.md)。

任务状态使用：待细化、待前置、可执行、进行中、待验证、已完成、阻塞、延期。G0、G1 已完成，G1-T01～T05 均已完成；G2-T01～T07 已完成，G2 已完成；G3-T01～T07 已完成，G3 已完成；G4-T01～T09 已完成，G4 已验收并发布；G5-T01～T06 已完成，区域地理、地图、真实地形后端、状态接入及实体显示通过，T07 可执行，航线／任务／区域图层待后续。一次只推进一个主要实现任务。

固定收尾要求：每项任务先验证，再在根级 [worklog.md](../worklog.md) 追加过程、成果、问题及处理办法，同步状态与本清单；确认完成后按用户持续授权自动提交、推送并核对远程，再交付。受阻或中断也记录实际进展。日志模板见 [AGENTS.md 第 10 节](../AGENTS.md#10-固定工作流程与工作日志)。G0 的历史过程已补记，原验收证据保持不变。

## 1. G0 完成记录

| 任务 | 状态 | 本次交付与验收证据 |
| --- | --- | --- |
| G0-T01 源码基线 | 已完成 | 报告第 1 节及哈希附录：根提交、远程地址、3 个 tree、3787 个已跟踪文件、原 6 项未跟踪文件；上游 SHA 标为未知 |
| G0-T02 环境核查 | 已完成 | 报告第 2、7 节：系统／查找范围／实际路径与输出；Git 和真实 Python 退出码为 0，Java 等限定范围内未发现 |
| G0-T03 依赖与示例 | 已完成 | 报告第 3～5 节及附录：7 个 MDM、Anod 声明与补丁、6 个随库 JAR、6 个消息引用、端口时刻、输出目录约定 |
| G0-T04 风险与交接 | 已完成 | 报告第 6、8 节：风险归属与验收；本文件的 5 张 G1 任务卡；总体计划和 AGENTS 状态同步 |

G0 验收针对清点完整性和可追溯性。未执行安装、代码生成、构建、仿真或自动 Git 提交，工具缺失不判为 G0 失败。

## 2. G1 顺序与交付

统一输入：G0 固定的当前工作区源码、七个 MDM、WaterwaySearch 场景。G1 总体验收：指定场景在 Windows 原生 AMASE 运行，最小客户端持续解析真实实体状态及仿真时间，并能追溯实际使用的生成器和消息库。

| 任务 | 状态 | 前置条件 | 主要交付 |
| --- | --- | --- | --- |
| G1-T01 环境与生成物管理 | 已完成 | G0 已通过 | [Java 环境验收](g1-java-validation.md)：固定清单、项目内 JDK／Ant、精确忽略规则，8 组检查通过 |
| G1-T02 构建 LmcpGen | 已完成 | T01 已通过 | [构建验收](g1-java-validation.md#6-g1-t02本地-lmcpgen-构建与验收)：本地生成器 JAR、来源清单、六组检查通过 |
| G1-T03 统一消息代码与 Java 库 | 已完成 | T01、T02 已通过 | [消息库验收](g1-lmcp-validation.md)：三种语言代码、Java 库、跨语言样本和新旧库比较，八组集成验收通过 |
| G1-T04 AMASE 构建与运行 | 已完成 | T03 已通过 | [AMASE 验收](g1-amase-validation.md)：正式产物、两种模式动态状态、十一组自动检查、人工确认和正常退出 |
| G1-T05 实际状态接收 | 已完成 | T03、T04 已通过 | [TCP 验收报告](g1-tcp-validation.md)：独立客户端、两种模式真实样本、十四组自动验收、GUI 确认与正常退出 |

此处“阻塞”仅表示明确的前置任务尚未执行，没有发生构建／运行失败。若实施中出现失败，在对应任务补记复现命令、退出码、已尝试方案和下一次最小实验。

已建立 [docs/g1-java-validation.md](g1-java-validation.md)，当前记录 T01、T02 证据，后续按任务追加工程构建和运行结果；原始日志、配置和样本按 `out/runs/<run-id>/` 分开保存。报告保存可复现命令和关键结果，不能只链接未提交的本机日志。

T03 的统一消息库和兼容性证据单列于 [g1-lmcp-validation.md](g1-lmcp-validation.md)，保留上述 T01／T02 历史。T03 修正了早期关于 AMASE TCP 外层的判断，并确认旧 UXTASK 7 与当前 8 的差异；后续任务须依据新报告。

### G1-T01：准备 Java 环境与生成物管理

完成记录（2026-09-17）：项目内 Temurin 11.0.32.1+1／Ant 1.10.18 可运行；Java 8／11 探针、重复使用、中文空格路径及故障处理共 8 组检查通过。未构建两个实际工程，未生成消息代码。详细证据见 [Java 环境验收](g1-java-validation.md)。以下保留任务卡的原始目标与边界。

- **目标**：建立能实际运行的 JDK 11 兼容基线和 Ant，先落实生成物管理，再允许首次构建。
- **前置条件**：G0 已通过；重查用户工作区及工具状态。若已有合适工具优先验证，避免重复安装。
- **修改范围**：必要的 Windows 环境／启动辅助脚本、根级精确忽略规则、阶段记录；按本任务需要准备 JDK 与 Ant。
- **范围边界**：不准备 C++／Node，不升级业务依赖，不生成消息代码或构建 AMASE。
- **实施步骤**：选择并固定具体 JDK 发行版／补丁版本、Ant 版本及来源；记录下载校验值、许可入口和实际安装路径；尽量由项目脚本管理进程环境；确定 Ant build／dist 的输出策略；加入 out 与必要中间产物的精确忽略规则。
- **验收条件**：同一 PowerShell 环境下 java、javac、ant 版本命令成功，退出码为 0；java 与 javac 来自同一 JDK；记录 Java 8 源码与 Java 11 工程的后续兼容验证安排；git check-ignore 显示计划生成路径被正确忽略，随库 JAR 仍受跟踪。
- **证据**：实际路径、版本输出、命令与退出码、来源及校验记录、忽略规则检查。具体版本在本任务固定，G0 不预填“已验证”值。
- **回退方式**：恢复本任务更改的环境与脚本；仅处理本任务新增且确认归属的工具／输出位置，保留原环境和随库文件。
- **停止条件**：JDK／Ant 来源不可确认、版本命令无法运行，或必须改变 Java 源码标准才能继续；记录有限排查结果后重新评估，不同时迁移整个构建体系。
- **关联风险**：R02、R07。

### G1-T02：构建当前工作区的 LmcpGen

完成记录（2026-09-17）：从 G0 固定的本地 LmcpGen tree 新编译 21 个 Java 源文件，发布生成器 JAR 与 build-info.json；184 个 DTD／模板资源哈希一致，三个关键类为 Java 8 字节码，CLI 帮助及 CMASI 检查通过。重复执行、中文空格路径及三种故障共六组验收通过。未修改上游源码、构建文件或模板，未生成消息代码。入口为 [build-lmcpgen.ps1](../scripts/windows/build-lmcpgen.ps1)，证据见 [T02 报告](g1-java-validation.md#6-g1-t02本地-lmcpgen-构建与验收)，过程见根级 worklog 的 WL-20260917-005。以下保留原任务边界。

- **目标**：得到由本地基线构建的新生成器 JAR，并验证命令行入口。
- **前置条件**：T01 已通过；当前 LmcpGen tree 与 G0 一致，或已记录任务产生的源码差异。
- **修改范围**：LmcpGen 的必要构建兼容修复、Windows 构建入口、阶段记录；生成器业务逻辑默认保持现有行为。
- **范围边界**：不以下载的预构建 JAR 代替，不生成正式消息库，不默认使用 Anod 另检出的 develop 分支。
- **实施步骤**：明确 Ant 的工作目录、输出属性及输入路径；从干净的任务输出执行现有 jar 目标；核对新 JAR 的清单、入口与哈希；使用源码支持的 `-help` 和 `-checkMDM` 验证 CLI。
- **验收条件**：构建退出码为 0，新 JAR 路径／时间／哈希可追溯；`java -jar <实际JAR路径> -help` 能输出参数说明；以 CMASI 为输入的 `-checkMDM` 未报告模型错误。检查输出内容，不能只依赖退出码。
- **证据**：源码路径、Git 基线、JDK／Ant 版本、构建命令与日志、JAR 哈希、CLI 输出。
- **回退方式**：撤回本任务构建适配；只清理已确认属于本任务的输出，保留失败日志。
- **停止条件**：需要更换生成器版本或改动消息语义，或持续依赖未知外部源；先形成差异说明和最小复现。
- **关联风险**：R05、R07。

### G1-T03：统一生成消息代码并构建 Java 消息库

完成记录（2026-09-17）：同一生成器及七个模型快照生成 1,013 个文件；Java 编译 196 类，Python 导入 203 个消息模块并创建 164 个结构类型；六个跨语言帧样本及八组集成验收通过。旧库 UXTASK 7→新库 8、RendezvousTask 旧接口移除已记录；当前 AMASE 静态引用的 101 类未发现公共接口缺失。没有修改 MDM／模板／上游源码，也没有替换旧库或执行网络联调。入口见 [generate-lmcp.ps1](../scripts/windows/generate-lmcp.ps1)，证据见 [T03 报告](g1-lmcp-validation.md)，过程见 worklog 的 WL-20260917-006。以下保留任务原始边界。

- **目标**：Java／C++／Python 代码来自同一生成器和七个 MDM，得到 AMASE 可使用的 Java 消息库。
- **前置条件**：T02 已通过；重新核对全部 MDM 版本与哈希，输入发生变化时说明原因并更新生成清单。
- **修改范围**：生成／打包脚本、必要的生成器兼容修复、消息库接入说明；输出位于 `out/generated/lmcp/{java,cpp,py}/` 与约定的构建／产物目录。
- **范围边界**：C++ 只生成不编译，编译留给 G2；不引入新的业务消息，不靠手改生成代码解决输入或模板问题。
- **实施步骤**：用已验证的 JAR 和 `-mdmdir OpenUxAS/mdms` 分别执行 `-java`／`-cpp`／`-py`；保存命令与输入哈希；编译并打包 Java 库；导入生成的 Python 包；比较随库 lmcplib.jar 与新库的系列／类型／版本，说明 AMASE 切换影响。
- **验收条件**：三种语言生成成功且文件齐全，Java 编译成功；Java 与 Python 对选定的 CMASI 状态消息进行序列化／反序列化检查，实体 ID、时间、经纬高等字段一致；记录固定样本与预期值。Python 3.14.7 如遇兼容问题，先保存具体失败证据再选择最小调整。
- **证据**：生成器哈希、7 个 MDM 哈希／版本、各语言输出清单／哈希、Java JAR 哈希、编译及样本验证结果、旧新消息库比较结论。
- **回退方式**：保留旧随库 JAR；按生成清单定位并清理本任务产物，恢复生成脚本改动，不覆盖原始 MDM。
- **停止条件**：必须变更模型版本或业务字段才能编译，跨语言固定样本不一致，或旧新库差异影响 AMASE 必需类型；先定位输入／模板／消费者之间的具体差异。
- **关联风险**：R03、R09；也为 G3 的 R04 提供统一输入。

### G1-T04：接入统一 Java 消息库，验证 AMASE

交接起点：使用 `out/artifacts/lmcp/java/lmcplib.jar`，先检查与生成目录清单的运行编号及哈希，再通过 `file.reference.lmcplib.jar` 属性显式引用；保留旧随库 JAR，排除运行 classpath 中的重复类。Java 工厂含 Sentinel 外层，不能直接套用 Python 原始帧 TCP 客户端。

完成记录：AMASE 构建及两种模式的真实内部状态通过；无界面显示依赖作两处最小修复。启动入口提供显式 EntityPortOffset，隔离全局及实体端口。十一组自动验收通过；用户在 GUI 验收请求后回复“很好，继续”，随后进程正常退出并成套发布正式产物，见 [T04 报告](g1-amase-validation.md)。

- **目标**：AMASE 使用 T03 的统一 Java 消息库完成构建，在 Windows 上运行 WaterwaySearch 的 GUI 与无界面模式。
- **前置条件**：T03 已通过；端口重新检查；记录场景、插件和资源路径及其哈希。
- **修改范围**：AMASE 必要的构建／classpath／启动适配、Windows 入口、运行配置副本和阶段记录。
- **范围边界**：保留原界面和场景，不改写自主任务算法、不删除 WorldWind、不接入 UxAS／Cesium，不把无界面配置当成远程控制已经实现。
- **实施步骤**：显式引用 T03 的 JAR，排除旧同名类的重复加载；构建 AMASE；先运行 GUI，再验证无界面配置；从脚本自身位置解析路径，处理原相对资源与新运行目录关系；确认场景启动机制、实际地形来源或无 DTED 的缺省行为；保存 stdout／stderr 与进程退出信息。
- **验收条件**：新构建 JAR 可追溯到统一消息库；指定场景可启动，实体 400／500 及仿真时间有可观察变化；无界面模式无需 GUI 交互即可按记录方式推进仿真，出现窗口或需人工动作时明确列为未通过项；正常退出并释放本次进程和端口；日志可定位错误。
- **证据**：实际 classpath、JAR／配置哈希、完整启动命令、工作目录、两种模式的日志与观察结果、地形限制；至少验证从不同工作目录调用启动入口的资源解析。
- **回退方式**：保留旧 JAR 和原配置，通过独立启动入口／配置副本恢复原行为；仅结束本任务启动的进程。
- **停止条件**：无界面运行依赖无法隔离的显示模块、指定场景因资源缺失不能推进，或新 LMCP 库不兼容；先记录最小失败场景，必要的有限修复留在本任务，不扩展为全面 UI 重构。
- **关联风险**：R03、R05、R06、R08。

### G1-T05：接收并解析实际状态，完成 G1 验收

完成记录（2026-09-17）：十四组自动验收通过，包含 24 项协议／接收检查；GUI 与无界面分别通过真实 TCP 接收，中文空格路径与重复运行通过，三种产物哈希故障明确失败。用户回复“很好，确认GUI”；原窗口人工重置后触发单调时间检查，保留诊断，以相同未变更程序受控复验并正常退出，未修改来源清单或放宽条件。最终批次 `g1-t05-finalize-20260917-222451-856588` passed，进程和六个端口已清理；G1 已完成。入口为 receive-amase.ps1 和 amase-tcp.tests.ps1，证据见 [T05 报告](g1-tcp-validation.md)，过程见 worklog 的 WL-20260917-009。以下保留原任务卡。

交接起点：T04 已发布 `out/artifacts/amase/OpenAMASE.jar` 及 build-info.json，启动入口默认使用该目录。先核对 T04／T03 正式来源清单与当前端口；单实例默认主端口 5555，实体 400／500 端口 9400／9500。本次仿真进程均已退出，T05 需自行按任务启动并记录新运行；T04 的内部事件证据不替代 TCP 客户端验收。

- **目标**：用最小客户端接收 AMASE 实际输出，证明消息库、实体状态和仿真时间可供后续系统复用。
- **前置条件**：T03、T04 已通过；具备已确认的 AMASE 地址、运行配置和 Python 消息包路径。
- **修改范围**：最小接收验证脚本、固定样本／验证记录及 G1 验收报告；客户端位置在实施时明确（建议 `scripts/validation/`），G0 不创建。
- **范围边界**：不建设完整网关、不接浏览器、不提前修复 AMASE ↔ UxAS 桥，不把静态 XML 或模拟状态作为实际状态证据。
- **实施步骤**：客户端使用统一生成消息包连接 AMASE 的真实 TCP 服务；按照实际 LMCP 格式处理增量缓冲、长度和校验；接收实体状态及 SessionStatus，保存消息类型、实体 ID、原始仿真时间、位置和墙钟接收时刻；核对样本与场景实体。
- **验收条件**：在一段有明确起止墙钟时刻的连续运行中，实体 400 和 500 各至少收到 10 条可解析状态，源时间推进、位置有实际变化，同时收到仿真时间状态；记录时间单位和消息数，不将仿真时间解释成 UTC；输入错误与正常退出可区分，客户端断开后无遗留进程。TCP 基本分包解析需有验证，完整桥接重连矩阵留给 G3／G4。
- **证据**：运行模式／配置、生成库版本、命令与退出码、原始消息片段及解码字段、接收统计与首尾样本、G1 验收结论。截图只能辅助，不能代替实际消息。
- **回退方式**：退出本次客户端和所属仿真进程，保留样本；验证脚本与业务系统分离，撤回脚本不影响原程序。
- **停止条件**：只收到初始化静态消息、时间不推进、实体不齐或持续解析失败；先判断仿真未开始、订阅／网络还是模型问题，不靠更换为模拟消息通过验收。
- **关联风险**：R04、R09。

## 3. G1 阶段结束检查

2026-09-17 完成记录：以下六项已逐项核对，G1 验收通过；T05 实现与文档留在工作区，本轮未自动提交推送。下一关卡为 G2，先细化任务与工具链准备，不自动安装 C++ 环境。

1. T01～T05 均有可复查的命令、退出码和结果，版本、配置、哈希互相一致。
2. GUI／无界面模式的能力与限制分开记录；真实状态证据关联到本次 AMASE 运行。
3. Python 与 Java 消息验证已完成；C++ 编译明确移交 G2，AMASE ↔ UxAS 双向兼容明确移交 G3。
4. 追加根级 worklog.md，更新 status、backlog、总体计划与 AGENTS 中已变化的事实；新增经验证的标准命令。
5. 确认原源码、生成物、日志和本机路径的变更／忽略范围正确；Git 提交按用户授权执行。
6. 只有上述证据齐全才登记 G1 已完成，再细化 G2；不得将“构建成功”写成“系统闭环通过”。

## 4. G2 顺序与任务卡

2026-09-18 首先完成任务细化，细化时只写方案、任务卡和状态记录。随后按用户授权完成 T01～T07：工具链、依赖、C++ LMCP、CMake 构建图、UxAS 原生候选、HelloWorld 内部消息与退出、完整复验与正式发布。技术方案、源码依据和验收矩阵见 [G2 实施方案](g2-windows-uxas-plan.md)。

统一输入：当前工作区的 OpenUxAS／LmcpGen、G1 同批七模型生成代码及跨语言样本、原 HelloWorld 配置。主线为 Windows 11 x64、MSVC v143、CMake 3.31、Release `/MD`；采用固定 vcpkg manifest／baseline 和必要 overlay ports，Zyre／串口默认关闭，CZMQ 与必需 TCP／ZeroMQ 通信保留。具体工具补丁版本在 T01、依赖配方和锁定值在 T02 验证后记录。

| 任务 | 状态 | 前置条件 | 独立交付 |
| --- | --- | --- | --- |
| G2-T01 准备并锁定原生工具链 | 已完成 | G1 已通过；固定来源验证完成 | [T01 记录](g2-cpp-toolchain-validation.md)：十组完整检查、两个生成器及两类路径下真实 C/C++ 编译运行通过 |
| G2-T02 固定并构建第三方依赖 | 已完成 | T01 已通过 | [T02 记录](g2-dependencies-validation.md)：89 个 ports 的源码重建、缓存复用、11 组验收及迁移发布通过 |
| G2-T03 构建并验证 C++ LMCP 库 | 已完成 | T01、T02 及 G1 来源核对通过 | [T03 记录](g2-lmcp-cpp-validation.md)：183 个库源文件、164 类型、六组验收及迁移发布 |
| G2-T04 建立 UxAS CMake 入口 | 已完成 | T01～T03 已通过并复查 | [T04 记录](g2-uxas-cmake-validation.md)：122 个源码、40 项服务、5 个资源、桥能力探针和隔离故障通过 |
| G2-T05 完成必要平台适配 | 已完成 | T01～T04 来源有效且复验通过 | 两类路径的原生候选、平台与来源验收；见 [T05 报告](g2-uxas-build-validation.md) |
| G2-T06 运行 HelloWorld | 已完成 | 新 T05 候选及来源有效 | [T06 记录](g2-uxas-helloworld-validation.md)：原生运行、双向消息、正常退出与真实配置拒绝 |
| G2-T07 复验、发布与阶段交接 | 已完成 | T01～T06 已通过 | [复验发布报告](g2-uxas-release-validation.md)、正式包、G3 输入与限制 |

T01 的来源阻塞及 T02 的旧配方兼容问题均已解决，历史失败保留；T07 已完成，G2 验收通过；本轮止于 G2，不自动开展 G3。按 T01 → T07 顺序推进，独立调查不能被登记为下游实现卡通过。

### G2-T01：准备并锁定原生工具链

- **状态**：已完成（2026-09-18）。固定 Build Tools 17.14.41／v143、SDK 发布 10.0.26100.7705、CMake 3.31.12、Ninja 1.13.2 和 vcpkg 提交；微软安装器退出 0、无需重启。十组完整验收通过，VS／Ninja 在普通和中文空格路径构建运行八个 C/C++ 探针，环境恢复及实际 CRT 来源已核查；来源修复及全部证据见 [T01 记录](g2-cpp-toolchain-validation.md)。
- **目标**：建立可复现的 MSVC／SDK／CMake／Ninja／vcpkg 基线，证明真实 x64 C/C++ 编译、链接与运行可用。
- **前置条件**：G1 已通过，用户已确定 MSVC＋CMake 和 vcpkg 路线；任务开始时重查 Git、实际工具及适用说明，不将 PATH 未找到等同于全机未安装。
- **修改范围**：Windows 工具准备／环境辅助入口、工具版本与组件清单、最小探针、必要忽略规则及记录。MSVC／SDK 按官方安装机制管理；独立工具和缓存放入 `.tools/`，记录实际安装影响。
- **范围边界**：不构建第三方业务依赖、LMCP 或 UxAS；不启动 AMASE；不永久修改 PATH，不建立全局 vcpkg 集成。
- **实施步骤**：优先验证可复用工具；固定 Build Tools 2022／v143、SDK、CMake 3.31、Ninja 具体版本及 vcpkg checkout 提交；记录来源、许可入口、下载校验和组件；建立进程环境启用／恢复入口；编译并运行使用标准库、线程和固定宽度整数的最小 C/C++ 探针。
- **验收条件**：版本查询及探针命令退出 0，目标实际为 x64；编译器、链接器、SDK、CRT 来源明确；C++ 参数为 `/std:c++14`、Release `/MD`；不同工作目录调用成功，结束后父进程环境不被持久修改；缓存及输出受忽略规则覆盖。
- **证据**：工具路径、版本、安装组件、来源校验、Git 基线、环境前后差异、探针源码及编译／链接／运行命令和退出码。版本可运行与项目兼容性分开记录。
- **回退方式**：撤回本任务脚本和配置，恢复任务自身进程环境；只处理确认属于本任务的工具输出，不自动卸载共享 MSVC／SDK 或已有工具，保留诊断。
- **停止条件**：来源或完整性不可核实、必须更换已定工具链主路线、安装受系统条件阻止或最小程序无法运行；提交有限排查、影响和恢复条件后调整任务。
- **关联风险**：R02、R07、R10。

### G2-T02：固定并构建第三方依赖

- **状态**：已完成（2026-09-18）。固定 89 个 ports，保留 Boost 历史模块路线；中文空格路径禁用二进制缓存读取的源码重建、VS／Ninja 功能消费、来源／CRT、故障隔离及迁移发布全部通过。证据见 [T02 记录](g2-dependencies-validation.md)。
- **目标**：通过 vcpkg 构建与现有 UxAS API 相容的必需依赖，建立源码、补丁、配方和二进制之间的可追溯关系。
- **前置条件**：T01 已通过；固定工具链可用；重新核对原 Anod 版本声明及本地补丁。
- **修改范围**：根级 vcpkg manifest／配置、overlay ports／triplet、依赖构建入口与功能探针、版本和证据记录。
- **范围边界**：不构建 Zyre／serial，不改变 UxAS 算法，不整体升级依赖，不以下载未知预构建库绕过验证；传递依赖须登记。
- **实施步骤**：以 libzmq 4.3.1、cppzmq 4.2.2、CZMQ 4.0.2、pugixml 1.12.1＋补丁、Boost 1.74.0、SQLite 3.39.4、SQLiteCpp 1.3.1 为起点；固定 registry baseline，用 overrides／overlay 承载历史版本；锁定源码校验、配方、补丁及工具集 triplet；统一静态库与动态 CRT；保存 pugixml `as_int64`、SQLite 列元数据及 SQLiteCpp 使用外部 SQLite 的行为；核查 vcpkg 实际选用的 CMake／Ninja。
- **验收条件**：必需依赖均成功构建；cppzmq／CZMQ 二进制／空帧／多帧及本机 TCP STREAM、pugixml 大于 2⁵³／有符号边界／缺省值、Boost filesystem／system／regex／date_time／geometry／graph／dynamic-bitset、SQLiteCpp 事务及整数／文本／二进制和列元数据通过；头文件和链接库与清单一致，Release 不混入 Debug 或静态 CRT；默认图不含 Zyre／serial。源码重建、缓存复用、中文空格路径、其他工作目录、迁移消费及隔离故障均验证，失败保留旧合格指针。
- **证据**：完整直接／传递依赖清单、vcpkg 提交及 baseline、port-version、overlay／补丁哈希、triplet、实际工具和编译选项、探针输入输出及退出码；说明 cache 命中与本次真实构建的区别。
- **回退方式**：恢复本任务清单和配方，隔离失败依赖批次，保留上次合格产物；不清空用户全局缓存或改写原 Anod 配方来隐藏失败。
- **停止条件**：主要依赖版本必须变化、原补丁行为无法保留、必须修改通信语义或混用运行库才能继续；先提交最小复现、兼容修复尝试及版本差异后调整方案。
- **关联风险**：R05、R07、R10。

### G2-T03：构建并验证 C++ LMCP 库

- **状态**：已完成（2026-09-18）。先核对 T01／T02 及 G1 来源；七模型 183 个源文件、164 类型、三语言双向六帧、错误拒绝、VS／Ninja 消费和迁移全部通过，见 [T03 记录](g2-lmcp-cpp-validation.md)。
- **目标**：编译全部七模型 C++ 代码，并以既有样本证明与统一 Java／Python 消息库一致。
- **前置条件**：T01、T02 已通过；G1 生成器、模型、generation-info.json 和产物来源相互匹配；样本可由既有受控入口取得。
- **修改范围**：根 CMake 的 LMCP 目标、Windows 构建入口、C++ 跨语言探针及来源清单；确有必要时修复生成器／模板。
- **范围边界**：不手改生成目录，不只编译 HelloWorld 用到的消息子集，不新增消息语义；不构建 UxAS，不把内存／文件样本计为双向 TCP 验收。
- **实施步骤**：核对全部生成输入和文件哈希；复用生成源建立可独立构建的库目标；加入与 G1 普通／大 ID AirVehicleState、列表、时间及 UXTASK 8 TaskActive 对应的探针；比较原始 LMCP 层，Java Sentinel 外层按既有方式解析；模板变更时统一重新生成三种语言并复验受影响的 G1 链路。
- **验收条件**：七模型全部编译为 x64 库；C++ 解码既有 Java／Python 样本并重编码字节一致，C++ 生成样本也可由同批 Java／Python 验证；int64 精度、字段及校验模式一致；损坏校验和、截断及旧 UXTASK 版本明确识别，不能仅以 getObject 返回非空判通过。
- **证据**：模型／生成器／生成批次、实际源码与库哈希、编译选项和命令、样本原始字节及字段对比、错误样本结果。原任务构建 `g2-t03-build-20260918-143841-186`、验收 `g2-t03-test-20260918-143927-259`；T04 因 CMake 输入变化重新构建并完整验收，当前合格批次与证据见 [T04 报告](g2-uxas-cmake-validation.md)。已发布到 `out/artifacts/lmcp/cpp/`，独立清单关联 G1 父级记录，不覆盖其历史清单。通过 `Resolve-LmcpCppPackage` 复核后消费。
- **回退方式**：保留上一批合格生成物及 Java／AMASE 产物；候选失败不发布，恢复本任务构建／模板变更，保留失败样本和诊断，不将新批次混入旧正式产物。
- **停止条件**：字节或模型版本差异需要改变消息语义、统一重生成后影响既有 G1 验收，或必须手工修补生成输出才能继续；先定位模型、模板、字节序或编译器差异。
- **关联风险**：R03、R07、R10。

### G2-T04：建立 UxAS CMake 入口

- **状态**：已完成（2026-09-18）。真实 VS 配置、File API 构建图、独立桥能力探针、中文路径和 15 项隔离故障通过，见 [T04 记录](g2-uxas-cmake-validation.md)。
- **目标**：得到覆盖必需源码、依赖和服务注册的 Windows CMake 构建图，明确可选桥边界。
- **前置条件**：T01～T03 已通过；必需依赖和统一 C++ LMCP 库有效。
- **修改范围**：在 T03 根 CMake 上新增 UxAS 目标及 presets；桥管理器、相关源码／头文件的必要条件编译；配置拒绝路径及来源检查。
- **范围边界**：不将 CMake 配置成功写成完整编译成功；不删除必需任务服务或 AutomationDiagramDataService；不改动原 Makefile 默认行为、不验收 Zyre／串口启用组合。
- **实施步骤**：与 Makefile 实际源码集合逐项对照，保留 Services 中的 AutomationDiagramDataService 及 resources 下的嵌入文件；明确服务注册对象链接方式；接入 T02 依赖与 T03 消息目标，阻断过期输入；增加默认 OFF 的 `UXAS_ENABLE_ZYRE`／`UXAS_ENABLE_SERIAL`，同时处理源码、头文件、包装工具和实例化分支；实现配置请求未编入桥时的明确失败。
- **验收条件**：Windows Release x64 配置和生成命令成功；编译单元、服务注册和依赖清单完整可审查；默认构建图不含可选桥或其依赖残留，CZMQ／TCP 等保留；共享拒绝逻辑经独立 C++ 探针验证，完整主程序的真实运行拒绝在 T06／T07 验证；若尚有编译错误，准确移交 T05。
- **证据**：配置命令与退出码、生成器和工具集、原 Makefile 与新构建图对照、保留／排除源码清单、服务注册及可选桥条件分支核查结果。
- **回退方式**：恢复本任务 CMake 和条件分支，保留原 Makefile、可选桥源码、T03 库和失败配置日志；仅处理本次构建目录。
- **停止条件**：无法保留必需服务注册、关闭可选桥要求重写通信框架，或依赖图必须改变已定消息契约；提交最小差异后重新拆分。
- **关联风险**：R05、R07、R10。

### G2-T05：完成必要平台适配

- **状态**：已完成；见 [T05 报告](g2-uxas-build-validation.md)。候选 `g2-t05-build-20260918-173741-000`，验收 `g2-t05-test-20260918-173955-149`；T03 重建和 T04 完整复验通过。
- **目标**：使用已核查的构建图，从干净输出完整编译、链接得到候选 Windows `uxas.exe`。
- **前置条件**：T04 已通过；工具、依赖、消息库来源有效，构建错误有本次日志可复现。
- **修改范围**：目标宏 `DPSS_STATIC`／`NOMINMAX`、进程 UTF-8 清单和 Boost 独立路径 facet；候选构建／验收／解析入口与来源记录。时间实现和任务算法未修改。
- **范围边界**：不重构任务算法、不通过删服务或假实现绕过错误；不将候选直接发布为正式产物，不启动 AMASE 或 WaterwaySearch 闭环。
- **实施步骤**：逐类复现编译和链接错误，进行最小修复；核查 Windows 64 位类型、UTF-8 路径、UTC 时间与线程／退出相关差异；对实际改变的行为建立必要探针；重新从独立干净输出编译完整目标，收集依赖与运行库来源。
- **验收条件**：完整编译／链接退出 0，候选确为 x64；必需服务及通信源码进入最终程序，静态库与 `/MD` 一致；涉及路径／时间等行为的适当探针通过，仍需真实运行核验的事项明确交给 T06；源码与实际输入匹配。
- **证据**：原错误、有限尝试、修复差异、干净构建命令、退出码、候选哈希、122 个实际对象、40 项服务及运行库清单；两种路径、4 次平台探针、3 次启动前冒烟和 11 项隔离故障通过，收据绑定候选字节。未运行 HelloWorld。
- **回退方式**：恢复本任务平台修复，保留失败日志及上一合格候选，不改写 T01～T04 记录；构建失败不更新正式目录。
- **停止条件**：修复需要更改算法、消息语义或主要依赖版本，或无法保留必需功能；形成最小复现及影响说明，不能改用 Linux 产物计为通过。
- **关联风险**：R02、R06、R07、R10。

### G2-T06：运行 HelloWorld

- **状态**：已完成；见 [T06 报告](g2-uxas-helloworld-validation.md)。启用原有 INFO 日志后，T03／T04／T05 均按原入口重新完整验收；运行入口通过 `Resolve-UxasCandidate` 绑定新候选双编号，普通／中文目录双向消息、退出 0 和真实配置拒绝通过。
- **目标**：证明候选 UxAS 的两个 HelloWorld 服务通过真实内部总线相互收发，并按原时长正常结束。
- **前置条件**：T05 候选和来源清单有效，T01／T02 所需运行库可用；配置来自原 HelloWorld 示例。
- **修改范围**：Windows 运行入口、配置副本、消息日志验收器和必要启动／退出适配；失败时仅进行本任务范围内的最小修复。
- **范围边界**：不启动 AMASE，不新增外部 TCP 桥或用模拟消息充数；不修改原示例时长、1000／5001 ms 周期和两条文本；不以固定 sleep 代替就绪和结果检查。
- **实施步骤**：核对候选及依赖来源，复制配置到独立 `out/runs/<run-id>/`；从脚本位置解析资源，记录实际命令、工作目录、PID 和日志；解析不同服务的 Received Id／Sent Id／Message 对应；按配置运行 10 秒，设置 30 秒进程超时；验证缺失配置及请求关闭桥时的真实失败行为。
- **验收条件**：两个不同服务各至少收到一条来自对方的 KeyValuePair，发送者／接收者及文本对应，不能只统计自收消息；无需精确条数或严格周期；程序正常退出 0，无残留进程，日志归属于本次运行。超时、强制终止或配置被静默忽略均不通过。
- **证据**：候选及配置哈希、命令和实际工作目录、PID、墙钟起止、原始 stdout／stderr、消息明细／统计、退出码和故障诊断；默认 HelloWorld 无外部 TCP 桥，不借用 AMASE 端口验收。
- **回退方式**：正常结束本次实例；超时只清理确认归属的进程并保留 failed 记录；移除本任务启动入口不影响原示例及旧产物。
- **停止条件**：只有启动日志、只有单向或自收消息、退出挂住、必须改变示例语义或扩大到 AMASE 才能运行；先定位注册、订阅、依赖、定时器与退出顺序。
- **关联风险**：R06、R07、R10。

### G2-T07：复验、发布与阶段交接

- **状态**：已完成（2026-09-18）；干净重建、连续三次正常启停、中文路径构建／运行、故障矩阵和正式发布通过，见 [T07 报告](g2-uxas-release-validation.md)。当时 G2 已完成，G3 尚未启动；当前进度见下方 G3 记录。
- **目标**：完成可重复构建、运行、故障和路径验收，发布有完整来源的正式 UxAS，并准备 G3 输入。
- **前置条件**：T01～T06 均有合格证据；本轮工具／依赖／消息／候选版本一致；未解决项不影响 G2 的通过条件。
- **修改范围**：Windows 复验／发布入口、隔离故障用例、G2 专题验收报告、使用说明、状态和工作日志；必要修复回到对应任务复验。
- **范围边界**：不将第二个干净部署环境或断网演示提前纳入 G2，不自动开展 G3；完成验收后的提交与推送按用户持续授权执行，不把内部消息证明写成 AMASE 双向通过。
- **实施步骤**：在独立干净输出重建；连续三次正常启停；验证其他工作目录及中文空格路径下构建／运行；在隔离副本测试依赖缺失／损坏、生成来源不符、配置缺失／损坏、关闭的桥和运行超时；检查运行库、退出与产物保留；全部通过后成套发布到 `out/artifacts/uxas/`。
- **验收条件**：构建和正常运行均可重复；故障明确返回失败，未覆盖旧合格产物或正式输入；正式 exe 与验收候选哈希一致，清单记录同批来源和结果；七张任务卡及阶段结束检查齐全。工具／依赖缓存可按锁定输入复用，不能冒称从空缓存重建。
- **证据**：复验矩阵、每次命令／配置／工作目录／退出码、消息结果、故障拒绝及旧产物保留证据、正式产物哈希、运行库清单、关键结论与可复制命令。
- **回退方式**：发布失败保留或恢复上一合格正式产物，保留失败候选与记录；仅结束本次进程、处理本次隔离输出，不覆盖既往验收报告。
- **停止条件**：来源链不一致、出现不可解释的重复运行失败、路径／退出问题仍影响目标环境，或任一必要故障被误判成功；保留实际状态，不登记 G2 完成。
- **关联风险**：R03、R06、R07、R10。

## 5. G2 阶段结束检查

以下六项已依据 T01～T07 实际证据完成，G2 当时的批次、正式包及交接见 [T07 报告](g2-uxas-release-validation.md)；当时 G3 尚未启动：

1. T01～T07 的命令、版本、输入哈希、退出码、配置和结果齐全且相互一致；原始证据受忽略规则覆盖，报告包含关键结论。
2. Windows 原生完整构建和 HelloWorld 双向消息、正常退出通过；连续三次启停、干净输出重建、中文空格路径及必要故障均通过。
3. 正式 UxAS 与验收候选一致，统一 LMCP 库、vcpkg 依赖、CRT 和工具来源可追溯；失败没有替换旧合格产物。
4. 必需任务服务、AutomationDiagramDataService 与 TCP／ZeroMQ 通信保留；Zyre／串口关闭及其延期事项明确，未把未验证组合写成可用。
5. 追加工作日志，更新验收报告、标准命令、status、backlog、总体计划及 AGENTS；只有事实变化才新增已验证能力。
6. 向 G3 交接正式 UxAS、同批 LMCP、既有 AMASE、服务／桥清单和运行约定；封装、来源过滤、启动顺序、实时状态及真实规划命令执行仍待 G3 验证。

## 6. G3 顺序与任务卡

2026-09-19 已形成 [G3 阶段方案](g3-system-integration-plan.md)。**G3-T01～T07 已完成，G3 已完成；见 [阶段报告](g3-stage-validation.md)与 [G4 交接](g3-g4-handoff.md)。** T01 实际资格与输入验收见 [报告](g3-input-baseline-validation.md)；T02 已完成协议联调，T03 已完成两模式受控初始化，见 [协议报告](g3-protocol-validation.md)及 [启动报告](g3-startup-validation.md)。按用户最新要求，统一目标为 Windows 原生 GUI／无界面任务执行链完整、AMASE 20 米栅格覆盖统计正确及正常退出；不设最低覆盖率，算法／参数寻优另列后续。T01 原报告保留历史口径，当前契约修订见 [阶段方案](g3-system-integration-plan.md)。

| 任务 | 状态 | 前置条件 | 独立交付 |
| --- | --- | --- | --- |
| G3-T01 输入与验收基线 | 已完成 | G2 已完成 | [资格检查、35 个冻结输入、消息流及判据](g3-input-baseline-validation.md) |
| G3-T02 双向协议与来源验证 | 已完成 | T01 通过 | [真实双向协议、来源矩阵、修复与正式验收](g3-protocol-validation.md) |
| G3-T03 受控启动与初始化 | 已完成 | T02 通过 | [两模式真实初始化、单次触发、正常退出及故障反例](g3-startup-validation.md) |
| G3-T04 规划命令实际执行 | 已完成 | T03 通过 | [两模式分段命令、内部导航和任务航段实际执行](g3-execution-validation.md) |
| G3-T05 任务完成与覆盖统计正确性 | 已完成 | T04 通过；新 AMASE 发布及当前资格通过 | [正式两模式完整任务、可靠 TaskComplete、覆盖统计独立复算及反例通过](g3-completion-validation.md) |
| G3-T06 稳定性与故障复验 | 已完成 | T05 通过 | [三次短程实际执行、路径／故障及新组恢复矩阵](g3-stability-validation.md) |
| G3-T07 阶段验收与 G4 交接 | 已完成 | T06 通过 | [最终两模式全程、本轮 GUI 确认、正常退出及交接](g3-stage-validation.md) |

“待前置”仅表示顺序依赖，未发生实现失败。每卡按验证 → 工作日志 → 状态／待办 → 提交推送及远程核对收尾；失败或中断保留证据与恢复条件。原始记录放入独立 out/runs，专题报告写入 docs，拟建入口在实现并验证前不列为标准命令。

### G3-T01：输入与验收基线

- **状态**：已完成（2026-09-19）；[报告](g3-input-baseline-validation.md)，正式资格检查 `g3-t01-check-20260919-093348-422` 及 10 项输入检查通过，缺失 Python 按预期拒绝。原场景、产物及历史收据保持，未运行仿真或网络联调。
- **目标**：建立正式产物、场景、消息流与验收判据的可追溯基线，消除用静态样本冒充实时闭环的可能。
- **前置条件**：G2 已完成；读取当前状态、G2-T07 交接、G3 方案及最近日志；重查 Git、适用说明和实际工具。
- **修改范围**：G3 输入／场景清单、消息方向与来源表、验收判据文档；必要的独立只读核查入口，不改变旧资格解析接口。
- **范围边界**：不启动 AMASE／UxAS 联调，不调参、不修改桥或算法；规划期读取记录不代替本卡完整资格复核。
- **实施步骤**：通过既有入口复核正式 UxAS、AMASE、同批 LMCP、工具、CRT 与 handoff；登记原 XML 和任务文件哈希、实体 400／500、任务 1000、90 个点、785 秒；追踪两种来源 ID、静态注入、原巡航命令、规划命令、任务完成和覆盖分析的数据流；列出两模式初始消息、端口、超时及退出判据。
- **验收条件**：实际包、模型和运行库身份一致；场景／任务清单与源码一致；明确两实体实时状态、实际分配实体执行、完成链及原始计数／覆盖统计正确性的判定方法（当前修订口径，原验收报告保留）；全部待验证项归属到 T02～T07，无联调通过声明。
- **证据**：Git 基线、解析命令／退出码、输入及产物哈希、工具实际版本、源码定位、消息流表和验收检查表。
- **回退方式**：恢复本卡文档／核查脚本，仅隔离本次核查输出；保留正式包、来源记录及原始场景。
- **停止条件**：来源链、模型批次或场景不一致且无法通过既有重建／复验恢复，或判据依赖尚不明确的业务语义；记录差异、恢复条件和最小验证，不进入 T02。

### G3-T02：双向协议与来源验证

- **状态**：已完成（2026-09-19）；正式真实双向／直连、分包／粘包、来源／PUB、断线整组恢复及正常关闭通过；本轮 GUI 确认、AMASE／UxAS 复验发布及当前输入资格通过，见 [T02 报告](g3-protocol-validation.md)。
- **目标**：以真实 Java／C++ 两端证明现有 TCP 主链路可用，并固定来源、订阅、封装和分包规则。
- **前置条件**：T01 通过；合格产物和独立端口可用，协议／消息清单已登记。
- **修改范围**：独立协议捕获与严格解析探针、双向样本、来源／过滤矩阵；实际缺陷所需的最小桥或解析修复，按影响重建复验。
- **范围边界**：以现有 TCP 桥直连为主，不预建原始 LMCP 专用适配器；不以模拟端替代真实端验收，不建设网关快照或自动重连。
- **实施步骤**：抓取双方真实发送字节，逐层核对 Sentinel／属性及 LMCP 长度、校验、类型与字段；复用 G1 严格解析规则；隔离测试双向半包／粘包和坏帧；对照 ConsiderSelfGenerated 的 true／false、TCP 出口和 PUB 来源过滤，登记发送连接与改写前后属性；断开后清理残包并验证正常关闭。
- **验收条件**：真实两端双向样本一致，分包正确，方向和来源可追溯，无转发回路；坏帧不能成为合格业务证据，验收器拒绝与真实后端行为分别记录；正式主链路显式保留来源，不依赖 PUB 提供全部 AMASE 状态。
- **证据**：原始流及帧位置、两层校验、解码结果、配置／属性对照、畸形输入处理、端口／PID 和关闭结果；修复前后差异及受影响的重建收据。
- **回退方式**：撤回本卡修复、恢复隔离配置；保留旧正式包和失败样本，停止本次持有的进程，不改写旧验收清单。
- **停止条件**：现有拓扑不能兼容、来源无法辨认、存在未解决回路／残包污染，或必须改变消息契约／主要依赖；保留最小复现后调整任务，不进入 T03。

### G3-T03：受控启动与初始化

- **状态**：已完成（2026-09-19）；11 组真实运行／故障、10 项屏障反例及独立 GUI 收尾通过，见 [启动报告](g3-startup-validation.md)。
- **目标**：建立可重复的暂停启动、连接就绪、真实初始化、单次任务触发和正常关闭流程。
- **前置条件**：T02 双向封装和来源规则通过，T01 来源复查仍有效。
- **修改范围**：G3 独立 Windows／Python 编排、AMASE 验收插件、运行配置副本和初始化检查；新接口接受模式、配置、Python 路径及运行编号。
- **范围边界**：不改变 G1 的 GUI 20 秒／无界面 120 秒验收，不新增浏览器控制接口，不修改原示例或用静态实体文件补齐数据。
- **实施步骤**：隔离输出和所有端口，AMASE 初始化暂停后再启动 UxAS 与观察连接；运行副本取消静态配置／状态和定时任务注入；开跑并确认 400／500 配置及多条时间递增、位置变化的真实状态进入 UxAS；单次注入任务，收到 TaskInitialized 后单次发送 AutomationRequest；通过本地插件和既有停止消息正常关闭。
- **验收条件**：GUI／无界面均区分监听、连接、解析、初始齐全四层就绪；缺配置、静态状态、提前请求反例拒绝；初始化、任务初始化／规划和关闭各 30 秒超时有效，正常例退出 0、无强制终止且端口释放；实例只管理本次进程。
- **证据**：配置差异、插件来源、运行状态迁移、两实体初始化消息、任务／请求单次发送、原始收发与内部事件、命令／PID／退出码和环境恢复。
- **回退方式**：停用 G3 插件与副本入口，保留旧 G1／G2 标准流程；仅正常关闭或在失败清理时结束本次进程，保留失败记录。
- **停止条件**：无法防止提前开跑、真实初始消息缺失、只能依赖固定延时或重复请求、正常关闭失败；记录有限排查，不进入 T04。

### G3-T04：规划命令实际执行

- **状态**：已完成（2026-09-19）；GUI／无界面任务航段实际执行、23 项证据反例／边界检查和正常退出通过，见 [执行报告](g3-execution-validation.md)。
- **目标**：建立请求、规划、分段命令与 AMASE 真实执行之间的完整关联。
- **前置条件**：T03 两模式真实初始化和生命周期通过，全部消息来源与本次运行绑定。
- **修改范围**：G3 消息／事件关联器、AMASE 内部执行观测、最小闭环验收与报告；必要服务兼容修复按影响复验。
- **范围边界**：不伪造规划命令、实体状态或完成事件，不要求两架都被分配；本卡不以短程结果宣称完整任务或覆盖统计正确性通过。
- **实施步骤**：追踪任务 1000 初始化、原请求内容、实际请求／响应身份、分配结果和 MissionCommand；将原始巡航与新规划、分段／重叠及正常续传分别登记；关联 AMASE 接收事件、CurrentCommand、CurrentWaypoint、AssociatedTasks、任务起点与实际轨迹，保留 TaskActive。
- **验收条件**：GUI／无界面均证明命令进入 AMASE 并驱动被分配实体执行任务航段；当前命令、航点、任务及轨迹可交叉核对；仅静态状态、仅发送／接收命令、错误任务／实体及初始巡航运动不能通过。
- **证据**：原始计划和分段命令、来源及时间顺序、内部接收／执行事件、动态状态／轨迹、反例结果和正常退出收据。
- **回退方式**：恢复本卡关联器／探针改动及隔离配置，保留 T03 合格流程和失败消息样本；不降低执行门槛。
- **停止条件**：无法关联请求与计划、命令未执行、分段重复执行、任务／实体身份错误，或需要改变规划算法语义才能继续；保留最小复现，不进入 T05。

### G3-T05：任务完成与覆盖统计正确性

- **状态**：已完成（2026-09-19）。末端提前完成及线搜索统计修复经对照验证；新 AMASE 的 11 组自动复验、本轮 GUI 确认、正常退出和发布完成，UxAS 同一二进制复验发布以更新交接绑定，inputRevision=3 资格通过。正式两模式完整任务、可靠 TaskComplete、覆盖独立复算与 19 项离线检查通过，未开展算法／参数寻优或设置最低覆盖率。见 [T05 报告](g3-completion-validation.md)。
- **本轮倍率说明**：无界面全程 1 倍；GUI 实际 1→5→10 倍，经用户明确接受本轮变速，倍率审计和补充接受收据保留。不改变后续默认启动 1 倍，不宣称本轮正式 GUI 全程固定 1 倍；无算法／规划参数寻优。
- **目标**：两模式分别完成任务执行，验证 AMASE 覆盖计算与报告正确，如实保存实际覆盖率，不设数值门槛。
- **前置条件**：T04 实际执行通过；任务全水道、20 米栅格、原参数基线及同批产物已锁定。
- **修改范围**：完整任务判定、GUI／无界面分析导出、进度窗口隔离、原始计数与漏覆盖导出、统计正确性对照及必要最小修复；不开展搜索算法／参数寻优。
- **范围边界**：仅为功能兼容、正常执行和统计正确性作必要调整，保留原 XML 对照；实体身份、90 点完整水道及顺序、20 米栅格固定，不合并运行。可修复统计实现缺陷，不得为提高数值放宽评分条件。
- **实施步骤**：保留原参数与分析行为，关联 TaskActive、任务航段、末端完成与 TaskComplete；导出 XML、seen／total 和漏覆盖位置；用已知覆盖／未覆盖、重复覆盖去重、波段与 AllAny、小样本人工复核及同一事件记录两模式一致性验证计算，必要修复按影响复验。原时长 785 秒、两模式 1 倍速，为任务正常执行必要延长最多 1800 仿真秒／2700 自动墙钟秒，固定共同功能验证配置。
- **验收条件**：GUI／无界面各自完成执行链，完成事件对应实际末端；total > 0、0 ≤ seen ≤ total，覆盖率由 100 × seen / total 计算，报告与原始计数一致且正确性对照通过，无界面无窗口依赖。拒绝错误计数、漏报、空统计、提前 TaskComplete 及缺末端；经验证正确的低覆盖率可通过，进程正常退出。
- **证据**：原参数记录、必要修复／配置差异及原因、正确性对照和预期值、完整任务／轨迹链、分析 XML／计数／漏覆盖位置、倍率和两类时间、来源与退出结果；最终配置身份及实际覆盖基线。
- **回退方式**：保留原示例和 T04 流程，撤回本卡分析适配／统计修复或恢复配置；新源码影响产物时走既有重建／发布规则，保留修复前后证据。
- **停止条件**：统计正确性对照失败、完整执行超时或缺证据、同一记录两模式统计不一致且无法解释，或需放宽评分条件才能通过；登记阻塞与最小复现，不进入 T06。低覆盖率本身不构成阻塞。

### G3-T06：稳定性与故障复验

- **状态**：已完成；见 [T06 报告](g3-stability-validation.md)。10 个独立运行、6 项隔离来源、8 项重放对照及总预算检查通过；真实 1 倍、正常收尾、故障 failed 保留和新组实际执行均已验证。
- **目标**：验证冻结配置的重复使用、路径兼容、故障诊断、断线清理和整组重启恢复。
- **前置条件**：T05 两模式全程执行及覆盖统计正确性通过，功能验证配置与产物身份固定。
- **修改范围**：独立重复运行、路径、故障及基础恢复验收，必要的生命周期修复和报告；改动后复验受影响任务。
- **范围边界**：三次短程每次须到实际执行，但不替代 T05／T07 全程；不实现在线自动重连、缓存快照或旧任务续跑，不破坏实际正式包或占用者进程。
- **实施步骤**：连续三次短程闭环正常启停；覆盖其他工作目录和中文空格路径；隔离检查缺失／损坏／混批来源、端口冲突、子进程提前退出和超时；受控断开并保留残包位置，当前运行判失败，清理后以新编号整组重启至真实执行。
- **验收条件**：正常例退出 0、无强制终止且进程／端口回收；故障有定位信息，底层 failed 与上层预期拒绝分开；新运行不混入旧字节、任务、命令或完成事件；环境恢复，无关进程与合格包保持。
- **证据**：重复／路径／故障矩阵、每次运行身份、PID 与端口、退出码、断线尾部及清理、新运行初始化和执行关联、正式包前后哈希。
- **回退方式**：恢复本卡测试和编排改动，保留所有故障及恢复记录；只处理归属明确的进程与副本。
- **停止条件**：重复运行不稳定、退出或端口泄漏、故障被误判成功、残包跨运行污染或路径问题未解决；记录根因和恢复条件，不进入 T07。

### G3-T07：阶段验收与 G4 交接

- **状态**：已完成；最终两模式实际 1 倍全程、本轮 GUI 确认与正常退出通过，见 [T07 报告](g3-stage-validation.md)和 [G4 交接](g3-g4-handoff.md)。
- **目标**：完成最终两模式全程复验、本轮 GUI 人工确认和可复用的唯一连接配置交接。
- **前置条件**：T01～T06 全部通过；最终脚本、业务配置、产物及必要 G1／G2 重建／发布收据完整。
- **修改范围**：G3 阶段验收／GUI 收尾入口、合格配置清单、完整闭环报告、G4 handoff 与状态文档；按资格要求发布受本阶段修改的产物。
- **范围边界**：不建设 G4 网关、不将观察客户端写成网关；不复用旧 GUI 人工确认，不把候选配置或失败运行作为正式阶段交接。
- **实施步骤**：重新复查输入，用冻结业务参数分别完成 GUI／无界面全程；汇总真实状态、请求／规划／执行、可靠完成与覆盖计数；GUI 保留本轮可审查结果，取得人工确认后正常关闭；核对 T06 矩阵和全部来源；登记唯一有效拓扑、模式配置、消息方向、初始化、实际时间源与恢复缺口。
- **验收条件**：两模式各自完成任务且覆盖统计与报告正确，无最低覆盖率要求，GUI 人工确认真实有效，所有本次进程正常退出并释放端口；失败、待确认、强制终止均不算通过；下节阶段结束检查全部满足才登记 G3 完成。
- **证据**：最终两次全程运行编号、配置及来源哈希、执行／覆盖报告、GUI 确认与退出记录、完整矩阵、G4 交接和可复现标准命令。
- **回退方式**：未通过时保留 T06 及此前合格记录、原配置与旧包；不更新正式合格指针或 G3 完成状态，保留待确认／失败记录。
- **停止条件**：任一全程执行、覆盖统计正确性、来源、GUI 确认或正常退出条件缺失；保持实际状态，说明恢复条件，不提前进入 G4。

## 7. G3 阶段结束检查

以下七项已由 T01～T07 证据及本轮人工确认落实；最终运行与交接见 [T07 报告](g3-stage-validation.md)，提交／普通推送的实际结果见工作日志 WL-20260919-010：

1. T01～T07 输入、来源、命令、配置、消息和退出证据齐全；Java／C++／Python 消息同批，必要的上游重建复验已完成。
2. 唯一主链路真实双向通过，来源／过滤和启动顺序明确；400／500 的动态状态进入 UxAS，规划命令进入 AMASE 并实际执行。
3. 冻结的同一业务配置在 GUI／无界面各自走完整个搜索任务；可靠完成链与覆盖统计正确性同时通过，实际百分比如实保存，不设最低值；20 米栅格及完整水道保持。
4. 重复／路径／故障和基础整组恢复通过；每次运行隔离，正常例无强制终止、遗留进程或端口。
5. 本轮 GUI 人工确认及正常退出完成；正式交接只引用合格配置和产物，保留原例、失败候选与历史验收记录。
6. 工作日志、状态、任务清单、阶段报告、总体计划与 AGENTS 同步，提交／普通推送及远程核对结果如实记录。
7. G4 收到唯一连接配置、协议样本、来源／方向矩阵、初始数据和时间语义、完整任务／覆盖报告及缺口清单；自动重连／快照补齐归 G4，重置分段归 G6。


## 8. G4 顺序与任务卡

用户已确认 [G4 实施方案](g4-message-gateway-plan.md)：先两实体，再 20 实体、6 点／8 线／6 矩形区域、固定一架一任务；恢复限于观察链路。G4-T01～T09 已完成，G4 已验收并发布；G5-T01～T06 已完成，T07 可执行，航线／任务／区域图层待后续。以下每卡依次以前一卡通过为前置，独立保留收据；原始运行物放入 out/runs，报告放入 docs。

| 任务 | 状态 | 独立交付 |
| --- | --- | --- |
| G4-T01 输入、环境与契约 | 已完成 | [正式来源、依赖锁与 HTTP／WebSocket 冒烟及路径／拒绝](g4-environment-validation.md) |
| G4-T02 协议核心与真实接入 | 已完成 | [严格核心、两模式真实接入和故障／队列检查](g4-protocol-validation.md) |
| G4-T03 状态模型与持久记录 | 已完成 | [状态、持久重建与真实并发日志修复](g4-state-validation.md) |
| G4-T04 浏览器接口 | 已完成 | [两模式三客户端、快照增量及真实诊断页面](g4-web-validation.md) |
| G4-T05 自动重连与补齐 | 已完成 | [恢复、真实完成补齐及后端拒绝](g4-recovery-validation.md) |
| G4-T06 两实体完整验收 | 已完成 | [原例两模式全程、三客户端及完成补齐](g4-completion-validation.md) |
| G4-T07 混合任务资格 | 已完成 | [三类实际执行、统计对照及正式发布资格](g4-mixed-validation.md) |
| G4-T08 二十实体扩展与稳定性 | 已完成 | [规模诊断、必要修复及验收进度](g4-scale-validation.md) |
| G4-T09 阶段验收与 G5 交接 | 已完成 | [本轮确认、完整复验、正常退出、正式发布与入口验收](g4-stage-validation.md)；[G5 交接](g4-g5-handoff.md) |

### G4-T01：输入、环境与契约

- **目标／前置**：G3 完成；重新核查正式产物和交接，固定可复现的 Python Web 环境及接口。
- **修改范围**：独立 G4 配置、依赖锁、环境／资格脚本、v1 契约及报告，不修改业务源码和生成输出。
- **实施步骤**：调用现有资格接口；绑定 G3 handoff 与正式包；创建项目隔离环境并锁定版本／下载哈希；定义运行身份、来源、时间和对象映射；运行独立 HTTP／WebSocket 冒烟。
- **验收／证据**：安装、导入、HTTP／WebSocket、缺依赖／错来源拒绝及环境恢复通过；记录版本、模型／产物／交接摘要和命令退出码。冒烟不等于真实网关。
- **回退／停止**：失败保留旧环境和收据，仅隔离本次环境；来源失配、Python 不符或依赖不兼容时停止，不用未核查安装替代。

### G4-T02：协议核心与真实接入

- **目标／前置**：T01 通过；建立可复用、有界、只读的两路真实网络接入。
- **修改范围**：协议核心、TCP 接收与原始记录、独立测试／运行入口；不接浏览器控制。
- **实施步骤**：按现有严格规则实现 Sentinel／属性／LMCP 检查；绑定连接、来源和本次运行；分别接收 AMASE 状态与 UxAS 观察消息，保存字节偏移及尾部。
- **验收／证据**：两真实实体、SessionStatus 和 UxAS 消息与后端记录一致；分包／粘包、坏长度／校验、未知类型、超限、残包均受控处理，退出无残留。
- **回退／停止**：关闭本次接收器不影响主任务；不能证明权威来源、无界积压或非法数据进入状态时停止。

### G4-T03：状态模型与持久记录

- **目标／前置**：T02 通过；建立实体、点／线／区域任务、航线、命令、区域与时钟的统一状态。
- **修改范围**：对象归约器、规范化契约、日志／检查点和只读 UxAS 数据库适配。
- **实施步骤**：固定来源优先级与 ID／时间字符串；显式更新／删除／过期；关键事件以日志分片和行号排序去重；按提交边界重建并绑定运行身份。
- **验收／证据**：同记录结果一致；超大 ID、重复／乱序、删除、旧运行污染和不完整日志反例通过；在线短事务读取不会阻塞后端。
- **回退／停止**：保留原始记录，以新目录重新构建；日志不可追溯、重建不一致或将缺口当作完整时停止。

### G4-T04：浏览器接口

- **目标／前置**：T03 通过；提供只读 v1 HTTP／WebSocket 与轻量诊断页面。
- **修改范围**：Web 服务、页面和浏览器契约测试；不引入 Cesium、Node 构建或仿真控制。
- **实施步骤**：同连接快照后接增量；明确 run_id／stream_id／sequence；每客户端有界队列和慢消费隔离；展示连接、过期、时间及真实对象。
- **验收／证据**：晚加入、刷新、快照期间更新、三个客户端一致性、浏览器整数精度和慢客户端隔离通过；HTTP live 与 ready 分开。
- **回退／停止**：仅关闭网关／页面，保留后端；快照缺口、客户端互相阻塞或浏览器动作改变仿真时停止。

### G4-T05：自动重连与补齐

- **状态**：已完成（2026-09-20）。六场景、9 项恢复拒绝与 9 项导航取证检查通过；无界面真实完成跨观察缺口补齐，三个持续客户端通过；主链路／后端退出明确拒绝，新运行整组恢复通过。见 [报告](g4-recovery-validation.md)。
- **目标／前置**：T04 通过；观察链路自动恢复，完整网关重启后恢复本次场景。
- **修改范围**：重连、身份核对、日志补齐、快照切换及故障矩阵。
- **实施步骤**：新连接丢弃旧解析尾部；从已提交日志补齐一次性消息；重建期间标记 recovering，完成后换流身份发布快照；主链路／后端故障明确失败。
- **验收／证据**：两路分别／同时断开、半帧、浏览器刷新、网关重启跨越配置／规划／完成／删除；日志缺失／损坏和旧运行拒绝；原 TCP 字节缺口不伪造。
- **回退／停止**：保留失败与恢复记录；仅恢复完整性可证明的流，关键事件缺失或误续跑后端时停止。

### G4-T06：两实体完整验收

- **状态**：已完成；[两模式完整任务报告](g4-completion-validation.md)，实际 1 倍、完成跨缺口补齐、统计独立对照及正常退出通过。
- **目标／前置**：T05 通过；原例两模式完整任务接入网关。
- **修改范围**：独立 G4 编排／验收与报告；复用 G3 业务配置、统计及执行关联。
- **实施步骤**：当前资格复核、初始化屏障、单次任务／请求、真实规划执行、可靠完成及覆盖独立对照；插入浏览器／观察恢复；正常关闭。
- **验收／证据**：GUI／无界面均完整完成，实际 1 倍、原水道／20 米保持，统计正确且无覆盖门槛；网关对象与源一致、无重复任务或命令执行、退出／端口通过。
- **回退／停止**：保留原 G3 合格包与失败 G4 记录；任务链、统计或正常退出不通过时不进入混合任务。

### G4-T07：混合任务资格

- **状态**：已完成（2026-09-20）。三类单例及混合真实全程／独立审计、当前 GUI 确认与正常退出、正式发布及来源资格 g4-t07-qualified-20260920-081353-913195 全部通过；见 [报告](g4-mixed-validation.md)。
- **目标／前置**：T06 通过；当前 Windows 正式组合支持点、线和矩形区域真实任务。
- **修改范围**：独立场景副本、三类适配／统计与通用任务关联，运行副本桥白名单和每实体航点管理服务。
- **实施步骤**：依次验证三类独立单例，再验证三实体各一任务；等待 TaskInitialized 后逐项单实体请求，实际执行并行；所有初始状态来自 AMASE。
- **验收／证据**：每类均规划、执行和可靠完成；后发请求不覆盖其他实体计划；点观察时间与线／区域覆盖分开对照，不设效果门槛。
- **回退／停止**：失败冻结在本卡，先最小复现及必要功能修复；不能以静态状态、伪完成或调参寻优绕过。业务修复按来源规则重建／发布。

### G4-T08：二十实体扩展与稳定性

- **状态**：已完成（2026-09-20）。当前正式组合的 20 实体两模式各 30 分钟、40 组逐架执行／完成证明、三类统计、恢复、资源边界和正常退出均通过；见 [报告](g4-scale-validation.md)。
- **目标／前置**：T07 通过；20 架真实实体各自执行独立混合任务，验证规模与恢复。
- **修改范围**：独立 20 实体场景／清单、并发运行与资源采样及验收矩阵。
- **实施步骤**：400／500／600～617，8 线／6 点／6 矩形，固定一架一任务与空间网格；矩形 1000×500 米；逐项单实体请求；GUI／无界面分别实际 1 倍到 1800 仿真秒，三个客户端含刷新与慢消费。
- **验收／证据**：逐架执行／完成链全部通过，两模式各 30 分钟，关键事件完整、内存与队列有界；记录机器、采样／推送率、延迟分位数、资源峰值和日志体积。每模式自动预算 2700 墙钟秒，人工确认另计。
- **回退／停止**：保留两实体及三实体合格配置；任一架缺执行／完成、持续泄漏、记录缺口或客户端影响后端即失败，不减少实体数冒充通过。

### G4-T09：阶段验收与 G5 交接

- **状态**：已完成；原两实体两模式、本轮真实 GUI 确认及同版本完整 20 实体受控复验、关闭后独立审计、正式包和日常入口验收通过；原冻结检查失败记录保留。
- **目标／前置**：T01～T08 全部通过；发布可供 G5 消费的真实网关。
- **修改范围**：阶段复验／发布、GUI 收尾入口、协议和运行手册、来源／接口交接及状态。
- **实施步骤**：复核当前来源和前置证据，最终两实体与 20 实体复验，生成可审查 GUI 结果；取得本次人工确认，正常退出并发布合格包。
- **验收／证据**：完整任务与恢复矩阵、当前 GUI 确认、正常退出／端口释放、规范化与原始数据对照及来源绑定齐全；确认未取得时保持 awaiting-gui。
- **回退／停止**：未通过不更新合格指针；保留旧包／失败记录。历史确认、自动审查、强制终止或部分任务不能代替本次通过。

## 9. G5 顺序与任务卡

用户已确认 [G5 实施方案](g5-cesium-display-plan.md)，现按后续决定与 T01～T06 证据修订：原全球矢量＋USGS 区域主地形、Copernicus 只作对照；WGS84 经纬度／EGM96 正高共同网格派生 AMASE DTED 和 Cesium 椭球高度场，区域外仅作未验收参考椭球浏览。G5-T01～T06 已完成；T07 可执行，T08～T11 待前置。以下任务依次以前一卡通过为前置，一次只推进一个主要实现任务。

原始地理数据只读，派生产物及收据放入 out；每卡独立验收、追加工作日志并同步状态，再按持续授权提交、普通推送及核对远程。G4 的零高程运行保持历史资格，不能替代 G5 真实地形验收。接口、坐标／时间、5 公里边界、100 米仿真净空及场景高度适配统一按方案执行，不开展覆盖率寻优。

| 任务 | 状态 | 前置与独立交付 |
| --- | --- | --- |
| G5-T01 环境与输入基线 | 已完成 | [环境报告](g5-environment-validation.md)；固定工具／依赖、正式来源复查、两类路径构建、真实 Edge 及 14 组矩阵 |
| G5-T02 地理数据与高程资格 | 已完成 | [地理检查报告](g5-geography-validation.md)；USGS 主地形、WGS84 统一经纬度、同源 DTED／Cesium、Copernicus 对照及八组正式验收通过 |
| G5-T03 地图与本地资源服务 | 已完成 | [地图报告](g5-map-validation.md)；两类路径各十组验收、本地矢量／地形／字体、在线影像、Range／代理及断网浏览 |
| G5-T04 真实地形后端资格 | 已完成 | [地形后端报告](g5-backend-terrain-validation.md)；三份场景及 +390 米固定适配、两模式三类任务、独立执行／统计、拒绝及正常退出通过 |
| G5-T05 前端状态与真实接入 | 已完成 | [状态接入报告](g5-state-validation.md)；32 项检查、两模式真实首次／晚加入／刷新、独立帧重放和暂停快照对照、联合入口及正常退出 |
| G5-T06 实体、姿态与时间 | 已完成 | T05；OSGB 选用资产转换、模型／轨迹／详情／视角、位置姿态和时间校准 |
| G5-T07 航线、任务与区域 | 可执行 | T06；完整规划、当前命令、执行证据、任务／区域图层 |
| G5-T08 生命周期与恢复 | 待前置 | T07；删除、过期、完成保留、观察及网关恢复与后端隔离 |
| G5-T09 两实体完整联调 | 待前置 | T08；原例地形副本两模式完整执行、统计及正常退出 |
| G5-T10 二十实体与稳定性 | 待前置 | T09；混合 20 实体两模式各 30 分钟、三客户端及恢复 |
| G5-T11 阶段验收与发布 | 待前置 | T10；最终来源复核、本轮 Cesium 人工确认、发布及 G6 交接 |

### G5-T01：环境与输入基线

- **状态／前置**：已完成；G4 来源及日常入口复查、独立工具、最小构建和 14 组验收 passed，见 [环境报告](g5-environment-validation.md)。
- **目标／修改范围**：建立 Windows 原生前端及独立地理处理环境、输入清单、最小前端工程、准备／构建入口、依赖锁、忽略规则与环境报告；不改 AMASE／UxAS 业务源码或地理数据。
- **实施步骤**：复查合格后端、生成模型、网关包与日常入口；锁定 Node 24 LTS 的具体版本、TypeScript／Vite／Cesium 及所需地理工具和完整依赖；登记原始数据位置、来源、工具摘要与本机 8000／8080／5173 端口，原始数据和压缩包不入 Git。工具按项目进程启用，地理工具环境与 G4 Python 环境隔离。
- **验收／证据**：普通、中文空格路径和不同工作目录的最小构建及 Cesium 静态资源加载通过；记录实际工具／依赖版本、来源摘要、输入身份与退出码，验证缺依赖、错来源和端口冲突拒绝。最小页面不作为真实联调证据。
- **回退／停止**：失败只隔离本次工具或候选，保留现有环境与数据；来源失配、下载校验失败或依赖不兼容不得进入 T02，不用修改信任摘要或持久 PATH 绕过。

### G5-T02：地理数据与高程资格

- **状态／前置**：已完成；T01 已通过。原始 z10 异常历史保留，用户选定 USGS 主地形、Copernicus 对照。正式准备及八组验收通过，含全量 PMTiles 摘要／索引、独立复合坐标转换、标准化节点、同源 DTED／Cesium、边界及坏数据拒绝；后端真实地形飞行／统计仍归 T04。见 [地理检查报告](g5-geography-validation.md)。
- **目标／修改范围**：新增地理资源清单、只读校验及派生工具、独立样点测试与报告；建立验收区域同源地形，不改原始包。
- **实施步骤**：核验 PMTiles 头部／索引、代表性瓦片、边界和文件摘要；检查 DEM 解码、XYZ 方向、像素中心、缺失／坏片及接缝。核实验收区域实际来源与垂直基准，固定 USGS 原生输入和匹配的水平／垂直转换资源，Copernicus 统一基准后单独对照，生成 WGS84 经纬度／EGM96 正高的标准化高程栅格、DTED Level 1 与 Cesium 高度场，登记采样和插值方法。
- **验收／证据**：独立参考样点、跨瓦片边界、正负高程格式／数值检查、AMASE 负高程拒绝、重复改正拒绝及损坏／缺失反例通过；实际区域 DTED 为非负，不能登记 AMASE 负高程兼容已通过。保存输入／派生摘要、覆盖范围和转换清单，区分源／派生间距、DTED 量化、转换模型不确定性与数值误差；最近邻、双线性和网格插值分别核查。
- **回退／停止**：未知垂直基准、缺失转换网格、验收区域不完整或不一致时停止资格流程，不以零高程或未声明的其他级别静默替代；失败派生物保留独立编号。

### G5-T03：地图与本地资源服务

- **状态／前置**：已完成；T02 同批候选及收据已复查，普通／中文空格路径构建及各十组正式验收通过，见 [地图报告](g5-map-validation.md)。
- **目标／修改范围**：新增本机页面／资源服务、PMTiles 读取与有界工作线程渲染、地形和影像适配器、地图图层操作及资源测试；不增加仿真控制接口。
- **实施步骤**：按方案配置页面 8080、网关代理 8000 和开发服务 5173；本地支持有界 Range／工作线程读取 PMTiles，接入 T02 椭球高度场，区域外仅作未验收参考椭球浏览。默认本地矢量，按切换加载 Esri 并保留来源。Cesium 资源、Noto CJK 字体及许可已本地化，选用模型资格与打包分别归 T06／T11。
- **验收／证据**：开发及生产包加载、Range 正确性、有界缓存、中文标签／方位、区域地形瓦片共边、图层切换、在线失败回退、断网、资源缺失及端口冲突通过；记录请求、资源摘要及实际渲染器。代理的超大整数／503／1013 使用协议夹具，真实业务接入归 T05；区域裁切边缘不登记为全球地形接缝合格。
- **回退／停止**：请求整包、无界缓存、缺瓦片被伪装成合格地形、生产资源漏包或底图故障影响仿真时停止；保留合格数据，不切换正式前端指针。

### G5-T04：真实地形后端资格

- **状态／前置**：已完成；T03 来源复查、三份场景、两模式三实体实际执行／完成及独立地形／统计通过，见 [地形后端报告](g5-backend-terrain-validation.md)。
- **目标／修改范围**：新增地形场景副本、受控编排、DTED 装载核验和独立地形统计检查；必要后端功能修复按影响重建／复验，原例和 G4 历史收据不变。
- **实施步骤**：按 [方案 3.1 节](g5-cesium-display-plan.md#31-t04-地形场景与后端检查) 复查 T02 双编号及当前后端，冻结平面几何、标准化地形赋高和统一飞行高度适配。先审计 DTED 负值误读及 dlat／dlon 十倍间距对飞行、传感器、分析调用的影响；受影响时修复源码并重建复验，未受影响时保留调用证据及区域限制。绑定本次地形与运行身份，再完成点／线／区域小规模真实规划、执行、完成和统计。
- **验收／证据**：确认 DTED 实际加载、正确间距／边界及非零查询，证明地形参与本轮计算；核查 5 公里外围、100 米仿真净空与 DTED 量化影响，冻结原值／新值／基准／抬升量。任务完成身份及实际航段正确，20 米栅格统计／点观察时间按本次窗口独立一致。两类读取通过不能代替完整调用证据；G4 零高程结果不替代新检查，不设效果门槛。
- **回退／停止**：基准不一致、负高程未获后端兼容资格、间距错误影响计算、地形缺失、实际规划／飞行越界、净空不满足、统计不一致或退出失败时停止。查询边界沿用西／南含、东／北不含；不回退零高程、旧 DEM 或 Copernicus。必要修复后以新来源绑定和运行重验，不调参追求覆盖率或改写旧收据。

### G5-T05：前端状态与真实接入

- **状态／前置**：已完成；使用 T04 合格场景／地形及正式后端，两模式真实接入和独立验收通过，见 [报告](g5-state-validation.md)。
- **目标／修改范围**：新增 TypeScript 协议类型、快照／增量归约器、只读连接、健康状态和运行／资源绑定配置；保持 G4 v1 线协议。
- **实施步骤**：同连接先快照后连续增量，字符串保存 ID／时间／序号并用 BigInt 比较；upsert 替换完整对象、delete 显式删除；处理 ready、503／1013 及身份变化。真实接入当前合格后端／网关，初始化、晚加入和刷新均重新取快照。
- **验收／证据**：真实 G4 归约结果与独立网关快照对照；超大整数、未知版本、旧流、重复／乱序／缺口、业务包早于快照等反例通过；缓存有界且浏览器不发送控制或业务回送。保持地图 ready 与后端 ready 分开，T03 代理夹具不代替本卡首次接入／刷新／晚加入证据。
- **回退／停止**：序号或身份失配即弃用旧流并重建；无完整快照不能显示 live，不能拼接独立 HTTP 快照与另一个 WS 的增量。

### G5-T06：实体、姿态与时间

- **状态／前置**：已完成；[T06 实体验收](g5-entities-validation.md)：UCAV GLB、用户尺寸决定／快捷键、85 个位置／10 组姿态／九项生命周期及两模式真实显示、刷新／正常退出、离线地图回归通过。后续 [阵营色与描边](g5-affiliation-display-validation.md) 已按用户 400／500 蓝方、600 红方完成新候选与两模式复验；[固定屏幕尺寸及立体光照](g5-model-display-validation.md) 已通过近／中／远真实截图、手动缩放和两模式回归；[逐帧平滑移动](g5-motion-validation.md) 已通过真实消息间位置连续变化、跟随、暂停和缓冲边界检查；最新 [金属机体／阵营细描边预览](g5-metal-display-preview.md) 已完成两模式真实截图及回归，外观供用户查看；[近景跟随修正](g5-close-motion-validation.md) 已消除轨迹重建阻塞相机与前后帧尺寸错配，384 px／43 米／旋转／快速重跟随的最终屏幕检查通过。
- **已登记输入**：用户提供 models/ 中 16 个 OSGB，约 154.1 MB；完整摘要及转换／材质／尺寸／模型轴检查安排见 [模型输入登记](g5-model-inputs.md)。本轮选用 AFSIM UCAV 已转换并取得显示资格，用户确认 1 源单位＝1 显示米；其余保持原件登记。
- **目标／修改范围**：实现实体注册表、模型／标签／详情、有界轨迹、跟随与定位、后端时钟和坐标／姿态测试。
- **实施步骤**：按 [模型接入约定](g5-cesium-display-plan.md#25-用户模型接入) 先选适合仿真实体的 OSGB 样本验证转换，再冻结 glTF 2.0／GLB 工具、输入／产物摘要及实体资产映射；检查几何、材质、纹理、单位、轴、原点和离线资源，资产固定修正与姿态分开。按共享高程配置转换位置，保留源值／显示值；后端时间驱动有界逐帧插值，以本地单调时间安排已收到样本的播放，不外推；暂停／断流／结束停止，缓冲耗尽不推进，轨迹为 10 分钟／每实体 2048 样本，新流清空。
- **验收／证据**：选用模型完成离线加载与单位／尺寸／轴校准，独立已知点及四向、俯仰／左右滚转通过；数值位置误差 ≤1 米、姿态误差 ≤0.1°，真实属性可追溯。跟随验收同时核对最终屏幕锚点、尺寸与局部相机偏移，覆盖近景／放大／轨迹更新，不能只检查世界坐标连续。若选用 UH-60／AH-64D，记录转换后几何、纹理及内存开销；high／lo／icon 文件名不替代 LOD 验证。暂停、样本不足、缺口、跟随／复位正确，不超出已接收时间；未选用原件保持登记，不新增对应仿真能力或场景要求。
- **回退／停止**：模型转换失败、选用资产缺依赖、单位／模型轴、高程或时间语义不清时停止该显示资格；保留原件和失败候选，不以改后缀或屏幕观感冒充转换／校准，不把地形精度写成数值转换精度。

### G5-T07：航线、任务与区域

- **状态／前置**：可执行；T06 已通过，使用其共同高度配置与实体模块。
- **目标／修改范围**：实现完整计划、航点、当前命令、有证据的执行航段、三类任务、区域及属性／关联查询；不加入编辑、量测或任务注入。
- **实施步骤**：完整 planned_mission 与分段命令分别存储和展示；执行状态以实际消息关联为依据。点／线／矩形任务和 KeepIn／KeepOut／OperatingRegion 按契约绘制，逐类登记原高度语义并使用 T04／T06 共享转换配置，禁止重复加地面高程或 N96；显示高度范围、状态和关联，接入图层、选择、定位与视角复位。
- **验收／证据**：三类任务几何、高度、身份及状态逐项与源定义对照；后发命令不缩短完整计划，收到／执行／完成不混用，TaskComplete 不删除任务或冻结实体；独立区域与删除样本明确标注来源。
- **回退／停止**：几何、关联或执行证据不足时显示未确认，不以规划响应、预测时间或视觉位置推断真实完成；业务对象和底图要素不能混用身份。

### G5-T08：生命周期与恢复

- **状态／前置**：待前置；T07 通过。
- **目标／修改范围**：完善前端恢复状态机、对象／样本清理与独立恢复验收；沿用网关旁路观察和后端独立生命周期。
- **实施步骤**：验证合法删除、过期和任务完成保留；覆盖晚加入／刷新／重连、两观察链路分别及同时断开、网关重启和慢客户端。清理旧 Viewer／检查句柄及业务样本，每次恢复重新快照；保留绝对浏览器 profile、销毁状态检查及截图失败仍执行 Browser.close 的回归，不回送任务或保留旧插值轨迹。
- **验收／证据**：503／1013 期间清晰标为恢复中／过期且时间冻结；新流完整状态、完成身份与时钟正确，旧对象不复活，最终与独立快照一致。真实后端下验证浏览器／网关及在线影像故障不停止后端、不重复任务；所属进程正常退出，T03 夹具或强制收尾不能代替。
- **回退／停止**：后端退出、主任务链路或运行身份改变时停止旧运行消费，必须整组新运行；恢复失败保留原缺口与日志，不虚构丢失的原始消息或历史轨迹。

### G5-T09：两实体完整联调

- **状态／前置**：待前置；T08 通过。
- **目标／修改范围**：新增原 WaterwaySearch 地形副本两模式完整联调、真实浏览器对照与独立审计报告；保留 400／500、任务关系及完整水道。
- **实施步骤**：复查正式来源与本次地形资格，GUI／Headless 后端顺序以实际 1 倍运行完整任务；核查规划、命令、实际航段、正确身份 TaskComplete、地形统计及 Cesium 状态，覆盖刷新／重连并正常收尾。
- **验收／证据**：两模式真实完整链通过，前端和后端对象／时间可追溯，20 米统计独立一致；本次来源不变、所属进程正常退出和端口释放。旧 G4 运行、静态样本或单纯截图不能替代。
- **回退／停止**：任务、统计、恢复或退出任一失败即保留失败记录，不进入规模验收；原合格 G4 包和零高程配置可用于独立回归，不冒充本卡通过。

### G5-T10：二十实体与稳定性

- **状态／前置**：待前置；T09 通过。
- **目标／修改范围**：新增 20 实体地形副本、三客户端编排、浏览器／后端资源采样与规模审计；保留 8 线／6 点／6 区域、固定一架一任务及平面网格。
- **实施步骤**：GUI／Headless 后端分别真实 1 倍到 1800 仿真秒，任务完成后继续真实终端飞行；三个客户端覆盖普通查看、刷新／晚加入及慢消费，并纳入观察／网关恢复。每模式自动预算 2700 墙钟秒，人工等待另计。记录实际渲染器、视角、分辨率、图层／模型组合、源更新率和采样方法；跟踪地图／模型／业务缓存及 JS 堆、浏览器／服务进程、GPU 资源，不能用 T03 单次堆快照代替。
- **验收／证据**：逐架规划／执行／完成及统计独立一致；两模式各 30 分钟、资源有界、界面可操作、关键事件完整、恢复后状态一致，正常退出／端口释放。区分 Cesium 未用瓦片缓存目标和可见 GPU 对象，核查选用模型的重复加载／释放；记录 FPS、延迟分位数、CPU／GPU、内存及恢复时长，约 30 FPS 为目标，无硬 FPS 或 250 ms 门槛。
- **回退／停止**：持续泄漏、任一架缺执行／完成证据、数据不一致、地形越界或客户端影响后端即失败；不能降低实体数、缩短时长或引用 G4 零高程结果冒充通过。

### G5-T11：阶段验收与发布

- **状态／前置**：待前置；T10 通过且 T01～T10 独立证据齐全。
- **目标／修改范围**：实现最终来源复核、候选／资格／发布及正常收尾入口，形成运行手册、阶段报告与 G5 → G6 交接。
- **实施步骤**：核查前端、地理派生、运行入口与后端／网关、转换网格、数据／模型版本绑定；打包本地 Cesium 资源、Noto CJK／许可及选用模型全部依赖。最终组合复验，准备可审查的本轮页面、真实任务及自动结果；取得本次人工确认后正常退出，复核来源与收据，再发布合格包及指针。
- **验收／证据**：自动检查、当前页面人工确认、完整任务及恢复证据、正常退出／端口释放和生产入口消费均通过；前端、地理派生和运行入口分别有资格，交接高度／时间规则、实际性能及限制。
- **回退／停止**：本卡已进入人工确认环节但未确认时保持 awaiting-gui，不回写已独立通过的 T01～T03；自动截图、历史确认或强制退出不能替代。来源失配须按影响新建候选重验，未全部通过不切换正式指针，不改写下载／失败收据；扩大区域先重验地理及后端。第二机器和离线卫星影像包仍归 G8。

## 10. 后续关卡与延期事项

| 关卡 | 状态 | 入口与继承问题 |
| --- | --- | --- |
| G3 原系统闭环 | 已完成 | [阶段报告](g3-stage-validation.md)；两模式全程、本轮 GUI 确认与正常退出通过 |
| G4 消息网关 | T01～T09 已完成并发布 | [阶段方案](g4-message-gateway-plan.md)；两实体 → 三类资格 → 20 实体混合任务、观察恢复及三客户端 |
| G5 Cesium 显示与同源地形 | T01～T06 已完成，T07 可执行 | [修订方案](g5-cesium-display-plan.md)；[地图验收](g5-map-validation.md)；[后端地形验收](g5-backend-terrain-validation.md)；[状态接入](g5-state-validation.md)；[实体显示](g5-entities-validation.md)；航线／任务／区域待验 |
| G6 仿真控制 | 待细化 | 开始／暂停、倍速、重置；真实后端反馈与运行隔离；只读进度条显示当前仿真时间和进度，不含拖动回放 |
| G7 记录回放 | 待细化 | 记录格式、快照索引、一致性验证 |
| G8 部署与离线验收 | 待细化 | 第二个干净目标环境与指定断网演示场景 |

2026-09-21 用户确认本轮先完成 T06 配色与描边，控制按后续任务实施。G6 时间进度条只显示当前权威仿真时间及进度；总时长必须来自已确认的场景配置，无有效总时长时只显示时间。历史拖动回放仍归 G7，不因此提前实施；下一张卡保持 G5-T07。

覆盖效果优化另列后续任务：使用 G3 的真实运行与覆盖基线评估规划算法、飞行／传感器参数及自动寻优方案；不作为 G3、G4 或当前 T02 的隐含前置，不宣称现有系统具有按覆盖指标自动调优能力。

延期事项：Zyre／串口桥的 Windows 启用构建和运行验收另立任务；保留源代码及原依赖声明，本轮不获取可选依赖、不宣称启用可用。由实际业务需要决定后续归属，不阻塞本轮默认配置的 G2 验收。

精确上游版本追溯在需要升级源码时另建任务，使用 G0 的本地 tree 与关键哈希对比；不作为 G1 本地构建的无边界前置工作。
