# G5-T06 金属材质与阵营细描边预览

日期：2026-09-21，Asia/Shanghai。用户要求先查看金属质感、阵营色细描边的效果。本轮交付可运行的外观预览及真实截图；自动显示检查通过，外观是否合意由用户后续反馈，不将其登记为 T11 人工确认或正式前端发布。

后续近景抖动已定位修正，当前可启动候选见 [近景跟随修正](g5-close-motion-validation.md)；下文材质候选与截图保留历史。

## 当前外观

- 机体采用银灰金属，主体不再染成阵营色；保留原面板、舱盖纹理细节。运行期材质以金属度 0.92、粗糙度 0.30 计算 GGX 高光、Fresnel 反射及明暗，原 OSGB／GLB 字节不变。
- 使用相对观察视角固定的解析展示环境，包含冷色环境与柔光反射面，不依赖外网环境贴图。不代表真实太阳、地面物体反射或传感器成像。
- 描边始终使用阵营颜色：400／500 青蓝，600 红；中立绿、未知黄及后端明确阵营优先的规则保持。普通宽度 1 像素，选中 1.5 像素，选中时不再换白边。
- 默认 96 CSS px 包围直径、+／−／0 快捷键、48～384 px 范围及逐帧平滑移动保持；标签、列表和轨迹仍用阵营色。

实现：[display.ts](../apps/cesium_entities/display.ts)、[layer.ts](../apps/cesium_entities/layer.ts)、[阵营配置](../config/g5-entities.json)。共享 CustomShader 在实体层销毁时释放。已核对锁定 Cesium 1.145.0 的 czm_modelMaterial／czm_pbrLighting／色彩转换实现及 [CustomShader](https://cesium.com/learn/cesiumjs/ref-doc/CustomShader.html)、[ModelGraphics](https://cesium.com/learn/cesiumjs/ref-doc/ModelGraphics.html) 官方接口。UNLIT 通道仅避免引擎对自算光照再次叠加场景灯光，不是无明暗的纯色输出；HIGHLIGHT 使用白色保持金属主体，不叠加阵营染色。

## 候选与检查

| 项目 | 记录 |
| --- | --- |
| 正式来源复查 | g3-t01-check-20260921-121235-215 |
| 中文候选 | g5-t06-build-20260921-133544-913 |
| 普通候选 | g5-t06-build-20260921-133636-599 |
| 两模式／地图验收 | g5-t06-test-20260921-133556-243，passed |
| acceptance SHA256 | 162b3bb9349d9bbe850d6866a43be680192abebf8d0ac7bab31675902ec87827 |

使用已固定的 Python 3.14.7、Node 24.21.0、Cesium 1.145.0 和模型工具；不安装依赖，不修改正式后端、G4 或 T02～T05。入口仍为 build-g5-entities.ps1／tests/windows/g5-entities.tests.ps1，绑定上述 BuildRunId 和基线。

实际浏览器逐模型核对银灰材质的白色乘色、阵营描边颜色及 1／1.5 像素宽度；后端 Unknown 保持不变，动态后端阵营优先和选择状态也经生命周期检查。截图检查先关闭标签／轨迹，再在同一暂停位置、同一相机下成对拍摄模型隐藏／显示画面，差分提取真实机体，避免把地图背景或仅有彩色边缘当成金属主体。

| 后端模式 | 蓝方放大图中性机体像素／全部模型像素 | 青蓝边像素 | 中性机体亮度跨度 | 红方放大图中性机体像素／全部模型像素 | 红边像素 |
| --- | ---: | ---: | ---: | ---: | ---: |
| headless | 2432／2816 | 383 | 61 | 3361／3735 | 374 |
| gui | 2790／3189 | 399 | 49 | 3288／3667 | 379 |

亮度跨度只统计中性机体像素的第 90／10 百分位差，排除阵营高亮边缘，不用描边替代机体明暗证据。蓝方放大图实际预览宽 192 CSS px 包围直径，截图中的可见轮廓随姿态变化。近／中／远距离及两档缩放通过。原坐标／姿态／高度、消息间逐帧移动与跟随、暂停、刷新、断流、快捷键和资源释放回归通过；离线地图、Range、缓存、接缝、端口冲突及客户端重置通过。所有测试所属进程正常退出。

本机 Edge 153.0.4234.48，实际 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11。两模式实际截图保存在各自 browser/metal-blue-preview.png、metal-red-preview.png，已查看 Headless 红蓝近景。短程显示检查不替代 T09／T10 全程或性能验收。

## 调整过程与限制

首候选 g5-t06-build-20260921-133137-581 的首运行 g5-t06-test-20260921-133149-268 在暂停对象全字段比较失败，正常收尾。该版未保存第二次对象明细，不能确定差异字段或归因于材质；补充 frozen 诊断记录，保持原严格判据。新候选 g5-t06-build-20260921-133312-180／验收 g5-t06-test-20260921-133324-991 两模式通过，原失败未覆盖。

中间截图的金属主体偏暗，因此提亮解析环境和柔光反射。同步改进截图测量：仅凭全部模型像素亮度差可能把彩边混入，最终改为单独检查中性机体，保留原 18 亮度跨度／25 色阶要求。最终候选重新完成两模式检查，暂停诊断记录也保留；前述一次暂停差异没有复现，未将未查明原因写成已修复问题。

新外观替代上一版主体荧光染色与深色／白色粗描边；旧报告作为历史保留。最终来源及普通／中文生产文件一致性见本轮 final-audit.json。下一张任务仍为 G5-T07，控制按钮和倍速继续归 G6，当前演示入口仍在任务完成后自动暂停。

当前预览使用普通候选，会话 g5-t06-session-20260921-133958-135，启动器 out/runs/g5-metal-preview-20260921-133957-915/，PID 32436。8080 代理 ready、1 倍速、3639→5239 ms 及三实体位置变化已实际核对，刷新 http://127.0.0.1:8080 查看；可按 + 放大模型。新预览仍运行，正常结束创建本次会话 request-stop；任务完成后自动暂停，不登记本次新预览正常退出或人工确认。前一 g5-t06-session-20260921-132132-134 已在修改源码前正常结束。
