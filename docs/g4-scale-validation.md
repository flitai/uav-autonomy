# G4-T08 二十实体扩展与稳定性

日期：2026-09-20，Asia/Shanghai。**已完成。** 前置 [T07 正式资格](g4-mixed-validation.md) 已完成并归档 d43821b。

固定映射为 400／500／600～605 八项线搜索，606～611 六项点搜索，612～617 六项矩形区域；任务 ID 按顺序 3000～3019。全部沿用 T07 已资格的场景平移、完整水道、1000×500 米矩形及飞行／传感器配置；场景为明确测试用途。每架真实配置及至少五个动态状态齐全，逐项 TaskInitialized→单实体请求→非空响应，执行并行。

计划独立运行 Headless、Gui，各实际 1 倍到 1800 仿真秒；每模式自动墙钟预算 2700 秒。三个持续消费者为正常、10 秒刷新、每增量延迟 150ms 的慢客户端。计划在 60／900 秒重启整个网关；120 秒 AMASE 半帧断开、140 秒恢复；240 秒 UxAS 观察断开、260 秒恢复；700～780 秒同时断开两路观察以跨越线任务完成。主任务链保持独立；补齐期间 snapshot 必须 503，原始字节缺口保留。

新增编排按增量读取日志，原始捕获落盘后清理编排内存中的已消费行；网关保持原有有界接收／发布队列。每 5 秒采样 AMASE、UxAS、网关、三客户端进程和编排器的私有内存、工作集及 CPU；CPU 100% 表示一逻辑核。声明网关私有内存上限 1 GiB、Java 最大堆 2 GiB；每客户端仍为 256 条／8 MiB。记录更新／推送率、延迟分位数和日志体积，无 250ms 延迟门槛。

## 已发现的规模兼容问题

首次 60 秒诊断 `g4-t08-diagnostic-20260920-082105-760256` failed。20 架真实初始状态齐全后，数据库分片 messageLog_1_0.db3 第 203 行 CreateNewService 包含 20 个 EntityConfigurations 和 20 个 EntityStates，而 UXNATIVE.xml 分别限制为 16；网关严格解析拒绝，错误为 XML array exceeds limit。此时只注入了单实体 AutomationRequest，该请求不是超限来源。所有原始记录保留，不能将诊断登记为稳定性通过。

TaskManagerService 原先无条件将全体实体装入新任务服务。必要修复仅针对显式 EligibleEntities 的 CMASI 点／线／区域三类任务，在全局实体超过 16 时按该集合初始化；小规模和其他任务保持原流程。若仍超过模型上限，明确拒绝创建，不能截断数据或放宽协议校验。TaskServiceBase 原有配置处理本就按任务 EligibleEntities 筛选；其他任务可能需要非参与实体作为目标，因此不做全类型过滤。没有改 MDM、生成库、算法或飞行参数。

新 UxAS 构建 g2-t05-build-20260920-082330-977、双路径候选验收 g2-t05-test-20260920-082558-938、HelloWorld g2-t06-test-20260920-082835-289、发布复验 g2-t07-test-20260920-083019-966 及正式发布 g2-t07-publish-20260920-083245-918 均通过。AMASE 保持 T07 正式包；inputRevision=5 保留历史，仍为 45 项冻结输入。首次资格 g3-t01-check-20260920-083353-364 因旧资格入口要求大写摘要而失败，规范配置中的十六进制大小写后，新资格 g3-t01-check-20260920-083505-519 passed；没有替换信任字节或修改旧收据。

Web 环境复验 g4-t01-20260920-083609-418 passed，六项错误规模来源链检查 g4-t08-lineage-20260920-083609-845796 passed。最小复现及加速统计对照 g4-t08-controls-20260920-083203-248470 passed：原 20 项服务初始化继续被严格拒绝，地理包围盒提前排除后的独立计算与 T07 四场景结果完全相同；各可能覆盖点仍使用原独立几何判定。此优化仅用于验收工具。

## 证据边界

60 秒诊断仅检查规模初始化、逐架规划、客户端和重启／收尾，不计作完成任务或 30 分钟验收。正式全程还须独立核对每项实际任务航段、内部导航、末端到达、同身份完成、三类统计和持续运行记录。T09 最终阶段及当前 GUI 人工确认另行进行。

修复后诊断 g4-t08-diagnostic-20260920-083628-695397 中，20 项真实服务初始化经独立数据库核对均仅包含对应的一架配置／状态，20 项非空规划、网关三实例、最终三客户端同状态及后端正常退出均已得到证据；整轮仍为 failed，因为慢客户端只经历两个流身份，旧三实体测试强制要求每客户端看到全部三个实例。该慢客户端最终已确认相同快照，过程中跳过一个中间实例符合“重连取当前快照”的契约。T08 检查改为普通／刷新客户端至少三个流，慢客户端至少两个流，仍必须全程有序、同运行身份和最终明确边界确认；不要求看到已关闭的每个中间实例。新诊断 g4-t08-diagnostic-20260920-083857-868424 进行中，旧结果不改写。

上述新 60 秒诊断已 passed，20 项规划、两次网关重启、三客户端最终状态及正常退出／端口释放全部通过；实际 SessionStatus Running／Paused 倍率均为 1。记录隔离测试另从这轮真实 3800 行日志建立副本，验证 20 个实体／任务／航线、重复去重、持久重开、修改行拒绝、旧运行拒绝、慢队列隔离、20 项删除重建、缺失／损坏源和规范化记录拒绝共 13 项；合成删除仅用于隔离归约器测试，未发往真实后端。首次夹具在固定锚点后未重建 Journal，按预期触发 binding changed；修正夹具生命周期后重跑通过，网关代码不变。

首轮长跑 g4-t08-capture-20260920-084111-252471 在 12 项任务报告完成后主动正常收尾并保留 failed：资源检查发现 Windows venv/python.exe 是启动器，原采样漏掉实际解释器子进程，不具备有效的内存／CPU 证据。新采样使用 Windows 进程快照追踪各所属进程树，含实际解释器和 conhost；编排器统计排除其他已单列的所属树，避免重复计量。真实 32 MiB 分配子进程检查 g4-t08-process-checks-20260920-084544-156531 passed，确认启动器 PID 与解释器 PID 不同、实际分配计入、正常退出。原长跑不计入 30 分钟，通过新编号重新计时。

第二轮 g4-t08-capture-20260920-084606-755741 在 UxAS 观察恢复时 failed，后端正常退出。严格对照发现 200 个待关联关键哈希全部在数据库中，首项为第 19227 行；网关正在重建的边界仅到第 19223 行。重建耗时超过 10 秒后，原实现使用重建结束时钟判缺失，尚未读取的新边界事件被误拒绝。修复为以已完整消费边界的取得时刻判断日志缺失；若仅重建耗时导致陈旧，继续 recovering、快照不可用，后续读取新边界再核对。10 秒真实日志缺失保护保留。

同时为规范化持久库增加消息哈希索引：关键消息即使离开 4096 项内存缓存，仍可按索引核对完整记录摘要与运行身份，不重复应用事件；损坏仍拒绝。真实失败记录检查 g4-t08-critical-checks-20260920-085415-846073 的七项通过，包括旧边界保持恢复、全部 200 哈希补齐、缓存淘汰后的查找、真实缺失和记录损坏拒绝。13 项隔离状态／损坏矩阵再次通过。规模编排允许恢复最多 120 秒（总预算仍 2700 秒），等待期间持续采样进程树，避免漏记重建峰值；这不是放宽完整性或延迟指标。

既有状态／恢复／发布回归 g4-t08-regression-20260920-085650-501657 passed：32 项状态与历史日志检查、9 项恢复拒绝、8 项快照并发和慢队列检查。当前新完整取证为 g4-t08-capture-20260920-085508-125483，从 out/tmp 工作目录调用绝对入口；尚未结束，不登记稳定性通过。

该轮 Headless 于 09:26 完成取证，案例 passed：最后源时钟 1800009 ms、356 次资源采样、719 项文件证据，20 项完成报告，三客户端最终同摘要，全部所属进程正常退出与端口释放。普通／刷新／慢消费客户端分别接收 69426／66915／10714 条有序增量；普通六次快照、刷新 152 次、慢消费 31 次，均确认相同暂停边界。断开次数包括恢复期间尚未就绪而收到 health 后关闭的重试，不应解释为后端故障次数。GUI 运行及逐任务独立审计尚未完成。

两模式完整取证已于 09:57 passed，父收据 SHA256 `a0a11e9838202278bd2955107c82d90d9e9a593d8cdf64eb700c9b2eb0c778fc`。Gui 也达到 1800009 ms、357 次资源采样、正常退出与端口释放。独立缺口核对 g4-t08-gap-checks-20260920-095810-716470 passed：每模式 8 个线任务的完成均不在断线观察 TCP 中，20 项完成在持久库中唯一，780 秒恢复快照中的线任务身份与时间逐项一致。端口冲突 g4-t08-port-checks-20260920-095101-363156 passed，第二实例在建立观察连接前拒绝，现有实例继续运行。

首次独立审计 g4-t08-audit-20260920-095806-639763 的无界面 20 项执行与统计已通过，但整体 failed 于性能夹具：把 AMASE 连接时的 Time=0 初始快照到首个动态状态的 2040 ms 当作连续运行采样缺口。两模式所有超 2000 ms 间隔均只发生在此启动转换；动态间隔另行核对。修订只涉及 audit.py 和 performance.py，原字节存于 out/build/g4-source-revisions/g4-t08，原收据不改；其余采集／网关／业务源码仍必须与原收据完全一致。执行与统计函数通过 AST 等价核对，不放宽任务证明。初始转换单列统计，动态区间仍要求严格递增、最大间隔 2000 ms 及覆盖整轮。

八项审计修订检查 g4-t08-audit-revision-20260920-100536-769672 passed，包括真实动态缺口、重复零时刻、缺少初始快照、运行源码变化及未知审计前驱拒绝。新审计 g4-t08-audit-20260920-100537-000770 仅按固定摘要复用首次审计已通过的无界面执行／统计文件，重新做性能核对并继续 GUI 全审计；不会将先前整体 failed 改为 passed。

## 运行与证据入口

从任意工作目录调用入口时使用脚本绝对路径。下例从仓库根运行；不传 BaselineRunId 时入口先执行新的正式来源检查。

```powershell
$pythonExe = Join-Path $env:LOCALAPPDATA 'Python/pythoncore-3.14-64/python.exe'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\run-g4-scale.ps1 -PythonExecutable $pythonExe -Mode Both
# 仅检查 60 秒初始化与重启；不能替代完整验收。
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\run-g4-scale.ps1 -PythonExecutable $pythonExe -Mode Headless -Diagnostic
# 完整取证结束后，将占位符替换为本次 passed 编号，执行独立审计。
& $pythonExe -I -B -X utf8 .\scripts\g4_scale\audit.py --root . --run-id '<G4_T08_CAPTURE_RUN_ID>'
```

每案例包含 scene 清单、原始双向观察记录、AMASE 内部导航／分析、每次网关连接与数据库边界、三客户端最终确认和 resources.jsonl。独立审计逐项关联任务与规划、命令、内部导航、实际航段和同身份完成，另从原始传感器记录重算线／区域覆盖及点观察时间；完成报告数量不是执行证明。性能测量为同机 AMASE 观察收到实体状态至浏览器消费者收到对应增量的时间，包含补齐和慢消费；负值按观察线程竞争单独记录，不伪装成网络端到端延迟，也不采用 250ms 门槛。

## 最终验收结果

独立审计 g4-t08-audit-20260920-100537-000770 passed，taskExecutionValidated／taskCompletionValidated／coverageValidated／stabilityValidated 均为 true；T08 完成，stageQualified 仍为 false，当前阶段 GUI 和网关发布仍待 T09 独立完成。两模式均核对 20 个 CreateNewService，每项只含对应实体的一份配置和状态；每模式 40 次任务／请求注入按屏障顺序、20 项真实规划与执行并行且互不覆盖。

下表每行均已有规划、命令收到、内部导航、真实任务航段、末端到达及同身份 TaskComplete 证据。完成为仿真毫秒；线／区域列为覆盖格数，点列为观察秒数。统计累计到本轮暂停，不把观察时间当作完成时间；所有结果与原生分析一致，不设效果门槛。平移后的线网格因纬度投影离散产生 723 或 724 格，保留完整 90 点水道。

| 实体 | 任务 | 类型 | 完成 ms：无界面／GUI | 统计：无界面／GUI |
| --- | --- | --- | --- | --- |
| 400 | 3000 | 线 | 740709／740709 | 724/724／724/724 |
| 500 | 3001 | 线 | 741769／741769 | 724/724／724/724 |
| 600 | 3002 | 线 | 740709／740709 | 724/724／724/724 |
| 601 | 3003 | 线 | 740709／740709 | 724/724／724/724 |
| 602 | 3004 | 线 | 740709／740709 | 724/724／724/724 |
| 603 | 3005 | 线 | 739639／739639 | 723/723／723/723 |
| 604 | 3006 | 线 | 739639／739639 | 723/723／723/723 |
| 605 | 3007 | 线 | 739639／739639 | 723/723／723/723 |
| 606 | 3008 | 点 | 110410／110410 | 310.021 s／309.441 s |
| 607 | 3009 | 点 | 110410／110410 | 309.461 s／308.411 s |
| 608 | 3010 | 点 | 109910／109910 | 308.951 s／310.011 s |
| 609 | 3011 | 点 | 109910／109910 | 310.021 s／310.011 s |
| 610 | 3012 | 点 | 109910／109910 | 310.021 s／310.011 s |
| 611 | 3013 | 点 | 109910／109910 | 309.481 s／310.521 s |
| 612 | 3014 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |
| 613 | 3015 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |
| 614 | 3016 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |
| 615 | 3017 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |
| 616 | 3018 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |
| 617 | 3019 | 矩形 | 125910／125910 | 1250/1250／1250/1250 |

实测环境：Windows 11 专业版 10.0.26200 x64，Intel Core Ultra 7 265，20 核／20 逻辑处理器，物理内存 67,975,188,480 字节；Python 3.14.7。下列均为本机 20 实体、三客户端、约 5 秒进程树采样的有限运行结果。

| 指标 | 无界面 | GUI |
| --- | --- | --- |
| 自动取证墙钟秒 | 1881.145 | 1884.231 |
| 资源样本数 | 356 | 357 |
| 每实体动态源频率 Hz | 1.8806 | 1.8806 |
| 最大动态采样间隔 ms | 541 | 541 |
| 已发布增量数 | 69751 | 69654 |
| 客户端队列峰值 条／字节 | 256／1925230 | 256／1606343 |
| 慢消费阈值关闭次数 | 11 | 9 |
| 日志字节数 | 2473078128 | 2471800161 |

| 进程树 | 私有内存采样峰值 MiB：无界面／GUI | CPU P95 %：无界面／GUI |
| --- | --- | --- |
| amase | 711.58／1483.09 | 25.45／25.92 |
| uxas | 27.66／26.69 | 13.56／13.75 |
| gateway | 73.3／72.66 | 80.69／98.18 |
| clients | 58.95／63.38 | 3.43／3.72 |
| controller | 57.12／59.4 | 5.58／5.3 |

CPU 100% 为一逻辑核。AMASE 高峰发生于已暂停的分析导出阶段；运行末段约为无界面 240 MiB／GUI 374 MiB，Java 最大堆仍为 2 GiB。网关始终低于本次 1 GiB 验收上限，队列固定有界。此证据覆盖指定 30 分钟，不推断无限运行的内存表现。

| 客户端 | 增量 Hz：无界面／GUI | 延迟 P50 ms：无界面／GUI | P95 ms | P99 ms |
| --- | --- | --- | --- | --- |
| 普通 | 36.91／36.91 | 125.08／131.21 | 226.26／217.72 | 324.78／320.97 |
| 10 秒刷新 | 35.57／35.63 | 124.29／130.62 | 226.56／218.3 | 329.67／349.5 |
| 150ms 慢消费 | 5.7／5.58 | 24861.04／23885.49 | 50594.88／49084.65 | 57205.2／56012.39 |

GUI 普通及刷新消费者各有两个约 -5 ms 样本，来自两条本机观察接收顺序竞争，保留原值及计数；无 250ms 门槛。此延迟不包含没有增量送达期间的停顿，恢复时长另列如下；慢消费者最终通过新快照与所有客户端在相同边界一致。

| 恢复动作 | 从动作前健康检查至恢复快照的墙钟秒：无界面／GUI |
| --- | --- |
| 60 秒整网关重启 | 5.114／5.623 |
| 140 秒半帧观察恢复 | 14.828／15.204 |
| 260 秒 UxAS 观察恢复 | 20.847／21.327 |
| 780 秒双观察恢复 | 42.863／44.729 |
| 900 秒整网关重启 | 53.671／48.368 |

恢复仍须重建本次已提交历史，期间 health 标记 recovering、snapshot 返回 503；上述时长是实际测量，G5 应据健康状态显示恢复进度。原始 TCP 缺失字节保持缺口，日志只补齐语义。
