# G5-T09 原两实体完整任务联调

日期：2026-09-23，Asia/Shanghai。**G5-T09 已完成；T10 可执行。** 本卡复用已合格的 Cesium 覆盖候选 `g5-coverage-build-20260922-140926-685`、T08 恢复收据 `g5-t08-test-20260923-223641-852`、正式 G4 网关／后端，以及 T04 冻结的原 WaterwaySearch 真实地形副本。未修改页面、UxAS、AMASE、地形或正式发布指针。

## 输入、入口与任务语义

地形副本保留原场景的 400／500 两实体、任务 1000 的 90 个水道点、一次原始自动规划请求和 785 秒场景时长；只按 T04 已验收规则为飞行高度统一增加 390 米，并为任务地面点赋同源 DTED 正高。两架飞机均初始化并持续送出真实状态；原始规划请求由 UxAS 自主选择执行者，不强制一架一任务。本轮实际规划选择 500，生成非空规划，完成消息也应只列出 500。400 的实际状态仍须保留和核对。

```powershell
$pythonExe = Join-Path $env:LOCALAPPDATA 'Python/pythoncore-3.14-64/python.exe'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\g5-full.tests.ps1 `
  -PythonExecutable $pythonExe -BuildRunId g5-coverage-build-20260922-140926-685 `
  -LifecycleRunId g5-t08-test-20260923-223641-852
```

入口自行复查前置基线；本轮复用已通过的 `g3-t01-check-20260923-214731-692` 时显式给出 `-BaselineRunId`。编排见 [runtime.py](../scripts/g5_full/runtime.py)，阶段绑定见 [manage.py](../scripts/g5_full/manage.py)。每模式单独启用正式网关、覆盖只读线程、本地资源服务和真实 Edge 页面；浏览器先接入，完整任务中刷新并重启网关，完成后暂停导出 AMASE 原生统计、比较页面与网关全量状态，按正常入口关闭进程并核对端口。

## 诊断与修正

`g5-t09-test-20260923-224852-781` 在网关启动前发现编排器漏设正式包 SHA256，后端正常收尾，未算通过。`g5-t09-test-20260923-224944-437` 在约 750.839 仿真秒收到任务 1000 的唯一 TaskComplete，脚本错误要求两实体同时出现在 `EntitiesInvolved`；原始规划实际只分配 500。按规划响应核对后，保留原接收记录并完整重跑。

`g5-t09-test-20260923-230337-589` 真实完成、暂停、分析导出、页面全量状态对照和正常退出均执行完毕；严格 G3 内部导航审计发现一次分段重叠交接的航点号从 14 回到 12、13，0.13 秒后追上 14。原始位置每步最多约 1.47 米，之后第一条公开实体状态为新命令的航点 14，未见公开状态倒退。新增 [有界交接核查](../tests/g5_full/handoff.py)：只允许同一已规划重叠段、换命令、1 秒内追上、位置连续并由下一条公开状态确认；原始日志不改写，略过的内部重叠样本另列证据。将核查后的导航视图交给原 G3 完整审计，两份真实 Headless 记录均通过。位置跳变、同命令倒退和公开状态倒退三种反例均被拒绝；不以此放宽任务末段、公开执行或 TaskComplete 检查。

第二轮诊断的原生 20 米统计有 724 格、已覆盖 724 格。独立 DTED／相机事件复算结果为 724／724，实际参与覆盖的传感器来自 400／500，任务执行分配仍为 500；页面暂停时的累计覆盖快照也为 724／724。该历史诊断整组收据为 failed，不能代替本轮最终两模式资格。

## 最终验收与边界

正式运行 `g5-t09-test-20260923-232112-235` 的 `result.json`、`entry-result.json`、`runtime-result.json`、`acceptance.json` 均为 passed，`fullSimulationDisplayQualified=true`、`stageQualified=false`；验收收据 SHA256=`941caf70a0c534f26e70d85743916182fa8ab190f9c8274d073a7e290fff9372`。55 项输入哈希复核一致，T08 收据及前端、后端、地形候选绑定一致。

| 模式 | TaskComplete 仿真毫秒 | 执行者 | 原生／独立／页面累计 20 米格 | 页面／网关 | 收尾 |
| --- | ---: | --- | --- | --- | --- |
| Headless | 750839 | 500 | 724／724、724／724、724／724 | 暂停全量状态一致 | AMASE／UxAS／Edge 退出 0；覆盖线程和网关正常；端口释放 |
| Gui | 749239 | 500 | 724／724、724／724、724／724 | 暂停全量状态一致 | AMASE／UxAS／Edge 退出 0；覆盖线程和网关正常；端口释放 |

两模式均经历真实浏览器刷新及同后端运行的网关重启，恢复后取得新流身份；原规划、当前命令、实体和覆盖对象持续关联任务 1000。两模式最终 `segmentHandoffs` 均为空，**正式资格直接通过原 G3 连续航点审计，没有使用诊断时的重叠样本过滤**。独立原生统计确认 400／500 的相机事件均可贡献任务格，执行者只按规划与完成消息确定为 500。所有收据及浏览器原始状态留在被忽略的 `out/runs/g5-t09-test-20260923-232112-235`，未将运行文件提交 Git。

T09 完成也不代表 T10 的 20 实体 30 分钟稳定性、资源采样或 T11 页面人工确认与发布已通过。用户对累计覆盖外观的反馈将在 T11 当前页面确认中关闭。
