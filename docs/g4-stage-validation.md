# G4-T09 阶段验收与网关发布

状态：G4-T09 已完成，G4 验收通过并发布。原两实体两模式、本轮真实 GUI 确认、相同候选完整 20 实体受控复验、关闭后独立审计及正式日常入口检查通过；全部所属进程正常退出。原 GUI 审查后推进 1030 ms 的冻结失败记录完整保留，未改判。

## 当前范围

最终组合为 T07 正式 AMASE、T08 正式 UxAS、原统一七模型消息库，以及 T08 同字节网关核心／HTTP 服务。完整来源链继续由 config/g4-baseline.json 校验，不能绕过已发布产物解析或改写旧收据。

T08 合格取证 g4-t08-capture-20260920-085508-125483 与独立审计 g4-t08-audit-20260920-100537-000770 证明同版本 20 实体、两个模式各 1800 仿真秒、三客户端、恢复和稳定性；详情见 [T08 报告](g4-scale-validation.md)。T09 固定其摘要，核查离线采样审计修订链和所有业务输入；不把离线审计两文件修订误当运行源码变化，也不放宽其他来源校验。

本轮最终任务为原两实体 Headless／Gui 完整 WaterwaySearch，加 20 实体 Gui 到 785 仿真秒、每架独立规划／执行／完成及统计。全程使用候选包实际导入的网关文件，保留加载路径／摘要收据；原始运行和浏览器证据不由文档生成。

## 新入口与发布机制

- scripts/windows/run-g4-stage.ps1：当前资格复查、候选构建、顺序完整运行、等待本轮 GUI 确认、正常关闭后独立审计、发布。
- scripts/g4_stage/finish.py：检查当前 ready／review 摘要、nonce、后端及控制器 PID 创建时间，登记新的真实用户确认；不复用 T07 确认。
- scripts/windows/start-g4-session.ps1：正式日常入口，Original／Mixed20、Gui／Headless；网关自动恢复期间后端持续运行，运行结束后暂停保留。原 run-g4-session.ps1 仅保留历史取证用途。
- scripts/windows/run-g4-gateway.ps1：以当前运行清单独立启动旁路网关；浏览器和网关均不拥有后端任务生命周期。

包位于 out/build/gis-gateway 下的不可变候选；正式发布按 build／publication 编号保留，最后原子切换 out/artifacts/gis-gateway/current.json。包清单绑定业务文件、启动器、生成模型、依赖锁和独立环境清单。调用入口检查合格发布收据及工作区来源，实际启动再次验证已安装依赖。目录内未知文件、文件变化、错误环境、旧运行和提前发布均不能视为合格。

开发工作区仍是依赖来源；本卡不是第二机器打包验收。使用约定见 [G5 交接](g4-g5-handoff.md)，HTTP／WebSocket 字段见 [浏览器协议](g4-browser-contract.md)。

## 已执行与保留的诊断

当前输入资格 g3-t01-check-20260920-102910-298 与独立 Web 环境 VerifyOnly g4-t01-20260920-103254-705 passed。候选来源、中文空格迁移、摘要／业务文件／依赖变化、错误清单启动、旧确认／自动确认／空确认等 17 项检查已通过；最终发布已再次复核来源收据。

首轮诊断 g4-t09-stage-20260920-103431-086805 已启动真实原两实体，候选实际加载通过。独立端口冲突检查 g4-t09-live-package-20260920-103744-923766 passed，第二包拒绝 WinError 10048，原实例 run_id／stream_id 不变且 live。发布入口复核发现还应固定环境清单摘要，并将日常会话与故障注入验收分开；通过本轮所属 AMASE 的正常 shutdown 请求结束诊断，子运行明确 failed（stream-failed:amase:None），normalExit=true、portsReleased=true，无强制结束。该诊断不计作完整任务或阶段通过。

更新后的普通会话入口对两实体／20 实体分别进行真实初始化、实际规划、HTTP／WS 快照边界及正常停止。短程仅证明运行入口，不替代完整任务；最终任务使用新运行编号重新取证。曾从其他工作目录以错误相对路径调用语法辅助工具，立即改用仓库路径复查通过；仿真脚本始终使用绝对路径，该辅助命令失败未写成通过。

## 完成条件

当前 GUI 确认、同版本完整受控运行、正常关闭、关闭后审计、正式发布与正式入口消费检查已完成。新自动复验不代替或伪称人工查看，具体绑定和失败保留方式见下文。

## 本轮最终自动结果

总编排 g4-t09-stage-20260920-104409-452486 从其他工作目录使用绝对 Windows 入口启动；候选 g4-t09-build-20260920-104409-688458，package.json SHA256 为 dcc6b6294ff0687ae690f6518780b2ba563339b177ba7c79b22b3aa9ff41689e。启动收据确认 7 个实际导入模块均位于候选包内。包内容保持不可变，正式资格由独立 qualification 收据赋予。

| 证据 | 实际结果 |
| --- | --- |
| g4-t09-checks-20260920-104408-727139 | 17 项包来源、迁移与确认拒绝检查 passed |
| g4-t09-session-smoke-20260920-104252-670580 | 普通两实体／20 实体初始化、规划、WS 边界与正常停止 passed，未注入重启 |
| g4-t09-live-package-20260920-104537-367540 | 新包端口冲突拒绝，原 live 实例不受影响 |
| g4-t09-premature-20260920-110135-418378 | 真实部分最终矩阵不能提前发布，正式指针不变 |
| g4-t09-original-20260920-104409-452486 | Headless／Gui 全程 passed；每模式任务 1000／实体 400 在 740709 ms 完成，724／724；任务航段、完成跨观察缺口补齐、统计独立重算及两模式分析回放、三客户端、原生页面刷新、正常退出／端口释放通过 |
| g4-t09-mixed-20260920-104409-452486 | 20 实体真实 1 倍到 785569 ms 后暂停；逐架执行／统计关闭前审查 passed，随后冻结检查 failed 并正常关闭，失败保留 |
| g4-t09-browser-20260920-112051-201733 | 原生 Edge 校核全部 20 实体／20 任务、完成身份／时间及刷新一致，正常退出、调试端口释放，截图留存 |

混合审查 SHA256 为 f35edaae0fe97d6ae2842d8424302f3a8bff2fb2ef1f314789a83cf20919d567；三客户端在 sequence=40909 汇合，状态 SHA256 为 c2142ebf8c6bad81a2485d7dcfa61901926a9a26f2658d601a009920c5909038。审查依据为暂停后保留的只读记录前缀，明确 normalExitValidated=false、manualGuiAcceptance=false；该收据只证明关闭前状态，原运行后续冻结检查失败，正式资格采用下述独立受控复验。

线任务 3000～3004 分别 724／724、3005～3007 分别 723／723；矩形 3014～3019 均 1250／1250。点任务 3008～3013 的本轮观察秒数分别为 182.011、182.531、181.471、181.471、181.471、180.971，全部与原始分析记录独立复算一致，无效果门槛。完整执行关联保存在 review/execution.json，统计保存在 review/statistics.json。

本轮 785 秒用于最终完整任务复验；30 分钟稳定性仍使用固定摘要的 T08 同字节证据。沿用的规模取证基类将短于 1800 秒的记录标为 diagnostic，该标记不被用作 T08 稳定性通过依据；T09 的当前 20 项执行资格由独立审查、关闭后审计及新的 GUI 确认共同约束。

## 当前确认与受控收尾

用户已确认“本轮 20 实体 GUI 正常”。收尾时发现原混合运行在等待确认期间从 785569 ms 推进到 786599 ms，冻结检查按设计拒绝，11:32:11 原运行以 failed 正常关闭。AMASE／UxAS 退出码均 0、无强制终止、端口释放；原分析 XML、格子和分析事件文件与关闭前审查副本同摘要。后续仅有状态和心跳，没有新任务、命令或删除事件。

原失败 result SHA256=f0c20224910814c3179a4cd2e66dca5c03a39f0ef76b619e0ff9724547652f25，case SHA256=506f8d04228c81f083163c8ce3067eec18fc95af48610c3ea3d90a1cc64a992f。它们不改判、不改写。新增 config/g4-finalization.json 和 scripts/g4_finalize 只接受这一具体已知前驱、原审查和同一包摘要，继续拒绝未知变化；原正常验收的冻结规则保持。

参照项目既有同版本受控复验方式，本次真实视觉确认保存为 g4-t09-visual-confirm-20260920-113828-487244，仅绑定用户实际查看的原 GUI；scope=visual-only-same-version、visualRunStatus=failed、controlledRerunRequired=true。后续自动复验不声称是新的人工查看。该绑定的 8 项反例 g4-t09-finalization-checks-20260920-114055-815004 全部拒绝，旧身份、错误包、自动确认、空确认、过早时间和无界范围均不能使用。

同字节完整 20 实体 GUI 受控运行 g4-t09-controlled-20260920-113903-072650 已 passed。它保持原任务、真实倍率、三客户端及网关重启矩阵，关闭前完整审查后直接正常关闭；独立关闭后审计与自身关闭前执行／统计结果逐项一致，正式发布通过。业务源码、旧 T09 实现和候选包全部保持原摘要。

## 正式发布与入口消费

发布编号 g4-t09-publish-20260920-115418-723744，资格 SHA256=245d2fdc1d5699784c088f8f0e762365009e892e2aa19fbf413b45f66db39c51；stageQualified、任务执行、完成、统计、稳定性和正常退出标志全部 true。包清单保持上述候选摘要；current.json 原子指向按构建／发布编号保存的不可变目录。受控运行三客户端 sequence=40909、状态 SHA256=f14c14649ba7703faad8ecc2fab30fd12456d37b3684a1b4107ae23c75043aeb。关闭后审计 1483 条 Running 时钟全部为实际 1 倍，20 项任务逐架执行／统计通过；它不是新 30 分钟稳定性取证。

复核发现旧日常会话将网关暂时离线当作后端失败。保留冻结阶段代码，新增 scripts/g4_session/runtime.py 和 start-g4-session.ps1 分离两者生命周期；网关退出自动恢复，gateway-offline 可暂留观察离线，后端故障仍整组失败。来源合格入口由 session.json 单独绑定，不改写完整任务收据。

实际消费检查 g4-t09-release-check-20260920-115528-388842 passed：从中文空格工作目录调用 Windows 新入口，Original／Mixed20 分别真实初始化／规划；实际导入正式目录中的模块，HTTP ready、WS 快照后有序增量通过。每轮停止网关并留空至少 3 秒，后端进程与清单保持同一身份，仿真时钟继续；新网关自动启动，run_id 不变、stream_id 更新，新快照／增量正确，最后整组正常退出和端口释放。该短程证据仅验证运行入口，完整任务仍以上述最终矩阵及 T08 为准。

标准使用、只读契约、恢复提示和 G5 边界见 [交接](g4-g5-handoff.md)。G5 尚未实现；业务网关、后端及生成模型没有在本卡被修改，第二机器部署仍待 G8。

发布入口资格后，再从不同工作目录、不带 ValidateEntry 参数执行普通 20 实体 Headless 命令：g4-t09-demo-20260920-115944-865791 passed，真实 20 实体快照／live、正常停止和来源不变均通过。最终复查 5 个正式完整／入口运行的正常退出、全部 48 个声明端口可重新独占绑定、正式包及入口来源摘要一致；21 个新增 Python 语法、4 个 PowerShell 解析及文档链接检查通过。
