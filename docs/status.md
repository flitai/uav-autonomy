# 项目实施状态

更新日期：2026-09-17。依据：[总体实施计划](../04.项目总体实施计划与阶段验收.md)、[G0 验收报告](g0-baseline.md)、[任务清单](backlog.md)。

过程记录：[根级工作日志](../worklog.md)。已建立固定日志制度并补记 G0；每项阶段性任务收尾时追加。

## 当前关卡

**G0 已完成；G1 尚未启动，需先准备 Java 构建环境。**

| 项目 | 当前记录 |
| --- | --- |
| 最近通过的关卡 | G0：源码、环境、依赖、示例与风险清点 |
| 源码基线 | 根提交 `061685f7f2e9d17c2c9eb0434a309a3d66c3e648`；三个目录 tree 见 G0 报告 |
| 工作区 | 已跟踪源码无改动；原六份文档、三份 G0 文档及新增 worklog.md 均未提交 |
| 已验证工具 | Git 2.55.0.windows.3、Python 3.14.7 x64 实际可运行 |
| 尚未验证的能力 | Java／C++ 构建、消息生成、AMASE 仿真、系统闭环、Cesium |
| 下一任务 | G1-T01：准备 JDK 11 兼容基线与 Ant，落实生成物忽略和输出约定 |
| 执行边界 | G0 后补充工作日志制度；没有自动进入 G1 或执行 Git 提交 |

## 环境缺口与阻塞项

以下影响后续任务，不影响已完成的 G0。尚未尝试构建，因此不存在可报告的“构建失败日志”。

| 缺口 | 检查／已做工作 | 下一次最小验证 | 归属 |
| --- | --- | --- | --- |
| Java／javac／Ant 未就绪（R02） | PATH、常见位置与注册表限定范围内未发现 | 准备工具后实际执行版本命令，记录路径、来源及退出码 | G1-T01，当前首要前置项 |
| 生成物管理未落地（R07） | 未发现根级忽略规则；G0 仅记录输出位置 | 构建前加入精确规则，用 git check-ignore 核对，保留随库 JAR | G1-T01 |
| 消息库一致性未知（R03／R09） | 7 个 MDM 可解析，随库 JAR 已取哈希；Python 仅验证运行时 | 统一生成并编译 Java；导入 Python 包、解析固定样本 | G1-T03／T04 |
| AMASE 启动与数据条件未知（R06／R08） | 旧脚本路径已检查，未发现随库 DTED | 显式工作目录运行指定场景，保存地形来源／缺省行为和日志 | G1-T04 |
| AMASE 与 UxAS 封装有差异（R04） | 静态识别原始 LMCP 与属性消息／Sentinel 的差别 | 先在 G1-T05 取 AMASE 样本，再在 G3 验证双向链路 | G3 |
| C++／Node 工具尚未验证（R02／R10） | 在限定范围内未发现；C++ 依赖及补丁已清点 | 分阶段准备并做最小构建 | G2／G5 |

R01 上游 SHA 未知、R05 Anod 可能切换源码的处理与其他风险见 [G0 风险表](g0-baseline.md#6-风险归属与下一次验证)。全部已发现缺口都已归属任务，无需扩展 G0。

## 已确定的实施约定

- 以当前本地源码基线为起点；精确上游 SHA 保持“未知”，不使用浮动分支代替。
- Java／C++／Python 消息代码来自同一组 `OpenUxAS/mdms/`，生成到 `out/generated/lmcp/`；G0 未创建该目录。
- 构建中间物、可交付产物、运行记录、临时文件分别放入 `out/build/`、`out/artifacts/`、`out/runs/`、`out/tmp/`；原 Ant 默认输出例外在 G1 明确。
- 先完成 G1，再推进 G2、G3；G3 闭环通过后进入正式 GIS 集成。
- 工具或文件存在只作为发现证据；可运行、可构建、可联调分别验收。

## 下一次工作的起点

读取 [backlog.md](backlog.md) 的 G1-T01，先重新检查 Git 状态和已发现工具，避免覆盖用户新改动或重复准备已安装工具。G1 的版本固定、依赖获取和安装在后续实施任务中进行，本次没有选择全部后续版本。

每项任务结束先验证，随后追加根级 worklog.md、更新本页和 backlog，再交付；遇阻或中断也记录过程、问题与恢复条件。仅当对应验收证据齐全时，将状态改为“已完成”。环境改变后保留 G0 作为历史快照，更新当前状态和后续阶段报告。
