# 项目总体实施计划与阶段验收

编制日期：2026-09-16

文档状态：总体实施计划 V1.0，用于指导后续阶段细化与验收；实施状态更新于 2026-09-18。

当前进度：**G0、G1 已完成，G1-T01～T05 均已完成；G2-T01～T05 已完成，T06 可执行、尚未启动，见 [T01 记录](docs/g2-cpp-toolchain-validation.md)、[T02 记录](docs/g2-dependencies-validation.md)、[T03 记录](docs/g2-lmcp-cpp-validation.md)、[T04 记录](docs/g2-uxas-cmake-validation.md)和 [T05 记录](docs/g2-uxas-build-validation.md)。** Java／Ant、生成器、统一消息及 AMASE 已通过验收；GUI／无界面真实 TCP 状态、十四组自动检查、GUI 人工确认、同版本受控复验及正常退出均完成，原生工具及固定依赖探针、七模型 C++ LMCP 构建和三语言文件样本已通过，UxAS 已完整编译并通过候选验收，尚未验证 HelloWorld 或其双向网络闭环。证据见 [G0 报告](docs/g0-baseline.md)、[Java 环境与构建验收](docs/g1-java-validation.md)、[T03 消息库验收](docs/g1-lmcp-validation.md)、[T04 AMASE 验收](docs/g1-amase-validation.md)和 [T05 TCP 验收](docs/g1-tcp-validation.md)，G2 的待实施设计见 [阶段方案](docs/g2-windows-uxas-plan.md)，后续以 [当前状态](docs/status.md) 和 [任务清单](docs/backlog.md) 跟踪。

## 1. 总体目标与实施边界

本计划用于指导后续阶段设计、任务拆分、AI 开发和验收，采用“用户主导、AI 逐项实施”的组织方式。

首期交付目标：在 Windows 原生环境运行 OpenAMASE 与 OpenUxAS，通过真实消息驱动 Cesium 三维显示，并支持仿真控制、记录回放和指定场景的基础离线演示。

确定以下实施基线：

- 默认面向 Windows 11 x64 单机；其他系统版本另行增加验收环境。

- 保留 OpenAMASE 仿真内核、OpenUxAS C++ 服务和 LMCP 消息体系。

- CesiumJS 采用浏览器形态，消息网关负责后端协议与前端接口转换。

- 开发和依赖获取可以联网；交付包的指定演示场景必须能够断网运行。

- 首期保留原场景编辑工具，不建设完整 Web 场景编辑器。

- 强化学习集成、Ada 移植、实机接入、大规模离线地理数据生产不纳入本期。

当前 G0、G1 已通过验收，G2～G8 尚未完成。AMASE→Python 的真实 TCP 接收通过不代表 UxAS 双向闭环通过。T03 已更正 AMASE Java 库外层封装判断，T05 已验证当前接收封装；G3 仍须验证来源过滤与真实命令执行，不预先增加原始 LMCP 专用桥。GUI 重置的分段验收留给 G6，不把单段时间单调检查当成重置测试。此前基于小团队提出的 8～12 周估算，不直接用于本次“用户＋AI”的排期；通过基础构建与协议关卡后再估算剩余工作。

现有 [03.项目目录分析与 Windows／Cesium 改造计划](03.项目目录分析与Windows_Cesium改造计划.md) 保留为技术依据，实施状态和推进顺序统一采用本计划的 G0～G8 关卡。技术背景和长期训练方向参见 [02.团队实施技术建议书与资源清单](02.OpenAMASE_OpenUxAS_TorchRL_BenchMARL_团队实施技术建议书与资源清单.md)，AI 工作约定参见 [AGENTS.md](AGENTS.md)。

## 2. 实施阶段与关卡

| 关卡                     | 主要工作包                                      | 阶段交付物                             | 通过条件                                   |
| ---------------------- | ------------------------------------------ | --------------------------------- | -------------------------------------- |
| **G0：基线与环境**           | 清点源码和现有修改；核查工具链；确定依赖来源、产物目录和示例配置           | 基线清单、环境报告、风险清单、G1 任务表             | 能明确说明当前使用什么源码、缺什么环境、下一步如何验证            |
| **G1：消息生成与 Java 仿真**   | 构建 LmcpGen；统一生成消息代码；构建 AMASE；验证 GUI 与无界面模式 | 构建记录、消息库版本记录、AMASE 状态样本           | 指定场景能够运行，客户端能解析持续变化的真实实体状态             |
| **G2：Windows 原生 UxAS** | 建立 CMake 构建；固定依赖；处理必要平台差异；运行最小服务           | Windows 构建入口、依赖锁定记录、HelloWorld 日志 | 从干净输出目录构建成功，HelloWorld 正常运行和退出         |
| **G3：原有系统闭环**          | 验证并修复 TCP 封装兼容；核对消息版本、来源过滤及启动顺序            | 唯一有效的连接配置、协议样本、闭环验证报告             | AMASE 实时状态进入 UxAS，规划命令返回 AMASE 并实际影响运行 |
| **G4：消息网关**            | 建立协议适配、状态缓存、快照与增量、时间映射和记录能力                | 接口契约、网关程序、协议与重连测试                 | 新客户端能够恢复完整场景，数据与原始消息一致，没有重复转发和旧运行混入    |
| **G5：Cesium 态势显示**     | 显示实体、轨迹、航线、任务、区域和时间；校准位置与姿态                | 三维显示版本、坐标／时间核对记录                  | 使用真实仿真数据完成展示，刷新和重连后恢复正确状态              |
| **G6：仿真控制**            | 实现开始、暂停、倍速、重置；建立请求反馈与运行隔离                  | 控制接口、AMASE 控制扩展、控制测试报告            | 浏览器操作引起对应后端行为，界面状态来自后端反馈               |
| **G7：记录与回放**           | 完善记录格式、运行清单、快照索引；实现暂停、倍速和时间跳转              | 可回放记录、回放界面、一致性报告                  | 跳转后恢复对应时刻的实体、任务、区域与时间状态                |
| **G8：部署与离线验收**         | 打包运行库、配置、前端资源和演示数据；验证安装、启停与断网运行            | 部署包、使用手册、验收报告、已知限制清单              | 在第二个干净目标环境复现完整演示，无开发目录隐式依赖             |

推进规则：

- 主线按关卡顺序推进，G3 是进入正式 GIS 集成的前置门槛。

- 前端可以提前使用固定样本验证显示方案，但该结果不能计入真实集成验收。

- WSL／Linux 仅用于参考验证或问题定位，不能替代 G2、G3 的 Windows 原生验收。

- 未解决的问题如果影响下一关卡的输入正确性，必须先关闭；其他问题进入待办并注明影响。

- 原界面和原始示例配置保留到替代能力通过验收，方便对照与回退。

## 3. 任务细化与推进机制

采用三层结构：**总体计划 → 当前阶段实施方案 → 单项任务卡**。只详细拆分当前阶段，并准备下一阶段的任务轮廓。

2026-09-18 已形成 [G2 阶段方案](docs/g2-windows-uxas-plan.md)和 [G2-T01～T07 任务卡](docs/backlog.md#4-g2-顺序与任务卡)：工具链 → vcpkg 依赖 → C++ LMCP → CMake 构建图 → 平台适配 → HelloWorld → 复验发布。采用 MSVC v143／CMake 3.31、Release x64／动态 CRT，Zyre／串口默认关闭但保留 CZMQ。T01 工具清单及入口已落地，来源阻塞经微软原生签名验证及负向对照解除，MSVC／SDK 已安装；十组完整验收通过，VS／Ninja 在普通及中文空格路径完成真实 C/C++ 编译运行。T02 已完成固定 89 个 ports 的中文空格路径源码重建、缓存复用、11 组验收与迁移发布；Boost 使用历史模块 ports，原依赖版本和必要补丁行为保留。T03 已完成 183 个库源文件构建、164 类型和三语言双向样本、故障与迁移发布；T04 已完成 122 个源码、40 项服务、5 个资源的配置图和独立桥探针；T05 已完成两类路径的 122 源码完整构建、UTF-8／文件／时间／异常探针、运行库来源及隔离故障验收；T06 可执行、尚未启动，T07 继续等待。原文档细化阶段没有安装或构建。G2 终点保持 HelloWorld 正常收发与退出，WaterwaySearch 双向闭环仍属 G3。

每个阶段在编码前明确：输入基线、实现范围、接口变化、工作包、验证方法和停止条件。涉及工具链版本、协议拓扑或数据格式的选择，先做有限验证，再记录选型结果。

单项任务以半天至两天的工作量为拆分参考，采用以下模板：

```text
任务编号：Gx-Tyy
目标：一个能够独立验证的结果
前置条件：已通过的任务、版本与配置
修改范围：允许修改的模块
范围边界：本次不包含的功能
实施步骤：具体操作及依赖顺序
验收条件：可以观察或自动检查的结果
证据：命令、退出码、日志、样本和配置位置
回退方式：如何恢复此前通过验收的状态
停止条件：出现什么情况需要重新评估任务
```

执行约束：

- 同时只保留一个主要实现任务；遇阻时可以开展一项独立调查，避免多个半成品并存。

- 一项任务尽量只改变一个主要因素，避免同时迁移构建、升级全部依赖和重构业务逻辑。

- 探索任务先分配半天至一天的验证窗口；到期提交证据和判断，不无限扩大排查范围。

- 任务状态统一为：待细化、可执行、进行中、待验证、已完成、阻塞、延期。只有验收证据齐全才标记“已完成”。

- 阻塞记录必须包含复现方法、已尝试方案和下一次最小实验；缺少新证据时不重复同一失败操作。

- 新需求先进入待办。影响架构、依赖拓扑、交付范围或验收标准时，由用户确定取舍；已授权范围内的常规修复继续执行。

- 验收后保留可回退的修改边界；用户已于 2026-09-18 持续授权阶段性工作确认完成后自动提交与推送，先同步工作日志和状态，再核对远程结果；具体约定见 AGENTS 第 7、10 节。

维护以下记录（状态和待办已在 G0 创建，根级工作日志于 2026-09-17 建立）：

- [docs/status.md](docs/status.md)：当前关卡、最近通过的基线、阻塞项、重要决策及下一任务。

- [docs/backlog.md](docs/backlog.md)：任务编号、依赖关系、状态和验收条件；已登记 G0／G1 完成证据及 G2-T01～T07 完整任务卡。

- [worklog.md](worklog.md)：所有阶段性任务的过程日志，记录实施内容、成果、验证、问题、失败尝试、处理办法、重要决定和遗留事项，按时间顺序持续追加。

每项任务结束先验证，随后追加根级工作日志、同步状态与待办，再形成交付回复；任务受阻或中断同样留下实际进展与恢复条件。阶段结束核对交付物、遗留项和下一阶段入口。日志模板及维护规则见 [AGENTS.md 第 10 节](AGENTS.md#10-固定工作流程与工作日志)。`AGENTS.md` 只同步稳定约定和已验证的标准命令。

首批任务固定为：

1. **G0-T01**：清点源码来源、版本、现有修改和目录状态。
2. **G0-T02**：核查 JDK、Ant、C++ 工具链、CMake、Python、Node 的真实可用性。
3. **G0-T03**：整理依赖获取方式、生成物目录、日志位置和示例配置。
4. **G0-T04**：形成 G0 验收记录，并细化 G1 的构建与状态接收任务。

G0 以检查和记录为主；依赖安装、代码生成和构建进入后续明确任务，不在环境检查中顺带展开。

## 4. 技术接口与验证要求

阶段细化必须维护四类约定：

| 接口边界      | 必须明确的内容                                 |
| --------- | --------------------------------------- |
| 消息生成与组件互通 | MDM／生成器基线、消息库版本、外层封装、消息方向、来源标识与订阅范围     |
| 网关与浏览器    | 接口版本、完整快照、有序增量、对象删除、重连同步；int64 标识符采用字符串 |
| 仿真控制      | 请求标识、接受／完成／失败反馈、开始与暂停语义、重置后的运行隔离        |
| 记录与回放     | 原始消息、规范化事件、仿真时间、版本配置、快照边界和时间跳转规则        |

具体字段与函数签名在对应阶段的实施方案中冻结；总体计划确定接口责任和验收要求，避免在协议未验证前提前固定错误的数据结构。

必须覆盖以下验证场景：

- **构建与环境**：干净构建、依赖缺失、不同工作目录、空格及中文路径。

- **消息链路**：跨语言样本一致性、双向通信、TCP 半包／粘包、来源过滤、断线重连、重复消息。

- **运行生命周期**：启动就绪、正常退出、端口冲突、子进程失败、连续启停 10 次。

- **显示正确性**：已知坐标、四个基本航向、俯仰／滚转、高度基准、计划航线与实际轨迹区分。

- **时间与控制**：暂停、倍速、重置、切换场景、旧运行数据隔离。

- **回放与离线**：跳转后的完整状态恢复；断网加载本地页面、底图、模型和指定场景。

每份验收记录至少包含源码版本或工作区差异说明、工具版本、输入配置、执行命令、退出码、关键日志、预期与实际结果。截图用于辅助说明，不能替代协议、控制和运行结果。

性能验证先记录基线，再固定目标。沿用现有计划的候选指标：连续运行 30 分钟、10～20 个显示实体、常用视角约 30 FPS、网关接收到界面应用更新的 P95 延迟不超过 250 ms。真实仿真与合成数据压力测试分别报告，并记录测试硬件和数据更新率。

## 5. 范围控制与最终交付

由用户负责业务优先级、范围调整和重大技术取舍；AI 负责在任务范围内实施、验证、记录证据并报告限制。阶段检查以已有验收证据为依据，不要求每个小修改重复审批。

重点风险提前处理：

- **协议不兼容**：在 G1～G3 完成样本检查和双向验证。

- **旧依赖阻塞 Windows 构建**：采用最小构建验证和可选模块隔离，记录每次依赖调整。

- **仿真与界面耦合**：先建立可信状态来源，再接显示与控制。

- **时间、坐标和高度语义错误**：使用固定样本核对，保留原始值及转换依据。

- **离线工作膨胀**：首期限定一个已验收的小场景和本地资源包，不扩展为通用地理数据平台。

最终交付同时满足：

1. **可构建**：按固定版本和文档重建核心产物。
2. **可运行**：Windows 原生完成真实仿真与规划闭环。
3. **可操作**：Cesium 支持态势显示、控制和回放。
4. **可部署**：指定场景可在干净环境中安装并断网演示。
5. **可接续开发**：状态、待办、接口约定、验证入口和已知限制完整，下一位开发者或 AI 能据此细化后续任务。
