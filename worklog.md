# 项目工作日志

创建日期：2026-09-17。统一时区：Asia/Shanghai（UTC+08:00）。

本文件是项目根目录下固定的工作日志，持续记录阶段性任务的背景、工作过程、修改内容、成果、验证、问题、解决办法、遗留事项和下一步。每完成一项任务卡或阶段性交付，都要在交付回复前追加记录；任务中断、受阻或仅完成部分工作时，也要记录当时的真实状态。

执行规范和新记录模板见 [AGENTS.md 第 10 节](AGENTS.md#10-固定工作流程与工作日志)。记录按时间顺序追加，保留历史；后续发现错误时追加更正说明并引用原记录编号。补记必须注明依据和不确定信息，不能编造执行时间、命令或验证结果。

[总体实施计划](04.项目总体实施计划与阶段验收.md)确定关卡与边界，[当前状态](docs/status.md)记录最新进度，[任务清单](docs/backlog.md)管理任务和依赖，本日志保存推进过程。阶段报告与原始运行记录作为证据关联，本日志仍须写清主要过程与结论。

## 历史资料索引

建立日志时已存在以下前期成果。这里登记可核查的文件和作用；未留存的逐次操作、准确完成时间和原始输出不补造。

| 资料 | 内容与用途 |
| --- | --- |
| [01.思路与技术途径参考](01.无人自主智能策略研发_思路与技术途径参考.md) | 项目研发方向和研究背景 |
| [02.团队实施技术建议书与资源清单](02.OpenAMASE_OpenUxAS_TorchRL_BenchMARL_团队实施技术建议书与资源清单.md) | 仿真、自主策略及后续训练集成的技术参考 |
| [03.目录分析与 Windows／Cesium 改造计划](03.项目目录分析与Windows_Cesium改造计划.md) | 现有目录职责、改造思路及技术风险 |
| [04.总体实施计划与阶段验收](04.项目总体实施计划与阶段验收.md) | G0～G8 顺序、任务拆分方式、范围与验收要求 |
| [AGENTS.md](AGENTS.md)、[CLAUDE.md](CLAUDE.md) | AI 协作规范与导入入口；统一在 AGENTS 维护规则 |

前期文档的存在不代表 Windows 构建、仿真联调或 Cesium 已实现。G0 的实际检查及交付补记如下。

## WL-20260917-001｜G0 基线、环境与后续任务准备（补记）

### 时间、关联任务与状态

- 工作日期：2026-09-17；检查开始于 09:23，文档验收复核于 09:38，最终复核于 09:39。详细时刻以 G0 报告和当次执行结果为准。
- 补记日期：2026-09-17，建立本日志时补录。
- 关联任务：G0-T01、G0-T02、G0-T03、G0-T04。
- 状态：已完成；G1 尚未启动。
- 依据：[G0 基线与验收报告](docs/g0-baseline.md)、[任务完成记录](docs/backlog.md#1-g0-完成记录)及当次工具执行结果。

### 背景、目标与范围

为 Windows 原生运行和后续 Cesium 改造建立可信起点，先确认使用的源码、工具环境、消息输入、依赖、示例和风险，再准备 G1 任务。用户明确限定 G0 为检查与文档整理，不安装依赖、不生成代码、不构建或启动仿真，不自动进入 G1。

### 工作过程与实施内容

1. **读取规范与检查工作区**：检查 AGENTS、总体计划和已有文档；运行 Git 状态、提交、远程地址和目录树查询。确认根提交为 `061685f7f2e9d17c2c9eb0434a309a3d66c3e648`，三个上游项目为普通源码目录，没有独立 Git 元数据或子模块；已跟踪文件共 3787 个，源码无改动。保留六份原有未跟踪文档。
2. **固定本地内容**：记录 LmcpGen、OpenAMASE、OpenUxAS 的 tree 标识，将精确上游提交标为未知；解析七个 MDM，读取系列与版本，计算消息模型、示例配置、待发送消息、随库 JAR 和依赖补丁的 SHA-256。
3. **核查实际工具环境**：查询系统和 PowerShell，检查 PATH、常见安装位置和注册表。实际执行 Git 与真实 Python 解释器，记录路径、版本和退出码；将 WindowsApps 入口与可运行解释器分开。Java、Ant、C++、Node 工具链仅记录为限定范围内未发现。
4. **清点依赖及构建入口**：阅读 Anod 配方、repositories.yaml、Java 工程属性和随库 JAR 引用，区分仓库声明版本与已验证版本。发现 pugixml／serial 本地补丁，以及 Anod 可能另行检出 AMASE／LmcpGen 的来源风险。
5. **核对示例与资源**：检查 HelloWorld、WaterwaySearch 的配置关系、实体 400／500、六个待发送消息和旧脚本工作目录；记录 5555、5560、5561、9999、9400、9500 的端口角色及 09:24:26 的监听快照。未发现随库 DTED，未将其视为资源齐备。
6. **建立交接材料**：整理输出目录约定、风险 R01～R10 及解决阶段；细化 G1-T01～T05 的目标、前置条件、范围、步骤、验收、证据、回退和停止条件。
7. **验收文档与变更范围**：检查本地链接／锚点、Markdown 基本结构、严格 UTF-8、MDM 版本、文件哈希和 Git 状态。G0 文档交付后未跟踪文件为九个；该数字是当时的快照，后续新增工作日志不回写历史报告。

### 成果与修改文件

| 文件 | 当次变更／成果 |
| --- | --- |
| [docs/g0-baseline.md](docs/g0-baseline.md) | 新增源码、环境、依赖、示例、风险、命令与输出、30 个关键文件哈希及验收报告 |
| [docs/status.md](docs/status.md) | 新增当前关卡、环境缺口、阻塞归属与下一任务 |
| [docs/backlog.md](docs/backlog.md) | 新增 G0 完成证据及五张 G1 任务卡 |
| [04.总体实施计划](04.项目总体实施计划与阶段验收.md) | 更新 G0 状态及文档入口 |
| [AGENTS.md](AGENTS.md) | 更新已验证事实、状态入口与生成物管理要求 |

01～03 和 CLAUDE 保持原内容；没有修改业务代码、消息模型或运行配置，没有创建构建输出目录，没有 Git 提交或推送。

### 验证与实际结果

| 检查 | 当次结果 |
| --- | --- |
| Git 与真实 Python 执行 | Git 2.55.0.windows.3、Python 3.14.7 x64 可运行，退出码均为 0 |
| 源码基线 | 根提交、三个 tree 与已跟踪文件数核对一致；已跟踪源码无差异 |
| MDM 与消息文件 | 7 个 MDM XML、6 个示例待发送消息 XML 可解析；不等同于生成器语义或运行验证 |
| 文件哈希 | 30 项 SHA-256 与实际文件一致 |
| 端口 | 指定检查时刻六个端口无 TCP Listen 记录；不是未来启动保证 |
| 文档 | 5 份新增／更新文档、61 个本地链接、20 个表格通过基本结构与严格 UTF-8 检查 |
| 用户原有文件 | 4 份未涉及文档的哈希保持不变 |
| 最终验收 | 只读校验退出码为 0；G0 已完成，构建环境未全部就绪 |

关键命令包括 `git rev-parse HEAD`、`git ls-tree HEAD LmcpGen OpenAMASE OpenUxAS`、`git status --short --untracked-files=all`、`Get-Command -All`、真实解释器版本查询、`Get-FileHash -Algorithm SHA256` 和 `Get-NetTCPConnection -State Listen`。工作目录、可复查命令及关键输出完整整理在 [G0 报告第 7 节](docs/g0-baseline.md#7-检查命令与关键结果)。

### 问题、尝试、解决办法与处理状态

| 问题／现象 | 分析与处理办法 | 状态／后续归属 |
| --- | --- | --- |
| 无法从目录内容确认上游 SHA | 用根提交与 tree 固定本地源码，保留上游 SHA“未知”；需要上游升级时另行追溯 | 已建立基线，来源精确追溯仍未完成（R01） |
| PATH 中 Python 入口不能证明运行时可用 | 检查实际安装目录并执行真实解释器，确认版本、架构和退出码 | 运行时核实完成；项目兼容性留 G1-T03／T05（R09） |
| Java／Ant 等构建工具尚未发现 | 扩展至常见目录和注册表核查；保留查找范围，不安装或宣称整机未安装 | G1-T01 先准备 Java 环境，其他工具归 G2／G5（R02） |
| 原脚本工作目录和产物路径不适合直接沿用 | 记录旧路径关系和清理行为；约定后续显式工作目录与运行配置副本 | G1-T04、G2、G3（R06） |
| 消息库／TCP 封装存在兼容性疑点 | 保存随库 JAR 哈希和源码依据，安排统一生成与实际双向样本检查 | Java 库归 G1-T03／T04，桥接归 G3（R03／R04） |
| Anod 来源与本地依赖补丁易被遗漏 | 登记浮动引用、实际源码绑定要求，以及 pugixml／serial 补丁 | G1／G2 后续核查（R05／R10） |
| 缺少根级忽略规则，未发现随库 DTED | 只登记缺口；生成物管理在首次构建前处理，场景地形行为在运行时确认 | G1-T01、G1-T04、G5（R07／R08） |
| PowerShell 5.1 的 foreach 语句不能直接接所用管道形式 | 先将 foreach 输出赋给任务变量，再传入 ConvertTo-Json | 检查命令已纠正并重新执行 |
| Windows 下将 README*／路径通配符作为 rg 位置参数报错 | 改为搜索目录并用 `-g` 筛选文件 | 查询已纠正并重新执行 |
| 检查脚本的引号／反引号导致解析失败 | Python 版本查询避免嵌套引号；JavaScript 模板中用于匹配 Markdown 反引号的正则改用字符编码 | 校验已重新执行并通过，未产生源码修改 |

记录这些查询问题是为了避免后续重复踩坑。它们不是项目构建失败；当次没有执行构建。长期风险的状态也不因“已记录解决方向”而改为“已解决”。

### 决策、遗留事项与下一步

G0 验收的是清点完整、结论可信和下一步明确，因此缺少构建工具不阻止 G0 通过。采用当前本地源码与同一组 MDM，后续输出分为 generated、build、artifacts、runs、tmp；本阶段仅记录约定。

下一任务为 **G1-T01**：准备并固定 JDK 11 兼容基线与 Ant，落实输出和精确忽略规则。G1-T02～T05 等待各自前置任务。未进行的构建、真实状态接收和双向闭环不得计为已完成。

## WL-20260917-002｜建立固定工作日志制度

### 时间、关联任务与状态

- 工作日期：2026-09-17；开始核对时间为 09:44，文档校验时间为 09:49（UTC+08:00）。
- 关联任务：项目协作流程完善，不改变 G0～G8 的实施顺序。
- 状态：已完成。

### 背景、目标与范围

用户要求后续形成固定工作方式：每完成阶段性任务，在项目根目录 `worklog.md` 记录工作过程、内容、成果、问题、解决办法及其他相关事项。本次将要求落为长期规则、统一模板及首批记录。

### 工作过程与实施内容

1. 重新检查 Git 状态、AGENTS／CLAUDE、总体计划与 G0 交付文件，确认根目录尚无 worklog.md；保留全部已有修改和未跟踪文件。
2. 建立本日志，登记已有规划材料，依据报告和当次执行结果补记 G0。明确补记日期、证据来源、实际检查范围和未完成的验证。
3. 在 AGENTS 增加固定流程：接续时阅读相关日志，执行中记录证据，任务结束先验证，再追加日志并同步状态，最后交付；阻塞和中断也要留下记录。
4. 在总体计划、状态页和任务清单中补充日志入口和任务收尾要求。CLAUDE 已导入 AGENTS，继续复用同一规则。
5. 核查新增／更新文档的链接、Markdown 结构、UTF-8 和修改范围，完成后将实际结果追加至本条。

### 成果与修改文件

- 新增 [worklog.md](worklog.md)：固定日志入口、历史资料索引、G0 补记及本次制度建立记录。
- 更新 [AGENTS.md](AGENTS.md)：日志义务、追加方式、内容要求和统一模板。
- 更新 [04.总体实施计划](04.项目总体实施计划与阶段验收.md)、[docs/status.md](docs/status.md)、[docs/backlog.md](docs/backlog.md)：日志索引和任务结束流程。

### 问题、解决办法与决定

| 问题 | 处理办法 | 当前结论 |
| --- | --- | --- |
| 过去只有阶段报告，缺少统一过程日志 | 根目录固定使用 worklog.md，按时间顺序逐条追加 | 已建立统一入口 |
| 等完整 G 阶段结束才记录容易遗漏 | 每张任务卡或阶段性交付结束就记录；遇阻、中断也记录已做工作和恢复条件 | 固定为日常任务收尾要求 |
| 日志、状态与计划容易重复或不一致 | 日志写历史过程，status 写当前进度，backlog 管任务，阶段报告提供详细证据；收尾时一起核对 | 已加入协作规范 |
| 补记历史可能把未知事项写成已验证 | 明确补记来源，保留历史快照；缺失的执行信息标为未留存／未核实 | G0 未完成的构建和兼容性验证仍保留为待办 |
| 既有“运行日志不提交”容易与人工工作日志混淆 | AGENTS 明确程序原始运行日志与作为项目文档维护的 worklog.md 的区别 | 不改变自动提交权限 |

### 验证结果与下一步

工作目录为仓库根目录，使用 PowerShell 5.1 执行只读检查：以严格 UTF-8 解码读取五份新增／更新文档，检查 Markdown 围栏、表格列数、本地链接及锚点；用 Get-FileHash 比较修改前后的文件，用 `git diff --name-only HEAD`、`git status --short --untracked-files=all` 和 `git rev-parse HEAD` 核查范围与提交基线。

2026-09-17T09:49:10.6712212+08:00 的校验退出码为 0，结果为：5 份文档、64 个本地链接、14 个表格通过检查；已跟踪文件改动为 0，根提交未变。新增 worklog.md，修改四份已有规范／索引文档；其余五份原有文档（01～03、CLAUDE、G0 报告）的哈希保持不变。当前共有十个未跟踪文档，未执行 Git 提交。检查未发现缺失链接、损坏编码或 Markdown 基本结构问题。

本次仅修改文档，不涉及业务代码、依赖安装、构建或仿真；未运行业务测试。固定工作流程与日志制度已落地，后续任务按模板持续追加。下一项工程任务仍为 G1-T01；本次没有启动 G1。

## WL-20260917-003｜提交并推送项目文档

### 时间、关联任务与状态

- 工作日期：2026-09-17，UTC+08:00；10:00 开始检查，10:02 完成主要文档的成功推送。
- 关联任务：用户明确授权的文档提交与推送，包含前期规划、G0 交付和工作日志制度。
- 状态：主要文档已提交并推送；本条日志及状态页更新随独立收尾提交归档，其提交标识以 Git 历史为准。

### 背景、范围与工作过程

1. 用户要求“提交并推送”。检查当前分支为 `main`，上游为 `origin/main`，远程地址为 `git@github.com:flitai/uav-autonomy.git`；当时只有十份未跟踪 Markdown 文档，已跟踪源码无改动。
2. 执行 `git fetch origin main`，确认本地与远程基线一致，`git rev-list --left-right --count HEAD...origin/main` 输出 `0 0`，无需合并。
3. 按明确文件列表暂存十份文档：01～04、AGENTS、CLAUDE、docs 下三份 G0 文档及 worklog。核对暂存统计为 10 个文件、2220 行新增；严格 UTF-8 解码通过，暂存差异检查通过。保留 Markdown 有意使用的行末双空格，检查命令为 `git -c core.whitespace=-blank-at-eol diff --cached --check`。
4. 创建提交 `docs: add implementation plans, G0 baseline and worklog`。首次推送被 GitHub 以 GH007 邮箱隐私保护拒绝；本地提交成功，但当时远程未接收。
5. 使用 SSH 身份回显确认当前账号为 flitai，并从 GitHub 公开用户接口核对账号 ID 为 213015430。将未发布提交的作者与提交者邮箱改为该账号的 `213015430+flitai@users.noreply.github.com`，作者名称保持 zhiyu。使用单次 `git -c user.email=...`，没有修改全局／仓库邮箱配置，也未关闭 GitHub 隐私保护。
6. 只修正刚创建且尚未发布的提交，然后正常执行 `git push origin main`。推送退出码为 0，远程从 `061685f` 前进到 `05d4708`；没有强制推送或重写远程历史。
7. 按固定工作流程补记本次检查、失败原因、处理办法及成功结果，更新当前状态页的“文档未提交”信息，再提交日志收尾。G0 报告及前两条日志继续保留当时的历史快照。

### 成果与验证

主要文档提交：`05d47084a148bde44e32c7511490dcf0a002a3b7`。

| 项目 | 实际结果 |
| --- | --- |
| 工作目录／执行环境 | 仓库根目录，PowerShell |
| 文档提交 | 10 份文档已纳入 Git；提交命令退出码为 0 |
| 远程推送 | 正常推送到 origin/main；重试成功，退出码为 0 |
| 首次成功推送后的版本核对 | HEAD 与 origin/main 均为 05d47084a148bde44e32c7511490dcf0a002a3b7 |
| 首次成功推送后的工作区 | git status --short 无输出；随后仅修改本条日志和状态页进行收尾 |
| 业务范围 | 没有改动业务代码、安装依赖、构建或启动仿真；未运行业务测试 |

### 问题、解决办法与后续注意

| 问题／现象 | 处理办法与结果 |
| --- | --- |
| GH007：推送会公开受保护邮箱 | 核对真实 SSH 账号，使用对应 GitHub noreply 地址修正未发布提交；正常重推成功 |
| git add 提示后续检出可能将 LF 转为 CRLF | 保留已有 Git 行尾策略，没有为此重写文档或修改 .gitattributes；此次提示未阻止暂存与提交 |
| SSH 身份检查返回“认证成功，但不提供 shell”及退出码 1 | 根据明确的认证回显确认账号；该返回不等同于 Git 推送鉴权失败，后续实际推送成功 |
| 当前配置邮箱仍可能触发未来的 GH007 | 本次仅使用命令级邮箱覆盖；后续提交应复用该 noreply 地址，或由用户选择其他已验证且允许公开的邮箱 |
| 推送后状态页仍写着“未提交” | 收尾更新 status 的当前事实，保留 G0 报告与历史日志不变 |

下一项工程任务仍为 G1-T01。本次仅完成已授权的版本归档与日志记录，没有启动 G1。

## WL-20260917-004｜G1-T01：准备 Java 环境与生成物管理

### 时间、关联任务与状态

- 工作日期：2026-09-17，Asia/Shanghai（UTC+08:00）；实施核查从 11:26 开始，首次安装于 11:32 完成，八组工具验收于 11:35～11:36 完成，随后整理文档。
- 关联任务：G1-T01；状态：已完成。G1 进行中，G1-T02 可执行但尚未启动。
- 输入版本：`7a3d5e6c2f8060adb40b7728fc270be387f12ac2`，开始时工作区干净。
- 依据：用户确认的实施计划、G1 任务卡、[Java 环境验收报告](docs/g1-java-validation.md)及实际执行记录。

### 规划核查（补记）

本条同时补记此前规划模式中的只读工作：检查工程的 Java 8／11 源码配置、Ant 属性和工具状态；用户选择“本轮只完成 T01”和“项目内免安装”。通过官方发布资料、Adoptium API 和官方校验文件固定 Temurin 11.0.32.1+1、Ant 1.10.18 及压缩包哈希。规划时没有安装工具或修改文件。

规划核查发现当前执行策略为 Restricted，采用新进程的单次 Bypass 参数，不改变永久策略。读取 JDK 校验文件时，HTTP 内容为字节数组，直接 Trim 失败，改为先按 UTF-8 解码再处理，成功核对官方 SHA-256。

### 目标、范围与工作过程

1. 重新检查 Git、AGENTS、状态与既有日志，确认没有用户新改动；记录进程／用户／机器五项工具相关环境变量和五个执行策略作用域的初始值。
2. 先新增根级精确忽略规则，再创建固定版本清单。忽略范围仅为 .tools、out 和两个 Java 工程各自的 build／dist／nbproject/private；不忽略所有 JAR 或 XML。
3. 实现安装与启用两个入口及内部共用函数：按脚本位置定位项目，验证固定清单，下载至临时文件并核对哈希，解压到专用临时目录，创建安装记录并校验关键文件；已有目录不匹配时停止。
4. 实际运行安装入口。下载 JDK 199461714 字节、Ant 10413759 字节，两项均与锁定校验值一致；实际 java、javac、ant 版本命令均退出 0。工具放在项目 .tools 下，没有系统安装。
5. 增加可重复执行的环境验收脚本，使用 out 下的独立 Java 类和 Ant 配置验证 Java 8／11 编译、运行及实际 java.home；不涉及上游工程。
6. 验证重复安装、不同工作目录、中文空格项目路径、损坏缓存、缺失工具、失败下载和被改动的安装副本。故障均发生在隔离目录，正式工具再次核对通过。
7. 比较环境快照、执行策略与 Git，确认没有持久配置变化或上游源码改动；核对八个忽略路径与五个应保留路径。
8. 新增 Java 环境报告，更新总体计划、状态、任务卡与 AI 标准入口，并在此记录结果。G0 报告和既有工作日志不回写。

### 成果与修改文件

| 文件／目录 | 成果 |
| --- | --- |
| [.gitignore](.gitignore) | 八项精确目录忽略规则 |
| [固定工具清单](config/windows-java-toolchain.json) | 精确版本、URL、压缩包、哈希、安装路径、来源、许可证和关键文件列表 |
| [安装入口](scripts/windows/setup-java.ps1) | 下载／校验／安装／复用／版本检查、结果日志与环境恢复 |
| [启用入口](scripts/windows/use-java.ps1)及[共用函数](scripts/windows/java-common.ps1) | 校验后启用当前进程环境，避免两个入口重复维护关键逻辑 |
| [验收脚本](tests/windows/java-toolchain.tests.ps1) | 八组可复验场景，生成物均写入 out |
| [Java 环境报告](docs/g1-java-validation.md) | 版本、校验、命令、证据、Ant 输出属性与后续限制 |
| 总体计划、AGENTS、status、backlog、worklog | 同步 T01 完成与 T02 待启动状态 |

### 验证命令与结果

工作目录为仓库根目录，使用 PowerShell：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\setup-java.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\java-toolchain.tests.ps1
```

两次执行退出码均为 0。首个安装日志目录为 `out/runs/g1-t01-setup-20260917-113208-149/`；验收日志目录为 `out/runs/g1-t01-validation-20260917-113530-456/`。

- 实际工具：Temurin JDK 11.0.32.1+1 x64、javac 11.0.32.1、Ant 1.10.18；版本检查均退出 0。
- 八组验收全部通过；最小 Java 8／11 类文件主版本分别为 52／55，两次输出 PROBE_OK，Ant 使用锁定 JDK。
- 不同工作目录和 `project 中文 space` 路径实际安装、编译均成功；重复安装标记 reused，重复启用不重复添加 PATH。
- 预期故障被拒绝，子进程退出 1；损坏缓存未被解压，失败下载记录 failed，临时下载文件清理，缺失工具未部分改变进程环境。
- 15 项环境值、5 个执行策略保持不变；三个上游源码目录无差异，两个工程 build／dist 和项目 out/generated／out/build／out/artifacts 均未产生。
- 11:45:36 完成交付复核：12 个文件、88 个本地链接和 22 个 Markdown 表格通过编码、语法或结构检查；两个压缩包哈希再次核对一致。五份未涉及的历史文档和既有日志内容未变，Git HEAD 仍为本轮输入提交。

### 问题、解决办法与决定

| 问题／现象 | 处理办法与结果 |
| --- | --- |
| 两个工程源码级别不同且没有 Java 工具链 | 固定 JDK 11，先用 release 8／11 探针验证基本能力；实际工程兼容性仍由 T02／T04 验证 |
| PowerShell 5.1 会将 java -version 的 stderr 表示成错误记录 | 捕获输出并依据实际退出码判断，避免把正常版本输出误判为失败 |
| 写文件编排的模板插值与 PowerShell 变量语法冲突 | 首次调用在执行前失败，未写入脚本；改用不冲突的子表达式后重试成功 |
| 缓存或现有安装被修改时容易误用旧工具 | 对缓存和安装记录列出的关键文件分别校验；失败不覆盖，隔离故障测试通过 |
| 安装检查可能污染调用者环境 | 使用 finally 恢复五项进程环境值和临时设置；验收直接比较调用前后值，通过 |
| 现有 Ant 默认写入源目录 build／dist | 记录显式 build.dir／dist.dir／dist.jar 映射，后续 T02／T04 实测；未修改上游构建文件 |
| 文档更新补丁对同一路径同时声明删除与新增被拒绝 | 改为原位更新，保留既有文件；未影响源码或已通过的工具验证 |
| 汇总校验时将 JSON 数组额外包裹，误判验收条目数量 | 去掉多余数组包装，重读实际 results.json；八组均通过，汇总复核退出 0，无需重复工具测试 |

故障测试是人为构造的预期失败，不是正式工具安装失败。首次正式安装成功，正式工具未被测试破坏。隔离副本保留在 out/tmp 供追查，不能供后续构建使用。

### 遗留事项、回退与下一步

Java 环境准备与根级生成物忽略规则已经完成；LmcpGen／AMASE、消息库和运行链路的兼容性尚未验证。下一任务为 **G1-T02：构建当前工作区的 LmcpGen**，使用已锁定工具与输出属性，随后才推进消息生成和 AMASE。

关闭启用工具的进程即可结束临时环境；需要清理时，仅在核对解析后的绝对路径后处理本任务的工具／输出目录。本轮没有安装 C++／Node，没有构建上游工程、生成消息、启动仿真或提交／推送 Git。

## WL-20260917-005｜G1-T02：构建并验证本地 LmcpGen

时间／时区：2026-09-17，Asia/Shanghai；14:30 开始实施核查，14:34 首次构建，14:37 完成六组集成验收，14:47 完成边界修复后的复验，随后复核交付。

关联任务与状态：G1-T02 已完成；G1 进行中，下一任务 G1-T03 可执行但尚未启动。

背景、目标与范围：用户要求实施已确认的 T02 方案，从本地源码构建可追溯的新生成器并验证 CLI。沿用 T01 锁定工具与输出规则，保留 T01 尚未提交的成果。本轮不生成消息代码、不构建 AMASE、不安装 C++／Node、不启动仿真，也不提交或推送 Git。

### 规划核查与实施过程

1. 前序规划阶段只读检查确认 Ant 可自动生成缺失的 manifest.mf，现有 GUI 主类收到参数时转入 CLI；没有在规划阶段构建工程。发现 `-checkMDM` 捕获异常却可能返回 0，缺失文件也可能没有诊断，据此确定同时检查输入存在、退出码和输出。
2. 实施阶段复核根提交 `7a3d5e6c2f8060adb40b7728fc270be387f12ac2`、未提交修改、项目工具和现有任务卡；保存文档、环境与受保护文件快照到 `out/runs/g1-t02-session/`。LmcpGen tree 仍为 `5a03ccc3bead5fdbb98922a96d4e84fbfbf69c68`。
3. 新增 Windows 构建入口，按脚本位置定位仓库；复用工具启用脚本，使用锁定 JVM 调用官方 Ant Launcher，显式指定独立 build／candidate 目录、source／target 1.8 和 jar 目标，不下载依赖或引入用户 Ant 扩展。
4. 14:34 首次真实构建成功：21 个 Java 源文件完成编译，184 个 DTD／模板资源与输入哈希一致，三个关键类为主版本 52，帮助及 CMASI CLI 退出 0。没有修改上游源码或构建文件。
5. 首次编译诊断出现字符集乱码；在构建进程统一 UTF-8 和英文 JVM 诊断，保留原编译警告。补齐可重复的集成验收入口，实际再次构建，并验证调用者环境恢复。
6. 在含中文和空格的隔离项目副本中完成真实构建；使用复制的已验证工具，不复用 T01 的故障副本。依次测试缺失模型、损坏模型和编译错误，确认失败不会替换已有合格产物及其来源清单。
7. 六组验收全部通过；检查正式工具、源码、输出位置、用户／机器环境及执行策略，随后追加专题报告，更新状态、任务清单、总体计划与 AGENTS。CLAUDE 继续引用 AGENTS，历史 G0 及既有工作日志保持原样。
8. 收尾审查发现运行编号目录重名时，finally 可能覆盖旧运行记录；改为仅向本次成功创建的目录写结果，并在第一组验收中强制复现重名。旧 result.json 哈希保持不变，失败后的环境、目录和编码恢复；由于构建脚本有新修复，重新执行六组验收并更新正式 JAR 及来源清单。

### 成果与修改文件

| 文件／目录 | 成果 |
| --- | --- |
| [构建入口](scripts/windows/build-lmcpgen.ps1) | 输入及工具核查、独立构建、资源与 CLI 验证、成功后发布、失败记录和环境恢复 |
| [集成验收](tests/windows/lmcpgen-build.tests.ps1) | 六组实际构建、路径、环境及故障检查；测试数据只写入 out |
| `out/artifacts/lmcpgen/` | 新构建 LmcpGen.jar 与 build-info.json；属于被忽略的本机构建产物 |
| [Java 环境与构建报告](docs/g1-java-validation.md#6-g1-t02本地-lmcpgen-构建与验收) | 追加 T02 基线、输出、命令、哈希、结果、警告及边界；T01 历史章节保留 |
| [状态页](docs/status.md)、[任务清单](docs/backlog.md)、[总体计划](04.项目总体实施计划与阶段验收.md)、[AI 说明](AGENTS.md) | 标明 T02 已完成、T03 可执行但未启动，并加入实际可用构建入口 |

### 验证命令、证据与结果

工作目录为仓库根目录，实际执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\build-lmcpgen.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\lmcpgen-build.tests.ps1
```

两个入口均退出 0。首次日志目录 `out/runs/g1-t02-20260917-143411-694/`；最终正式构建为 `out/runs/g1-t02-20260917-144647-639/`；六组验收为 `out/runs/g1-t02-validation-20260917-144647-347/`。

- 正式 JAR 为 1,763,536 字节，SHA-256：`AE569E9C6D39741D88F2CA3712EC8C6D2B40BA3174E6D33E667FD7BF731B29BB`；主类为 avtas.lmcp.lmcpgen.LmcpGenGUI。
- 来源清单记录 225 项输入哈希，含 220 个 LmcpGen 文件、CMASI、工具清单和三个脚本；记录实际工具版本、完整命令参数、工作目录、起止时间和退出码。
- 重复构建使用新的输出目录；中文空格路径副本的实际编译与 CLI 成功；184 个资源逐项哈希一致，LmcpGen／LmcpGenGUI／MDMReader 字节码均为 52。
- 缺失模型停在 preflight、编译错误停在 ant-build；损坏模型的原 CLI 退出 0 但有 Error／Fatal Error，构建入口正确退出 1。三种故障均未更改副本已有 JAR 及来源清单，正式工具和产物不受影响。
- 十项进程环境值、调用工作目录与控制台编码恢复；20 项用户／机器环境值及执行策略未变。上游跟踪文件和 HEAD 不变，默认工程 build／dist 与 out/generated 均未产生，输出忽略规则生效。
- 14:48 交付复核通过：本次八个变更文件的 UTF-8、两个脚本的 PowerShell 语法、100 个本地链接／锚点和 27 个 Markdown 表格正常；225 项构建输入与正式 JAR 哈希再次核对一致。既有工作日志前缀、T01 报告第 1～5 节和受保护文件未变，结果保存于 `out/runs/g1-t02-session/final-validation.json`；30 项进程／用户／机器环境值的比较结果在 environment-final.json。

### 问题、处理办法与决定

| 问题／现象 | 原因、处理办法与结果 |
| --- | --- |
| 仓库没有 manifest.mf | 上游 Ant 已有自动创建逻辑；实际 JAR 主类正确，无需添加文件或改 GUI 入口 |
| 模型校验可能返回假成功 | 原 CLI 吞掉异常的失败退出码；入口先检查文件，再拒绝诊断输出，故障测试实际复现并正确拒绝 |
| 首次 javac 中文警告乱码 | JVM 默认字符集与 UTF-8 控制台不同；统一进程 UTF-8 与英文诊断，重复构建日志恢复可读，退出时恢复控制台编码 |
| JDK 11 编译 source 8 有警告 | 保留 bootstrap class path 警告及弃用／未检查提示，不顺手升级标准；仅声明字节码 52 与 JDK 11 运行通过，不宣称 JDK 8 运行已验证 |
| 失败后容易误用旧 JAR | 每次独立编译，验证后才发布；旧产物与来源清单一起保留，以本次 result.json 判定成败 |
| 重名运行目录可能影响旧日志 | 增加本次目录创建成功标记，禁止异常路径写入既有运行目录；强制重名验证被拒绝且旧日志哈希不变 |
| 首次文档批量补丁未匹配 backlog 完整行 | 工具拒绝该批修改；重新读取实际行后使用完整上下文重试成功，未影响构建产物与验收结果 |

三种故障为验收设计的预期失败，不是正式构建失败。首次正式源码编译即成功；没有进行上游兼容补丁、消息语义修改或工具升级。

### 遗留事项与下一步

T02 只验证生成器构建与 CMASI 读取入口；七个模型关联、Java／C++／Python 代码生成、Java 消息库、Python 导入与跨语言样本留给 **G1-T03**。AMASE 构建、旧消息库兼容性和运行闭环仍待后续任务。

T03 开始前重新检查 Git 状态、正式 JAR 与 build-info.json 哈希以及七个 MDM 输入；不得从 out/tmp 的测试副本取生成器。回退或清理仅针对已核对路径属于本任务的输出，保留失败证据和此前合格产物。T01、T02 的源码外围脚本和文档仍未提交，未自动进入下一任务。

## WL-20260917-006｜G1-T03：统一消息代码与 Java 消息库

时间／时区：2026-09-17，Asia/Shanghai；15:08 开始实施核查，15:17～15:18 首次完整生成与验证，15:22～15:23 完成八组集成验收，随后整理文档并复核交付。

关联任务与状态：G1-T03 已完成；G1 进行中，下一任务 G1-T04 可执行但尚未启动。

背景、目标与范围：按用户确认方案，用同一个生成器和七个 MDM 生成 Java／C++／Python 消息代码，构建 Java 消息库并完成双向序列化验证。沿用已有 JDK 11、Ant、Python 3.14.7，保留 T01／T02 未提交成果。本轮不编译 C++、不替换旧 Java 库、不构建或启动 AMASE、不联调网络、不提交或推送。

### 规划核查与本次发现

1. 前序规划阶段只读核对 T02 JAR 和全部 225 项输入哈希、七个 MDM、生成模板及旧 Java 库字节码；未生成代码。确认当前模型合计 164 个结构、19 个枚举；旧库 UXTASK 为版本 7，当前 MDM 为 8，其余系列版本一致。
2. 深入 Java 模板与旧库后发现 packMessage(true) 输出 Sentinel／属性外层，getMessageBytes 也读取该外层；AMASE TcpServer 调用这些方法。早期仅根据方法名判断“AMASE TCP 为原始 LMCP”不准确。决定保留现有封装，用文件／内存样本分开验证外层和内部消息，不在 T03 重构协议或提前联调。
3. 静态检查发现生成的 Python LMCPClient.py 在导入时主动连接网络，因此对顶层示例只检查语法；对全部生成消息包实际导入并实例化，不启动示例客户端。

### 工作过程

1. 实施前重新检查 Git、AGENTS、任务卡、工具与产物；根提交保持 `7a3d5e6c2f8060adb40b7728fc270be387f12ac2`。保存文档、环境、执行策略和受保护文件快照到 `out/runs/g1-t03-session/`。
2. 新增七模型固定清单，记录版本、命名空间和 G0 已核查的 SHA-256。Windows 入口复用 Java 工具启用流程，接收真实 Python 路径，临时清除干扰选项并在结束后恢复。
3. 用 Python 标准库实现生成编排，固定模型集合、验证 T02 来源、建立七模型快照，依次生成三种语言；Java 使用生成工程的 Ant jar 目标，输出全部放在本次 out/build 子目录，不手改生成文件。
4. 新增 Java／Python 文件与内存探针，共享固定样本；独立 JVM 加载新旧库，核对结构、系列、类型编号和公共接口；验证两种语言的消息字段、原始帧和重新编码字节。
5. 首次完整执行即通过：生成 1,013 个文件、Java 编译 196 个源文件，Python 导入 203 个消息模块并创建全部 164 个结构类型。六个帧样本双向一致，旧 Java 的 CMASI 样本也一致。
6. 比较发现 UXTASK SeriesEnum 和 29 个结构的版本改变，RendezvousTask 的部分旧接口被当前模型删除；当前 AMASE 静态引用的 101 类没有公共接口缺失。没有因此降级模型、覆盖旧库或宣称完整兼容。
7. 增加八组集成验收。首次测试入口的中文路径转换发生 UnicodeEncodeError，尚未执行场景；删除多余 ASCII 转换后，重复执行、中文空格路径、缺失模型、哈希错误、损坏模型、损坏样本、发布中断恢复和正式环境边界均通过。
8. 发布中断测试只修改隔离副本，在生成目录切换后使产物目录发布失败；恢复后上一批所有生成文件、JAR 与清单哈希完全不变。正式工具、模型、源码与产物未被故障测试修改。
9. 新增 T03 报告，更新总体计划、任务卡、状态及 AI 说明；更正目录分析的 TCP 判断和预先增加专用原始桥的建议。G0 和既有工作日志保持历史原文，T01／T02 报告只更新顶部历史定位及 T03 链接，原验收章节保留。
10. 收尾复核本次 16 个实现／文档文件、153 处本地链接与锚点、47 个 Markdown 表格；重新核对 19 项直接输入及 1,013 个生成文件哈希、正式 JAR、六个跨语言帧和八组集成结果。历史日志前缀、T01／T02 验收章节、受保护文件与上游目录均未改变；两份新增 PowerShell 脚本语法、36 项环境值、执行策略及两个工具的安装记录／文件哈希均通过。证据保存在 `out/runs/g1-t03-session/final-validation.json`、`environment-final.json` 和 `tool-final.json`。

### 成果与修改文件

| 文件／目录 | 本轮成果 |
| --- | --- |
| [模型清单](config/lmcp-models.json) | 七个 MDM 的版本、命名空间、哈希与来源基线 |
| [Windows 入口](scripts/windows/generate-lmcp.ps1)、[编排实现](scripts/lmcp/generate.py) | 检查、快照、三语言生成、Java 构建、消息验证、清单及成套发布／恢复 |
| [Java 探针](tests/lmcp/JavaProbe.java)、[Python 探针](tests/lmcp/python_probe.py)、[固定样本](tests/lmcp/samples.properties) | 独立类型／接口比较，int64 精度与原始 LMCP／Sentinel 样本验证 |
| [验收入口](tests/windows/lmcp-generation.tests.ps1)、[验收实现](tests/lmcp/generation_checks.py) | 八组实际生成、路径、故障和回退验证 |
| `out/generated/lmcp/`、`out/artifacts/lmcp/` | 三种语言代码及统一 Java JAR，配套同一 runId 的来源清单，均受忽略规则管理 |
| [T03 报告](docs/g1-lmcp-validation.md) | 输入、版本、哈希、命令、样本、旧新库差异、协议更正和 T04 交接 |
| AGENTS、目录分析、总体计划、status、backlog、Java 历史报告、worklog | 同步当前状态及已验证入口，保留历史证据 |

### 验证命令与结果

仓库根目录下执行，Python 变量指向已实际核查的 3.14.7 x64 安装：

```powershell
$pythonExe = Join-Path $env:LOCALAPPDATA 'Python/pythoncore-3.14-64/python.exe'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\generate-lmcp.ps1 -PythonExecutable $pythonExe
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\lmcp-generation.tests.ps1 -PythonExecutable $pythonExe
```

正式生成入口退出 0；修正测试路径构造后的集成验收入口退出 0，八组均 passed。首次完整生成记录为 `out/runs/g1-t03-20260917-151758-772465/`；最终正式批次为 `out/runs/g1-t03-20260917-152214-037296/`；最终验收为 `out/runs/g1-t03-validation-20260917-152213-517445/`。

- Java／C++／Python 分别生成 209／588／216 个文件，共 1,013 个，不含本项目额外的 generation-info.json；没有编译 C++。196 个 Java 目标类检查为字节码 52，Ant 成功。
- Java JAR 为 1,241,157 字节，SHA-256 为 `FD6F40587AF46C380B959BD6036A2D269D87CB2D62FC50BF3AC2333E31D07E4C`。两份清单 runId 一致，build-info.json 保存 19 项直接输入哈希，并通过 T02 清单追溯源码。
- Python 导入 203 个消息模块、创建 164 个结构，类型编号、系列标识及版本与 Java 一致；没有导入会建立连接的示例客户端，没有写入 __pycache__。
- 两个 AirVehicleState 样本帧长 188／204 字节，包含 ID=`9007199254740993` 和非空关联任务数组；TaskActive 帧长 51 字节。各以零校验和／计算校验和验证，六个原始帧双向字段及字节一致。
- Java Sentinel 帧通过现有流方法在内存中提取内部 LMCP，与 Python 帧相同。旧库 CMASI 样本也一致；新 Python 拒绝旧 UXTASK 7，旧 Java 拒绝新 UXTASK 8，均为预期结果。
- 缺失模型、生成器哈希不符、损坏 XML、损坏消息和注入发布故障的入口均退出 1；同进程预期失败另被捕获用于检查环境恢复，不误记为成功生成。
- 对比 36 项进程／用户／机器环境值及执行策略，保持不变；HEAD 和三个上游源码目录保持不变，旧 JAR、生成器及 T01／T02 工具脚本未改动。

### 问题、解决办法与决定

| 问题／现象 | 处理与结论 |
| --- | --- |
| 早期 AMASE 原始 TCP 判断不准确 | 从模板、随库字节码和实际内存样本确认 Sentinel；更正当前文档，历史 G0／日志不回写。真实网络兼容仍留到后续验证 |
| UXTASK 旧版本 7 与新版本 8 不兼容 | 实测双向版本识别拒绝，保留当前模型版本；T04 显式引用新库并排除旧类，不降级或覆盖旧库 |
| RendezvousTask 存在公共接口删除 | 记录 Location、Heading、MultiLocationRendezvous 访问接口及旧构造函数差异；101 个静态引用类未发现缺失，完整 AMASE 和动态使用仍待 T04 |
| 顶层 Python 示例导入有网络副作用 | 示例仅检查语法，所有消息包实际导入；未启动网络程序 |
| Java／Python packMessage(true) 返回层级不同 | 保留实现；Java 通过原有 getMessageBytes 取内部帧后做严格比较，不手改生成代码，不提前重构协议 |
| Python getObject 不自动完成全部帧校验 | 探针先检查标记、长度、校验和及工厂 validate；后续客户端也须保留验证步骤 |
| 首次集成验收的中文路径构造失败 | Python 字符串已解释 Unicode，额外 ASCII 编码导致异常；直接使用 Unicode 路径后重跑八组通过。失败目录 `g1-t03-validation-20260917-152156-997918` 不作为通过证据 |
| 收尾临时检查直接加载脚本被 Restricted 拒绝 | 复用既定的单次进程 `-ExecutionPolicy Bypass -File` 方式检查工具，通过；未修改持久执行策略。另一条临时策略比较将 JSON 字符串与对象数组直接比较，产生误报；统一序列化两侧后确认五个作用域值完全相同。这两次检查调用错误不影响已通过的生成和集成验收 |
| 两个目录分步发布可能中断 | 校验通过后发布，异常逆序恢复；在隔离副本实测第二步失败，上一批 1,013 个文件及产物／清单哈希不变 |
| Java 8 目标在 JDK 11 下的编译警告 | 保留 bootstrap classpath、弃用 API 和未检查操作提示；不修改标准，不宣称已在 JDK 8 运行 |

### 遗留事项与下一步

**下一任务 G1-T04：接入统一 Java 库，构建并运行 AMASE；尚未启动。** 先复核两份来源清单与 JAR，通过 Ant 的 `file.reference.lmcplib.jar` 指向 `out/artifacts/lmcp/java/lmcplib.jar`，旧 JAR 保留且不进入同一 classpath。

本轮的 101 类静态引用检查不能替代完整 AMASE 构建，也不能覆盖全部反射／动态消息使用。G1-T05 才接收实际仿真状态；C++ 编译归 G2，真实双向协议、半包／粘包、属性、来源过滤及回路检查归 G3。不得从 out/tmp 故障副本取正式输入。

需要回退时，只操作经解析确认位于本任务 out 子目录的产物，保留前一批备份和失败记录。本轮没有修改上游源码、模板或模型，没有安装新依赖，也没有提交／推送或自动进入 T04。

## WL-20260917-007｜提交并推送 G1-T01～T03 成果

时间／时区：2026-09-17，Asia/Shanghai；15:57 开始检查，15:59 完成实现提交的推送和远程核验，随后补齐文档归档记录。

关联任务与状态：用户明确要求“先提交并推送”；G1-T01～T03 的实现、测试和文档已提交并推送。本条日志和状态页作为独立收尾文档提交归档，提交标识以 Git 历史为准。G1-T04 尚未启动。

### 工作过程与范围

1. 检查 `git status --short --untracked-files=all`、当前分支和远程；分支为 main，远程为 `git@github.com:flitai/uav-autonomy.git`，起点 `7a3d5e6c2f8060adb40b7728fc270be387f12ac2`。待归档包含 T01／T02 留存的未提交成果和 T03 成果。
2. `git fetch origin` 成功；`git rev-list --left-right --count HEAD...origin/main` 返回 `0 0`，没有分叉或待整合的远程提交。
3. 核对忽略规则、上一任务的最终验收记录和待提交文件范围；工具、缓存、生成代码、构建产物及原始运行日志均留在受忽略的 .tools／out 下。上游源码、随库 JAR 和模型保持不变。
4. 按 AGENTS 的路径管理约定，将目录分析开头遗留的本机绝对工作区路径改为“本仓库当前工作区”；未改变分析基线。检查新文件及文档，未发现新增私钥或访问令牌。
5. 复用 WL-20260917-003 已验证的 GitHub noreply 邮箱，通过单次 `git -c user.email=... commit` 创建提交；不修改全局或仓库身份配置。采用普通推送，不强推或改写已发布历史。
6. 创建 `304def94cf683810bb63eeed256053332357aff2`（`build: add Windows Java toolchain and unified LMCP generation`），共 24 个文件；`git push origin main` 成功，远程从 `7a3d5e6` 前进至 `304def9`。随后 `git ls-remote --exit-code origin refs/heads/main` 返回同一完整提交标识，工作区状态为空。
7. 用户随后告知已关闭 GitHub 邮箱隐私拦截。记录此设置变化来自用户说明，本轮未查询或修改 GitHub 设置；已成功发布的 noreply 提交保持原样，Git 全局／仓库邮箱配置不变。此前的 GH007 记录仍是历史事实，不再将其视为必然的当前阻塞。
8. 更新本条日志及 status 的当前归档状态；通过单独文档提交和普通推送收尾，不为记录提交哈希而改写已有提交。

### 验证、成果与后续

- 初始 `git diff --check` 通过；暂存未跟踪文件后，完整 `git diff --cached --check` 检出目录分析新改行的行尾双空格，以及四份 T01 脚本已有的末尾空行。移除文档行尾双空格；保留已验收脚本的原始字节与来源哈希，单次检查仅排除 `blank-at-eof` 提示，不修改 Git 配置或程序文件。Git 的 LF／CRLF 提示属于现有行尾策略，没有批量格式化文件。
- 沿用本轮已完成的实际验收：T01 工具及路径／故障检查、T02 六组构建验收、T03 八组集成验收及六个跨语言帧；本次只归档并整理文档，没有修改程序或重复构建。
- 实现提交、推送和远程核验均退出 0；远程与本地实现提交一致。提交文件清单不包含 .tools、out 或三个上游源码目录，暂存范围检查通过；末尾空行以外的暂存空白检查通过。此次没有提交或推送失败。
- 下一阶段仍为 G1-T04；本次授权仅用于提交、推送和相应日志／状态归档。

## WL-20260917-008｜G1-T04：AMASE 构建与 GUI／无界面验收

时间／时区：2026-09-17，Asia/Shanghai；16:19 首次实际构建，16:38 自动验收与收尾预检通过，16:39 收到用户确认后正常退出并发布正式产物。

关联任务与状态：G1-T04 已完成，实现已提交并推送；本条实际推送结果及状态页另随文档提交归档，提交标识以 Git 历史为准。GUI 按用户选择保留供人工确认，收到确认后才正常关闭并发布。

### 规划检查与实施范围

- 从干净的 `45debc2` 开始；核对 AGENTS、T04 任务卡、T03 两份清单、19 项直接输入及生成文件哈希。沿用项目内 JDK 11.0.32.1+1、Ant 1.10.18 和已验证的 Python 3.14.7，不安装新工具。
- AMASE 工程要求 source／target 11；保留这一声明。场景固定为 WaterwaySearch，原时长 785 秒，两实体为 400／500；已有 ConstructiveControl 可自动推进和结束。
- 规划阅读曾按单个 XML 文件查找配置，实际配置是目录；另一次 Java 版本检查被 PowerShell 原生 stderr 的错误包装中断，采用已有的输出捕获方式后确认 java／javac 均退出 0。未在规划阶段修改文件或运行仿真。
- 运行配置、场景、地图资源、临时目录及日志均放入独立 out 目录。显式列出新消息库和四个随库依赖，不使用 lib 通配符，不修改旧消息库、不接 UxAS／Cesium、不执行 T05 客户端任务。

### 实施过程、发现及修复

1. 新增 Windows 构建／启动／验收入口及标准库编排实现；构建检查源码、依赖、资源、主类和字节码，产物暂存为候选批次。
2. 新增独立验收插件，监听真实 AirVehicleState、SessionStatus；记录类加载来源、窗口、时间、位置及地形缺省值。GUI 验证使用现有 SimTimer 启动、运行至少 20 秒后暂停；不伪造消息。
3. 首次 Ant 构建成功，但验收插件误写 `getSeriesVersion()` 导致编译失败；修正为实际接口 `getLMCPSeriesVersion()` 后通过。失败构建未发布。
4. 原 AMASE GUI 成功显示实体并推进时间，严格无界面模式实际抛出 HeadlessException。仅修改 Application 在无界面时跳过闪屏并输出未捕获异常堆栈；UserExceptions 在无界面时写日志。未重构界面、消息或算法，未改文件选择器。
5. 修复后的无界面模式完整运行 785 秒仿真时间，墙钟约 39 秒，两实体各记录约 1,480 条动态状态，正常退出并释放端口。
6. 第一次 GUI 退出时，验收插件关闭日志与末尾事件写入发生竞态，出现日志写入异常。给插件日志关闭增加同步及关闭状态检查；只修验收代码，保留失败记录并重新构建候选批次。
7. 启动候选 GUI 并保留后，首次自动套件发现主端口虽然隔离，实体 9400／9500 端口仍冲突；同时异常输出混用 UTF-8／系统编码导致捕获线程解码失败。新增显式 EntityPortOffset，只修改运行副本的活动实体端口，检查全部端口占用、PID 归属和释放；测试子 PowerShell 固定 UTF-8，先保留原始字节。未结束其他进程或静默选端口。
8. 重建候选 `g1-t04-build-20260917-162820-915606`，核对 437 项输入、737 个类和 57 个资源；GUI `g1-t04-run-20260917-162836-187500` 自动运行 20 秒后暂停，两实体分别 38／37 条状态，保留窗口待人工确认。
9. 自动套件 `g1-t04-automatic-20260917-163023-871549` 的十一组全部 passed，入口退出 0。覆盖其他工作目录、中文空格路径构建与完整运行、重复运行，以及缺失／损坏场景、依赖缺失、消息库哈希不符、编译错误、端口占用和边界检查；六类预期故障均明确失败。正式消息产物与当前源码输入未被故障测试修改。
10. 正式无界面记录 `g1-t04-run-20260917-163024-703456`，两实体状态为 1,481／1,480 条，会话状态 1,484 条，最终 Stopped 为 `785009` 毫秒；含启动收尾约 40.76 秒，退出 0，5556／19400／19500 全部释放。36 项环境值与执行策略未变化。
11. 自动检查完成后，按用户此前选择发送 GUI 人工验收确认请求，同时整理专题报告、工作日志、任务清单、总体计划和 AI 说明。未收到实际确认前，不调用 Finalize 发布或提交推送。
12. 16:38 收尾预检通过：17 个实现／文档文件、156 处本地链接与锚点、43 个 Markdown 表格、437 项构建输入哈希、十一组自动结果及六个预期失败退出码均一致；T03 产物、模型、旧依赖和既往日志未改变。四个 PowerShell 文件语法正常，初始 36 项环境值及执行策略保持不变。该次预检时人工确认仍为 pending。
13. 用户在 GUI 验收请求后回复“很好，继续”。按此回复登记验收确认，未额外推断用户每次点击的细节；通过标准验收入口执行 Finalize，传入构建、GUI、自动验收编号与确认原文上下文，未再次询问已授权的推送。
14. 收尾记录为 `g1-t04-finalize-20260917-163948-623929`，入口退出 0；GUI 在 16:39:50 正常退出，退出码 0，5555／9400／9500 释放，未强制终止。正式目录成套发布到 out/artifacts/amase，JAR 哈希与候选一致；36 项环境值及执行策略不变。随后更新专题报告、当前状态、任务清单、总体计划和 AGENTS，T05 保持尚未启动。
15. 16:47 最终收尾复核通过，覆盖正式产物与验收候选一致性、人工确认及正常退出记录、437 项输入哈希、十一组自动验收、17 个文件的 UTF-8、156 处本地链接与锚点和 43 个 Markdown 表格；此前 T03 产物和历史日志均保持原样。本次使用的六个端口已无监听。提交前 fetch 成功，本地与 origin/main 均为 `45debc2`，没有分叉；使用现有 Git 身份配置和普通推送，不修改邮箱设置。
16. 显式暂存本阶段 17 个实现与文档文件，`git diff --cached --check` 完整通过；无生成物、工具或原始日志进入提交，也无未暂存改动。Git 提示将来检出可能转换 LF／CRLF，没有执行批量格式化或改变已验收输入字节。
17. 创建实现提交 `f2f73ab24067d377c0d210d115fa7065ab6b5f82`（`build: add Windows AMASE runtime and validation`）；`git push origin main` 成功，远程从 `45debc2` 前进至 `f2f73ab`。随后 `git ls-remote --exit-code origin refs/heads/main` 返回相同完整标识，提交、推送及远程核验均退出 0，未遇到邮箱隐私拦截。实际收据保存于 `out/runs/g1-t04-session/push-implementation.json`；本条及状态页通过独立文档提交收尾，不改写已有提交。

### 当前证据与后续

首次构建、原无界面失败及修复后完整运行分别见 `out/runs/g1-t04-build-20260917-161925-344865/`、`out/runs/g1-t04-run-20260917-162016-205896/`、`out/runs/g1-t04-run-20260917-162107-881269/`。本次初始环境及历史日志快照保存于 `out/runs/g1-t04-session/`。

G1-T04 已完成，正式 AMASE JAR SHA-256 为 `D4E80C5318517266C161B636FDA4771FBD4921B71967E45F3C37C3A85E5E167E`；本任务启动的进程已结束，实现已提交推送。下一任务为 G1-T05，可执行但尚未启动；G1 整体尚未完成。详细入口、端口分配、地形限制和实际网络待验事项见 [T04 报告](docs/g1-amase-validation.md)。

## WL-20260917-009｜G1-T05：真实 TCP 状态接收与 G1 阶段验收

时间／时区：2026-09-17，Asia/Shanghai；21:53 完成规划来源核查，21:59 保存实施基线，22:07 开始实际自动验收，22:09:34 首次自动检查完成；用户确认后受控复验，22:24:53 正常退出并完成 Finalize。

关联任务与状态：G1-T05 已完成，G1 已完成；用户选择 GUI、无界面两种模式均验收，本轮已确认 GUI。G2 待细化且尚未启动，本轮没有自动提交／推送或进入 G2 的授权。

### 规划发现与实施范围

1. 基线 `164927d37d3e3c069ba4bb6263d41384d3f9a769`，初始工作区干净；重新核对 T04 正式 AMASE、437 项输入，T03 的 19 项直接输入、1,013 个生成文件及同批 Java 库，全部一致。实际 Python 3.14.7 x64 可运行，沿用既有 JDK／Ant。
2. 阅读 TcpServer、Java／Python 工厂、SimTimer、ConstructiveControl 和既有运行入口。确认无需订阅即可收到状态，网络为 Sentinel／属性外层加原始 LMCP；Python getObject 不代替完整校验。规划时两处读取路径误指向 headless／util，按实际 amase_headless／scenario 目录纠正，仅属只读查找问题。
3. ConstructiveControl 接到 Stopped 会直接 System.exit，不能要求 TCP 必然收到最后一条 Stopped。保留原 785 秒场景；网络证明连续状态，完整结束由同次运行证据及正常进程退出核验。
4. 新增独立脚本与测试，未修改 T04 源码／入口／探针、生成器、模型、生成代码或正式产物；未安装工具、重建 AMASE、启动 UxAS／Cesium 或发送控制消息。规划阶段未写文件，实施后才创建任务输出。

### 实现、验证与实际结果

1. 新增 scripts/validation/sentinel.py：按声明长度增量解析 Sentinel，验证两层校验和、属性、系列／版本、对象标志、消费长度和类型一致性；限制正文 1 MiB、属性 4 KiB、数字字段及缓冲区，不在二进制正文内寻找结束符。合格帧必须实际通过内层校验，零校验和不作绕过。
2. 新增 scripts/validation/amase_tcp.py 及 Windows receive-amase.ps1／tcp-common.ps1：从本轮运行记录绑定本机端口、PID、场景、正式 JAR 和实际加载来源；用统一 Python 包解码。只接收不发送，连接等待 15 秒，连接后不重连，至少观察 10 秒、最多 30 秒。记录原始字节、接收块、完整帧索引、解码字段、统计、来源及退出状态；int64 保存字符串。
3. 新增 tests/amase_tcp/checks.py／acceptance.py 和 tests/windows/amase-tcp.tests.ps1，自动编排 GUI、三次无界面运行、中文空格副本、真实帧回放与隔离故障。Finalize 必须提供本轮实际人工确认，只做正常退出与核验，不重新发布 AMASE。
4. 解析器先只读解析 T03 已有 Java Sentinel 样本，正确得到 CMASI 状态、UXTASK 8 TaskActive 和 ID `9007199254740993`；这只是可行性预检，不作为真实 TCP 证据。随后 24 项协议／接收预检及三个 PowerShell 文件语法检查通过。
5. 实际主命令在仓库根目录运行：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\amase-tcp.tests.ps1 -PythonExecutable <已验证解释器路径>`；批次 `g1-t05-automatic-20260917-220701-441101`，起止为 22:07:01～22:09:34，入口退出 0，十四组自动验收全部 passed，36 项环境值及执行策略不变。
6. GUI AMASE `g1-t04-run-20260917-220704-047865`，客户端 `g1-t05-receive-20260917-220708-754597`，在 10.032 秒内接收实体 400／500 各 18 条状态和 18 条 SessionStatus，共 54 帧／23,580 字节。源时间从 `4709` 到 `13769` 毫秒，位置真实变化；GUI 自动推进至约 20 秒后暂停并保留。
7. 无界面 AMASE `g1-t04-run-20260917-220727-280501`，客户端 `g1-t05-receive-20260917-220730-576077`，在 10.026 秒内接收两实体各 383 条状态和 384 条会话状态，共 1,150 帧／501,882 字节。实体时间从 `69909` 到 `269769` 毫秒；客户端主动关闭后，AMASE 继续完整运行至内部 Stopped `785009` 毫秒，退出 0 并释放端口。
8. 中文空格副本只复制已验证源码、工具和正式产物，不重新构建；实际无界面与 TCP 接收通过。再于正式工作区重复运行通过。两次各收到 1,150 帧，均完整运行原场景，退出 0、端口释放；运行目录和客户端结果未混用。
9. 对四次真实流量分别执行逐字节、整流合并、固定随机切块、边界切块回放，字段及帧序列一致；54／1,150／1,150／1,150 条网络目标状态均与同次内部事件按源时间和位置交叉核对。原始数据尾部均为 0 字节；协议支持单独记录主动停止时未消费尾部，EOF 半帧明确失败。
10. 24 项合成协议／回环检查包括校验、长度、截断、版本、属性、int64、无数据、状态不足、时间／位置停滞、连接失败和提前 EOF，全部按预期通过。隔离副本再分别破坏生成 Python、AMASE JAR、LMCP JAR，接收入口均退出 1 且记录 failed，诊断准确；finally 恢复故障副本，正式输入和产物未变。未用合成状态替代真实运行。
11. 自动完成后已请求本轮 GUI 人工确认，要求地图与实体显示、暂停／继续正常且无异常对话框。未收到确认前保持待确认，不复用 T04 的历史确认，也不提前关闭 GUI 或登记 G1 完成；同时整理专题报告和当前状态。
12. 22:16 收尾预检通过：15 个本阶段文件、152 处本地链接与锚点、40 个 Markdown 表格、437 项 T04 输入和 7 项 T05 输入哈希、四次客户端原始证据及历史日志前缀均一致；UTF-8、Python／PowerShell 语法和 git diff --check 正常。正式产物、上游源码、既有 T04 实现、36 项环境值及执行策略保持不变；仅保留 GUI PID 24484 的 5555／9400／9500 监听，无界面三个端口均释放。
13. 另外验证 Finalize 缺少人工确认时退出 1、记录 failed，错误为 Actual GUI confirmation required；没有创建 request-shutdown，GUI 仍为 awaiting-manual-confirmation。该项是收尾保护验证，不代表取得用户确认或完成 Finalize；收据为 `out/runs/g1-t05-session/missing-confirmation.json`。
14. 用户随后回复“很好，确认GUI”。22:20 收尾核查发现原 GUI 已由窗口操作正常退出，Java 退出码 0，六个端口均已释放；但 T04 入口对整段内部事件重新检查时记录 failed，原因为 Entity time regressed。事件显示 22:19:14 有明确 Reset，实体源时间从 `21910` 毫秒回到 `0`，随后继续运行并于 22:20:22 触发正常 shutdown。stderr 无异常；这是人工重置与既有单段时间单调验收的冲突，不能把该 failed 记录直接改成通过。
15. 保留原运行、自动批次及实际网络样本；原网络观察早于人工重置，其 54 帧和对应证据未失效。为取得当前严格验收器认可的正常退出记录，使用同一份未修改程序、T03／T04 产物及 7 项 T05 输入重新执行既有完整自动入口，批次 `g1-t05-automatic-20260917-222209-133086`。复用用户对同版本 GUI 的真实确认，明确该确认针对原 GUI；新窗口仅用于受控自动复验，不虚构用户再次操作，也不修改来源清单或放宽时间单调条件。
16. 受控复验十四组全部通过，入口退出 0；GUI 收到两实体各 18 条及 19 条会话状态，55 帧／23,732 字节；无界面 1,147 帧，中文副本 1,151 帧，重复无界面 1,147 帧，均满足各实体至少 10 条、源时间推进和位置变化。四种切块回放及同次内部事件对应一致，三个无界面完整运行正常结束；三种故障产物继续明确失败。详细运行编号、数量及哈希写入 T05 报告。
17. 通过标准入口执行 Finalize，参数记录用户原文“很好，确认GUI”、原 GUI 编号以及同版本受控复验的适用说明。收尾批次 `g1-t05-finalize-20260917-222451-856588` passed，入口退出 0；复验 GUI `g1-t04-run-20260917-222211-755532` 在 22:24:53 正常退出，Java 退出码 0，T04 入口记录 automatic-passed。六个端口全部释放，36 项环境值及执行策略不变，无强制终止。
18. 22:29 最终核验通过：15 个变更文件、152 处本地链接与锚点、41 个 Markdown 表格、437 项 T04 输入及 7 项 T05 输入哈希一致；四次复验客户端证据、正式产物和原重置失败记录均完整，既往日志前缀未改变。父进程的 36 项环境值和执行策略不变，六个端口及两个 GUI PID 均已释放；git diff --check 通过，仅有既有 LF／CRLF 提示。同步 status、backlog、总体计划、目录分析和 AGENTS，登记 G1-T05 与 G1 已完成，G2 待细化且尚未启动；没有提交或推送。

### 问题、解决办法与交付

- 现有 Java 外层与 Python 原始帧不同：新增独立接收解析层，保持上游消息语义和模板不变；真实两种模式及四种分包回放通过。
- 生成工厂对校验和与解码范围检查不足：在调用生成类前严格验证帧，检查 unpack 消费长度；零校验、截断和未知版本均被故障测试拒绝。
- 正常进程结束不保证 TCP 最后状态送达：网络验收与进程结束证据分开关联；没有伪造 Stopped，也未更改仿真控制器。
- GUI 与无界面并行的实体端口沿用 T04 已验证的显式偏移；未发生占用冲突。测试子 PowerShell 统一 UTF-8，未再次出现此前 T04 的混合编码问题。
- 本轮首次实际自动套件即通过，未发生需要修复生产源码的构建或运行失败；随后人工重置触发既有全程时间单调检查，已按上述记录保留 failed 现场并用同版本受控复验解决收尾。验收器按重置划分时间段留待 G6，未在此扩展协议或控制功能。预期损坏、超时和 EOF 均为隔离故障用例，不计作成功运行。
- 新报告为 [g1-tcp-validation.md](docs/g1-tcp-validation.md)。初始环境、原日志快照及协议预检保存在 `out/runs/g1-t05-session/`，原始数据受忽略规则覆盖。

### 遗留事项与下一步

本轮 GUI 已确认，Finalize 及正常退出通过；G1-T01～T05 均已完成，G1 已完成，G2 待细化且尚未启动。C++ 编译、UxAS 双向通信、完整重连矩阵、网关、真实地形及 Cesium 均不在本轮；重置分段验收按 G6 处理。本轮实现和文档尚未提交，不自动提交推送。

## WL-20260917-010｜提交并推送 G1-T05 成果

时间／时区：2026-09-17，Asia/Shanghai；22:32 开始提交范围核查。

关联任务与状态：用户在 G1 完成后明确要求“提交并推送”；G1-T05 的实现、测试、专题报告、状态说明及工作日志已提交并推送。本条实际推送结果和状态页另随文档提交归档，提交标识以 Git 历史为准。G1 已完成，G2 仍待细化，尚未启动。

### 过程、验证与边界

1. 检查工作区、分支与远程：main，origin 为 `git@github.com:flitai/uav-autonomy.git`，起点 `164927d37d3e3c069ba4bb6263d41384d3f9a769`。待提交为 15 个本阶段实现／文档文件，无无关修改；三个上游源码目录无改动。
2. `git fetch origin` 退出 0，`git rev-list --left-right --count HEAD...origin/main` 返回 `0 0`，没有分叉或需整合的远程提交。
3. 复核最终自动批次 `g1-t05-automatic-20260917-222209-133086` 的状态为 passed，7 项实现输入 SHA-256 与当前文件一致。沿用已完成的十四组自动验收、24 项协议／接收检查、人工确认及正常退出，不因归档重复启动仿真。
4. 核对根级忽略规则，工具、依赖缓存、生成代码、构建产物、原始流量及运行日志留在 .tools／out；仅显式暂存实现、测试和项目说明。提交前 git diff --check 通过；使用现有 Git 身份与普通推送，不修改邮箱设置或强推。

5. 显式暂存 15 个文件，核对暂存清单与预期完全一致；`git diff --cached --check` 通过，没有未暂存修改。Git 的 LF／CRLF 提示为现有策略，未格式化已验收程序或改变来源哈希。
6. 创建实现提交 `d77dd78298cfe23e113ec779c1e719eee196b52a`（`feat: validate live AMASE TCP states on Windows`），`git push origin main` 成功，远程从 `164927d` 前进至 `d77dd78`；随后 `git ls-remote --exit-code origin refs/heads/main` 返回同一完整标识。提交、推送及远程核验均退出 0，无身份或邮箱隐私拦截。
7. 实际收据保存于 `out/runs/g1-t05-session/push-implementation.json`；更新本条和状态页，通过独立文档提交及普通推送归档实际结果，不改写已发布提交。此前日志中的“未提交”保留为当时事实，由本条记录后续授权与执行。

此次归档不启动仿真、不修改生产实现、不扩大到 G2 实施。G1 已完成，下一步仍为 G2 任务细化。

## WL-20260918-001｜G2 任务细化与文档落地

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；09:10 开始实际工作区复核，09:21 完成文档预检。此前规划对话以本会话的用户选择及本次明确要求实施的方案为依据，不将该对话写成历史安装或构建结果。

关联任务与状态：G2 阶段方案和七张任务卡细化已完成；G2 已细化，实施尚未启动。G2-T01 可执行、尚未启动，T02～T07 因前置任务未完成标为阻塞，未发生 C++ 构建或运行失败。

### 背景、目标与范围

用户明确要求落地已确认的 G2 细化方案，本次授权仅涵盖阶段方案、完整任务卡、状态同步与工作日志。后续 G2 目标为 Windows 原生 UxAS 干净构建和 HelloWorld 正常双向收发／退出；本次没有安装工具、编写构建实现、生成消息、运行仿真、提交或推送。WaterwaySearch 双向闭环仍归 G3。

### 工作过程与重要决定

1. 从仓库根目录执行 `git status --short`，无输出、退出 0；`git rev-parse HEAD` 得到 `0e9da36ea97d447f178bfdedd5e04f4b6a5b53e2`。G1-T05 实现及收尾已归档，当前工作区干净，纠正规划时“尚有未提交 T05 修改”的旧快照；本次没有重写 G1 历史记录或提交。
2. 读取 status、backlog、AGENTS、总体计划和最近工作日志，复核 UxAS Makefile、依赖配方、HelloWorld 配置、服务及主入口、桥管理器和 TCP 收发源码。确认构建包含 resources 下的 AutomationDiagramDataService，原 HelloWorld 时长为 10 秒、周期为 1000／5001 ms，TCP 仍调用 CZMQ 的 zframe 接口。
3. 本轮 `Get-Command` 在 PATH 未找到 cmake、ninja、cl、MSBuild、vcpkg；常见 Visual Studio／CMake 安装目录及 vswhere 不存在。该结果限定于实际查找范围，没有据此宣称全机不存在工具。真实 Python 版本命令输出 3.14.7、退出 0。
4. 使用 PowerShell 读取统一生成清单并逐项 `Get-FileHash -Algorithm SHA256`：批次 `g1-t03-20260917-152214-037296` 的 588 个 C++ 相关文件全部匹配。这里只验证生成输入完整性，没有编译 C++，没有替换正式 Java／AMASE 产物。
5. 落实用户三项选择：MSVC＋CMake；首期关闭 Zyre／串口桥；使用 vcpkg manifest。规划时曾提供“固定源码与补丁”作为依赖管理建议，用户选择 vcpkg，因此最终方案采用固定 checkout／registry baseline、overrides 与必要 overlay ports，不另建并行的源码包管理主路线。
6. 固定待实施边界：MSVC v143／CMake 3.31、Release x64／动态 CRT，源码保留 C++11 兼容目标而 MSVC 使用 `/std:c++14`；T01 再固定具体工具补丁版本，T02 验证原配方依赖版本与补丁。MSVC／SDK 使用官方安装机制，独立工具及缓存项目内管理，避免把所有组件误写成便携工具。
7. 将任务分为工具链、依赖、C++ LMCP、CMake 构建图、平台适配、HelloWorld、复验发布七张卡，每张写齐状态、目标、前置、修改范围、边界、步骤、验收、证据、回退、停止条件和风险。T04 只验收构建图及静态拒绝路径，T06／T07 再验证关闭桥的真实失败；CZMQ 保留，可选桥启用支持延期。
8. 同步当前状态和下一任务为 G2-T01；历史 G1 报告与 backlog 的 G0／G1 部分保持原样。目录分析文档只更新开头的当前进度提示，避免仍显示 G2 待细化。全部拟建入口明确标注尚未实现，未新增未经运行的“标准命令”。

### 成果与修改文件

- 新增 [G2 实施方案](docs/g2-windows-uxas-plan.md)：输入基线、三项选择、工具与依赖策略、可选桥范围、任务顺序、拟建接口、验收矩阵、停止及回退条件。
- 更新 [任务清单](docs/backlog.md#4-g2-顺序与任务卡)：七张完整任务卡、阶段结束检查、G3～G8 轮廓和 Zyre／串口延期事项。
- 更新 [当前状态](docs/status.md)、[总体计划](04.项目总体实施计划与阶段验收.md)、[AGENTS](AGENTS.md)和 [目录分析](03.项目目录分析与Windows_Cesium改造计划.md)的当前进度与引用，并追加本条日志。共七个项目文档，无业务源码或构建实现变更。
- 编辑前字节快照与一次性文档检查器／结果位于被忽略的 `out/tmp/g2-docs-20260918/`；检查器仅服务本次文档验收，不列为项目标准入口。

### 验证、问题与解决办法

工作目录均为仓库根目录。

1. `git diff --check` 退出 0；Git 的 LF／CRLF 提示来自现有检出策略。逐文件核查发现原文件均为 LF，本次继续保留 LF／UTF-8，没有批量改行尾或格式化历史文档。
2. 通过已验证的 Python 运行 `python.exe -B out/tmp/g2-docs-20260918/check_docs.py`。09:21:14 首次退出 1，唯一问题是一次性检查器把目录分析第 3 行原有的两个尾部空格误判为非法空白；这两个空格是 Markdown 的合法硬换行。修正检查器以识别该语法，原文不变，首次 failed 收据 `validation-092114.json` 保留。
3. 09:21:28 同一入口复查退出 0，`validation-092128.json` 为 passed：七个文档的 UTF-8／行尾／代码块、172 处本地链接与锚点、39 个表格、七张任务卡各 11 个字段、状态及前置依赖一致。588 个 C++ 相关文件哈希仍全部匹配，G0／G1 backlog 历史及既有工作日志字节前缀未变；Git HEAD 和暂存区未变，根级 CMake／vcpkg 实现仍不存在。这里的链接数为追加本条日志前的预检统计，收尾按同一入口复查新增日志引用。
4. 纯文档任务未运行构建、仿真、HelloWorld 或既有 G1 自动套件；没有使用文档检查结果宣称工具链或依赖组合已经通过运行验收。除上述检查器误判外，本轮没有未解决的文档故障。
5. 09:22:54 追加日志后的收尾复查退出 0，`validation-092254.json` 为 passed：七个文档、178 处本地链接与锚点、39 个表格及全部任务字段通过，588 项 C++ 文件哈希和历史前缀保持一致；`git diff --check` 退出 0，变更仅含七个项目文档，业务源码、构建脚本、G1 专题报告及暂存区均未变化。

### 遗留事项与下一步

G2 实施仍未启动，具体工具补丁版本、SDK、vcpkg baseline／overlay 配方以及旧依赖的 MSVC 兼容性待 T01／T02 实际验证。下一项为 G2-T01；它的执行授权不由本次文档交付自动扩大。G2-T03～T07 按顺序推进，G3 再验证 AMASE↔UxAS 双向协议、来源过滤与真实命令执行。当前文档修改保留在工作区，本轮不提交推送。

## WL-20260918-002｜提交并推送 G2 细化文档

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；09:28 开始归档核查，09:30:47 完成方案提交、推送及远程核验。

关联任务与状态：用户在文档细化完成后明确要求“先提交推送”；G2 细化方案及相关文档已提交推送。本条实际结果与状态页另随收尾文档提交归档，提交标识以 Git 历史为准。G2 已细化，实施尚未启动，G2-T01 保持可执行、尚未启动。

背景、目标与范围：归档 G2 阶段方案、七张任务卡、状态说明和工作日志，共七个项目文档；不扩大为安装、构建或运行任务。WL-20260918-001 中“未提交”保留为该次交付时的事实，本条记录后续授权与执行。

工作过程与验证（工作目录：仓库根目录）：

1. 检查工作区、分支和远程：main，origin 为 `git@github.com:flitai/uav-autonomy.git`，起点 `0e9da36ea97d447f178bfdedd5e04f4b6a5b53e2`；六个已跟踪文档修改及一个新方案文件，无其他变更，暂存区为空。
2. `git fetch origin` 退出 0，`git rev-list --left-right --count HEAD...origin/main` 返回 `0 0`；没有分叉或需要合并的远程提交。
3. 读取上轮 `validation-092322.json`，状态 passed；逐项复核七个文档 SHA-256，均与验收时一致。沿用已通过的文档／链接检查，不为归档重复运行构建或仿真。
4. `git diff --check` 退出 0；工具、产物和一次性检查器／收据位于忽略目录，业务源码、构建脚本及 G1 验收报告无变更。Git 的 LF／CRLF 提示来自已有检出策略，本次不统一行尾。
5. 追加本条并同步 status 后，文档检查器再次退出 0，七个文档、178 处本地链接与锚点、39 个表格和任务字段均通过。显式暂存七个项目文档，暂存清单与预期一致，`git diff --cached --check` 退出 0，无未暂存修改；使用已有 Git 身份和普通推送，不修改身份设置或强推。
6. 创建方案提交 `4f0d4eb793fd4b9b8b44b65e2fba99f3bdc4a345`（`docs: detail G2 Windows UxAS tasks and acceptance`），`git push origin main` 成功，远程从 `0e9da36` 前进至 `4f0d4eb`。随后 `git ls-remote --exit-code origin refs/heads/main` 返回同一完整标识；提交、推送和远程核验均退出 0。
7. 实际收据保存于被忽略的 `out/tmp/g2-docs-20260918/push-plan.json`。将本条进行中的归档记录补齐为实际结果，并同步 status，通过独立收尾文档提交与普通推送归档，不改写已发布的方案提交。

成果与修改文件：已归档 G2 方案、backlog、status、AGENTS、总体计划、目录分析开头进度提示及 worklog；收尾只更新 status 与本条日志，未新增业务实现。

问题与解决办法：本次未遇到合并冲突、权限或邮箱隐私拦截；普通推送成功，无未解决归档问题。

遗留事项与下一步：G2 方案已归档；下一项仍为 G2-T01，实施尚未启动。本次归档不自动安装工具或开始构建。

## WL-20260918-003｜G2-T01 工具准备与部分验收（阻塞）

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；11:08 开始复查，11:14 完整准备首次失败，11:25 启动最终便携复验及完整准备复查，随后完成文档收尾。

关联任务与状态：G2-T01 部分完成／阻塞，尚未通过；G2 已启动，T02～T07 继续等待前置条件。用户已明确要求实施 T01；此前已选择系统默认 MSVC 安装目录。本轮不提交推送、不进入 T02。

背景、目标与范围：建立锁定 MSVC／SDK／CMake／Ninja／vcpkg 的准备、进程环境与验收入口，目标为真实 x64 C/C++ 编译、链接、运行。源码仍保持 C++11 兼容，MSVC 计划使用 `/std:c++14`、Release `/MD`；微软工具由官方安装器管理，便携工具与缓存位于忽略目录。

### 工作过程与成果

1. 重读 AGENTS、状态、任务卡、最近日志及现有 Java 工具脚本。起点为 `48c4d6bed9ef09bc06be24b8b56ea355f0a59fd8`，main 工作区干净；常见目录、PATH 及此前安装记录核查未发现所需 C++ 工具链，当前进程未提升权限。
2. 落地 `config/windows-cpp-toolchain.json`，固定 Build Tools 17.14.41／17.14.37710.0、v143 包 14.44.35229、SDK 发布 10.0.26100.7705、CMake 3.31.12、Ninja 1.13.2，以及 vcpkg 提交 `9e593bb18ea69cc5095e012465dcd675a822ed0d` 和 2026-07-27 工具。来源、SHA-256、组件和许可入口一并记录，发布包版本与未来实际二进制版本分开处理。
3. 新增 `scripts/windows/cpp-common.ps1`、`setup-cpp.ps1`、`use-cpp.ps1`，支持缓存与文件校验、固定 checkout、微软签名核查、安装器单独 UAC 提升、禁止自动重启、失败证据保留和进程环境恢复。为来源阻塞时继续验证独立部分，提供显式 `-PortableOnly`；`portable-ready`／`portable-passed` 不表示完整工具链验收通过。
4. 首次完整准备在微软组件目录 SHA-256 校验处退出 1，安装器未启动。使用 curl 加查询参数、PowerShell identity 编码与禁缓存再核查，三种方式返回相同不匹配文件；官方 channel 仍声明原哈希和长度。没有修改信任值或绕过校验，停止 MSVC／SDK 安装，继续独立便携部分。
5. CMake／Ninja 下载、解压文件校验通过；vcpkg 使用完整 Git 历史、固定 detached HEAD 和匹配 checkout 元数据的二进制。实际版本查询通过，未构建任何端口。vcpkg 默认清单指向 CMake 4.4，因此在项目进程内固定使用已校验的 CMake 3.31.12／Ninja 1.13.2；两条 `vcpkg fetch` 的实际路径均匹配，退出 0。
6. 新增 `tests/windows/cpp-toolchain.tests.ps1` 及 `tests/cpp_toolchain/` 下三个探针文件，准备 VS／Ninja、Release x64、整数／标准库／线程／Windows API、CRT 与路径验收。便携部分真实执行；原生安装、编译和运行部分保留为待验证，不当作已通过能力。
7. 新增 [T01 实施记录](docs/g2-cpp-toolchain-validation.md)，同步 status、backlog、AGENTS、总体计划、目录分析的进度提示及 G2 方案；G1 历史报告、上游源码和消息生成物保持原状。

### 验证、问题与处理

工作目录为仓库根目录，入口均使用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`；路径验收另从临时目录调用。证据位于被忽略的 `out/runs/`，故障只注入 `out/tests/` 副本。

- `setup-cpp.ps1`：`g2-t01-setup-20260918-111401-925` 和最终实现的 `g2-t01-setup-20260918-112530-241` 均退出 1。官方组件目录长度 30,443,537、SHA-256 `6e470016e4324c84c255ffd0beb3767d17ec89cc8561e9409ee3e1f6d29400f5`；实际长度 17,954,732、SHA-256 `f0a50ea157222c29abd5ea6ff01bfc3c33b04e011c5e45ee2ca38ef0778e5643`。bootstrapper 自身哈希匹配且微软签名有效，但目录不匹配仍阻断安装。原因尚未确认，不能判定为已确认的微软源站故障。
- 来源复核 `g2-t01-source-check-20260918-112152-652` 保存官方 channel 原始文件与实际结果。一次性诊断最初未正确处理二进制 HTTP 响应，`official` 字段为空；改为先保存原文件再解析并获得正确 payload，保留原诊断记录并注明被更正，目录名不代替实际时间。
- `setup-cpp.ps1 -PortableOnly`：`g2-t01-setup-20260918-111621-484` 退出 0；增加真实 vcpkg 解析检查后的 `-PortableOnly -VerifyOnly` 批次 `g2-t01-setup-20260918-111805-014` 退出 0。CMake、Ninja、vcpkg 可复用，无全局集成或持久 PATH 修改。
- `cpp-toolchain.tests.ps1 -PortableOnly` 首轮 `g2-t01-tests-20260918-112041-668` 退出 1：前八组通过，最后发现空的 `COLORTERM` 被环境恢复删除。原因是 .NET Framework 对空值的语义；修复为只写入变化项，必要时使用 Win32 区分空与缺失。小范围复查 68 项环境完全相同；原失败收据保留。
- 修复后 `g2-t01-tests-20260918-112402-237` 和最终 `g2-t01-tests-20260918-112530-237` 均退出 0、九组检查通过、状态 `portable-passed`。覆盖版本／哈希、实际工具解析、重复复用、其他工作目录、中文空格路径、损坏／缺失工具、版本不符、坏缓存、被改动 checkout、失败环境启用及环境恢复。最终记录含八个实现输入哈希，`notValidated` 明列原生安装和编译缺口。
- 四个新增 PowerShell 脚本语法解析通过；`git diff --check` 退出 0。一次性文档编辑脚本通过 PowerShell 默认编码管道时，中文匹配串变成问号，检查锚点即退出，未修改文件；随后使用原生补丁工具完成 UTF-8 编辑，没有改动全局编码或重写历史日志。
- 11:33:40 执行已核查的 Python `-B out/tmp/check_g2_delivery.py`，退出 0，收据为 `out/runs/g2-t01-docs-20260918-113340/result.json`：16 个交付文件的 UTF-8、194 处本地链接／锚点、41 个表格、代码块和当前状态通过；八个实现输入哈希与最终便携验收相符，九组检查通过。工作日志历史字节前缀、G0／G1 backlog 历史和暂存区未变，上游源码无改动。原始检查输出通过 Windows 控制台时中文文件名显示受终端编码影响，落盘 UTF-8 JSON 与文件内容检查均通过；不据终端显示重写文件。收尾追加此结果后按同一入口复查。

### 重要决定、遗留事项与恢复条件

本轮保持原信任哈希与工具链主路线。MSVC／SDK 安装器从未启动，无 UAC、系统组件安装或重启；真实 C/C++ 探针、编译器／SDK／CRT 来源、VS／Ninja 构建与中文路径下编译运行尚未执行。安装取消、失败和待重启分支仅代码检查，不能声称已通过真实安装测试。

下一步先取得与可信官方记录一致的固定版本组件目录，或可独立核验的官方更正依据；没有新来源证据时不继续重复失败下载。之后复用已验证便携工具，重查系统，执行完整准备与完整探针套件；全部通过后才登记 T01 完成、T02 可执行。无需卸载共享组件；保留失败下载、原始日志和现有合格工具。第三方依赖、registry baseline／overlay、LMCP、UxAS 和仿真均未进入实施。

## WL-20260918-004｜解除来源阻塞并完成 G2-T01

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；11:44 起取得微软原生来源验证证据，11:52～11:53 安装，12:05 完成最终完整验收，随后同步文档并检查交付。

关联任务与状态：G2-T01 已完成；G2-T02 可执行、尚未启动。承接 WL-20260918-003 的部分完成记录，用户明确要求“解决阻塞原因，然后继续”；沿用已授权安装范围，不进入 T02，不提交或推送。原阻塞日志和失败收据保留，本条记录后续修复，不回写历史为成功。

背景、目标与范围：查明来源校验为何阻断固定微软工具链，取得可独立验证的来源证据，继续原生工具安装、进程环境和真实 C/C++ 编译运行验收。保留固定产品版本、C++11 兼容源码、MSVC C++14 模式与 Release x64／动态 CRT。

### 工作过程、问题与修复

1. 保留工作区现有 T01 文件及 G1 产物，继续检查官方组件目录。不同 JSON 排版的摘要比较未得到 channel 声明值；系统代理核查也不能证明传输损坏，没有据此降低校验要求。
2. 改用已经核验 SHA-256 和微软 Authenticode 签名的固定 17.14.41 bootstrapper，仅创建最小下载 layout。`g2-t01-layout-diagnose-20260918-114403-618` 中准备与官方 `--verify` 均退出 0，目录实体仍为 SHA-256 `f0a50ea157222c29abd5ea6ff01bfc3c33b04e011c5e45ee2ca38ef0778e5643`；日志明确为 `ManifestVerifier Result: Success`，产品为 17.14.41／37710.0。
3. 在隔离副本只将目录 `productPatchVersion` 从 41 改为 42，同一官方校验器退出 `-2146233088`，记录摘要不符及 `InvalidSignature`。证据 `g2-t01-manifest-negative-20260918-114646-932` 保留，正式目录及共享组件未被修改。
4. 阻塞的直接原因是项目入口仅信任不一致的 channel 描述摘要，缺少利用微软原生签名验证实体的路径。根据正、负向新证据锁定已验证实体哈希，保留原 `channelDeclaredSHA256`／长度，不是仅根据本地下载值改写信任。channel 与实体摘要差异的发布端原因仍未知，未宣称微软已发布更正或网络已损坏。
5. 实现固定 layout 准备、原生签名及全缓存验证，再以 `--noWeb` 从已验证缓存安装；固定安装器 OPC 哈希和版本，保存各次命令、PID、退出码及安装日志。使用三个必要组件，不使用 `--includeRecommended`。完整批次 `g2-t01-setup-20260918-114914-983` 的安装器于 11:52:10～11:53:57 退出 0；仅安装器经 UAC 提升，安装在微软默认目录，完整且可运行，无待重启状态，没有自动重启。
6. 实际安装后修复两个之前未能动态验证的问题：安装包清单位于 `state.packages.json`，不是 `state.json.packages`；PowerShell 5.1 向 `cmd /c` 传递带空格的嵌套引号失败，改用签名有效的微软 DevShell DLL 和 `Enter-VsDevShell`，显式 Host／Target amd64、SDK 与不切换目录。两次总体 failed 记录 `114914-983`／`115452-665` 均保留，不将安装成功冒充完整准备通过。
7. 实测目录版本 14.44.35207、编译器 19.44.35229.0、链接器 14.44.35229.0、SDK kit 10.0.26100.0、资源编译器 10.0.26100.7705，分别记录包、目录和二进制版本。`setup-cpp.ps1 -VerifyOnly` 批次 `g2-t01-setup-20260918-115658-150` 退出 0；复用已有工具的完整准备 `g2-t01-setup-20260918-120408-848` 也退出 0，无重复系统安装。
8. 完整探针初次批次 `g2-t01-tests-20260918-115746-651` 在普通路径 VS／Ninja 均已编译运行后退出 1：检查器只认 `/std:c++14`，漏认 Ninja 输出的等效 `-std:c++14`。仅修正参数前缀匹配，保持编译期标准／动态 CRT 检查，随后全套通过。

### 成果与实际验证

修改 `config/windows-cpp-toolchain.json`、C++ 公共／准备／环境脚本及验收探针，完善 [T01 专题记录](docs/g2-cpp-toolchain-validation.md)。同步 [status](docs/status.md)、[backlog](docs/backlog.md)、[G2 方案](docs/g2-windows-uxas-plan.md)、AGENTS、总体计划和目录分析的当前进度；上游业务代码及既有 G1 验收报告未改动。

工作目录为仓库根目录，入口均为 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`；探针配置、构建、CTest 及直接运行从临时工作目录调用。原始证据位于被忽略的 `out/runs/`，layout／便携工具在 `.tools/`，隔离故障在 `out/tests/`。

- `tests/windows/cpp-toolchain.tests.ps1` 最终批次 `g2-t01-tests-20260918-120323-636`，12:03:23～12:05:30，退出 0、状态 passed。十组检查覆盖版本／哈希和 vcpkg 实际解析、重复准备、中文空格副本、损坏／缺失工具、版本不符、坏缓存、修改 checkout、真实构建运行及环境／忽略规则。
- VS 2022／Ninja × 普通／中文空格路径，共四个新建输出目录、八个 C/C++ 程序，Release x64／v143、C++14／动态 CRT，CTest 与直接执行全部退出 0。输出精确验证 `9007199254740993` 和 C++ 双线程累计 2000；PE 架构、实际编译参数、头文件及库搜索来源已记录，没有以旧程序替代本次构建。
- 四个 C++ 程序还实际读取加载模块并正常退出：VC DLL 为系统目录中的 14.50.35719.0，UCRT 为 10.0.26100.9444；安装清单 VC Redist 包为 14.44.35211，编译／链接工具及实际运行库分别记录，不降级共享组件。每个加载模块的本机路径和哈希只保存在原始记录。
- `out/tmp/check-use-cpp.ps1` 批次 `g2-t01-activate-20260918-120449-605` 从临时工作目录调用实际 `use-cpp.ps1`，Host／Target x64 与固定工具路径正确，退出 0，随后完整恢复进程环境。完整套件同时确认用户／系统 PATH 前后不变，无全局 vcpkg 集成、未安装任何端口。
- 最终验收保存八个实现输入 SHA-256。安装签名篡改拒绝及工具故障有真实负向证据；取消、普通安装失败、待重启分支仅代码检查，未为了测试故意中断安装或破坏共享组件。
- 12:11:38 运行已验证 Python 的 `-B out/tmp/check_g2_delivery.py`，退出 0，收据 `out/runs/g2-t01-docs-20260918-121138/result.json`：16 个交付文件、198 处本地链接／锚点、44 个表格、UTF-8／代码块及阶段状态一致；八个实现输入哈希与最终完整验收一致。四个 PowerShell 脚本语法解析、`git diff --check` 均通过；日志截至 WL-20260918-003 的原字节前缀、G0／G1 backlog 历史和暂存区未变，上游源码无改动。工具、产物、原始日志及本机路径未进入 Git 变更；Git 的 LF／CRLF 提示来自既有策略，未统一行尾。补齐本项实际结果后按同一入口完成收尾复查。

### 重要决定与下一步

来源问题依据微软原生签名链及篡改拒绝对照解决，保留矛盾元数据及旧失败收据。工具链验收已完成；channel 摘要差异的发布端原因仍未知，但不再阻断已独立验证的实体使用。后续来源、版本或完整性发生变化仍明确失败，不接受任意新下载摘要。

T02 可执行、尚未启动，下一项为固定业务依赖、baseline／overlay／triplet 及功能探针。尚未构建 LMCP／UxAS，未运行 HelloWorld 或 AMASE，不宣称 G2 已完成；没有根级 UxAS CMake 或业务依赖 manifest。保留共享安装、工具缓存及全部失败／成功证据，不自动卸载、提交或推送。

## WL-20260918-005｜持续授权完成后自动提交推送及 T01 归档

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；12:21 开始远程核查与约定更新，12:22:38 完成 T01 实现推送及远程核验。

关联任务与状态：项目协作约定更新及 G2-T01 实现已提交推送；本条实际结果与状态页另随收尾文档提交归档，提交标识以 Git 历史为准。T01 已完成，T02 可执行、尚未启动。

背景、目标与范围：用户明确要求“以后确定完成阶段性工作后，自动提交与推送”。将这一持续授权保存为项目约定，无需后续逐次确认；本次同时归档已经通过验收、尚未提交的 T01 实现和文档。WL-20260918-004 的“不提交”保留为当时边界，本条记录后续授权。

工作过程与成果：更新 [AGENTS](AGENTS.md) 第 7、8、10 节，将完成、验收、日志／状态同步、提交推送及远程核验纳入收尾流程；同步总体计划、backlog 与 status。保留用户后续指定暂不提交的优先权，仅提交本任务改动，普通推送，不夹带无关修改或改写已发布历史。归档不自动开始下一任务，也不扩大安装范围。

验证（工作目录：仓库根目录）：起点 main／`48c4d6bed9ef09bc06be24b8b56ea355f0a59fd8`，16 个 T01 交付文件待归档，无其他工作区改动、暂存区为空。`git fetch origin` 退出 0，`git rev-list --left-right --count HEAD...origin/main` 返回 `0 0`。沿用最终十组工具链验收及八个探针结果，提交前复核八个实现输入哈希、文档链接／UTF-8、历史日志前缀和忽略范围；本次仅修改约定及记录，不重复运行已通过且输入未变的编译套件。

归档结果：12:22 文档复查退出 0，收据 `out/runs/g2-t01-docs-20260918-122200/result.json` 包含 16 个交付文件、200 处本地链接／锚点、44 个表格，八个实现输入哈希与完整验收一致；`git diff --check`、显式暂存清单及暂存差异检查通过。创建提交 `78f10562e0542f9e85c05ebf5deaf8356ea3fd27`（`feat: prepare and validate pinned native Windows C++ toolchain`），`git push origin main` 退出 0；12:22:38 的 `git ls-remote --exit-code origin refs/heads/main` 返回相同完整标识，工作区干净。收据在被忽略的 `out/tmp/g2-t01-push.json`。随后仅补齐当前进行中的本条与 status，独立提交收尾文档并普通推送，不改写已发布实现提交。

问题与处理：没有远程分叉、冲突或推送失败；沿用现有 Git 身份。工具／下载缓存、生成物、原始日志和本机绝对路径受忽略规则覆盖，未进入提交；既有日志截至 WL-20260918-004 保持原文。

遗留事项与下一步：G2-T02 尚未启动。以后阶段性工作确认完成后按新约定自动提交推送，受阻或部分完成时记录真实状态；此持续授权不自动开展下一任务。

## WL-20260918-006｜G2-T02 固定第三方依赖、原生构建与验收

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）。实际依赖构建记录自 13:21 起；最终源码重建 13:54:26～13:56:10，首次完整验收 13:59:07～14:00:25，最终重复验收 14:03:20～14:04:37；随后进行文档与 Git 归档检查。

关联任务与状态：G2-T02 已完成；T03 可执行、尚未启动。G2 阶段尚未完成。本次仅构建第三方依赖和独立消费探针，没有编译 LMCP／UxAS 或运行仿真。

背景、目标与范围：按用户确认方案采用 vcpkg manifest／固定 baseline／overrides／overlay，保留原依赖版本、静态第三方库及动态 CRT。用户明确选择 Boost 历史模块 ports 加完整传递锁定和必要辅助修复，不采用整包配方。开始时 main／`4c132e25fdadfd6fb7a807653fd438abcbb5a318` 工作区干净；T01 固定工具完整性、版本及 CMake／Ninja 实际解析再次通过。

### 工作过程与问题处理

1. 对照原 Anod 配方、版本库和补丁建立清单。锁定 vcpkg checkout／registry baseline `9e593bb18ea69cc5095e012465dcd675a822ed0d`；递归历史图为 81 个 Boost 模块／辅助 ports，加六项非 Boost 依赖及两个 vcpkg CMake 辅助 ports，共 89 个。Boost 库保持 1.74.0 及固定历史 port-version，辅助基线 build 1.74.0#0、modular-build-helper 1.74.0#2、vcpkg-helpers 7#1；修改的前两者分别使用 overlay #1／#3。直接和传递依赖均登记，默认图不含 Zyre／serial。
2. libzmq 4.3.1、CZMQ 4.0.2、SQLite 3.39.4 新增固定源码配方；cppzmq 4.2.2、pugixml 1.12.1 和 SQLiteCpp 1.3.1 处理旧辅助调用、安装布局、静态传递链接及外部 SQLite。pugixml 原 `as_int64` 补丁只转换格式、固定窄字符，空／非法文本仍返回 0。SQLite 开启列元数据和线程安全；源码核查确认 SQLiteCpp 会追加 `/MT`，补丁移除后统一 `/MD`。保留 libzmq TCP／STREAM／TweetNaCl CURVE 与完整稳定 CZMQ，未升级主要依赖。
3. 初次运行发现固定 vcpkg 还需 PowerShell 主机工具，且子进程会清理便携 Ninja 的 PATH。补齐固定 PowerShell 7.6.3、7-Zip／7zr 26.02 及来源检查，triplet 传递受控 PATH；pkgconf 2.5.1-1／MSYS2 runtime 3.6.5-1 仅作构建辅助并登记固定来源。最终不复用系统 7-Zip。仅获取指定主机工具时切换下载设置，之后恢复并复核 CMake 3.31.12／Ninja 1.13.2。
4. 固定 vcpkg 的 SPDX 函数使用 CMake 4.x `STRING_ENCODE`，与锁定 3.31 不相容。增加项目 triplet 加载的等价 JSON 编码实现，保留源码资源／校验字段，未修改 vcpkg checkout；独立 JSON 往返覆盖中文、引号、反斜线、分号及控制字符。libzmq 已编译后因错误引用 LICENSE 安装失败，按真实源码改为 COPYING／COPYING.LESSER；失败记录保留。
5. Boost.Build 旧辅助代码先不识别 v143，随后中文路径中的系统编码与 UTF-8 混用使准备文件／响应文件失效，库名仍假设 vc140。增加真实 v143 分支、固定 cl 路径与 vc143 名称；拆分 include 参数，为 b2 嵌入进程 UTF-8 manifest，并在最终响应文件写入时只加一个 BOM。曾在 Jam 表达式加 BOM 导致每个对象前重复标记，按真实链接错误改到写入层。没有伪装 v142、修改机器代码页或删除传递模块。
6. 所有 ports 安装后，PowerShell 5.1 将 `Get-Content -Raw` 的 provider 属性深度序列化，导致记录进程异常耗时；核对 PID／命令行后只停止本次记录进程，批次 `133855-877` 登记 failed，保留安装结果，记录文本改用 `File.ReadAllText`。允许图直接／传递名称未去重造成 `134551-953` 计数拒绝，修正后重新执行。`135216-386` 在运行期间公共辅助输入变化，结束时哈希校验如实拒绝；以稳定输入重新完整构建，不更改旧收据冒充成功。
7. 消费探针修正了旧版 API 假设：pugixml 1.12.1 的公开宏实际为 1120；SQLiteCpp 使用 `SQLITE_OPEN_*`／`getText()`。探针取消 CZMQ 暴露的旧 `snprintf` 宏以免污染 Boost 头，没有修改 CZMQ 解析或消息行为。Ninja 长链接行的无 BOM 响应文件误读中文库路径，验收 CMake 将库参数移到 Unicode 命令行；此限定与后续 UxAS 适配边界写入报告。Ninja 会吸收头文件输出且将路径相对化，改从真实 `ninja -t deps` 数据库解析绝对来源后核对。
8. 短进程曾返回 null 退出码，虽然 CTest 文本显示通过，整套仍判失败；保留 native handle 后取得真实退出码。首次发布通过后，重复验收发现 `.NET File.Replace` 的空备份路径在 PowerShell 5.1 下无效，批次 `140053-860` 退出 1且保留旧指针／旧包；改用唯一真实备份路径后，`140320-336` 全部通过并原子更新指针。

### 成果与实际验证

新增根级 `vcpkg.json`／`vcpkg-configuration.json`、[固定来源清单](config/windows-dependencies.json)、`config/vcpkg/` 的 triplet／配方／补丁和许可说明；新增 `scripts/windows/build-deps.ps1`／`deps-common.ps1`、`tests/windows/deps.tests.ps1`、`tests/dependencies/` 及 [T02 专题报告](docs/g2-dependencies-validation.md)。`UxasDependencies` 提供七个 `UxasDeps::*` 消费目标，`Resolve-DepsPackage` 验证合格身份、来源和完整安装清单。同步 status、backlog、G2 方案、总体计划、目录分析进度与 AGENTS；上游源码和 G1／T01 实现不变。`.gitattributes` 仅保留统一 diff 上下文及历史辅助配方的空白格式；未统一全库行尾。

以下命令工作目录为仓库根目录，入口均使用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`；消费配置、构建和 CTest 从临时工作目录调用。

- `scripts/windows/build-deps.ps1 -Rebuild`：最终 `g2-t02-build-20260918-135426-199` 退出 0、candidate；全新中文空格目录，两个阶段读取二进制缓存均为 0，89 个 ports 成功，27 个静态库、10,602 个安装文件纳入 SHA-256 清单。保留工具版本、输入哈希、SPDX 来源、命令／退出码和完整安装状态。
- `tests/windows/deps.tests.ps1 -BuildRunId g2-t02-build-20260918-135426-199`：首次 `135907-382` 和最终 `140320-336` 均退出 0、11 组通过。每次 VS／Ninja × 候选／迁移正式包共四个新工程，16 次 CTest 和 16 次直接运行全部通过；覆盖其他工作目录、中文空格路径、重复功能运行及正常清理。
- ZeroMQ／CZMQ 验证二进制／空帧／多帧双向互操作、本机 TCP STREAM 的身份帧、空连接通知和数据收发、CURVE 能力；XML 验证超过 2⁵³、正负 int64 边界、缺省及原空／非法行为；Boost 验证四模块及 geometry、graph 最短路径、DynamicBitset；SQLiteCpp 验证数据库持久读写、事务提交／回退、64 位整数、UTF-8、二进制、列元数据与外部 SQLite 3.39.4。
- `scripts/windows/build-deps.ps1`：缓存复建 `g2-t02-build-20260918-135828-266` 退出 0，两个阶段恢复 8／81 个包；输入与源码重建一致，全部 10,602 个安装文件 SHA-256 一致。对照收据 `out/tmp/g2-t02-cache-comparison.json` 为 passed；缓存恢复未冒充新的源码编译。
- 编译参数和静态断言为 x64／C++14／Release `/MD`；27 个库指令、实际头文件／链接库来源及 PE 依赖核对通过，没有 Debug／静态 CRT 或第三方业务 DLL。实际 VC DLL 为 14.50.35719.0、UCRT 10.0.26100.9444，来源为系统目录，与编译器版本分别记录。
- 隔离缺失／损坏库、源码哈希不符、配方／工具清单变化、元数据错误和超时全部明确失败；只结束本次超时 PID。副本故障不改写旧合格指针；真实发布替换失败也保留旧包，最终发布保留旧指针备份。用户／系统 PATH 前后不变，进程环境恢复，无全局集成。
- `out/tmp/check-deps-resolution.ps1` 调用实际 `Resolve-DepsPackage`，退出 0；当前指向源码重建 `135426-199` 与最终验收 `140320-336`，来源／输入／文件核对通过，收据为 `out/tmp/g2-t02-resolve.json`。
- 14:11:46 交付检查退出 0，收据 `out/runs/g2-t02-docs-20260918-141146/result.json`：53 个交付文件、213 处本地链接／锚点、40 个表格、UTF-8／代码块及状态一致，46 个构建／验收输入哈希与最终证据一致；PowerShell 语法、忽略范围及暂存差异检查通过。原日志字节前缀和 G0／G1 backlog 历史保持，未改动上游源码或 T01 输入。`git fetch origin` 退出 0，提交前 `HEAD...origin/main` 为 `0 0`；`.gitattributes` 保留原 PDF 规则，并为补丁上下文／历史配方格式限定空白检查例外。

重要决定与影响：保留已选历史依赖和完整 Boost 模块图；兼容修复止于构建、安装和链接层。主机工具与业务库的来源分别记录；库包、运行记录、候选和正式指针分离。CMake 配置不隐式安装依赖，失败不使用旧程序假报成功；所有失败证据留在忽略目录，不提交原始日志、缓存或本机路径。

遗留事项与下一步：T03 可执行、尚未启动，开始时重新核对合格依赖及 G1 七模型生成来源。CZMQ 宏、更多 UxAS include／平台问题由 T05 按真实编译处理；Ninja 的更长命令／任意工程布局尚未验证，主工程仍用 VS 2022。没有 LMCP／UxAS 编译、HelloWorld、双向协议或仿真运行结果，不宣称 G2 通过。按持续授权，完成 UTF-8、Markdown／链接、来源一致性及提交范围检查后自动提交、普通推送并核对远程；不进入下一任务。

归档结果：53 个交付文件经显式暂存、`git diff --cached --check` 和无未暂存改动检查后，创建提交 `a13f6981868c7aedcc9d6ed5b3d1417288cee5c6`（`feat: build and validate pinned Windows native dependencies`）。`git push origin main` 退出 0；14:12:31 的 `git ls-remote --exit-code origin refs/heads/main` 返回相同完整标识，工作区干净，收据为 `out/tmp/g2-t02-push.json`。随后仅补齐本条实际结果与 status，独立归档收尾文档；不改写已发布实现提交，不进入 T03。


## WL-20260918-007｜G2-T03：Windows C++ LMCP 库与三语言验收

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）。

关联任务与状态：G2-T03 已完成；G2-T04 可执行、尚未启动，G2 阶段尚未完成。

背景、目标与范围：用户要求“继续 G2-T03”。起点 main／`1af7a91449b2159b672f094b343f72052bb00caf`，工作区干净。仅实施七模型 C++ LMCP 库、Windows 构建／验收入口、来源追溯和文件级跨语言探针；沿用用户持续授权，在验证完成后提交推送。未修改模型、模板、生成目录或第三方依赖，未进入 UxAS 构建、HelloWorld、AMASE 或双向网络联调。

工作过程：先读状态、任务卡、G1／T02 报告和最近日志，核对实际生成目录及工厂接口。重新验证 T01 原生工具、T02 合格指针及完整安装来源，核对 G1 生成器输入、父级清单、Java JAR、七模型和全部 1,013 个生成文件；其中 C++ 相关文件 588 个。根级 CMake 以来源清单选择 183 个库源文件，包括全部 164 结构类、14 个系列 Factory／XMLReader 和 5 个运行库实现；三个生成测试程序不编入库。复制原生成代码到独立中文空格目录，保持内容哈希，不修补输出文件。

新增 C++ 探针复用 G1 固定字段，检查全部 164 类型注册及默认对象往返，验证 Java／Python→C++ 和 C++→Java／Python 的字段与原始字节。C++ 输出还与 G1 留存样本逐项核对，六帧全部一致。Java Sentinel 外层由原方法取出；探针独立检查标记、长度及校验，明确区分零校验和与计算校验和。消息精度和版本未变，没有把文件测试当成真实网络通过。

成果与修改文件：新增 [根 CMake](CMakeLists.txt)、`cmake/LmcpSources.cmake`、`cmake/UxasLmcpConfig.cmake`、`scripts/windows/build-lmcp-cpp.ps1`、`scripts/windows/lmcp-cpp-common.ps1`、`scripts/lmcp_cpp/manage.py`、`tests/windows/lmcp-cpp.tests.ps1`、`tests/lmcp_cpp/` 和 [T03 验收报告](docs/g2-lmcp-cpp-validation.md)。同步 status、backlog、总体计划、G2 方案和 AGENTS。正式包含 lmcp.lib、382 个头文件、3 个 CMake 包文件，共 386 个安装文件；独立 build-info.json 关联 G1 父级及 T02，不覆盖历史 Java／AMASE 清单。包提供 `UxasLmcp`／`Uxas::lmcp`，后续先通过 `Resolve-LmcpCppPackage` 校验。

验证（入口工作目录为仓库根目录，所有构建／功能子进程显式在其他工作目录调用）：

- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/build-lmcp-cpp.ps1 -PythonExecutable <已验证解释器>`：最终构建 `g2-t03-build-20260918-143841-186`，Python 编排 14:38:56～14:39:13，退出 0；全新输出、VS 2022、Release x64／v143、C++14／MD。库架构、编译选项、CRT 指令与探针依赖核对通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/windows/lmcp-cpp.tests.ps1 -PythonExecutable <已验证解释器> -BuildRunId g2-t03-build-20260918-143841-186`：最终验收 `g2-t03-test-20260918-143927-259`，Python 编排 14:39:42～14:39:53，退出 0，六组全部通过。basic／wide AirVehicleState 和 UXTASK 8 TaskActive 的两种校验模式共六帧；大于 2⁵³ 的 ID／关联任务、时间、浮点字段及列表保持一致。
- VS 安装包与 Ninja 迁移包均在中文空格构建目录消费、重复运行；实际头文件和链接库来自对应 prefix。Ninja 用依赖数据库核对被其吸收的 include 输出；禁止 Debug／静态 CRT，未发现意外第三方 DLL。
- 六类坏帧均返回 2 并给出明确诊断；六类隔离来源故障均拒绝，包括缺失／损坏库、错误生成批次、输入变化、真实 CMake 拒绝过期生成源码及模型目录新增未登记文件。最终故障检查期间已有合格指针，其哈希未变；迁移消费成功后才原子更新，旧包及指针备份保留。
- 从 `C:/Windows/Temp` 调用隔离复查脚本，14:30:33 复查中间批次、14:41:08 复查最终批次，均确认 `Resolve-LmcpCppPackage` 返回单个有效路径、父进程环境不变，退出 0；最终证据为 `out/tmp/g2-t03-resolve.json`。正式构建和验收的 entry-result.json 均确认进程环境恢复、用户／系统 PATH 未变。
- 新 PowerShell 入口语法解析、Python AST、UTF-8、Markdown 围栏、仓库相对链接和忽略规则检查通过；源码／缓存／产物留在 out／.tools，worklog 保持跟踪，上游源码及 G1 实现没有差异。`git fetch origin` 退出 0，提交前本地／远程差异计数为 `0 0`。

问题与解决办法：首次构建 `g2-t03-build-20260918-142514-375` 的库已编译，但新探针误把生成库的非 const getter 当作 const 调用，发生 C2662；只修正探针的对象访问，未修改模板或生成源。修正批次 `g2-t03-build-20260918-142636-015` 及首次验收 `g2-t03-test-20260918-142720-052` 通过。收尾检查又补齐实际头文件／链接来源、G1 留存样本比较，修正 PowerShell 解析函数可能混入 Python stdout 的返回值；中间批次 `g2-t03-build-20260918-142827-601`／`g2-t03-test-20260918-142915-326` 通过。提交前来源审查进一步补齐 MDM 文件集合检查，并显式限定 LMCP 构建目标及安装组件，以便 T04 新增目标后继续独立复验；随后用上述最终新批次重建及复验，没有回写旧来源清单。上游未使用参数和长度窄化警告保留，当前没有阻塞。

重要决定与影响：LMCP 使用自己的生成运行库和标准库，不额外链接不需要的第三方业务库；T02 仍作为工程工具／依赖前置核对。严格帧验证只属于验收探针，原生成工厂允许零校验和，不宣称已完成通用不可信数据防护。T04 若修改已登记的根 CMake 等输入，需要重新构建／验收 LMCP，禁止手改旧清单使其通过。主库仍按 VS 2022 验收，Ninja 只验证当前消费布局。

遗留事项与下一步：T04 接入 Makefile 的完整 UxAS 源码、服务注册和可选桥开关；T05 处理真实平台编译问题，T06／T07 验收 HelloWorld。G3 继续负责双向协议、来源过滤与真实规划命令闭环。本次完成后按持续授权提交、普通推送并核对远程，不自动进入 T04。

归档结果：16 个交付文件经显式暂存、UTF-8／200 处相对链接检查、`git diff --cached --check`、合格包输入哈希及历史日志前缀复核后，创建提交 `f2dd834e55b1c44097886aab834ff62c3448a7cb`（`feat: build and validate Windows C++ LMCP library`）。`git push origin main` 退出 0；14:42:43 的 `git ls-remote --exit-code origin refs/heads/main` 返回相同完整标识，工作区干净，收据为 `out/tmp/g2-t03-push.json`。随后仅补齐本条实际结果与 status，独立归档收尾文档；不修改已验收实现，不进入 T04。


## WL-20260918-008｜G2-T04：UxAS CMake 构建图与桥配置能力验收

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）。

关联任务与状态：G2-T04 已完成；T05 可执行、尚未启动，G2 阶段尚未完成。

背景、目标与范围：用户批准实施 T04 方案，并已持续授权完成阶段工作后自动提交推送。起点 main／`0366169c207f9832850b2dceaa8975ef720b04ed`，工作区干净。本轮建立 Windows UxAS 构建图、来源检查与可选桥拒绝路径，用独立 C++ 探针验证共享逻辑；没有安装依赖、完整编译 UxAS、运行 HelloWorld 或启动仿真。方案核查中确认的源码和资源事实、选择独立 C++ 拒绝探针的决定在本条落实。

工作过程：读取状态、任务卡、近期日志及实际 Makefile／服务注册／桥管理代码。显式列出 125 个编译单元，默认只排除串口桥及两个 Zyre 实现，122 个直接编入 uxas 目标；保留 40 项服务注册、任务／规划／日志、SerialAutomationRequestTestService 和 SentinelSerialBuffer。确认 AutomationDiagramDataService.cpp 位于 Services，资源目录没有 .cpp；五个 .code 文件和其他资源均纳入来源清单。入口用 File API 对实际生成的编译／链接图核对，不凭源码清单直接认定完整构建通过。

根 CMake 默认继续独立构建 LMCP；UxAS 预设开启 UXAS_BUILD_EXECUTABLE 后消费合格 Uxas::lmcp 与七类 UxasDeps 目标，不重复编译生成消息库，不隐式运行 vcpkg。配置固定 VS 2022、Release x64、v143／14.44.35207、SDK kit 10.0.26100.0、C++14／MD，并核对实际 CMake／cl／link 的合格来源。源码、目录、服务、资源、T02／T03／G1 和工具实时校验失败即退出，configured 与 compiled 明确分开。

可选桥采用数值条件编译，覆盖三个实现、头文件、包装工具及实例化分支；未定义开关时保留原 Makefile 的启用行为。共享能力检查不启动网络：主程序在加载配置后、网络启动前拒绝关闭桥并返回 300；桥管理器创建前返回 false，单桥创建也拒绝。未扩展通用配置语义，不更改协议或任务算法。完整主程序的实际启动拒绝仍归 T06／T07。

成果与修改文件：[根 CMake](CMakeLists.txt)、[CMakePresets.json](CMakePresets.json)、[Uxas.cmake](cmake/Uxas.cmake)、[显式源码清单](config/uxas-sources.json)、[配置入口](scripts/windows/configure-uxas.ps1)、[验收入口](tests/windows/uxas-cmake.tests.ps1)、`scripts/windows/uxas-cmake-common.ps1`、`scripts/uxas/graph.py`、`tests/uxas_cmake/bridge_probe.cpp`、桥能力头及必要条件分支。[T04 报告](docs/g2-uxas-cmake-validation.md)保存证据索引；同步 status、backlog、总体计划、G2 方案和 AGENTS，忽略本机 CMakeUserPresets.json。原 Makefile、00_ServiceList.h、模型、模板、生成文件、依赖配方及已有专题验收快照保持。

验证：

- 因根 CMake／cmake 输入变化，用原 `build-lmcp-cpp.ps1`／`lmcp-cpp.tests.ps1` 入口重建、完整复验 LMCP。最终构建 `g2-t03-build-20260918-151142-342`（编排 15:11:57～15:12:15）和验收 `g2-t03-test-20260918-151229-483`（15:12:44～15:12:55）均退出 0；183 个源文件、164 类型、三语言六帧、坏帧、VS／Ninja 消费及迁移发布全部通过。新包来源哈希 `5ABC0F28733C9D0E6F26E74C16083FED7EF4139E1097A940ED26468F2C903171`。旧批次和来源清单保留；G1 未重新生成，Java／AMASE 父级清单未改。
- 从其他工作目录调用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File <仓库>/scripts/windows/configure-uxas.ps1 -PythonExecutable <已验证解释器>`，最终配置 `g2-t04-configure-20260918-151514-768`，编排 15:15:45～15:15:56，退出 0。原始配置、File API 及审查记录在本次 out/build/uxas 与 out/runs，结果仅为 configured。
- 同样从 `C:/Windows/Temp` 调用 `tests/windows/uxas-cmake.tests.ps1 -PythonExecutable <已验证解释器> -ConfigureRunId g2-t04-configure-20260918-151514-768`，最终验收 `g2-t04-test-20260918-151556-601`，编排 15:16:27～15:17:03，退出 0。三次实际图审查（原目录、中文空格新目录、重复配置）均核对 122 个源码、40 项注册、5 个资源、27 个依赖静态库加 1 个 LMCP 库，以及正确包含目录、定义、C++14／MD 和 Windows 系统库。
- 只编译两个独立探针。原 HelloWorld XML、TCP、SUB/PUSH、PUB/PULL、Impact SUB/PUSH 共五例返回 0；串口、Zyre、混合请求三例返回 300，含桥类型和开关；坏 XML 返回 100；未定义宏时的旧缺省探针返回 0，serial=1／zyre=1。共 10 例，未产生网络连接或服务运行证据。
- 15 项隔离故障均返回 1：两个开关 ON、缺失来源上下文、依赖／LMCP 来源不符、工具来源变化、缺失源码／资源、新增／重复源码为十项真实 CMake 故障；CZMQ 库、LMCP 库、生成源码、嵌入资源、MDM 输入副本损坏为五项生产哈希校验 CLI 故障。所有修改只在 out 副本，故障后依赖／LMCP 合格指针哈希未变，未覆盖合格包。正式配置／验收入口记录环境恢复、工作目录与编码恢复、用户／系统 PATH 未变；完整 uxas.exe 没有被生成。

问题与解决办法：首次配置 `g2-t04-configure-20260918-150947-051` 因错误假设 CMake 次版本 props 变量必须等于工具目录版本而失败；实际 CMake 3.31.12 在所选版本为默认目录时将其留空。对照随工具提供的官方变量说明和实际 cl／link 路径后，改为校验请求的工具集和真实可执行文件来源，保留精确编译器／SDK 约束。先前 LMCP 批次 `g2-t03-build-20260918-150806-612`／`g2-t03-test-20260918-150856-255` 已通过，但 CMake 修正使输入变化，因此另行重建并复验最终批次，没有改写旧清单。

第二次配置 `g2-t04-configure-20260918-151302-293` 通过；首次 T04 验收 `g2-t04-test-20260918-151352-512` 的十个桥探针通过，Zyre ON 也正确失败，但诊断被 CMake 自动换行，测试的连续字符串断言误判。仅规范化用于断言的空白，保留原始日志；随后从新目录执行上述最终配置及完整验收通过。当前没有阻塞。

重要决定与影响：UxAS 源码直接链接可执行目标以保留注册；可选桥关闭同时约束头文件、实现与创建路径。T04 的 339 项输入来源和 File API 证据只证明配置图完整，独立探针只证明共享能力判断；不能替代 T05 的完整链接或 T06 的真实服务消息。根 CMake／cmake 输入后续变化仍需重建、复验 LMCP。保留原先 Makefile 启用缺省值，不将旧 Linux 或可选桥完整构建宣称为已验证。

遗留事项与下一步：T05 从干净输出构建 UxAS，按实际错误处理必要 Windows 和 CZMQ 等旧依赖兼容问题，不删除服务；T06／T07 负责 HelloWorld 双向消息、完整主程序拒绝与正常退出；G3 负责 WaterwaySearch 双向协议和执行闭环。本轮止于 T04。收尾核查 UTF-8、Markdown／链接、输入哈希、忽略规则及提交范围后，按持续授权自动提交、普通推送并核对远程。

收尾检查：15:23:09 的 `out/tmp/check_t04_delivery.py` 退出 0，收据 `out/runs/g2-t04-docs-20260918-152309/result.json`：29 个变更文件、213 处本地链接、28 个表格、712 条构建／验收输入哈希、阶段状态和忽略规则通过；原日志字节前缀、G0／G1 backlog 历史保持。首次文档检查误把保留的 G1-T04 历史状态当成当前 G2 状态，仅修正检查范围为 G2 章节，未回写历史文档。PowerShell 5.1 语法解析、Python AST、`git diff --check` 及最终实时来源复查通过。`git fetch origin` 退出 0，提交前本地／origin/main 差异为 `0 0`；未发现无关改动，原始日志、输出、工具和本机路径不提交。补齐本项结果后再进行同入口收尾核查。

归档结果：15:24:15 的收尾复查通过（补充 T03 批次交接链接后共 214 处本地链接）；29 个文件显式暂存、`git diff --cached --check` 和无未暂存改动检查通过，创建提交 `b8ccf726cbf2dc128b6984ca5a226095d7bcd436`（`feat: configure Windows UxAS build graph and bridge capabilities`）。`git push origin main` 退出 0；15:24:48 的 `git ls-remote --exit-code origin refs/heads/main` 返回相同完整标识，工作区干净，收据 `out/tmp/g2-t04-push.json`。随后仅补齐本条实际结果与 status，独立归档收尾文档；不修改已验收实现，不进入 T05。


## WL-20260918-009｜G2-T05 Windows 平台适配与 UxAS 候选交付

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；本条写入时间：2026-09-18T17:34:14+08:00。规划诊断依据本机保留的 15:54 后记录一并记入，未将忽略目录内的试验当作此前已交付成果。

关联任务与状态：G2-T05 已完成；T06 可执行、尚未启动；G2 阶段尚未完成。

背景、目标与范围：用户批准 T05 方案，持续授权阶段完成后自动提交推送。起点 `68ed420df4208305c99d7f25a6d0c45d652909ce`，工作区干净。本轮从干净目录生成 Windows 原生候选，验证平台、来源与运行库；没有安装依赖、运行 HelloWorld／AMASE／WaterwaySearch、发布正式 UxAS 或进入 T06。

工作过程：读取状态、任务卡、近期日志和实际源码；先核对规划诊断。默认编译失败于 DPSS API 定义被声明为 dllimport；原 DpssDefs.h 已有 DPSS_STATIC 分支，正式修复在目标上定义 DPSS_STATIC／NOMINMAX，并保留 /EHsc、C++14、Release x64 和 /MD。规划中一次直接覆盖 CMAKE_CXX_FLAGS 的尝试丢失 /EHsc，导致 C4530 及两个 Boost throw_exception 未解析符号；恢复原异常选项后 122 个编译单元链接成功。该错误属于诊断参数，不是新增工程问题；没有添加异常处理空实现。原日志在 `out/runs/g2-t04-configure-20260918-155439-846/planning-*.log`。

按用户选择嵌入 activeCodePage=UTF-8 清单，并在 main 的文件、日志和线程使用前安装 Boost.Filesystem 自己的 UTF-8／UTF-16 路径 facet。没有修改全局 locale、系统代码页、系统时间、持久 PATH 或执行策略。保留 122 个源码、40 项服务及五个嵌入资源；CZMQ／TCP 保留，Zyre／串口关闭，任务算法和消息语义未变。

新增独立构建、验收和候选解析入口，逐次使用新输出目录；CMake 显式绑定合格包路径，拒绝缓存覆盖。审查 File API、MSBuild 实际对象／参数／读取文件、链接映射和运行库；构建前后重新核对输入。候选只含 exe 与 build-info，验收收据绑定构建／验收编号和哈希；没有更新正式 UxAS 指针或捆绑 CRT。

实现期间先后产生 `g2-t03-build-20260918-170609-490`／`g2-t03-test-20260918-170731-570`、`g2-t03-build-20260918-171200-282`／`g2-t03-test-20260918-171304-210`，均通过原 T03 验收；补齐真实日志依赖及改进构建参数后输入变化，重新生成最终合格批次，没有改写旧来源清单。T04 中间配置、复验记录也全部保留。

第一次候选 `g2-t05-build-20260918-171558-917` 完整编译成功；`g2-t05-diagnostic-20260918-171915` 的两种时区平台探针和启动前冒烟通过。发现 /showIncludes 与 /MP 冲突导致 D9030 和串行编译，移除冗余 /showIncludes，改用 MSBuild 跟踪日志保留真实头文件来源。新增探针使用 Windows API 读取 TZ，避免自身引入 getenv 弃用警告；进一步核对 CRT 时区偏移实际生效。稳定输入后按 T03 → T04 → T05 顺序重新完成验收。

成果与修改文件：[CMake 目标](cmake/Uxas.cmake)、[Windows 路径初始化](OpenUxAS/src/cpp/Includes/UxAS_WindowsEnvironment.h)、[程序清单](OpenUxAS/src/cpp/Includes/uxas.manifest)、[main](OpenUxAS/src/cpp/UxAS_Main.cpp)、[构建入口](scripts/windows/build-uxas.ps1)、[验收入口](tests/windows/uxas-build.tests.ps1)、`scripts/windows/uxas-build-common.ps1`、`scripts/uxas/build.py`、`scripts/uxas/loader.py`、扩展的 `scripts/uxas/graph.py`、[平台探针](tests/uxas_build/platform_probe.cpp)。[T05 报告](docs/g2-uxas-build-validation.md)保存完整证据索引；同步 status、backlog、总体计划、G2 方案及 AGENTS。原 Makefile、00_ServiceList.h、模型、模板、生成代码、依赖配方及历史报告未修改。

验证（PowerShell 5.1，Python 3.14.7 x64）：

- 最终 LMCP 构建 `g2-t03-build-20260918-172012-706` 和完整验收 `g2-t03-test-20260918-172109-035`，原 build-lmcp-cpp.ps1／lmcp-cpp.tests.ps1 入口从仓库根执行，均退出 0。七模型 183 个库源文件、164 类型、三语言双向原始字节、错误拒绝和 VS／Ninja 迁移消费通过；来源清单 SHA-256 为 `DF81299DA715F78744AE67FBAB074615F184C3EB29EA16CE7FB2D5E8F44A0B33`。G1 没有重新生成，Java／AMASE 父级来源不变。
- 最终 T04 配置 `g2-t04-configure-20260918-173529-088`、验收 `g2-t04-test-20260918-173618-796`，用原 configure-uxas.ps1／uxas-cmake.tests.ps1 从系统临时目录调用，均退出 0。构建图、十个独立桥探针和十五项隔离故障通过，没有编译主程序来冒充配置验收。
- T05 构建 `g2-t05-build-20260918-173741-000`：从系统临时目录调用 build-uxas.ps1 -PythonExecutable，退出 0；候选位于 `out/build/uxas/<编号>/candidate/`。实际 122 个对象、40 个服务符号、五个资源、44 个链接输入库及来源通过；PE32+ x64，实际 /MD／C++14／异常支持及 UTF-8 清单通过。exe SHA-256 为 `67118CCB9A2A63C2A5FF4516D4C0FDE35D56C0529A160653F550BC641A40F096`，build-info 为 `323504DDC34D5E335849C209832FE31729B990CD514D14C4104E3C3ACBF5CE44`。
- T05 验收 `g2-t05-test-20260918-173955-149`：从系统临时目录调用 uxas-build.tests.ps1 -PythonExecutable -BuildRunId 上述编号，退出 0。中文空格输出目录再次从空目录完整编译；两类路径各运行 UTC0／PST8PDT 两个平台探针，共四次，验证 ACP 65001、UTF-8 参数与路径、XML／真实文件日志、组合值／溢出异常、UTC 与周毫秒换算；CRT 时区偏移分别确认为 0／28800 秒，全局 locale 不变。
- 普通候选、中文输出程序和重复候选三次未知参数冒烟均按原 CLI 返回 DWORD 4294967295（-1），保留中文诊断，在服务初始化前退出。Windows 调试事件观察器记录实际模块路径和哈希：VC CRT 14.50.35719.0、UCRT 10.0.26100.9444 从本机 System32 加载；与编译器版本分开记录，无 Debug CRT、静态 CRT 或意外第三方 DLL。实际版本清单在验收目录 crt-versions.json；没有安装或打包运行库。
- 十一项隔离故障通过：缺失／损坏 exe、候选元数据不匹配、依赖／LMCP 来源不符、工具变化、项目输入过期、模型输入哈希不符、两个 CMake 包路径缓存覆盖、受控超时。前十项明确返回 1；超时作为预期失败保留 timedOut，结束本次拥有的睡眠子进程，不将强制终止记为应用通过。故障只修改 out 副本，旧候选、依赖／LMCP 合格包及原始记录保留；入口记录进程环境、工作目录、编码恢复和用户／系统 PATH 不变。

问题与解决办法：本轮没有需要更改算法、协议或升级依赖的阻塞。规划诊断中的 /EHsc 覆盖和实现阶段的 /showIncludes 参数冲突已按上述方式修正并重新验证。既有 C4244／C4267 转换、C4018 符号性、C4101 未用局部量、C4554 运算优先级提示和 C4996 弃用警告分类保留，没有整体屏蔽；MSBuild 汇总重复计数不能当成唯一问题数。收尾第一次在默认受限 PowerShell 直接 dot-source 解析接口，被执行策略拒绝，函数没有执行；随后改用既有 -ExecutionPolicy Bypass 子进程入口复查，不修改持久策略。

更正 WL-20260918-008 和 T04 报告中的库数量文字：T02 包共 27 个 .lib，但其中两个 Boost monitor 库位于 manual-link；CMake 实际链接顶层 25 个依赖库和一个 LMCP 库，原自动检查也一直按顶层 glob 核对。T05 的 link.read 跟踪再次确认该集合，另有 MSVC／SDK 系统输入合计 44 个库；不存在靠删除必需库绕过构建的情况。旧专题报告不回写。

重要决定与影响：目标级宏和程序级编码设置保持原接口；Boost 路径转换只作用于其独立 locale。完整候选通过并不等于 HelloWorld 或 G2 完成。后续必须用 BuildRunId 与 ValidationRunId 解析并重新核对候选，不能直接消费旧 exe 或修改来源收据。

遗留事项与下一步：T06 负责原 HelloWorld 两服务真实收发、10 秒运行及退出 0；T06／T07 验证完整主程序配置失败和关闭桥拒绝；T07 复验后才发布正式 UxAS。G3 负责 AMASE↔UxAS／WaterwaySearch 双向闭环。本轮收尾继续完成 UTF-8、链接、Markdown、忽略规则、输入哈希、提交范围及候选解析接口检查，再按持续授权提交、普通推送并核对远程，不进入 T06。


收尾复验：首轮正式候选 `g2-t05-build-20260918-172544-419`／验收 `g2-t05-test-20260918-172715-104` 已通过；审查发现独立平台探针还需要单独绑定二进制哈希。为 build-info 加入探针 SHA-256，验收前比对，运行前后再核对并保存证据。该修正没有改变程序源码、CMake 或 LMCP 输入，因此保留已合格的最终 T03 批次；重新完成 T04 配置／完整复验及上述最终 T05 的两类干净构建和全部验收。没有改写旧候选或旧收据。此前 `g2-t05-resolve-20260918-173028-065` 已通过首次接口验证，最终批次继续按同一接口复查。

文档过程：一次忽略目录中的文档辅助脚本因 Python 字符串的 Windows 反斜杠转义而在语法解析时失败，没有写入项目文档；改用原始字符串后成功。17:34:15 的初次交付检查通过 18 个变更文件、226 处本地链接、28 个表格和 1422 条输入哈希；随后针对探针哈希修正更新最终批次并再次核查。工作日志历史字节前缀、G0／G1 backlog 和旧专题报告保持。PowerShell 5.1 语法解析已通过；提交前 git fetch origin 成功，本地与远程差异为 0 0。

最终交接检查：`g2-t05-resolve-20260918-174200-479` 从其他工作目录通过原 PowerShell 子进程入口执行，退出 0，返回单一候选目录字符串；工具、T02／T03／G1、候选及双编号验收收据实时复查和环境恢复全部通过。17:42:00 的交付检查再次通过 18 个文件、226 处链接、28 个表格及 1422 条构建／验收输入哈希；随后把解析批次也纳入最终输入核对。Python AST、PowerShell 5.1 语法和 git diff --check 通过；无遗留 UxAS 进程，out/artifacts/uxas 尚不存在。剩余为本次授权的提交、普通推送和远程核对；不修改已通过的实现，不进入 T06。

归档结果：17:43:55 的交付检查通过 18 个文件、226 处本地链接、28 个表格、38 项记录检查及 1769 条构建／验收／解析输入哈希，收据 `out/runs/g2-t05-docs-20260918-174355/result.json`。18 个文件显式暂存，git diff --cached --check 和无未暂存修改检查通过，创建提交 `0fe8553f19cc720de07ddfcf8dbcbf46c99596d1`（`feat: build and validate native Windows UxAS candidate`）。git push origin main 退出 0；17:44:25 的 git ls-remote 返回相同完整标识，工作区干净，收据 `out/tmp/g2-t05-push.json`。随后只补齐本条实际结果和 status，单独归档收尾文档，不改动合格实现或进入 T06。


## WL-20260918-010｜G2-T06 HelloWorld 原生运行与验收

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；本条写入时间：2026-09-18T20:04:25.375421+08:00。

关联任务与状态：G2-T06 已完成；T07 可执行、尚未启动，G2 尚未完成。

背景、目标与范围：用户批准 T06 方案，要求验收后自动提交、普通推送及核对远程。本轮起点 main／`5c2d38793da755b29a911cb33b598d3f23f1e821`，工作区干净。本轮建立原生 HelloWorld 运行、真实内部消息和退出验收；不启动 AMASE，不新增外部 TCP 桥，不进入 T07 或发布正式 UxAS。

工作过程：读取 AGENTS、status、backlog、T05 报告及最近日志，核对本地／远程交接。规划期 `g2-t05-resolve-20260918-175051-762` 验证固定工具、Python 3.14.7 x64、T02／T03／G1、候选双编号和环境恢复；四个 CRT 文件当前哈希与 T05 记录一致。实施前再次确认工作区与交接，运行入口继续实时解析来源。新增编排放在独立 scripts/uxas_runtime，记录 11 项自身输入，保留旧构建的全目录来源规则。

首次 `g2-t06-run-20260918-194827-987` 使用原候选：真实 stdout 有两个方向共 13 条接收，约 12.223 秒退出 0；自动验收仍失败，因为预期的配置、创建 ID 与完整关闭日志没有输出。检查源文件确认普通 UXAS_LOG_INFORM 默认在编译时裁掉；不是消息或退出功能失败。保留原 failed 记录，不回写结论。采用最小目标级修正：为 uxas 启用既有 UXAS_INFO_LOGGING_ENABLED，不改算法、消息、定时器、原 XML 或全局日志源文件。配置输入变化后重新执行下述前置链路，没有修改旧清单。静态探索曾因猜测桥能力头／日志头路径而出现文件不存在，且一次 rg 通配符路径在 Windows 下无效，随后用 rg --files 定位后读取。规划期一次 Python -c 引号在 PowerShell 调用中丢失，改用标准输入确认解释器位数；这些辅助命令错误未改变工程文件。

成果与修改文件：[运行入口](scripts/windows/run-uxas.ps1)、[验收入口](tests/windows/uxas-helloworld.tests.ps1)、scripts/windows/uxas-runtime-common.ps1、scripts/uxas_runtime 下运行器和日志验收器、tests/uxas_runtime/checks.py，以及 [CMake 目标](cmake/Uxas.cmake) 的 INFO 定义。[T06 报告](docs/g2-uxas-helloworld-validation.md)记录标准命令、来源与限制；同步 status、backlog、总体计划、G2 方案和 AGENTS。T03／T04／T05 旧报告及原候选保留历史快照。

验证（PowerShell 5.1，Python 3.14.7 x64；T03 从仓库根、T04～T06 从系统临时目录以脚本绝对路径调用）：

- 原 build-lmcp-cpp.ps1／lmcp-cpp.tests.ps1 完成 `g2-t03-build-20260918-195001-565`／`g2-t03-test-20260918-195057-347`，均退出 0；七模型、164 类型、三语言样本、坏帧与 VS／Ninja 迁移消费通过并发布新 LMCP 批次，Java／AMASE 和 G1 生成来源未改。
- 原 configure-uxas.ps1／uxas-cmake.tests.ps1 完成 `g2-t04-configure-20260918-195135-036`／`g2-t04-test-20260918-195240-495`，均退出 0；122 源码、40 服务、五资源配置图、10 桥探针和 15 隔离故障通过。
- 原 build-uxas.ps1／uxas-build.tests.ps1 完成 `g2-t05-build-20260918-195355-288`／`g2-t05-test-20260918-195540-900`，均退出 0；两类路径干净构建、平台、实际 CRT 来源和 11 项故障通过。新 exe SHA-256 为 `4BB94E29FD49689A84845F7254663655740F32F8F42595BDD1896B74416AFD9F`，候选清单 `775FA96AB5771A4823EF152143D9753448479D4DE222541CC5F352160EF5973E`。此为 T06 日志修正引起的前置复验，不计为 T07 发布工作。
- 新 uxas-helloworld.tests.ps1 指定上述构建／验收双编号，批次 `g2-t06-test-20260918-195743-715`，退出 0。普通及中文空格目录均把原 XML 原样复制；日志绑定两个实际服务 ID 51／52 及原文本，各有 51→52 10 条、52→51 3 条，自收和未解析片段均为 0。服务运行区间为 10244／10230 ms，进程单调耗时约 12.249／12.231 秒，均正常退出 0、无强制终止、已回收。
- 同一真实候选的缺失配置返回 100，串口与 Zyre 分别返回 300，诊断包含实际路径或桥类型及 OFF 开关，均在网络／服务启动前拒绝。底层用例保持 failed，另记 expectedFailureVerified，不把程序失败改成正常运行通过。
- 验收器 13 个反例拒绝，另一个完整记录加损坏片段对照通过并报告原始位置。涵盖自收、单向、错误 ID／文本、配置／时长、退出证据和片段，不用派生样本充当真实消息。运行器使用自己启动的 Python 睡眠进程覆盖 0.2 秒超时清理，保留 failed／timedOut／forcedTermination 并确认回收；UxAS 超时完整矩阵仍归 T07。
- 新 run-uxas.ps1 独立入口最终批次 `g2-t06-run-20260918-195918-390`，退出 0；同一新候选通过原配置双向消息与完整生命周期检查。正常、故障和超时例均保留独立命令、PID、配置与日志哈希；入口环境、目录、编码恢复，持久 PATH 未变。候选、依赖／消息包与运行脚本来源前后检查通过。

问题与决定：首次静态阅读只确认日志调用存在，未注意其编译开关；实际失败后按源码定位并补齐证据，没有降低验收门槛。正常退出要求包括两服务实际启动、日志中原 10 秒运行、全部服务和 ServiceManager 客户端正常终止以及进程退出 0；关闭横幅不能替代这些条件。服务 ID 动态读取并按原配置顺序绑定，结果使用字符串；不固定条数或周期误差。启动后即进入拥有进程的 finally 清理范围，超时及异常都不按宽泛进程名结束其他实例。

遗留事项与下一步：T07 完整复验、连续三次正常启停、完整故障矩阵与正式发布尚未执行，out/artifacts/uxas 未建立；外部双向协议、AMASE／WaterwaySearch、完整重连留待后续。收尾检查文档链接、UTF-8、Markdown、历史前缀、来源哈希、忽略规则及提交范围后，按授权提交、普通推送并核对远程。不将 T06 通过登记为 G2 完成。

收尾检查：20:07:55 的交付检查通过 14 个变更文件、238 处本地链接、29 个表格和 1476 条输入／证据哈希，收据 `out/runs/g2-t06-docs-20260918-200755/result.json`。工作日志原字节前缀、G0／G1 任务记录和三个旧专题报告保留；最新候选、T06 消息／用例收据及八个前置／运行批次来源有效。PowerShell 5.1 语法、Python AST 已通过；无残留 UxAS 进程，没有正式 UxAS 目录；git fetch origin 成功，本地／origin/main 差异 0 0。

文档检查过程：初次检查发现 backlog 页首仍遗留 T04 前的当前状态，已同步到 T06 完成，仅更新当前摘要，G0／G1 历史保持。一次通过 PowerShell 标准输入发送中文替换脚本因默认 us-ascii 编码未匹配原文，改为 UTF-8 文件并断言匹配后完成；辅助检查器的 `.tools` 路径转义警告也已纠正。这些修正不改变已通过的运行实现。补齐本条后再核对交付清单，随后执行本次授权的提交、普通推送和远程核对。


归档结果：20:09:20 的最终交付检查再次通过 14 个文件、238 处本地链接、29 个表格及 1476 条输入／证据哈希，收据 `out/runs/g2-t06-docs-20260918-200920/result.json`。14 个文件显式暂存，git diff --cached --check 和无未暂存修改检查通过，创建提交 `a0a898976b01a836df89f929562fed2155f0863a`（`feat: run and validate native Windows HelloWorld`）。git push origin main 退出 0；20:09:35 的 git ls-remote 返回相同完整标识，工作区干净，收据 `out/tmp/g2-t06-push.json`。随后只补齐本条和 status 的实际归档结果，独立提交收尾文档；不修改已验收实现，不进入 T07。


## WL-20260918-011｜G2-T07 完整复验、正式发布与阶段交接

时间／时区：2026-09-18，Asia/Shanghai（UTC+08:00）；本条写入时间：2026-09-18T20:44:52.414102+08:00。

关联任务与状态：G2-T07 已完成，G2 验收通过；G3 待细化、尚未启动。

背景、目标与范围：用户明确要求“开始 T07”。起点 main 为 `8f99ba28993f43ba94502d6c701806df91986187`，工作区干净，本地／远程一致；读取 AGENTS、status、backlog T07、G2 方案、T06 报告和最近日志，按七卡结束条件实施。只做复验、正式发布与交接，不启动 AMASE 或 G3。

工作过程：从系统临时目录使用现有入口重新建立普通路径 UxAS 候选，完整 T05 验收再从中文空格路径干净构建并验证平台、CRT 和来源；新候选完整 T06 复验通过。固定工具、T02／T03／G1 来源在各入口重新验证，CMake／C++／模型／生成器／依赖／原 XML 未改变，无需重建未变的前置来源。本轮复用缓存和合格依赖／消息库，不冒称空缓存重建。

成果与修改文件：新增 scripts/windows/uxas-release-common.ps1、publish-uxas.ps1、run-uxas-release.ps1、tests/windows/uxas-release.tests.ps1；scripts/uxas_release/package.py、release.py 与 tests/uxas_release/checks.py。独立目录登记自身来源，未修改 T05／T06 编排或旧来源规则。新增 [T07 报告](docs/g2-uxas-release-validation.md)，同步 AGENTS、status、backlog、G2 方案、总体计划；旧专题报告与已结束日志原字节保留。

验证（PowerShell 5.1，Python 3.14.7 x64，入口从系统临时目录以绝对路径调用）：

- build-uxas.ps1：`g2-t05-build-20260918-201846-291`，退出 0；uxas-build.tests.ps1：`g2-t05-test-20260918-202115-333`，退出 0。122 编译单元、40 服务、5 资源保持，普通／中文路径完整构建、平台与实际 CRT 来源及 11 项故障通过。
- uxas-helloworld.tests.ps1：`g2-t06-test-20260918-202616-245`，退出 0；双向消息、原时长退出、配置拒绝、解析反例和辅助超时检查全部通过。
- uxas-release.tests.ps1：`g2-t07-test-20260918-203915-916`，退出 0；同一候选连续三次正常运行，再运行中文路径构建出的 exe 和迁移到待发布目录的 exe。正常例均保持原 XML，两个方向跨服务消息和完整关闭证据通过，退出 0、无强制终止且回收进程。
- 缺失／损坏配置的真实程序分别返回 100，关闭的串口／Zyre 返回 300，均在网络／服务创建前拒绝。真实 UxAS 超时例将隔离配置时长改为 60 秒，在已有启动和接收日志后到 30 秒触发清理，仅结束持有句柄的本次进程；保持 failed、timedOut、forcedTermination、非零退出并确认回收，原 XML 未改。
- 12 项隔离检查通过：缺失／损坏依赖静态库、LMCP 混批／G1 生成来源不符、包程序缺失／损坏、清单损坏、CRT 缺失／损坏、切换指针前失败、切换后提交失败回滚、首次发布失败无指针。调用实际校验器和指针事务；旧包副本和指针字节保持。未破坏系统 DLL 或实际合格包，不能把副本校验称为操作系统加载器故障试验。
- publish-uxas.ps1：`g2-t07-publish-20260918-204152-283`，退出 0；复制完整包到新目录，实际运行正式位置通过，环境恢复成功后原子提交 current.json。run-uxas-release.ps1：`g2-t07-run-20260918-204325-360`，退出 0，再次验证正式包双向消息和完整退出。
- 公开发布入口拒绝失败的首次 T07 编号，批次 g2-t07-publish-20260918-204335-608 退出 1，因缺少合格收据在发布前停止；真实正式指针哈希前后不变，环境／目录／编码恢复。负向结果保留 failed，隔离观察记录 out/tmp/g2-t07-rejected-publication.json。

正式结果：exe SHA-256 `CDE0AB551D557A7E469645D8144ECA652EEF62717934C4F91A0FF8BDEE115C7C` 与验收候选相同；包清单 `D664760FA8E30A717B19AE01E06F0A3F2A473C6304AFAD4C3174DECC679702F5`，T07 收据 `68869351EE4FA1400C91C659CF05932B6D16C9A1D6E268173658923B87ED4CAA`。包按构建／验收／发布批次保留，包含原 XML、来源副本、运行库记录、许可证与 G3 handoff.json；不捆绑 CRT。记录四个动态 CRT 来源，MSVC 三项为 14.50.35719.0、UCRT 为 10.0.26100.9444。开发工作区来源记录是正式入口消费前提，第二机器独立部署仍归 G8。

问题与处理：首次 T07 验收 g2-t07-test-20260918-203132-962 完成三次正常运行、中文路径、配置拒绝和真实超时后，于打包阶段失败：脚本误以为每个 T05 批次都生成 crt-versions.json。实际新 T05 只有加载路径／哈希记录，部分旧批次的版本报告属于当时的补充证据。改为从同批 ordinary-loaded-modules.json 绑定候选和四个 CRT，哈希核对后读取 Windows 固定版本资源，生成独立包内清单；保留原加载记录。隔离打包和 12 项故障预检通过，再以新编号完整复验，不修改失败记录或旧清单。若干只读探查猜测了不存在的 manage／loader 路径，随后用 rg 定位，无工程影响。PowerShell 默认标准输入为 us-ascii，中文文档更新使用 UTF-8 辅助文件并断言匹配。预期拒绝保持 failed，不改写成程序成功。发布采用环境恢复后提交指针，提交记录写入位于回滚边界内，既有候选／依赖／Java／AMASE 保持。

重要决定与影响：正式入口与候选入口并存，不改变已验收的旧命令。发布／消费绑定 T05 双编号、T06 收据、T07 完整证据和实时脚本／包哈希；输入变化需按影响重新构建／验收，不能修改历史清单。G3 输入核查只读复用已验收 AMASE 和统一七模型，没有启动仿真；服务及桥清单保留 TCP／CZMQ，Zyre／串口关闭。

遗留事项与下一步：G3 先细化 Sentinel／属性封装、来源过滤、启动顺序、WaterwaySearch 实际规划命令；完整重连、重置分段、真实地形、Cesium、训练和 G8 第二机器／离线演示按对应任务实施。本轮止于 G2。完成文档链接、UTF-8、语法、来源／收据、忽略规则、敏感信息和提交范围检查后按持续授权自动提交、普通推送并核对远程；后续在本条追加真实归档结果。

收尾复核：正式包解析接口在显式 -ExecutionPolicy Bypass 的新 PowerShell 进程内完成 g2-t07-resolve-20260918-204515-225，退出 0，返回当前正式包，来源和环境恢复通过。最初直接在默认受限会话点入脚本，被执行策略阻止，尚未进入业务入口；随后沿用项目既有的仅本次进程 Bypass 方式验证，未更改持久策略，报告补齐该调用前提。

20:46:29 的交付检查通过 14 个任务文件、251 处本地链接、30 个表格及 960 条输入／证据哈希，收据 out/runs/g2-t07-docs-20260918-204629/result.json。PowerShell 5.1 语法与 Python AST 通过；无残留 UxAS 进程，故障批次保持失败，正式包、候选和源输入有效。历史六份报告、工作日志原字节前缀及 G0／G1 任务记录保留；生成物／工具均未进入 Git，工作日志仍受版本管理。最后补齐解析接口和实际检查记录，核对提交范围后执行本次授权的普通提交与推送。

归档结果：20:47:12 的最终交付检查通过 14 个文件、251 处本地链接、30 个表格及 960 条来源／证据哈希，收据 out/runs/g2-t07-docs-20260918-204712/result.json。14 个文件显式暂存，git diff --cached --check 和无未暂存修改检查通过，创建提交 `6813d0a8298531cbbe6345f83aeaded515fe2603`（`feat: qualify and publish native Windows UxAS for G2`）。git push origin main 退出 0；2026-09-18T20:47:56.1477721+08:00 的 git ls-remote 返回相同完整标识，工作区干净，收据 out/tmp/g2-t07-push.json。随后仅补齐本条和 status 的实际归档结果，独立提交收尾文档；不修改已验收实现、正式包或来源清单，不启动 G3。


## WL-20260919-001｜G3 阶段方案与七张任务卡细化

时间／时区：2026-09-19，Asia/Shanghai（UTC+08:00）。

关联任务与状态：G3 文档细化；G3 已细化，实施尚未启动。G3-T01 可执行、尚未启动，T02～T07 待前置；本条不登记任何实现卡通过。

背景、目标与范围：用户要求“细化 G3 工作”，规划讨论后明确要求落实方案文档。用户选择完整搜索任务完成，并要求执行链与覆盖率同时达标，门槛为 AMASE 20 米栅格 ≥95%；允许独立副本受控调整飞行／传感器／规划参数及场景时长。基础断线清理与整组重启归 G3，自动重连／初始化补齐及完整恢复矩阵归 G4。本轮仅落地文档，未安装、编译、修改业务代码或启动 AMASE／UxAS 联调。

工作过程：起点 main／`aa0067ce53dd9b2c1ac88d2d1e59818f11240324`，git status --short 无输出；读取 AGENTS、status、backlog、G2 交接、总体计划和最近日志。规划期核对真实工具版本、原场景／任务、TCP 桥、PUB 过滤、任务完成和 AMASE 分析源码；实际 Python --version 为 3.14.7，java -version 为 Temurin 11.0.32.1+1，退出 0，未执行正式包资格解析或运行验收。源码确认原场景 785 秒、实体 400／500、任务 1000 共 90 点、两模式分析网格 20 米；静态几何估算只用于规划，未当作飞行时间或运行成果。本轮重查来源相关代码和配置，git ls-remote origin refs/heads/main 返回与起点相同的完整标识。

主要发现与处理：原 SendMessagesService 注入静态配置／状态，TCP 桥默认 ConsiderSelfGenerated=true 改写来源，PUB 只导出本地来源，TaskComplete 可因实体离开任务关联而触发，分析报告只有格式化覆盖率且分析路径创建进度窗口；G1 GUI 20 秒暂停和无界面 120 秒超时不适合全程验收。方案据此明确独立编排、真实初始化屏障、来源保留与过滤对照、末端执行核对及原始覆盖计数。以上均为源码确认，运行兼容性留给对应实现卡。规划探索曾猜测不存在的消息目录／源码路径，已用 rg --files 定位后重读，未据失败读取作结论；文档编辑辅助文件和检查器均位于被忽略的 out/tmp。

成果与修改文件：新增 [G3 阶段方案](docs/g3-system-integration-plan.md)；在 [backlog](docs/backlog.md#6-g3-顺序与任务卡)增加七张完整卡、状态依赖与结束检查；同步 [status](docs/status.md)、[总体计划](04.项目总体实施计划与阶段验收.md)和 [AGENTS](AGENTS.md)。六份任务文档包含本工作日志，G0～G2 已结束任务正文和历史报告保留。运行、验收、GUI 收尾入口及插件明确标为拟建，没有添加未验证的标准命令。

重要决定与影响：主链路沿用现有 TCP 桥，观察口承载只读采集与明确的编排消息；两模式顺序运行、默认 1 倍速；原例先验，最多比较三组调优候选，仿真时长最多 1800 秒、自动运行墙钟上限 2700 秒，初始化／规划／关闭各 30 秒。完整水道、实体身份、20 米栅格、覆盖计算和 95% 门槛固定，两模式独立统计；最终同一业务配置完成全程复验、本轮 GUI 人工确认与正常退出才登记 G3 完成。后续源码变化按影响走既有重建／复验／发布流程，不修改旧来源清单。

验证与收尾：本轮为纯文档任务，仅进行 UTF-8、Markdown、路径／链接／锚点、七卡字段／状态一致性、源码事实、历史保留、忽略规则、敏感信息和提交范围检查；不运行无关构建或业务测试。实际检查结果与提交／推送信息在下文追加，不预写成功。

遗留事项与下一步：G3 尚无双向闭环、完整任务、覆盖率或恢复验收结果。下一项为 T01 正式输入与验收基线，重新解析合格产物与同批消息来源，登记真实来源和可执行判据；本轮文档细化不代替 T01，不进入 T02 或后续实现。

检查结果：2026-09-19 09:09:26 的文档验收退出 0，6 份文件、251 处本地链接、23 处锚点、28 个表格、7 张任务卡全部通过，收据 out/runs/g3-planning-docs-20260919-090926-785956/result.json。UTF-8、代码围栏、标题间隔、卡片字段／前置状态、20 米／95% 一致性及原始场景事实检查通过；工作日志历史字节前缀、G0～G2 任务正文保持，改动仅限六份文档，辅助文件与收据被忽略，未发现新增本机绝对路径或敏感标记。git diff --check 通过。Git 提示部分既有混合行尾在未来检出时转换，本轮保留原字节与局部编辑，没有统一格式化历史文件。上述检查结果补记后再次检查文档与暂存范围，再按持续授权提交、普通推送及核对远程。

归档结果：09:10:31 的交付复核再次通过六份文档、251 处本地链接、23 处锚点、28 个表格及七张任务卡，收据 out/runs/g3-planning-docs-20260919-091031-707311/result.json。六份文件显式暂存，git diff --cached --check 及无未暂存修改检查通过，创建提交 65034b753d4cdef20e48e8baba214caf9b56a0bc（docs: detail G3 WaterwaySearch integration and acceptance plan）。git push origin main 退出 0；2026-09-19T09:10:56.7323884+08:00 的 git ls-remote 返回相同完整标识，工作区干净，收据 out/tmp/g3-planning-push.json。随后仅补齐本条和 status 的实际归档结果，单独提交收尾文档；G3 实施尚未启动，T01 保持可执行。


## WL-20260919-002｜G3-T01 输入资格与验收基线

时间／时区：2026-09-19，Asia/Shanghai（UTC+08:00）。

关联任务与状态：用户要求“启动 T01”；G3-T01 已完成，G3-T02 可执行、尚未启动，G3 尚未完成。

背景、目标与范围：将规划期输入确认落为实际资格检查，冻结原例、消息流和完整搜索／覆盖率门槛。仅 T01，不启动 AMASE／UxAS 仿真或网络联调，不调参、不修改桥／算法、构建输入或旧资格接口。起点 main／5c0658ddd3a75da690daf4b0a90ee35714d1cdbc，git status --short 为空；先读 AGENTS、状态、任务卡、G3 方案、G2 交接及最近日志。

工作过程：追踪正式包解析及 AMASE 来源链，检查原场景、两种配置与静态消息、Java／C++ 网络源码、请求／响应与任务生命周期、分析计数及时间源。新增独立 PowerShell 入口与 Python 核查，复用 Resolve-UxasPackage、AMASE 资格接口和既有工具验证，不复制一套发布规则；登记 35 个冻结文件、同批七模型、正式收据身份与后续验收契约。内存 XML 反例验证输入边界，不修改合格源文件；同步输入报告、当前状态、任务卡、方案、总体计划及 AGENTS。

成果与修改文件：新增 [T01 报告](docs/g3-input-baseline-validation.md)、[基线清单](config/g3-baseline.json)、[Windows 入口](scripts/windows/check-g3-baseline.ps1)、[核查实现](scripts/g3_baseline/check.py)、[输入反例](tests/g3_baseline/checks.py)；另修改六份当前文档（含本日志）。场景实体 400／500、任务 1000、90 点、原时长 785 秒和 20 米栅格保持。报告区分静态输入、实时消息、原巡航与规划命令，并给出初始数据、Unique 请求关联、末端执行、原始覆盖整数、端口／超时／退出条件。

实际验证：从仓库外系统临时目录以 PowerShell -NoProfile -ExecutionPolicy Bypass -File 调用新入口并传入实际 Python 3.14.7 x64，g3-t01-check-20260919-093348-422 退出 0，result／entry-result 均 passed；嵌套 g2-t07-resolve-20260919-093348-447 正式解析通过。Temurin 11.0.32.1+1、javac 11.0.32.1、Ant 1.10.18 版本查询通过；现有 C++ 工具、依赖、CRT 和候选／验收／发布来源复核通过。同批生成身份 g1-t03-20260917-152214-037296 一致，原 AMASE 历史 11 项自动结果、GUI 接收与正常退出证据核对通过，未复用为 G3 人工确认。运行目录保存 baseline.json、工具输出、来源与原始收据哈希；环境、目录、编码恢复，持久 PATH 保持，未启动业务进程。

在仓库根目录用隔离 Python 运行 tests/g3_baseline/checks.py，输出同一成功运行目录的 baseline-checks.json，退出 0，共 10 项通过（原例及九个内存 XML 反例），冻结输入哈希保持。缺失解释器通过正式入口测试：g3-t01-check-20260919-093452-631 退出 1，entry-result 保持 failed，错误明确且四项恢复检查通过；不将预期拒绝写成正常入口成功。PowerShell AST、Python 语法、文档与最终范围核查结果在下文追加。

问题与处理：早期探索用嵌套 -Command 传递 ErrorActionPreference 时引号被外层剥离，出现 Stop 不是命令的诊断，但后续解析仍执行并返回 0；不把该混合调用当作合格入口验收。保留探索解析 g2-t07-resolve-20260919-092445-004，改用受控 -File 入口后完整通过，未改持久策略。若干只读查询猜测了不存在的路径，随后用 rg／清单定位；未据失败读取作工程结论。文档编辑使用 UTF-8 辅助文件，仅对当前说明做局部修改；历史日志字节前缀、G0～G2 卡片和验收报告保留。

重要发现与决定：原任务 AllAny 会命中 LinearSearchHighlight.processSensor 的直接 return，按该源码路径不能累计覆盖；这是静态确认，没有实测覆盖百分比。本轮不修算法、不改参数，T05 保留原参数实测后，可在最多三组受控候选中使用与相机有效字段一致的具体波段。另登记旧复数相机字段／重复 ViewAngleList（T02／T05）、Java 0／0 来源及 TCP 改写／PUB 过滤（T02）、静态注入与无界面抢先启动（T03）、无 CMASI 关联 ID／同号原命令／分段新 ID（T04）、提前 TaskComplete／分析窗口与四舍五入（T05）、仿真时间不能盲转 UTC（T03／T04）。这些风险已明确责任，不阻塞输入资格卡，不能因此提前宣布完整任务可用。

遗留事项与下一步：下一项 T02，从真实 Java／C++ 帧、严格双层校验、来源保留／改写及订阅过滤验证开始；不进入 T03 或完整任务运行。当前正式包、原始 XML、生成输出及历史来源收据保持；必要修复按影响重建／复验／发布。最终完成检查后按持续授权显式暂存本轮文件、提交、普通推送并核对远程，真实归档结果追加下文。

交付检查：2026-09-19 09:42:54 的最终检查退出 0，11 个任务文件、292 处本地链接、27 处锚点、34 个表格、7 张任务卡及 51 条输入／收据哈希通过，收据 out/runs/g3-t01-delivery-20260919-094254-031707/result.json。PowerShell AST 与两份 Python AST 通过；UTF-8、Markdown、状态／依赖、验收边界、忽略规则和敏感标记核查通过，历史工作日志原字节前缀、G0～G2 卡片及历史报告保持。git diff --check 通过；既有混合行尾提示保留，不统一格式化。核查结果补记后显式暂存本轮 11 个文件，再检查暂存内容并按持续授权提交、普通推送。

归档结果：09:43:38 的交付复核再次通过 11 个文件、292 处本地链接、27 处锚点、34 个表格、7 张任务卡及 51 条输入／收据哈希，收据 out/runs/g3-t01-delivery-20260919-094338-410110/result.json。11 个文件显式暂存，暂存差异检查和无未暂存修改检查通过，创建主提交 cb3eacd2f82a4d437831036dd48dd2dd378a4542（feat: qualify G3 inputs and freeze WaterwaySearch acceptance baseline）。git push origin main 退出 0；2026-09-19T09:44:00.2780580+08:00 的 git ls-remote 返回相同完整标识，工作区干净，收据 out/tmp/g3-t01-push.json。随后仅补齐本条和 status 的真实归档结果，单独提交收尾文档；T01 已完成，T02 保持可执行、尚未启动。


## WL-20260919-003｜G3 验收聚焦系统闭环与覆盖统计正确性

时间／时区：2026-09-19，Asia/Shanghai（UTC+08:00）。

关联任务与状态：按用户最新说明修订 G3 验收范围；文档及契约调整完成，T01 仍已完成，T02 仍可执行、尚未启动，G3 未完成。

背景、目标与范围：用户明确当前重点是调通程序和系统，覆盖率计算正确即可，不追求必须达到某个指标，算法／配置参数优化归后续；同时询问覆盖验收是否影响功能及原系统是否能自主优化。该要求覆盖 WL-20260919-001／002 的历史 95% 门槛和调优候选约定。本轮只改当前文档与验收契约，不启动联调、不修业务源码或调参。

工作过程与依据：起点 main／3dd77fb2c98a3b2c130ce362e44f8546261962bb，git status --short 为空；阅读当前方案、任务卡、状态与日志，核对 AnalysisManager 的事件记录分析、SearchTaskAnalysis 的原始计数／百分比、LinearSearchHighlight 的 AllAny 返回分支，以及 UxAS 线搜索规划选项与原示例配置。源码确认覆盖率是分析结果；在本例 C++ 源码／配置中未找到 CoveragePercent、SearchTaskAnalysis 或 AnalysisReport 的反馈接入。UxAS 能按既定任务和约束进行分配／规划，当前链路没有根据 AMASE 覆盖报告自动修改算法／参数、反复仿真直至达标的流程。取消验收阈值不会移除这些运行功能，但避免把效果调优混入系统联调。

成果与修改文件：同步 [阶段方案](docs/g3-system-integration-plan.md)、[任务卡](docs/backlog.md)、[状态](docs/status.md)、[总体计划](04.项目总体实施计划与阶段验收.md)、[AGENTS](AGENTS.md)及 [当前契约](config/g3-baseline.json)，连同本日志共七份文件。契约 acceptanceRevision=2，取消 percentMinimum／maximumTunedCandidates，保留 35 个冻结源输入，增加正确性对照要求；T01 历史报告、原始收据和既有资格脚本保持，旧报告中的门槛由当前方案显式替代。本轮不重跑正式资格入口，也不把旧收据当新口径的运行验收。

重要决定：保留完整任务执行、可靠 TaskComplete、两模式运行与正常退出。覆盖率保存真实 seen／total、百分比和漏覆盖位置；正确性需已知覆盖／未覆盖、去重、波段／AllAny、可人工复核样本及同一事件记录两模式一致性对照，不能只凭计数范围或报告有数值判断。低覆盖率本身可通过，错误统计仍阻塞。AllAny 问题归 T05 语义核实与必要最小修复，不靠提高覆盖率调参绕过；必要源码修复按影响重建／复验／发布。原 785 秒及执行所需上限 1800 仿真秒／2700 自动墙钟秒保持，不因覆盖率低延长。覆盖效果提升与自动寻优另列后续任务，不作为当前关卡隐含要求。

验证、问题与下一步：检查七文件的 UTF-8、Markdown、链接、当前口径一致性、JSON 契约及冻结输入／历史字节保持；不运行无关业务测试。实际检查与归档结果追加下文。当前没有新增运行失败；已发现的统计风险仍待 T05 实现验证。本轮不进入 T02，下一步仍为真实双向协议和来源验证；按持续授权在检查通过后提交、普通推送并核对远程。

检查结果：2026-09-19 09:52:24 的验收口径检查退出 0，七个文件、281 处本地链接、26 处锚点、28 个表格及 35 个冻结输入哈希全部通过，收据 out/runs/g3-coverage-scope-20260919-095224-689968/result.json。当前无最低覆盖率、正确性对照与状态／前置一致；T01 历史报告、工作日志原字节前缀、G0～G2 卡片保持。JSON、UTF-8、Markdown、忽略规则、敏感标记及 git diff --check 通过；未改业务源码、执行入口或正式产物，也未重新执行仿真／资格验收。补齐本条后按持续授权提交并普通推送。
归档结果：09:52:59 的交付复核再次通过七个文件、281 处本地链接、26 处锚点、28 个表格及 35 个冻结输入，收据 out/runs/g3-coverage-scope-20260919-095259-600494/result.json。七个文件显式暂存并检查通过，创建提交 dbaa1c89904428848434fa53965dbf2d30d5297b（docs: focus G3 acceptance on integration and coverage correctness）；git push origin main 退出 0，2026-09-19T09:53:23.3382847+08:00 核对远程与本地标识一致，工作区干净，收据 out/tmp/g3-coverage-scope-push.json。随后仅补齐本条与 status 的真实归档结果，单独提交收尾文档；T02 尚未启动。

## WL-20260919-004｜G3-T02 双向协议修复及候选验收

时间／时区：2026-09-19，Asia/Shanghai（UTC+08:00）。

关联任务与状态：G3-T02 已完成；通信修复、候选／正式六组协议验收、受影响重建复验、本轮 GUI 确认、正常退出、正式发布与当前来源复查全部完成。以下保留等待确认与纠正过程；G3 整体未完成，T03 可执行但未启动。

背景、目标与范围：按用户“继续 T02”实施真实 Java／C++ 双向 TCP、Sentinel／属性／LMCP 严格验收、来源保留／改写、PUB 过滤和残包清理。沿用用户已修订的系统联调与覆盖统计正确性目标，不引入最低覆盖率或算法／参数调优。开始时 HEAD 为 a1c814bf257b8854a6897a83ef629e8a51b094a2，git status --short 为空。读取 AGENTS、状态、任务卡、方案与日志，根说明适用；未委派子代理。

输入复查：仓库根目录执行 check-g3-baseline.ps1 并传入实际 Python 3.14.7 x64，g3-t01-check-20260919-095541-064 通过，当前 acceptanceRevision=2、正式 AMASE／UxAS、35 个冻结源输入、CRT、固定工具及七模型同批身份复核通过。此后必要通信修复改变输入，新候选通过原有资格链重新检查；不改写旧来源收据。原场景、任务、MDM、生成器、生成输出、依赖及根 CMake 保持。

复现与排查：新增独立 Python 编排、Java 事件探针与链接已合格 ZeroMQ 的原生 SUB 捕获器，观察客户端不回送消息。首轮 g3-t02-reproduce-20260919-100417-711 过早检查实体端口而失败，实体创建后才开放端口，改为实际 PID／端口就绪轮询。g3-t02-reproduce-20260919-100707-252 的命令计数混入原场景命令 100，追查原始流确认两条原命令各被 UxAS 桥回送一次；没有隐藏此重复行为。修正测试命令筛选后，g3-t02-reproduce-20260919-101142-414 完成原程序对照：普通传输收到 97001／97002，分包标记后 AMASE 零接收；两组 UxAS 收到 KillService 仍因阻塞接收无法在 30 秒内退出，强制清理仅处理本次 PID，失败与字节均保留。若干只读路径猜测未找到文件，随后用 rg 定位，不据失败读取作结论。

修复及文件：AMASE 新增 SentinelMessageReader，在 TcpServer 接收边界读满分包、限制长度、校验两层真实校验和／属性／解码长度；连接内发送串行化，未手改生成库。UxAS 的 ZmqAttributedMsgSenderReceiver 增加 100 ms 内部接收超时，ZmqGenericReceiver 正确返回超时，TCP 桥跳过无效空消息；ThreadRunnerBase 使用原子停止标记，ZmqProxy 有界 poll 且先停止线程再销毁成员；STREAM socket 交由基类正常关闭。TCP 桥新增默认关闭的 ExportOnlyLocalMessages，G3 主链路开启且保留接收来源，过滤非本地来源或本桥自行导入的消息，避免原巡航命令回送。独立实现与探针位于 scripts/g3_protocol、tests/g3_protocol、tests/windows/g3-protocol.tests.ps1，说明为 docs/g3-protocol-validation.md；同步 status、backlog、G3 方案、总体计划和 AGENTS。

构建及复验：AMASE g1-t04-build-20260919-101509-695450 构建成功，GUI g1-t04-run-20260919-101533-522836 自动运行后在约 20 秒暂停；g1-t04-automatic-20260919-101706-907868 的 11 项自动检查全部通过。UxAS g2-t05-build-20260919-101514-494 新构建通过，g2-t05-test-20260919-101831-478 的普通／中文路径编译、平台、来源和故障验收通过；HelloWorld g2-t06-test-20260919-102042-847 的双向消息、正常退出、中文路径及反例通过。C++ LMCP、生成批次和固定依赖复用既有合格身份，没有改 CMake／消息来源导致的额外重建需求。

协议验收：候选专用入口同时绑定 AMASE 构建、UxAS 构建与候选验收，先调用原有解析器核对当前源码和产物。g3-t02-verify-20260919-102042-846 因允许差异清单漏列已修复的 TCP 桥头文件而拒绝，未启动业务进程，保留 failed 收据；补齐明确路径后重新运行。g3-t02-verify-20260919-102134-514 五组通过，g3-t02-verify-20260919-102501-138 补充直接连接及原任务载荷后六组通过。检查原始任务事件发现探针把 Wedge 子节点误写成 ViewAngle，造成有效视角摘要空列表；修正名称并加入 35／-60 的实际值断言，旧结果不回写。

最终候选验收从仓库外系统临时目录调用入口，g3-t02-verify-20260919-102900-997 退出 0，result／entry-result 均 passed：保留来源、改写来源、真实分包、受控断线半帧、整组新进程、新鲜直接 TCP 连接共六组通过验收。受控断线子运行仍为 failed，expectedFailureVerified 表示预期故障已处理；其他子运行 passed，全部 18 个业务子进程退出 0、无强制终止，试验端口均释放。GUI 人工确认用的独立 G1 窗口仍保留，不计入协议组关闭结论。两实体真实配置、至少十条时间递增且移动的状态、载荷哈希及无重复、实际类加载来源、新旧 PID 隔离及每个子运行文件摘要均检查。保留来源时 Java／观察状态 43／43、PUB 为 0；改写时 41／41／41；直接连接被动 Java 观察较早接入见 45 条，UxAS 见 43 条，所有 UxAS 状态都有真实 Java 载荷对应，不以补造消息消除时序差异。

严格解析与后端边界：最终 Python 对真实 Java／C++ 帧分别覆盖 151／342 个拆分及截断位置、粘包和每方向十类畸形反例；生产 Java 读取器的独立检查为 split=150、eof=151、coalesced=2、malformed=20。真实 Java TCP 的两合法消息粘包、坏外层校验和拒绝／关闭、半帧断开和新连接通过；真实 C++ 坏外层长度／校验和未进入总线，后续两条粘连合法消息各收到一次。内部 LMCP 坏帧拒绝是严格验收器的证据，不夸大为所有原生 C++ 后端已有完整不可信网络防护。C++ 同进程在线重连／多客户端残包隔离仍归 G4，本卡断线即失败并停止整组，用新进程／解析器恢复。任务 1000 的 90 点仅用于真实消息加载与接收，不是自主规划／完成证据。

字段实测及后续影响：两实体旧相机 XML 经当前 Java 库读取后的有效 SupportedWavelengthBand 均为 EO；重复 ViewAngleList 经实际 C++ 加载后为一个 Wedge，中心线 35／-60，两个 Extent 为 0；任务请求波段仍为 AllAny。原始事件、线缆数据和最终断言交叉验证，未更改参数。AllAny 的覆盖统计语义与必要修复仍归 T05，当前没有覆盖率实测值。

人工确认与待完成事项：已通过异步问题请求用户查看当前 AMASE GUI，明确引用 AGENTS“正式发布要求自动验收、GUI 人工确认及正常退出全部完成”。截至本条追加尚无回复，不能复用 G1 旧确认或自行视为确认。GUI 运行仍为 awaiting-manual-confirmation，保留窗口及 5555／9400／9500；协议进程已全部正常回收。用户确认后用既有 amase.tests.ps1 Finalize 正常关闭并发布，再按新候选执行 UxAS T07 复验／发布，登记当前输入修订并重查正式基线、复验正式协议、最后同步完成状态及提交普通推送。当前旧正式包和冻结清单保留，但已不匹配修改后的源码，正式来源检查应拒绝，不能绕过。T02 尚未完成，依 AGENTS 部分完成不触发自动归档，本轮暂未提交或推送。文档、UTF-8、链接、语法、范围及历史保护检查结果另行追加。

交付检查：PowerShell 入口 AST、三份 Python AST、26 个任务文件的 UTF-8／Markdown、293 处链接、24 处锚点、33 个表格及 240 条来源／运行证据哈希通过；历史日志原字节前缀、G0～G2 卡片、T01 基线与历史报告保持，git diff --check 通过。首轮交付检查 g3-t02-delivery-20260919-103513-013539 的本机路径扫描把 TCP URI 尾部误识为盘符，收紧盘符边界后 g3-t02-delivery-20260919-103526-711067 的内容检查 passed；PowerShell stderr 重定向使包装命令状态含糊，随后显式检查 Python 退出码再次复核，不将混合状态调用直接计作合格入口。现有 Git 行尾警告保留，不全库转换行尾。没有新增业务源码修改，最终协议收据输入摘要仍匹配；正式发布继续等待本轮 GUI 确认，未提交／推送。

GUI 状态更新：10:35:56 的真实运行收据变为 automatic-passed，GUI 退出码 0、端口释放，末条仿真时间约 380.76 秒，无时间回退／错误；不是编排强制关闭。此变化恰好发生在交付检查期间，g3-t02-delivery-20260919-103557-656648 因旧的“仍在等待窗口”断言而失败，另检出新日志中用于解释误报的 URI 片段；已更新检查为真实正常退出并改用文字描述，旧收据保留。人工确认消息仍未收到，不能仅凭用户可能操作了窗口作确认；无需再次开窗，收到该轮显示正常的明确回复后可继续 Finalize 和发布。

人工确认及正式收尾：用户本轮明确回复“GUI确认并正常退出”。按真实确认执行 amase.tests.ps1 Finalize，g1-t04-finalize-20260919-103730-933619 退出 0 并正式发布；GUI 原运行退出 0／端口释放通过。随后 uxas-release.tests.ps1 的 g2-t07-test-20260919-103739-909 连续三次启停、中文路径、真实超时和隔离故障矩阵通过；publish-uxas.ps1 的 g2-t07-publish-20260919-104007-031 通过发布目录运行、环境恢复后切换指针，旧批次与指针备份保留。此前待确认／发布状态已解除。

当前输入及正式协议：登记 inputRevision=2，保留原清单三个旧摘要与父收据，新增八项修复相关输入，当前共 43 项；acceptanceRevision=2 和无覆盖率门槛保持。check-g3-baseline.ps1 的 g3-t01-check-20260919-104119-819 退出 0，正式产物、同批模型、工具／CRT／handoff 和 43 项来源通过，原场景十项反例复验通过。协议入口补充 Python 3.14.7 x64／隔离模式显式核查。正式首轮 g3-t02-verify-20260919-104244-282 直连组初始配置观察遗漏而失败，前五组通过、全部正常退出；保留失败，未删断言。独立测试副本移除 ConstructiveControl 自动启动，探针待主链路、观察连接和真实 KeyValuePair 就绪后才开始 1 倍速仿真，保证取样完整。没有改正式二进制，未重用静态状态或提前实现 T03 任务／请求屏障。

最终 g3-t02-verify-20260919-104721-817 从仓库外调用正式模式，退出 0，result／entry-result 均 passed、candidateMode=false；六组、严格解析、正常关闭／端口释放及环境恢复通过，新正式收据为最终依据，候选和失败收据不改写。制作文档更新辅助脚本时一次 apply_patch 因补丁新增行缺少前缀被拒绝，未产生部分编辑，拆分后完成。

完成状态：T02 已完成，T03 可执行、尚未启动；当前仅证明协议与正常生命周期，测试命令来自 SendMessagesService，不宣称自主规划、完整任务或覆盖统计已通过。报告、状态、任务卡、方案、总体计划与 AGENTS 已同步。最终交付检查后按持续授权仅提交本任务文件、普通推送并核对远程，归档结果另行补记。

最终交付检查：2026-09-19 10:52:43 的 g3-t02-delivery-20260919-105243-699357 退出 0；27 个任务文件、299 处链接、26 处锚点、34 个表格及 289 条来源／证据摘要全部通过。PowerShell 与 Python AST、UTF-8、Markdown、当前输入修订、T02 完成／T03 未启动状态、真实 GUI 确认和正式指针通过；历史日志前缀、T01 与 G0～G2 报告保持，忽略规则、敏感标记及 git diff --check 通过。正式六组用例包含一个已验证的预期断线失败，各组生命周期均正常。仅剩按持续授权显式暂存、提交和普通推送，真实归档结果追加下文。

归档结果：10:54:25 的最终复核 g3-t02-delivery-20260919-105425-862883 再次通过 27 文件、299 链接、26 锚点、34 表格及 289 条摘要，全部 GUI／协议端口无残留。显式暂存本任务 27 个文件，暂存范围及差异检查通过，创建主提交 0feaf3f68749bd38db0bf6a0da41a1ccce04d31f（fix: qualify G3 bidirectional TCP and source handling）；git push origin main 退出 0，2026-09-19T10:54:40.559982+08:00 核对远程标识一致且工作区干净，收据 out/tmp/g3-t02-push.json。随后仅补齐本条及 status 的真实归档结果，以独立文档提交收尾；T02 已完成，T03 可执行且尚未启动。


## WL-20260919-005｜G3-T03 受控启动与初始化

时间／时区：2026-09-19，Asia/Shanghai，11:08 开始来源复查，11:33:51 完成独立 GUI 正常收尾；文档及归档随后完成。
关联任务与状态：G3-T03 已完成；T04 可执行、尚未启动；G3 尚未完成。
背景、目标与范围：按“执行 T03”落实暂停启动、真实初始数据屏障、单次任务／请求及正常关闭。沿用用户取消覆盖率指标的口径，只调通程序与系统，本卡不实现任务航段关联、完整完成或覆盖计算。

工作过程：起点 9325f3b48975bff1a723a9fe02ec7e4ea23eb69b，工作区干净。读取状态、任务卡、方案、最近日志、根说明及相关 README／源码；check-g3-baseline.ps1 的 g3-t01-check-20260919-110823-590 退出 0，正式 AMASE／UxAS、工具／CRT、同批七模型及 43 项冻结输入通过。新增 scripts/g3_integration 的标准库编排、初始化屏障、独立 StartupProbe 和 GUI 收尾客户端；新增 config/g3-startup.json、运行／验收／收尾三个 Windows 入口及 tests/g3_integration。插件仅在本次 out/build 编译，不改变正式 JAR、EXE、原 XML、MDM 或生成输出，无需重建旧正式包。

初始化实现：运行副本移除 SendMessagesService、PUB／PULL、ConstructiveControl 及未用实体端口，保留原业务规划服务和 785 秒场景。端口预检后 AMASE 暂停，真实连接与解析就绪后才 1 倍速启动；UxAS 内 400／500 的配置和至少五条不同时间且位置变化的状态，均与独立 AMASE TCP 取样原始摘要交叉核对。任务 1000 单次注入，TaskInitialized 与 AMASE 实际接收齐全后才单次请求，等待非空真实规划响应。控制观察桥明确改写本地编排输入来源；主桥保留 AMASE 0／0 来源，独立 UxAS 消息数据库见证控制注入，接收器不回送消息。初始化整体、任务初始化、规划和关闭分别有 30 秒上限。

问题与处理：第一次独立运行 g3-t03-run-20260919-111834-314 发现原 EntityControl 还列其他实体端口，修正副本仅保留 400／500。111856-707 因观察桥过滤自身注入而等待回显超时，改用 AMASE 真正收到探测消息、UxAS 原状态与 PID 就绪，控制属性由原 MessageLoggerDataService 见证。112025-941／112104-165 暴露 Python XML 工厂返回根节点下列表的语义，改为按原根 Series／类型调用生成类读取；112129-562 首次完整无界面启动通过。最终任务二进制与 T02 C++ 读取原 XML 的摘要一致，没有改变重复 ViewAngleList 的有效值。

矩阵首轮 g3-t03-test-20260919-112311-821 保留 failed：中文目录名进入探测文本触发旧 LMCP 字符串长度／UTF-8 边界；同时 UxAS 默认 5 秒返回空规划响应，实际失败原因不是编排超时。运行标识改为 ASCII，场景从真实中文工作目录相对加载，避免把本机路径带入 SessionStatus；未手改生成器或库。规划故障分为原有空响应拒绝，以及仅故障副本延长 UxAS 自身等待到 60 秒以实际触发编排器 30 秒上限。通用 Unicode 消息支持另登记后续契约修复。所有失败收据保留。一次只读统计命令的 PowerShell 管道语法错误已纠正；文档辅助脚本的反斜杠转义警告通过 raw 字符串修正，未改变业务源码或验收结果。

验证：从仓库外调用 tests/windows/g3-startup.tests.ps1 -PythonExecutable <已验证解释器> -BaselineRunId g3-t01-check-20260919-110823-590，最终 g3-t03-test-20260919-112800-197 退出 0，result／entry-result 均 passed。11 组包含 GUI、无界面、中文空格目录三组正常例，以及缺配置、静止状态、提前请求、任务初始化超时、规划超时、空响应、关闭超时、端口冲突八组预期失败；另有 10 项基于本轮真实消息的内存反例。正常例两实体屏障时状态数 6／5，实际规划请求／响应 ID 48，实体 400、命令 1、74 航点，仿真约 4.50～4.53 秒。三组两端均退出 0、无强杀、端口释放；关闭故障例仅在 30 秒后终止本次故障 AMASE，UxAS 正常退出，failed 子收据不改写。端口占用例未启动应用，占用者仍可连接。已有运行编号与缺失 Python 均拒绝，历史成功收据不变。

独立 GUI 复验：从仓库外以中文空格配置路径运行 run-g3.ps1 -Mode Gui -KeepGui，省略 BaselineRunId；新资格 g3-t01-check-20260919-113233-558 通过，GUI 批次 g3-t03-gui-final-20260919-113233-126 规划后实际暂停。finish-g3-gui.ps1 核对控制进程身份并正常关闭，两端退出 0、端口释放、环境恢复，11:33:51 result／entry-result 均 passed。收尾为自动入口验证，manualGuiAcceptance=false，不复用 T02 确认或冒充 T07 人工确认。

成果与修改文件：实现见 scripts/g3_integration、tests/g3_integration、config/g3-startup.json 和三个 Windows 入口；专题证据见 [T03 报告](docs/g3-startup-validation.md)。同步 status、backlog、G3 方案、总体计划和 AGENTS，历史验收报告保持原样；原始配置／字节／解码／内部事件／日志数据库／来源哈希／PID／退出证据保存在本次 out/runs，不提交生成物。

重要决定与遗留：本卡只将两模式初始化及规划响应登记为通过；下一项 T04 关联分段规划命令、AMASE 当前命令／航点／任务和实际轨迹，尚未启动。T05 继续处理 AllAny 与覆盖统计正确性，不追求覆盖率数值；G4／G6 涉及 Unicode 消息字段时须按模板和同批生成来源完整修复复验。阶段 GUI 人工确认仍归 T07。交付检查及实际 Git 归档结果追加下文。


最终时限复核与复验：11:37 的文档／交付检查已通过 16 文件、315 链接、27 锚点、30 表格、559 条摘要及历史前缀保持。最后审查发现 GUI 等待若占满 2700 秒，关闭还会另占时间；在独立运行器总预算内预留 50 秒供正常关闭等待、失败回收、接收线程和证据收尾，活动截止点设为 2650 秒。新增过期截止时间反例，未把该检查写成实际等待 45 分钟。来源变化后不沿用旧编排收据：从仓库外用新资格 g3-t01-check-20260919-113233-558 重跑完整矩阵 g3-t03-test-20260919-113944-818，11 组真实用例、10 项消息屏障反例和预算反例全部通过，退出 0。GUI 保留／收尾另用 g3-t03-gui-budget-20260919-114418-444 复验，两端退出 0、端口释放、result／entry-result passed。报告及 status 更新为最终新收据；前一通过批次与失败批次均保留，正式输入／产物未改变。


最终交付复核：2026-09-19T11:45:57.133834+08:00 的 out/tmp/g3-t03-delivery.json 通过；16 个任务文件、315 个链接、27 个锚点、30 个 Markdown 表格、559 条来源／证据摘要通过，Python／PowerShell 语法、UTF-8、状态一致性、历史报告与日志前缀保持、git diff --check 和敏感标记检查通过。源码与最终矩阵／GUI 收据一致，全部相关监听端口无残留。另验证已结束 GUI 的收尾请求被拒绝，旧请求文件摘要未变。按持续授权显式暂存本任务范围并普通推送，实际归档结果随后追加。


归档结果：显式暂存本次 16 个文件，暂存范围、敏感标记及差异检查通过，创建主提交 7fec15dcfe926ffef43b98a3002e05da5fbc3807（feat: add controlled G3 startup and initialization gates）。git push origin main 退出 0，2026-09-19T11:46:39.266445+08:00 核对远程标识一致、工作区干净，收据 out/tmp/g3-t03-push.json。随后仅以独立文档提交补齐本条和 status 的真实归档结果；T03 已完成，T04 可执行且尚未启动，G3 阶段仍未完成。


## WL-20260919-006｜G3-T04 规划命令实际执行

时间／时区：2026-09-19，Asia/Shanghai。
关联任务与状态：G3-T04 已完成；T05 可执行、尚未启动；G3 尚未完成。
背景、目标与范围：按“执行 T04”关联真实请求、规划分配、分段 MissionCommand、AMASE 收到命令、当前命令／目标／任务和实际轨迹。沿用用户明确的系统功能重点，不设覆盖率指标，不调算法／业务参数，不把本卡局部执行写成全任务完成。

工作过程：起点 50b282e2324023b20da1a0842cd51ab9e3103858，工作区干净。读取任务卡、状态、T03 交接、根说明及相关源码。初始资格 g3-t01-check-20260919-115021-911 通过。新增 scripts/g3_execution 编排／关联器与只读实体模块、tests/g3_execution、config/g3-execution.json 和两个 Windows 入口；直接复用 T03 合格初始化及清理，不改变旧来源输入。逐项核对 Unique 请求／响应、TaskAssignmentSummary、原始完整计划、巡航重封装、正常分段重叠与实际导航。500 虽未被分配仍持续有真实状态；400 执行任务。

问题、尝试与处理：首轮 g3-t04-test-20260919-115933-023 实际进入任务、正常退出，但直接原 XML 比较因旧 Label／省略默认字段拒绝。改用同批模型实际解析结果对照；Java 与 Python 的派生字段输出顺序和 float32 文本差异按语义比较，整数身份严格保留。短目标航点会在网络两条状态之间经过，增加运行副本末尾 EntityModule 逐次记录导航变化和约 0.2 秒位置样本，不改飞行属性、状态频率或执行命令。第二轮 g3-t04-test-20260919-120403-802 两模式实际子用例通过并正常退出，但静止时间反例被更早的内部同时间键冲突拒绝导致整组失败；前移时间单调验证并补齐内部／公开位置、重复／回退、相邻大 int64 反例，23 项离线复核通过。失败收据完整保留。只读检查中提前读取尚未生成的最终 context/result 报文件不存在，随后按就绪文件检查，未影响运行。

后续复验 g3-t04-test-20260919-121012-982 两端正常退出，但 5.239 仿真秒的公开目标 2 在步末已推进为 3，新增的内部交叉核对失败。根因是命令接收与首步推进之间缺少导航样本；探针补录命令真实应用后的属性，再以新编号重跑两模式，未放宽目标匹配或修改失败收据。

源码与实测发现：原 param.turnType 未被服务读取，实际 TurnShort；配置重叠 5 在当前实现中实际重叠 4 点。巡航 100 会被 UxAS 重新封装为 21／24，本轮实际任务分段 67／68 与它们明确区分。保留原行为，本卡不纠正为 FlyOver 或修改分段算法。公开状态的目标序列允许采样漏点，内部连续 NextWaypoint 证据负责确认无回退；TurnShort 的提前转弯不等同于末端任务完成。

验证：从仓库外 out/tmp 调用 tests/windows/g3-execution.tests.ps1，明确 Python 3.14.7 x64、中文空格 Configuration；前轮省略 BaselineRunId 取得新资格 g3-t01-check-20260919-121013-211，最终复用并复查其冻结来源。最终 g3-t04-test-20260919-121531-644 result／entry-result 均 passed、退出 0。GUI／无界面均证明任务 1000 的真实分段执行、TaskActive、至少四个任务目标及两段有几何推进的航段；23 项离线反例／边界检查通过。两实体持续真实动态状态，两进程退出 0、无强杀、端口释放，环境／目录／编码恢复、持久 PATH 不变。正式 AMASE／UxAS／同批 LMCP、43 冻结输入、原 XML 及旧 T03 输入不变，不需重建正式包。

成果与修改文件：实现、入口、配置及反例见 [T04 执行报告](docs/g3-execution-validation.md)；同步 status、backlog、阶段方案、总体计划与 AGENTS。原始字节／解码／内部事件／内部导航、配置差异、日志数据库、产物和运行输入摘要、PID／退出码保存在独立 out/runs；不提交 out 产物或本机路径。历史报告与日志前缀保持。

重要决定与遗留：taskExecutionValidated=true，taskCompletionValidated=false，coverageValidated=false。T05 接续完整末端／TaskComplete 与 AllAny 等覆盖统计正确性及两模式导出；低覆盖率本身不阻塞。重复启停／故障恢复归 T06，全程 GUI 人工确认归 T07，本卡 GUI 自动关闭不等同于人工确认。最终交付检查与提交／普通推送结果随后追加。

最终交付复核：2026-09-19T12:21:11.198384+08:00 的 out/tmp/g3-t04-delivery.json 通过，14 个任务文件、324 个链接、27 个锚点、30 个表格及 173 条来源／证据摘要通过；Python／PowerShell 语法、UTF-8、状态一致性、历史报告与日志前缀、git diff --check 和敏感标记检查通过。已使用旧运行编号实测拒绝，历史失败收据摘要不变；全部七个 G3 监听端口释放。文档差异复查恢复了辅助替换误删的 status 中 G1 历史批次段落，未改旧验收记录；一次通过 PowerShell 标准输入执行含中文文本的辅助脚本因编码转换而匹配失败，改为 UTF-8 文件执行后修正。按持续授权显式暂存本任务 14 文件，随后提交、普通推送并核对远程。

归档结果：首次暂存后，辅助归档脚本把 Git NUL 分隔输出的末尾空项计入文件集合，范围断言拒绝；过滤空项后核对仍为本任务 14 文件，无额外暂存内容。创建主提交 43b4f56d2f386cdb34c1322113d3d07175197ae7（feat: validate G3 planned mission execution），git push origin main 退出 0；2026-09-19T12:22:39.693197+08:00 的远程 main 标识与本地一致、工作区干净，收据 out/tmp/g3-t04-push.json。随后仅提交本段及 status 的归档说明，不改实现或验收输入。T04 已完成，T05 可执行、尚未启动，G3 阶段未完成。

## WL-20260919-007｜G3-T05 任务完成与覆盖统计正确性（进行中）

时间／时区：2026-09-19，Asia/Shanghai；本轮先完成来源复查，13:25 构建修复候选。
关联任务与状态：G3-T05 进行中，尚未登记完成；T06／T07 未启动。
背景、目标与范围：按用户要求执行完整任务、可靠 TaskComplete 与 GUI／无界面覆盖统计正确性；保留 90 点水道、20 米、原 785 秒及 1 倍速，不设最低覆盖率，不做算法／参数寻优。
工作过程：起点 f357e96d96d085a8dccfb406d1b917c72991b2aa，工作区干净；阅读项目约定、状态、任务卡、T04、T01 与近期日志，Python 3.14.7 x64 实测可用，资格 g3-t01-check-20260919-130929-695 通过。原版长程 g3-t05-original-20260919-131200 正常运行至约 783 秒并退出，约 730 秒发出 TaskComplete，但最后任务目标 73 仅保留 0.03 秒便进入同坐标、无任务的 74；独立完成审计拒绝。统计小样本实测 AllAny 应覆盖 3 格却为 0、长线应分 56 格却为 10、无界面报告触发 HeadlessException。
问题与处理：仅修复四个 AMASE Java 源文件：线搜索 AllAny 拒绝条件和静默十格上限；分析客户端配置传递、headless 进度窗口隔离及 GUI 窗口释放；原始计数／全栅格导出与重放初始化；WaypointFollower 的同坐标任务末端标记使用既有 fly-past 到达判定，普通 TurnShort 保持。完整水道独立计算为 724 格，原 669 格的差异来自 16 个被粗化的水道段，没有删水道或改业务输入。
验证进展：构建 g1-t04-build-20260919-132521-005392 成功；11 组自动复验 g1-t04-automatic-20260919-132647-813805 通过。对照 g3-t05-candidate-checks-20260919-132900 中，旧 JAR 覆盖／末端各检出 3 项失败，新候选均通过。先前 132700 末端夹具因未创建实体事件管理器发生空指针，改用独立 EntityData 夹具后重跑；失败日志保持。同一原飞行记录重放 g3-t05-replay-original-events-20260919-133000，旧 GUI 为 0／669，新候选两环境均为 724／724，逐格一致；独立 Python 复算 2942 条任务后状态一致。该对照不替代新版两模式完整飞行。读取脚本的最初 PowerShell 默认编码显示乱码，改为显式 UTF-8；Windows rg 通配目录参数的无效调用改为明确路径，均为只读命令问题。
成果与文件：scripts/g3_completion、tests/g3_completion、config/g3-completion.json、Windows 运行／验收入口及 docs/g3-completion-validation.md 已创建；源码、配置与实际执行证据分别记录。新版本完整运行仍在验证。
待处理：G1 本轮 GUI g1-t04-run-20260919-132541-198195 已暂停供真实人工确认，已依据 AGENTS 既有发布要求发出确认请求；新正式包发布等待该确认。独立未发布候选长程诊断 g3-t05-candidate-diagnostic-20260919-133400 正在运行，不绕过正式来源资格或冒称已发布。后续补齐来源修订、两模式完整运行、完成／统计反例及正常退出；通过后同步状态并按持续授权提交、普通推送。此条为本轮未结束记录，最终结果继续补充。

两模式实际结果：无界面 g3-t05-candidate-diagnostic-20260919-133400、GUI g3-t05-candidate-gui-20260919-134600 的 result／case-result 均 passed，明确为未发布候选、formalPublicationValidated=false。两次均依序执行任务目标 14～73，末段 45 个真实导航样本、前向推进约 223.997 米，原生末端到达约 740.310 秒，距目标约 3.728 米；首个退出任务状态及唯一同源／同任务／实体 TaskComplete 均为 740709 仿真毫秒。每次 724／724 格，独立 Python 复算各 2794 条任务后状态，相同记录 GUI／无界面 JVM 逐格一致，重复报告与清空重放一致。两次均约 744 秒收尾，AMASE／UxAS 退出 0、无强杀、各自端口释放。100% 只是计算结果，无最低值要求或调参寻优。

GUI 隔离与边界：为保留 G1 发布确认窗口，GUI 长程诊断显式使用 5557／29400／29500／9998，配置快照、所有端口归属、真实命令、截图及响应检查留证，不替代真实人工确认。诊断使用的 T04 context 模板 mode／verify 元数据未同步，本次实际 Gui 模式以 case-result、进程命令和配置快照为准，未改写旧记录；标准入口最终验收仍须使用新的合格来源。一次进程检查因已正常退出的 PID 不再存在而返回 1，独立确认发布窗口 PID 7112 仍存在且响应正常，未关闭或强杀该窗口。

独立检查：g3-t05-offline-controls-20260919-134700 的 19 项正反例通过，缺失／重复／错身份／提前完成、缺末段导航／目标／状态、静止轨迹、统计计数／百分比／坐标／漏导出等均明确拒绝；有效零覆盖可通过。g3-t05-entry-controls-20260919-134900 通过仓库外调用、缺失 Python／重复编号拒绝、环境恢复及历史收据保持。标准入口 g3-t05-stale-source-20260919-140400 以当前 AMASE 源码摘要不匹配旧来源而失败，未启动应用；这是预期拒绝，不能写成正式全程验收通过。

交接状态：本轮 GUI 人工确认仍未收到，G1 发布窗口保持暂停。仅等待该人工观察，不能复用旧确认；取得本轮明确回复后按报告内完整身份执行既有 Finalize，正常关闭并发布，再保留历史登记 inputRevision=3、复查资格及运行标准 T05 两模式验收。当前 inputRevision=2 和旧正式目录保持，UxAS／LMCP／业务 XML 未改变。已同步 status、backlog、阶段方案、总体计划和 AGENTS；T05 进行中，T06／T07 未启动，依部分完成约定本轮未提交或推送。

本轮交付复核：out/tmp/g3-t05-review.json 核对 22 个任务文件、331 处本地链接，UTF-8、Python／PowerShell 语法、git diff --check、历史日志前缀及 T01～T04 报告保护通过。out/tmp/g3-t05-evidence-review.json 的 647 条摘要复核通过，绑定两次真实运行证据、当前完整编排输入及候选构建来源，旧正式 JAR 保持。未全库转换行尾，保留 Git 的既有转换警告。最终监听检查仅保留发布确认进程 PID 7112 的 5555／9400／9500，窗口响应正常；全部候选全程验收端口已释放。真实 GUI 确认到达后从上述发布步骤继续，不重写历史失败或候选收据。

## WL-20260919-008｜G3-T05 人工确认后的正式收尾（进行中）

时间／时区：2026-09-19，Asia/Shanghai，15:40 继续。
关联任务与状态：承接 WL-20260919-007，G3-T05 正式收尾中；T06／T07 未启动。
背景与授权：用户明确回复“GUI 人工确认及正常退出  ，一切正常”。沿用本任务完成后的提交／普通推送持续授权，不重复请求人工确认。
发布进展：g1-t04-finalize-20260919-154103-934413 passed，绑定原候选、11 组自动验收及本轮 GUI 身份，真实退出码 0、端口释放，已正式发布相同 JAR。将四项已复验源码修复登记为 inputRevision=3，保留 revision 2、旧摘要和父级证据，不改变 acceptanceRevision=2 或无覆盖率门槛。
来源问题与处理：资格 g3-t01-check-20260919-154113-520 failed，原因是 UxAS 发布包的 handoff 仍绑定旧 AMASE。源码确认该绑定由现有 G2 发布流程生成；不能直接改旧 handoff。复用未变更的 UxAS 候选／验收与 T06 收据，按原 T07 复验和发布入口更新交接身份，然后重新核查 G3 基线。此次失败和旧正式批次保留，不改写为成功。只读检索两次使用了不存在的 Python 文件名，实际模块已从 rg 结果定位，无业务文件改动。

发布与资格收尾：g2-t07-test-20260919-154235-410 的连续三次启停、中文路径、真实超时及隔离故障全部通过；g2-t07-publish-20260919-154459-188 完成新发布目录运行、环境恢复后原子更新正式指针，旧包和指针备份保持。UxAS EXE 摘要未变。g3-t01-check-20260919-154623-382 result／entry-result 均 passed，正式产物、更新后的交接、43 项来源与同批七模型通过。正式 T05 从仓库外 out/tmp 调用标准入口，编号 g3-t05-test-20260919-154803-742；19 项覆盖小样本与 6 项末端导航对照已通过，顺序全程验收正在进行。完成状态同步辅助脚本均先检查真实 passed 收据，尚未提前写入完成结论。

正式全程结果：g3-t05-test-20260919-154803-742 的 result／entry-result 均 passed、退出 0。两模式各自完成全部任务目标、真实末段与可靠 TaskComplete，并通过每格独立复算、相同记录两环境重放、重复分析和重置重放对照；每次 724／724，漏覆盖 CSV 无缺格。无界面约 743.980 仿真秒收尾，独立复算 2794 条任务后状态；GUI 约 744.710 秒、2796 条。19 项基于本轮真实记录的离线正反例通过；两模式两进程均正常退出、无强杀、端口释放，来源／配置未变化，进程环境、工作目录、编码和持久 PATH 检查通过。

实际倍率差异及用户决定：收尾独立比较墙钟与仿真时间发现 GUI 快于 1 倍，进一步解码真实 SessionStatus：16:07:28 在仿真约 392.970 秒首次记录 5 倍，16:07:43 在约 469.770 秒首次记录 10 倍，此前为 1 倍。g3-t05-rate-audit-20260919-161048 保留 GUI 固定 1 倍检查 failed 的结论，不改原入口 passed 收据；此前入口只保证启动倍率、未拒绝界面运行中调速。原候选两模式与正式无界面均实测全程 1 倍。用户明确回复“接受本轮实际变速，明确保留倍率记录”；g3-t05-rate-acceptance-20260919-161131 将该单次指示与原始事件、倍率审计、正式 result／entry-result 的摘要绑定，接受本轮实际变速，不宣称正式 GUI 全程固定 1 倍，也不扩大为未来默认策略。规划、相机、传感器条件、785 秒场景、20 米和完整水道未改变，无算法或参数寻优。

完成结论与交接：本轮真实 GUI 发布确认和倍率差异接受均已取得并独立记录，G3-T05 完成；T06 可执行、尚未启动，T07 阶段全程 GUI 确认仍须另行完成。完成报告、状态、任务卡、阶段方案、总体计划和 AGENTS 已同步。out/tmp/g3-t05-final-evidence.json 的 294 条正式运行／来源摘要核对通过，并复核本轮人工确认、正式指针和倍率接受绑定。接着进行最终文档／范围复核，按持续授权提交、普通推送并核对远程；归档结果随后追加。

提交前复核：23 个任务文件、336 处本地链接，UTF-8、Python／PowerShell 语法、git diff --check 及历史日志前缀／T01～T04 报告保护通过；所有本轮 GUI、无界面、观察和候选端口均无残留监听，out 原始证据与临时脚本受既有忽略规则保护。远程 main 核对仍为起点 f357e96d96d085a8dccfb406d1b917c72991b2aa。文档草稿的完成同步只在真实 passed 后执行；一次预览输出受 PowerShell 默认编码影响，显式 Python -X utf8 后显示正常，文件 UTF-8 检查通过。明确只暂存本任务 23 个文件，进行普通提交／推送，不夹带 out 或生成物。

归档结果：显式暂存并核对本任务 23 文件，创建主提交 9c99e44c0b84a24871016263076aad2e85c1aa5c（fix: validate complete G3 missions and coverage statistics）；git push origin main 退出 0。2026-09-19T16:16:01.871277+08:00 核对远程 main 与本地一致、工作区干净，收据 out/tmp/g3-t05-push.json。随后仅补齐本段及 status 的真实归档说明，以独立文档提交收尾；不改正式产物、功能配置、验收实现或原始证据。T05 已完成，T06 可执行但尚未启动，G3 阶段仍未完成。

## WL-20260919-009｜G3-T06 稳定性与故障复验

时间／时区：2026-09-19，Asia/Shanghai。
关联任务与状态：G3-T06 进行中；T05 已完成，T07 尚未启动。
背景、目标与范围：用户要求执行 T06。读取 AGENTS、status、backlog、T04／T05 报告、阶段方案、最近日志、原例 README 与实际启动／执行／协议源码；起点 e1c2c83050b6e22ac638bfa4439301b09f45ea13，工作区干净，无相关端口占用。当前正式产物、90 点水道、业务参数和 1 倍默认保持，不设最低覆盖率，不开展寻优，不将 T05 的单次变速接受扩大到本轮。
工作过程：新资格 g3-t01-check-20260919-162134-898 passed；新增独立 config/g3-stability.json、scripts/g3_stability、tests/g3_stability 及 Windows 运行／验收入口，复用 T03 启动与关闭、T04 实际执行关联。矩阵按独立新编号启动子控制器，从其他工作目录运行，覆盖三次连续短程、中文空格应用路径和 GUI；故障底层 failed 保留，上层另验预期拒绝。受控主链路转发器只截断一条真实状态帧，保存完整源帧、已发送前缀和字节偏移，整组清理后新组从零时刻至实际执行。
先行诊断：g3-t06-diagnostic-disconnect-20260919-165300 的编号是手工标识，实际时间以收据为准（16:32），不能按编号误认 16:53。该运行按预期 failed，断开偏移 10037，真实帧 579 字节、发送 289／未发送 290；AMASE 与 UxAS 均退出 0、无强制终止、端口释放。独立诊断并不等于完整矩阵通过。
验证进行中：Python AST 与 PowerShell 语法检查通过。从 out/tmp 调用 tests/windows/g3-stability.tests.ps1，显式 Python 3.14.7 x64、CompletionRunId=g3-t05-test-20260919-154803-742、BaselineRunId=g3-t01-check-20260919-162134-898；正式矩阵 g3-t06-test-20260919-163443-663 正在运行，结束后追加实际结果。
问题与处理：几次只读检索使用不存在的辅助文件名或输出过长，改为实际文件和限定范围；一次内联 Python 的 PowerShell 引号使位宽查询 NameError，真实编排的 struct／隔离解释器检查已通过；一次生成新 PS 包装器的引号导致解析失败、未写文件，改用原生 here-string 后语法检查通过。以上均未改变业务源码、正式包或历史清单。
重要决定与遗留：正式 AMASE／UxAS／LMCP 和 43 项冻结来源保持；新增验收独立登记输入。实际 SessionStatus 必须全程 1 倍，旧字节偏移、旧就绪标识／时钟／任务／命令／完成的反例独立留证。T06 短程不替代 T07 全程和本轮 GUI 人工确认；在线自动重连／快照仍归 G4。待矩阵通过后同步报告与状态，按持续授权提交、普通推送并核对远程。

矩阵纠正：g3-t06-test-20260919-163443-663 的首轮实际执行与正常退出通过，但新隔离审计错误地只允许巡航 CommandID=100，将本轮 UxAS 重编号的 21／24 拒绝，整组 failed 保留。原始 XML 与 WaypointPlanManagerService.cpp 显示：原巡航被重新编号、批准，并按既有配置附加 payload=1／elevation=-60 的无任务云台动作；这不是旧任务。先改为内容比对仍因该默认动作未被识别而拒绝。g3-t06-test-20260919-163900-514 在该离线失败尚未核对时误启动，随即以该子运行的 request-abort 正常收尾并保留 failed（timeout:parent-budget 为现有请求退出诊断，不代表实际耗尽总预算）。未强制终止或修改旧记录。
修正仅作用于新增验收：忽略巡航重编号／批准和源码确认的精确默认无任务云台动作，其余内容仍须对应本轮原始巡航；实际旧规划命令仍拒绝。g3-t06-isolation-diagnostic-20260919-164008 的原样正向与七项变速／旧 token／偏移／时钟／任务／命令／完成反例通过。随后重新启动完整矩阵 g3-t06-test-20260919-164024-586，前两个失败矩阵及诊断均保留，尚未登记最终完成。

补充入口验收：g3-t06-entry-check-20260919-164238-155 在 out/tmp 验证缺解释器、重复历史编号、缺合格 T05 前置，均明确拒绝且环境恢复／历史结果保护通过；未启动业务应用。辅助脚本首试因 PowerShell 把预期 stderr 当终止错误而中断，改为捕获子进程退出码后以新编号重试，正式入口保持不变。正式矩阵中三次连续实际执行已通过（无界面／中文空格目录无界面／GUI），各约 135 墙钟秒；故障例的 failed 与上层预期验证分开，最后的新编号整组恢复仍在执行，完成后再登记结果。

完成结论：G3-T06 已完成。正式矩阵 g3-t06-test-20260919-164024-586 的 result／entry-result／acceptance 均 passed，入口退出 0，总耗时 685.84 秒。10 个独立子运行包括三次连续短程、端口冲突、AMASE／UxAS 提前退出、规划／关闭超时、半帧断线和新组恢复；六个故障子运行均保留 failed／退出 1，上层分别验证预期拒绝。另有六项隔离来源反例、八项原样／污染／倍率重放对照及总预算检查通过。四组正常运行分别在 121.97／121.99／121.98／121.94 仿真秒收尾，均有两个真实分段命令、四个任务目标与两段实际推进，238 条 Running SessionStatus 全为 1.0；AMASE／UxAS 均退出 0、无强制终止、进程回收和端口释放。关闭超时故障仅回收本次 AMASE（30.026 秒、退出 1），UxAS 退出 0；连接重置诊断保留，不混记为正常退出。
恢复证据：正式断线切断实体 400、4709 仿真毫秒的真实状态帧，完整 26 帧后在字节偏移 10037 保留 289 字节前缀，严格 EOF 检查拒绝，残缺状态未进入 UxAS。失败组清理后新编号 fresh-group 从零仿真时间、零解析偏移与独立就绪 token 重新规划并执行，旧任务／命令／完成污染反例拒绝。原源码、正式包、当前指针、43 项冻结来源和 T05 证据前后摘要保持；独立 out/tmp/g3-t06-final-evidence.json 验证 658 项摘要及收据绑定。所有记录应用 PID 均已退出，5555／5556／5557／9400／9500／19400／19500／9999 八端口无监听残留。
成果与交接：新增独立稳定性配置、三份编排模块、一份 Python 矩阵及两个 Windows 入口，专题报告 docs/g3-stability-validation.md；同步 AGENTS、status、backlog、G3 阶段方案、总体计划和本日志。没有修改业务源码、既有 T03～T05 入口、业务参数、生成代码或正式清单，因此不触发二进制重建／发布。T07 可执行、尚未启动，仍须最终两模式全程和本轮 GUI 人工确认；G3 阶段未完成。当前继续做最终文档／范围检查，按持续授权提交、普通推送并核对远程，实际归档结果随后记录。

提交前检查：14 个任务文件、342 处本地文档链接、UTF-8、Python／PowerShell 语法、git diff --check、旧工作日志前缀及 T03～T05 源码／报告保护通过。out 原始证据和辅助脚本均被忽略；未提交生成物、本机工具或绝对用户路径。一次将包含中文字面量的静态检查经 PowerShell 管道传入 Python 时被编码为问号，改用 UTF-8 文件执行后通过；仓库原文件无编码损坏。远程 main 核对仍为起点 e1c2c83050b6e22ac638bfa4439301b09f45ea13。现在仅暂存本任务 14 文件，完成提交和普通推送。

归档结果：主提交 b1f5e9b2ceb5cf9d05aca1cd5f9c8dbeeed0d1f2（test: validate G3 stability and whole-group recovery）包含已核对的本任务 14 文件；git push origin main 退出 0。2026-09-19T16:57:04.0394046+08:00 核对本地 HEAD 与远程 main 一致、工作区干净，收据 out/tmp/g3-t06-push.json。随后仅追加本段与 status 的真实归档说明，以独立文档提交收尾；运行配置、验收实现及原始证据保持不变。T06 已完成，T07 可执行但尚未启动，G3 阶段尚未完成。

## WL-20260919-010｜G3-T07 阶段验收与 G4 交接

时间／时区：2026-09-19，Asia/Shanghai；17:31 启动正式两模式全程。
关联任务与状态：G3-T07 进行中，G3 尚未完成，G4 未启动。
背景、目标与范围：用户要求执行 T07。读取 AGENTS、status、backlog、T05／T06 报告、阶段方案与最近日志，起点 6c0294e3b86c0519d4752b8b8ea8404962cc6c16，工作区干净。完成最终两模式全程、真实 GUI 人工确认与正常收尾，交接唯一配置、来源／方向、启动屏障、时间和后续缺口；无覆盖率门槛，不开展算法或参数寻优。
工作过程：新增独立 config/g3-acceptance.json、scripts/g3_acceptance、tests/g3_acceptance 和 Windows 运行／验收／收尾入口，复用已验收完整任务及逐格复算。新资格 g3-t01-check-20260919-172312-180 通过。只读预检 g3-t07-preflight-20260919-173023 复查 T01～T06 收据、当前产物与冻结输入、编译现有插件，并通过 12 项当前 GUI 确认绑定检查；其 status=qualified-only、simulationStarted=false、stageAccepted=false，不计作全程验收。
实施决定：两模式实际倍率均须保持 1；GUI 完成任务与统计后先保存不可变审查快照，逐格独立复算和同记录双环境重放通过后等待本轮确认。确认绑定本次编号、三个 PID、随机标识与审查摘要；等待期间持续检查进程、连接、暂停时刻与倍率，按已发布阶段方案单独计时，自动运行仍限制 2700 秒。旧 T05 的 GUI 确认及单次变速接受不复用于本轮。
验证进展：新 Python／PowerShell 语法通过；从仓库外 out/tmp 调用 tests/windows/g3-acceptance.tests.ps1，显式 Python 3.14.7 x64、StabilityRunId=g3-t06-test-20260919-164024-586、BaselineRunId=g3-t01-check-20260919-172312-180。正式运行 g3-t07-test-20260919-173116-474 正在无界面任务航段执行，尚未登记通过。
问题与处理：一次生成新 PowerShell 包装器的内联引号导致解析失败，未写文件，改用完整补丁后通过语法与预检；几次只读输出过长，后续限定读取范围。业务源码、T03～T06 实现、生成文件、正式包和旧收据保持不变。
下一步：完成两模式自动复验，保留当前 GUI 供用户审查；取得真实回复后执行绑定收尾并验证退出／端口，最后补齐报告、交接与状态。未完成前不提交或推送；通过后按持续授权普通归档并核对远程。

补充入口检查：g3-t07-entry-check-20260919-173945 从 out/tmp 验证缺解释器、重复预检编号、缺合格 T06，三项均明确拒绝；环境恢复、旧收据摘要保持、无业务应用启动通过。初步静态复查覆盖新增脚本语法、文档 UTF-8／链接、历史日志前缀和 T03～T06 实现／报告保护；正式两模式仍在执行，未提前登记阶段完成。

无界面全程已通过：本轮 headless/case-result.json passed，实际 744.030 仿真秒收尾，任务目标 14～73 全部执行，末端 740.310 秒到达，唯一正确身份 TaskComplete 与首个退出任务状态均为 740709 仿真毫秒。724／724 格，2794 条任务后状态独立复算、同记录两环境 Java 逐格重放和重复／清空重放一致；1405 条 Running SessionStatus 全为 1.0。AMASE／UxAS 均退出 0、无强制终止、端口释放。GUI 已顺序启动，本轮确认与正常收尾仍待完成。

GUI 审查观察点：17:56:40 生成本轮 gui-ready.json，result.status=awaiting-gui。GUI 同样完成任务、真实末段与唯一正确 TaskComplete（740709 仿真毫秒），724／724 格及 2794 条任务后状态独立复算通过；同记录两环境、重复／清空重放一致，实际全程 1 倍，暂停时刻 744019 仿真毫秒。不可变 review SHA-256 为 C3F2F29015AB54A06C6AAC6D90121BA4363E77BE58279D189B83DD9D441CD3F1；控制器／AMASE／UxAS PID 为 5928／18196／13784。依 T07 明确任务条件已向用户提出本次确认，当前未收到回复，不写入确认请求，不关闭保留窗口；G3 尚未完成，最终 acceptance／handoff 尚未生成。取得本轮回复后按阶段报告的 finish-g3-acceptance.ps1 绑定真实文字收尾。

本轮人工确认与完成：用户回复“本轮 GUI 正常，确认并正常退出”。17:58:52 通过 finish-g3-acceptance.ps1 绑定该原话、本次编号／PID／nonce／审查摘要，请求正常关闭；未复用 T05 确认或自动生成确认。收尾入口及原验收入口均退出 0；result／entry-result／acceptance／handoff 均 passed，stageAccepted=true。GUI 最终 744.020 仿真秒，与审查时的完成消息、覆盖统计和全部快照摘要一致；两模式各 1405 条 Running 状态全为 1.0，两个应用均退出 0、无强制终止、端口释放。自动墙钟 1540.591 秒，人工等待 131.658 秒独立记录，未超自动预算。

最终验证：19 项覆盖小样本、6 项末端夹具、19 项本轮真实记录完成／统计正反例、12 项当前确认绑定检查全部通过；有效零覆盖可通过，未设置最低覆盖率。独立 out/tmp/g3-t07-final-evidence.json 通过 423 项摘要与绑定核对，确认当前 GUI 原话、两个案例、运行实现、四份配置、正式包／指针、43 项冻结输入及 T01～T06 收据保持。所有本轮应用／控制器 PID 已退出，5555／5556／5557／9400／9500／19400／19500／9999 八端口无残留监听；环境、工作目录、编码和持久 PATH 恢复通过。

交付与边界：G3-T07 已完成，G3 阶段通过；新交付 docs/g3-stage-validation.md、docs/g3-g4-handoff.md 及独立阶段验收／收尾入口。唯一连接配置仍为 config/g3-startup.json，执行／完成／阶段配置各有独立职责，合格 handoff 绑定四份摘要和同批产物。T02～T04 明确为旧 AMASE 的历史记录，本轮实际全程消费 T05 修复后正式版本，没有改写历史。G4 待细化、未实施；观察工具不是网关，在线重连／快照归 G4，重置分段归 G6，零地形、Unicode 字段与第二机器部署边界保持。同步 AGENTS、status、backlog、阶段方案及总体计划；按持续授权完成最终范围复核后提交、普通推送，真实归档结果随后追加。

提交前复核：本任务 16 个文件、377 处本地文档链接、UTF-8、Python／PowerShell 语法、git diff --check、历史日志前缀及旧实现／报告保护通过。out 原始证据与辅助脚本受忽略规则保护，未纳入生成物、依赖缓存或本机用户绝对路径。远程 main 与本地起点仍为 6c0294e3b86c0519d4752b8b8ea8404962cc6c16；只暂存这 16 个任务文件，随后普通提交／推送并核对远程。

归档结果：主提交 c12590cfad13fe4993715b39832096b33b5bd997（test: complete G3 stage acceptance and G4 handoff）包含已核对的本任务 16 文件；git push origin main 退出 0。2026-09-19T18:02:30.6556440+08:00 核对本地 HEAD 与远程 main 一致、工作区干净，收据 out/tmp/g3-t07-push.json。随后仅补齐本段与 status 的真实归档说明，以独立文档提交收尾；正式包、阶段实现、功能配置和原始证据保持不变。G3-T07 及 G3 已完成；G4 待细化、尚未启动。

## WL-20260919-011｜G4 方案细化与九张任务卡

时间／时区：2026-09-19，Asia/Shanghai。
关联任务与状态：G4 规划交付；已形成文档，最终文档检查及归档结果见下文。T01 可执行，实现尚未启动。
背景、目标与范围：用户先要求“执行 G4，先细化”，在规划对话中明确观察链路恢复、先两实体后 20 实体、每架独立任务、功能与稳定性优先，进一步选定 6 点／8 线／6 矩形区域和固定一架一任务；随后明确要求实施所确认方案。工作区起点 d07207e，干净；本项先落实阶段方案和任务卡，后续逐卡执行。
工作过程：阅读 AGENTS、当前状态、G3 阶段／交接、最近日志、协议解析和运行编排；源码确认 TCP 无历史重发、CMASI 单次请求最多 16 实体、点／区域需追加主桥导出白名单。只读核对历史 UxAS 数据库 6834 条连续记录和关键类型；不把历史完整内容写成在线恢复通过。Python 3.14.7 x64 实际可用，Web 依赖尚未安装；官方 FastAPI 文档确认能力，当前项目组合仍待 T01。几次检索路径不存在／输出截断后改用实际路径和局部读取，无文件污染。
成果与修改文件：新增 docs/g4-message-gateway-plan.md；docs/backlog.md 增加九卡目标、步骤、验收、证据、回退和停止条件；同步 status、AGENTS、G3 方案当前状态和总体计划。既有 G3 报告、正式包、业务源码和历史日志未改写。
验证：当前工作目录为仓库根目录；文档 UTF-8、相对链接、git diff --check 及范围／历史保护检查将在本次文档归档前执行，实际结果在下文追加。未安装依赖或启动仿真，不宣称 G4 实现完成。
重要决定与影响：后端与主链路故障明确失败／整组重启；网关只读、独立进程；实时 TCP 配合本次只读日志语义补齐，保留原始字节缺口；20 实体逐项单实体请求后并行执行，不修改 16 实体模型约束。两模式各 1800 仿真秒、实际 1 倍、三个客户端，自动预算每模式 2700 秒，人工等待独立计时。
遗留事项与下一步：完成本项检查和普通提交／推送后进入 T01。点／区域任务、在线恢复与 20 实体均待真实验收；不设覆盖率、观察时长或延迟硬门槛，不开展效果寻优。

文档验证完成：7 个任务文件、372 处本地链接、UTF-8、git diff --check 和历史日志前缀保护通过，收据 out/tmp/g4-doc-check.json。没有运行软件测试或仿真；文档规划交付已完成。按持续授权只暂存上述任务文档并普通提交／推送，归档实际结果随下一任务日志记录。

文档归档结果：提交 908fdfdd17d93c5a2f774bf120f1980e65fbe5f3（docs: define G4 gateway and mixed-mission implementation plan）已普通推送 origin/main，退出 0；git ls-remote 与本地 HEAD 一致，工作区干净。

## WL-20260919-012｜G4-T01 输入、环境与契约

时间／时区：2026-09-19，Asia/Shanghai；22:44 开始重新核查正式来源。
关联任务与状态：G4-T01 进行中，T02～T09 待前置。
背景、目标与范围：按已确认九卡顺序落实独立 Web 环境和 v1 契约；不以依赖冒烟代替真实网关验收。
工作过程：新资格 g3-t01-check-20260919-224451-856 通过，绑定 G3-T07 合格 handoff；pip 官方索引只解析 Windows x64／CPython 3.14 可用 wheel，选择 FastAPI 0.141.1、Uvicorn 0.53.0、Pydantic 2.13.5、websockets 17.1 及完整 14 项依赖，固定每项版本、官方 URL 和 SHA-256。新增独立环境入口、环境／来源校验器、HTTP／WebSocket 冒烟、G4 基线与依赖锁、网关配置和浏览器契约。当前隔离环境安装正在执行。
成果与修改文件：config/g4-*、scripts/g4_environment、scripts/windows/setup-g4.ps1、tests/g4_environment、docs/g4-browser-contract.md，原业务源码、生成库和历史收据保持。
验证：仓库根目录运行现有 check-g3-baseline.ps1，退出 0；实际 Java 11.0.32.1+1／Ant 1.10.18 和正式来源通过。新入口 setup-g4.ps1 的完整安装／冒烟结果待追加；不预先登记通过。
问题与处理：读取不存在的 g3-acceptance-common.ps1 后确认实际入口直接复用通用工具脚本，无新增依赖；来源错误必须拒绝，不修改历史哈希。
重要决定与影响：Web 环境位于忽略的 .tools/g4/environments，成功冒烟后才切换当前指针；只读复验按安装文件摘要核查。协议 v1 明确运行／流身份、同连接快照边界、十进制字符串 ID／时间、权威来源和日志语义补齐。
遗留事项与下一步：完成安装、冒烟、拒绝及路径检查；通过后独立归档 T01，再进入真实协议接入 T02。

完成结果：安装运行 g4-t01-20260919-224933-026 的 result／entry-result passed；14 项 wheel 哈希校验、离线安装、pip check、Pydantic v2、HTTP、WebSocket 双向 JSON、超大整数字符串、正常退出与端口释放均通过。g4-t01-checks-20260919-225116 验证从中文空格且不同工作目录复验、重复编号拒绝并保留原收据、缺解释器拒绝；错误 handoff 与依赖摘要拒绝。补齐测试入口来源登记后，g4-t01-20260919-225218-157 的 VerifyOnly 全部通过；旧初次安装收据保持。环境／目录未改变，未启动仿真。专题报告 docs/g4-environment-validation.md；T01 已完成，T02 可执行。

问题与解决办法：本轮安装和运行未出现功能失败；pip 解析／下载需要等待，保留每个文件官方 URL 和 SHA-256，不用重新计算下载内容替代信任值。测试专用随机端口是显式绑定的隔离样本，生产端口冲突规则保持。将新添加的入口检查源文件加入最终来源记录后重新 VerifyOnly，而不改写初次收据。接下来完成语法／文档及范围复核，再普通归档；实际提交结果随后记录。

归档结果：Python AST／PowerShell 语法、8 份文档／370 处本地链接、UTF-8、git diff --check、旧日志前缀保护通过。T01 的 17 个任务文件提交 ca210329e82820e9146d62767562ef32258ef80c（feat: qualify G4 Python environment and browser contract），普通推送 origin/main 退出 0，本地／远程一致、工作区干净；out 与 .tools 保持忽略。

## WL-20260919-013｜G4-T02 协议核心与真实接入

时间／时区：2026-09-19，Asia/Shanghai；23:01 启动首次实际两模式验证。
关联任务与状态：G4-T02 进行中；初次两模式 passed，补齐队列／网络故障测试后的最终复验进行中。
背景、目标与范围：按方案新建框架无关的严格协议核心和有界只读接收器；复用 G3 启动主任务，独立接收并对照，不改变业务源码、原协议验收、生成文件或正式包。
工作过程：新增 src/sim_bridge 的 codec／transport；用七 MDM 建立字节预检，先限制数组／嵌套与数值，再调用同批生成解码器，避免直接信任生成工厂。接收与消费使用 64×64KiB 有界队列，诊断最近记录限定 64 条；原字节／索引写盘，满队列关闭本次观察连接并报告失败。保留每连接尾部和偏移，不发送业务帧。
验证：历史 G3-T07 的 AMASE 5451 帧、观察口 6821 帧全部通过新解析器；状态／命令全部切分位置、粘包、25 项坏帧／未知类型／非法坐标／实体及大整数检查通过。首次真实运行 g4-t02-test-20260919-230151-562 在 Headless／Gui 均 passed，来源、真实动态状态、规划／命令与独立 G3 观察记录一致，正常退出。另有本机真实 socket 分包、半帧 EOF、坏校验及慢消费者队列上限检查通过；slow-consumer 保留 Full 故障及残包，不计正常接收。加入这些检查并去除不适用的 KeepGui 参数后启动最终复验，结果待追加。
问题与解决办法：首次历史重放暴露字节预检漏列 MDM byte 基元，按生成器 PythonMethods 的实际 B 编码补齐 byte，并按 c 编码支持 char 后全量重放通过；未修改生成代码。少数 Windows rg 通配符按字面路径报错，改用目录加 -g 或精确路径。错误记录保留在本次交互，离线重放退出 1 不冒记成功。
成果与修改文件：src/sim_bridge、scripts/g4_protocol、tests/g4_protocol、Windows 运行／测试入口；报告与当前状态在最终通过后更新。
重要决定与遗留：框架无关核心不引用验收脚本；只在独立测试／编排中复用 G3。业务字符串仍按既定 ASCII 契约拒绝非 ASCII，七模型类型预检不等于全部业务类型实际联调。T02 不证明任务完成、浏览器接口或在线重连，分别归后续任务。

最终结果：g4-t02-test-20260919-230412-248 的 result／entry-result passed。Headless／Gui 各接收 AMASE 81 帧、UxAS 88 帧，两路各 41 条真实状态，合法运行尾部均为 0；队列实测峰值 Headless 为 2、Gui 为 1，独立观察摘要逐帧匹配。两模式后端均正常退出、端口释放、接收线程结束，业务发送帧数 0。整组含历史 12272 帧、全切分、25 项反例及四组真实局部 socket 检查；故障样本 error／残包保持。T02 已完成，报告 docs/g4-protocol-validation.md，T03 可执行。另以只读脚本对历史日志 XML 重新编码核对 11 条 MissionCommand、1 条 AutomationResponse、TaskActive／TaskComplete 各 1 条，全部与原观察字节摘要一致；这是后续日志适配的可行性依据，不计在线恢复通过。

归档结果：T02 的 15 个任务文件提交 22cacc6f321f6c5039832029c616f49eee296e0c，普通推送 origin/main 成功并核对本地／远程一致；工作区干净，所有所属进程和端口已回收。

## WL-20260919-014｜G4-T03 状态、持久记录与日志并发修复

时间／时区：2026-09-19，Asia/Shanghai；23:11 后继续状态实现，23:21 开始首次在线验收。
关联任务与状态：G4-T03 进行中；历史与离线检查通过，在线发现日志丢失，修复及正式重建正在执行，未登记完成。
背景、目标与范围：建立独立状态归约和当前运行日志消费，支持实体、点／线／区域任务、规划航线、当前命令、区域、时钟及删除；关键事件以分片／行号标识，原始网络证据继续保留。
工作过程：新增 sim_bridge/journal.py、state.py、T03 编排和独立检查。日志以 mode=ro、query_only 及 128 行批次读取，每批读取完立即关闭连接，再解析／持久化；严格验证 XML 必填字段、枚举及生成后字节布局，不默认接受坏字段。规范化事件用独立 SQLite 持久化并核对重复内容，状态只保存有限对象和墓碑，不缓存全历史。
验证：6834 条真实 G3 历史日志完整重建，独立源重读和规范化重放得到相同 SHA-256 bd03238d841deaa08ea55c4f0a7a8bf98b583259ad46478dbce861eb75bf2234；29 项身份、删除、字段、分片、乱序及损坏检查通过。三类任务仅为生成消息夹具，不冒称点／区域真实执行。原 WaterwaySearch 两实体中只有 400 获得任务航线，已按原事实检查；空可选对象被 C++ XML 省略，按 MDM Optional 允许，缺必填仍拒绝。
在线失败：g4-t03-test-20260919-232115-864 的 Headless 真实运行正常退出，但 Controller import/loop 检查失败。日志 stdout 明确出现两次 database is locked，丢失 SessionStatus 和 LineSearchTask；任务本身实际产生规划，数据库却缺任务定义，因此不能宣称完整恢复。旧 SQLite 默认 rollback journal、写端无等待，短只读事务也能触发插入／提交失败；insertValuesIntoTable 的异常分支还错误返回成功。
最小复现与修复：tests/g4_state/logging_checks.py 在隔离库持有只读事务，DELETE 模式确定重现写失败，WAL 模式写入成功且旧读事务快照稳定，读端写入被拒绝；Python SQLite 3.50.4，仅作为锁行为复现，实际 C++ 修复仍需正式验收。DatabaseLoggerHelper 改为唯一写端开启并核对 WAL、busy_timeout=5000，插入异常返回 false；网关保持短只读事务，不修改后端数据库配置。依据 SQLite 官方 WAL／busy_timeout 文档，正式运行限定本机存储与单写端。未调整任务、规划、飞行或覆盖参数。
重建进展：首次 g2-t05-build-20260919-232403-798 因固定旧 SQLiteCpp 无 getString() 构建失败，退出 1 且旧包保留；按本机已锁定 Column.h 的 getText() 修正后重新干净构建。旧 G3 handoff 不改写；G4 增加受收据约束的 UxAS 来源修订，待候选／HelloWorld／发布矩阵通过后记录实际新包，再重跑 T03。
其他纠正：生成工厂 unpackFromXMLNode 返回列表，改为显式创建对象后解析；测试的 SQLite with 只提交而不关闭，导致 Windows 文件重命名被拒，使用 closing 后解决。前述失败保留，未改写成通过。
遗留事项与下一步：完成新 UxAS 构建、双路径候选验收、HelloWorld、正式发布，重新核查来源及 T01 环境；T03 两模式在线读取与原始消息逐项对照通过后更新报告／状态并独立归档。浏览器、自动重连及完整任务仍归 T04～T09。

正式重建与修订完成：g2-t05-build-20260919-232557-986 候选、g2-t05-test-20260919-232811-379 双路径验收、g2-t06-test-20260919-233042-631 HelloWorld、g2-t07-test-20260919-233237-819 发布矩阵和 g2-t07-publish-20260919-233516-550 正式发布均通过。重复正常启停、中文路径、真实超时和隔离来源／发布回滚反例通过。G4 来源修订登记前后源码摘要、新旧包身份和 12 份合格收据；构建 result 的 candidate 状态按实际保留，只把验收／入口 passed 收据纳入合格列表，未把 candidate 改写为 passed。旧 G3 handoff、43 项冻结输入、AMASE、LMCP、依赖及构建图保持。

最终验证：新输入资格 g3-t01-check-20260919-233710-372 与 g4-t01-checks-20260919-233824 环境／中文工作目录／入口复验通过。g4-t03-test-20260919-233834-574 的 result、entry-result 和两模式案例全部 passed；32 项状态／记录检查、7 项新来源修订拒绝、两组日志锁复现及 T02 回归通过。Headless／Gui 各在线读取 54 批，最终各 101 条已提交消息，实际日志模式均 wal；各 7 个关键语义摘要与真实 TCP 对应，日志无插入错误，在线快照与该次持久记录重建一致。两模式四个后端 PID 全部退出 0、无强制终止，七个所属监听端口无残留。历史 6834 条完整任务重建与本轮短程规划取样分别记录，未冒称本轮全程完成或恢复已通过。

交付与决定：新增 docs/g4-state-validation.md，扩展 v1 契约字段与生命周期说明；AGENTS、status、backlog、总体计划和 G3 方案当前状态同步为 T01～T03 已完成、T04 可执行。状态采用已提交日志顺序，TCP 原字节仍独立保存，不使用不安全的 immutable 读取或数据库文件拷贝绕过并发锁。后续 T04 建立独立只读 Web 服务，T05 验证整进程／观察链路恢复；GUI 人工确认仍由 T09 处理。下一步完成本卡最终范围／语法／文档复核后普通提交、推送并核对远程。

归档结果：T03 的 20 个文件、8 份文档／373 处本地链接、Python／PowerShell 语法、30 项验收来源前后摘要、UTF-8、git diff --check 与历史工作日志前缀保护通过。提交 71c590650c6fe65ac3b94e02249bbee4dc366e83（feat: qualify G4 state rebuild and concurrent UxAS journaling）普通推送 origin/main 成功，已核对本地／远程一致，工作区干净。

## WL-20260919-015｜G4-T04 浏览器接口与诊断页面

时间／时区：2026-09-19，Asia/Shanghai；23:43 后进入 T04，23:52 开始首轮两模式接口验收。
关联任务与状态：G4-T04 进行中；首轮真实接口通过，补充本机浏览器渲染复验中，未登记完成。
背景、目标与范围：建立独立只读网关进程，提供 health、snapshot、stream 和无外部资源依赖的中文诊断页面；任务注入和后端关闭仍由独立运行编排负责。
工作过程：新增 sim_bridge/publication.py、gateway.py、windows.py，以及 apps/gis_gateway。快照和客户端登记共用状态锁，保证同连接先快照后边界之后的增量；发布队列同时限制条数和字节，慢客户端关闭后必须重新同步，其他客户端和后端不等待。Win32 只读查询 PID／创建时间，阻止运行身份混用；网关不持有后端停止接口。规范化更新按已提交日志顺序发布，两路真实 TCP 独立记录并用于连接与新鲜度判断。
验证进展：g4-t04-test-20260919-235220-447 的 Headless／Gui 均 passed；三个真实 WebSocket 客户端在同一目标序号的快照＋增量状态严格相同，晚加入、刷新、HTTP POST 405 和 WebSocket 控制消息 1008 拒绝通过；健康／实体／任务均来自真实后端，正常退出和端口回收通过。发布器夹具验证快照生成期间并发事件不丢不重，慢消费者达上限后隔离，超大序号字符串保持；夹具不冒称真实网络。新增字节上限检查和 Edge 真浏览器渲染、刷新、截图及正常退出后进行最终复验。
成果与修改文件：独立核心发布／工作线程、FastAPI 服务及页面、scripts/g4_web、tests/g4_web 和 Windows 入口。没有更改已发布 UxAS、AMASE、MDM 或场景；测试使用已锁定 Web 环境，本机 Edge 153.0.4234.32 使用独立用户目录和显式测试调试端口 9222，不修改用户浏览器配置。
问题与边界：目前未发现首轮接口功能失败。Windows PID 时间查询依据微软 GetProcessTimes／OpenProcess 文档，仅请求 PROCESS_QUERY_LIMITED_INFORMATION；可用性以本机实测为准。网关观察连接断开会明确降级，自动恢复尚归 T05；这不是 Cesium，也不包含控制或历史时间轴。
遗留事项与下一步：完成含真实浏览器的最终两模式验收，保存截图并检查诊断页显示；补齐专题报告和状态后独立归档，再进入 T05 自动恢复矩阵。

浏览器失败与修复：g4-t04-test-20260919-235535-837 中真实页面两实体／任务和 JavaScript 均正确，但 Edge 在 15 秒内未退出，所属浏览器被清理，该案例保持 failed；三个业务进程正常退出。增加隔离浏览器的 disable-background-mode／disable-extensions，关闭等待上限调整为 30 秒；独立空白页最小关闭验证约 1.73 秒退出 0。刷新验收增加 performance.timeOrigin 必须变化，避免重载刚发出时读取旧 DOM。g4-t04-test-20260919-235825-978 两模式及真实浏览器正常退出通过。

最终结果（2026-09-20，Asia/Shanghai）：截图审查发现任务表没有展示实际已知分配，已使用 assignments／active／complete 的对应实体，列名明确“分配／参与实体”，三类类型显示中文。最终 g4-t04-test-20260920-000102-608 的 result／entry-result／两模式案例均 passed；42 项来源未变、8 项发布边界／条数／字节队列检查及 T03／T02 回归通过。两模式各三个真实客户端从序号 74／74／77 重建到同一序号 77，状态严格一致；刷新、HTTP 405／WS 1008 拒绝通过。网关 Headless／Gui 分别持久记录 128／116 条、发布 93／81 个增量，实测队列峰值 1 条／9237 或 9232 字节。真实短程未触发慢客户端超限，超限为独立夹具证据，不冒称 20 实体压力已通过。

真实浏览器与收尾：Edge 153.0.4234.32 两模式各显示两实体、任务 1000 和分配实体 400，刷新 timeOrigin 改变、序号保持同步，JavaScript 异常 0；已查看最终 GUI 截图核对字段。两模式 AMASE／UxAS／网关及测试 Edge 均正常退出，无强制终止，8000／9222 及七个后端监听端口无残留。网关业务发送帧始终为 0；浏览器测试的 9222 和隔离 profile 仅用于验收，不属于产品接口。

交付与边界：docs/g4-web-validation.md、apps/gis_gateway/README.md 和 v1 契约同步；当前状态、任务卡、总体计划、G3 方案当前摘要及 AGENTS 更新为 T01～T04 已完成、T05 可执行，核对日期更新至 2026-09-20。T04 是短程真实接口验收，不等于全程任务、完整恢复或 Cesium；下一卡实现断线后新流／快照、关键事件补齐、网关重启及后端故障拒绝。先完成本卡最终文档、来源和范围复核，再按持续授权普通归档。

归档结果：T04 提交 f3b4d0896fe31ecb42da0cef9acbb37d3205f3ab 已普通推送 origin/main，已核对远程与本地一致；随后进入 T05。

## WL-20260920-001｜G4-T05 观察连接恢复与故障拒绝

时间／时区：2026-09-20，Asia/Shanghai，00:07 后开始。
关联任务与状态：G4-T05 进行中；短程 GUI 恢复通过，完整矩阵复验中，尚未登记完成。
背景、目标与范围：恢复浏览器、两路观察 TCP 和独立网关进程；后端退出或主任务 TCP 故障必须失败，使用新运行重新启动。关键事件由当前运行已提交日志补齐，保留原始 TCP 缺口。
工作过程：增加 Win32 TCP 所属 PID／连接元组只读核查，运行清单固定主链路身份及日志文件身份／首行摘要。持久事件库独立于每个网关实例，重读相同分片／行号时同时核对摘要和内容；解析器每次重新创建，恢复期间关停旧发布流，核查到提交边界后以新 stream_id 快照接续。HTTP 端口先独占绑定，避免端口冲突时启动第二个观察写入者。加入新鲜度、恢复进度、日志写入错误拒绝，所有原收包和残包保留。
成果与修改文件：src/sim_bridge 的 gateway、journal、publication、windows，apps/gis_gateway/server.py，独立 scripts/g4_recovery／tests/g4_recovery 和 Windows 入口；报告 docs/g4-recovery-validation.md。未更改已发布业务二进制、MDM、算法或飞行参数。
已验证：g4-t04-run-20260920-001827-014 无界面真实接口及 Edge 页面回归通过。g4-t05-run-20260920-002610-817 GUI 短程恢复通过，含初始配置／规划缺口、两路分别和同时断开、半帧、网关重启、删除补齐及删除后再重启；恢复快照与相同序号的日志归约一致，各所属进程正常退出，端口释放。此批没有真实任务完成，不冒称完整执行通过。
失败与修复：g4-t05-run-20260920-002510-774 在隔离日志夹具中遇到 WinError 32，真实后端尚未启动；再次确认 Python SQLite with 只处理事务，不关闭连接，测试显式 closing 后通过。旧失败收据保留。
当前验证：完整矩阵新增三个持续 WebSocket 客户端，分别正常消费、定时刷新和慢消费；各自要求新连接先快照、同流序号连续，跨断线和网关重启重新同步。原 WaterwaySearch 无界面全程将在 700 仿真秒后断开观察连接，跨越真实 TaskComplete 再补齐，并复用 G3 内部导航、实际航段和覆盖独立对照；随后验证主链路半帧故障、两类后端退出以及新整组运行。
重要决定与边界：故障代理为独立测试设施，不加入产品控制接口；删除验收仅扩展运行副本的 Remove* 导出白名单。网关不停止或重发后端业务任务；日志损坏、身份改变和主链路失败不得作为可恢复观察故障。人工 GUI 确认仍待 T09。
遗留事项与下一步：完成 T05 完整矩阵及收尾，核对来源未变、补齐报告／状态后独立归档，再进入 T06 原例两模式全程验收。

完整首轮与定位：g4-t05-test-20260920-002852-273 的真实任务在 740709 仿真毫秒完成，网关跨 700 秒后的观察缺口补齐同身份完成事件，三个持续客户端通过，所有所属进程正常退出；但旧 G3 审计报 internal-waypoint-regression-or-skip，整轮保持 failed。原始导航 444／445／446 显示 94.54 秒已指向 13，新分段命令 68 在 94.59 秒按最近上报的 FirstWaypoint 暂设 12，94.61 秒紧接真实 step 仍指向 13；12／13 无任务和动作，网络状态没有命令 68／航点 12 的回退。这是命令应用与实际执行取证混在一起，未发现任务航段实际回退。

取证修正：新增 scripts/g4_recovery/navigation.py 生成独立执行视图，原始记录保持。只有同实体新分段、完全相同且无任务／动作的相邻重叠点、即时下一 step 保持原目标、相邻证据齐全及网络状态无回退时，才将该 command-applied 分类为 receipt-only-overlap；不删除命令收到证据，也不豁免任何真实 step 回退或任务／动作重入。九项实际样本与变化反例通过，原完整记录经此明确规则独立复算通过，旧 failed 不改写。首次离线脚本直接比较原 XML 缺省字段与生成对象导致 original-request-differs；改为按原入口使用统一生成库读取后规范化比较，完整原记录复算通过，保留 sourceHashes。

恢复开销修正：首轮在观察口持续拒绝期间反复重扫全日志，6834 条来源累计解码超过十万次。现仅增量消费已提交日志，检测到有效接收（暂停时为稳定连接）后才重建；每 128 条以及开放快照前复查后端／主链路身份。该修正不改变任务、算法和仿真时间。更新后另启完整新运行，尚未登记最终通过。


最终结果：g4-t05-test-20260920-004801-605 的 result／entry-result、六场景均 passed，52 项输入摘要未变，九项恢复拒绝和九项导航取证检查通过。无界面实际 1 倍全程于 740709 仿真毫秒完成；700 秒后的观察缺口跨过真实 TaskComplete，网关原始 TCP 中该事件 0 条、持久语义事件 1 条，恢复身份／时间一致。真实执行、末端到达、20 米栅格 724／724 及独立统计复算通过，无最低覆盖率要求。GUI 恢复、新整组 GUI 运行均正常；主链路半帧、UxAS 退出和 AMASE 退出全部触发网关 degraded／快照 503，故障网关保留 failed、正常退出 1，外层只记录拒绝正确。

持续客户端与收尾：三个客户端分别取得 8／71／11 快照、4967／4788／4319 增量，各 8 个流身份，序号连续。真实队列峰值 39 条／159530 字节，未触发生产队列上限；不把夹具超限写作实测网络超限。三个正常恢复案例均保留半帧证据，删除后再重启无对象复活。全部所属进程正常退出、无强制结束，11 个声明监听端口无残留。重试改进使完整案例第二个网关实例累计解码由旧失败轮 100222 次降为 20512 次；同一日志行因重建重复解码但不会重复应用到同一发布流或重复持久化。

交付与下一步：恢复报告、v1 恢复字段、网关 README、AGENTS、status、backlog、总体计划及 G3 当前摘要同步为 T01～T05 已完成、T06 可执行。保留旧失败与独立离线复算；业务源码／正式包不变。先完成本卡最终范围／来源／文档复核与普通归档，随后实现 T06 原例两模式全程；G4 阶段 GUI 人工确认仍待 T09。

归档结果：T05 的 23 个文件、9 份文档／384 个本地链接、52 项验收来源、语法、UTF-8、git diff --check 和历史日志前缀保护通过。提交 a206fe56762ec5e7fe2159c84ff7d61577aa1102（feat: qualify G4 observer recovery and backend failure isolation）普通推送 origin/main 成功，本地／远程一致，进入 T06 前工作区干净。

## WL-20260920-002｜G4-T06 原两实体网关完整验收

时间／时区：2026-09-20，Asia/Shanghai，01:07 后开始；01:09 启动完整两模式矩阵。
关联任务与状态：G4-T06 进行中，先无界面、后 GUI 全程；尚未登记完成。
背景、目标与范围：在原 WaterwaySearch 全任务上验证网关、三个持续客户端、观察恢复及真实浏览器完成状态。保留 400／500、任务 1000、90 点完整水道、实际 1 倍、785 秒原时长和 20 米栅格，无最低覆盖率条件。本卡不删除实体或任务。
工作过程与成果：新增 config/g4-completion.json、scripts/g4_completion/runtime.py 和 Windows 运行／验收入口。重新复核正式产物、同批消息库、T05 合格收据及 52 项来源；复用独立网关进程宿主、故障代理、九项反例已验收的导航取证视图及 G3 全任务／统计探针。两模式分别验证启动屏障、单次任务请求、三持续客户端、半帧观察中断、整网关重启、跨完成事件观察缺口和末端 Edge 诊断页。
当前验证：g4-t06-test-20260920-010936-703 已进入 Headless 真任务；工作目录为仓库根目录，命令为 tests/windows/g4-completion.tests.ps1，PythonExecutable 指向已验证的 3.14.7 x64，BaselineRunId 为 g3-t01-check-20260919-233710-372。本次还未结束，不预写退出码或 GUI 通过。
问题与边界：当前未发现新的功能问题。未改业务源码、正式包、MDM 或算法参数；G4 当前阶段人工 GUI 确认仍待 T09，自动化 Edge 渲染不替代人工确认。过程与证据入口见 docs/g4-completion-validation.md。
遗留事项与下一步：完成两模式真实全程及统计／网关状态对照，核对所有所属进程退出与端口释放；补齐报告和状态后独立归档，再进入 T07 三类任务资格。

最终验证：01:35 两模式矩阵 g4-t06-test-20260920-010936-703 通过，result／entry-result 与 Headless／Gui 案例均 passed，退出 0；57 项实现来源、43 项冻结输入未变。实际导航、任务航段、末端到达及可靠完成通过，两模式完成身份为任务 1000／实体 400／740709 ms。覆盖均为 20 米 724／724，Headless 2794、Gui 2792 条原始状态分别独立复算一致，Java 分析在两模式重放一致；采样数差异来自收尾时间，各轮分别核验，不设置覆盖门槛。

独立审计：主运行结束后增加 scripts/g4_completion/audit.py，只读核对父收据、57 项来源、两模式 495／496 项证据摘要、全部 Running SessionStatus 以及跨缺口的完成事件。运行 g4-t06-audit-20260920-013549-190195 passed、退出 0；两模式各 1405 条实际倍率全部为 1，倍率 2 和无运行时钟反例正确拒绝。网关原始 TaskComplete 均 0 条，持久事件均 1 条，与原任务身份和完成时间一致。审计自己的源码摘要及父收据摘要独立记录，没有将后加脚本补写进旧输入清单；后续消费同时核对主运行及独立审计。

客户端与收尾：Headless 三客户端正常／刷新／慢消费增量 5011／4833／4449，Gui 为 5014／4836／4448；各经历四个流身份，每模式最终三客户端状态摘要相同。两模式 Edge 153.0.4234.32 真实页面显示、刷新和正常关闭通过，JavaScript 异常 0；已查看两张截图核对实体、任务分配、时间和倍率。每模式保留 400／500 和完成任务，任务／请求只注入各一次，网关控制帧为 0。全部所属进程正常退出，无强制结束，11 个声明监听端口无残留。

交付与下一步：T06 完成报告、AGENTS、status、backlog、总体计划和 G3 当前摘要同步为 T01～T06 已完成、T07 可执行；正式业务包不变。T06 本轮没有新增业务失败。原始字节缺口保留，只声明语义补齐；T09 当前人工 GUI 确认仍待执行。完成最终范围／语法／链接核查并按持续授权普通归档，随后开展点、线、矩形区域独立及三实体混合资格。

归档结果：T06 的 12 个文件、7 份文档／381 个本地链接、Python／PowerShell 语法及历史日志前缀核查通过；提交 e8c6421e972fa5f7848ec86e8136514694eab69f 普通推送 origin/main 成功，已核对远程一致、工作区干净。

## WL-20260920-003｜G4-T07 三类任务与混合资格

时间／时区：2026-09-20，Asia/Shanghai，01:39 后。
关联任务与状态：G4-T07 进行中，尚未通过；T06 两模式与独立审计已完成归档。
背景、目标与范围：先三类单任务、再三实体各一任务，按真实规划、执行、完成及统计含义验收，为二十实体固定映射提供资格。原例和历史收据保持；必要统计／兼容修复先复现，再按既有规则重建、自动验证和发布。
源码发现：点与区域仍使用会拒绝 AllAny 的旧波段判断；点报告 TimeSeenSec 直接输出内部毫秒；点观察累计逻辑将零 DwellTime 的连续观察反复归零。区域经度网格使用乘 cos、首行纬度位于边界外也需最小样本核对。这些是待实测的统计正确性问题，不把静态阅读登记为已通过。
下一步：建立独立确定性相机／几何样本，在当前正式 AMASE 上保留失败证据，确定最小修复；随后构建候选并推进真实小场景。涉及 AMASE 正式发布时仍需本轮真实 GUI 确认，自动检查不替代人工观察。

复现与修复：独立 SearchChecks 在旧正式包上产生 g4-t07-statistics-20260920-014137-694730 failed，24 项中 17 项失败；750 ms 被报告为 750 秒、手算 12 网格得到 16、AllAny 区域为 0，点零驻留和倒序采样存在零／负时间。修复三个统计类，点按连续合格采样区间并集累计（间隔上限为原分析周期两倍 1000 ms，排除缺口、重复及跨相机重叠）；报告转换为秒。区域修正经度换算、向内的中心采样及波段条件，不再静默用 50 格限制改变分辨率，超过 100 万格明确拒绝。没有修改任务规划、飞行／传感器参数或 LMCP。

候选与自动验收：构建 g1-t04-build-20260920-014300-423420 passed；24 项原样本复验 g4-t07-statistics-20260920-014316-704501 passed，加入正驻留边界／相机部分重叠／无效网格后 31 项 g4-t07-statistics-20260920-014531-255663 passed。GUI g1-t04-run-20260920-014335-273348 运行后暂停；g1-t04-automatic-20260920-014412-428880 的 11 组自动检查全部通过、退出 0。本轮人工核查已通过异步问题请求，确认尚未收到，正式发布与正常关闭保持待办；旧正式产物和历史结果未改写。

场景准备：新增 scripts/g4_mixed/scene.py 和 tests/g4_mixed/scene_checks.py，生成三个单实体场景及三实体混合副本；固定一架一任务、原 90 点水道完整平移、矩形 1000×500 米、单实体请求及 RedoAllTasks=false。点／区域使用现有示例结构，搜索要求固定继承原线任务，不据覆盖结果调参。g4-t07-scenes-20260920-014948-950180 四组静态生成检查及 12 条严格编解码通过；未将此写成真实规划／执行通过。来源与观察时间／区域网格语义记录在 docs/g4-mixed-validation.md，继续准备通用执行与统计关联。

原线任务回归：新增只读 line_regression.py，g4-t07-line-replay-20260920-015231-764519 通过；补齐原参考 pixels.csv 的父收据摘要核查后，g4-t07-line-replay-20260920-015949-436471 再次通过。两个 T06 原始分析记录分别在候选 GUI／无界面分析模式重放，报告、增量／重置后分析报告和逐格结果完全一致；不改旧证据，不把分析重放当作新飞行或 G6 仿真重置通过。

真实规划诊断：独立 planning.py 消费经过构建与 11 组自动验收的明确候选，结果保留 stageQualified=false；当前旧正式 AMASE 不被冒充为新候选。g4-t07-planning-20260920-015736-953095 的点、线通过，区域收到足迹请求但无人响应，最终空规划，整轮 failed 且后端正常退出。依据实际请求及原区域示例配置，补齐运行副本 SensorManagerService 后，g4-t07-planning-20260920-015926-116466 四组全部通过、正常退出，未改 UxAS 二进制或超时。单例点／线／区域航点 8／75／5；混合 400 线／500 点／600 区域航点 74／15／5，逐项请求只生成各自实体计划。新 gate 按全部真实实体初始化后逐项发任务／请求，宿主已准备泛化实体清单。

后续边界：区域实际规划只有一个带任务标记的目标，其入航段仍须真实执行；不能沿用水道至少四任务航点的数量假设。继续通用内部导航、真实航段、末端到达和同身份完成取证。人工 GUI 确认仍未收到，进程 8836 的候选窗口保持暂停（5555／9400／9500），只关闭诊断所拥有的无界面后端；正式发布尚未执行。

首轮全程：候选 g4-t07-capture-20260920-020525-461072 四场景完整取证并正常退出，37 项来源未变；取证 receipt 保持 stageQualified=false。逐任务内部执行独立核查通过：单例点／线／区域完成于 108410／740709／124410 ms；混合 400 线／500 点／600 区域分别完成于 740709／137510／125410 ms。点规划首个无动作起点由原 WaypointPlanManagerService 明确从第二航点开始，审计单列规划起点并拒绝再跳目标；九项错误身份／时间／缺失导航等反例均拒绝。

审计失败与修正：g4-t07-audit-20260920-023546-348977 failed，父收据前三案例的 ports 被后续混合场景的可变配置引用覆盖；独立案例收据和实际 XML 没变。另发现三实体慢客户端固定等待 5 秒仍落后，最终状态摘要不一致，不能登记完整通过。分别改为每场景独立端口配置，以及三个客户端确认同一暂停快照边界；滞后客户端明确重连取快照并登记次数，90 秒上限，禁止把固定等待当成已追上。保留旧收据和原始文件。

统计含义复核：AMASE 原生 SearchTaskAnalysis.checkSensors 将每个真实相机用于每个任务图形，不按 EligibleEntities 过滤观测；该字段约束规划执行。首轮混合水道全局并集 724／724，指定实体 400 自身 724，500 相机还看到其中 230，600 为 0；点 165861 ms，区域 1250／1250。独立统计分别登记全局并集、逐实体贡献和指定实体值，逐格对照原生报告，不将偶然观测算作任务执行。零观察及“未分配相机保留全局观测、指定实体仍为零”等四项隔离统计边界通过。此前按地理平移推定仅指定实体可见的假设撤销，不改几何／飞行参数以掩盖实际观测。

在线复验新故障：g4-t07-capture-20260920-024133-724155 在点场景第一次网关重启时明确 failed，错误为 Main task TCP connection is absent or ambiguous；主链故障未被观察恢复掩盖，所属后端正常退出。检查实际接入时序及 TcpServer 源码发现 accept／接收／仿真线程共享可变 ArrayList。新确定性双线程最小实验 g4-t07-tcp-20260920-024539-165077 在旧候选 19 项中失败 9 项，四种广播期间增删连接均复现 ConcurrentModificationException；原 SocketThread 捕获此异常后会关闭自身连接。真实失败没有异常堆栈，因此异常触发的判断结合独立复现，不声称旧原始日志已输出该堆栈。

并发修复与新候选：TcpServer 使用 CopyOnWriteArrayList 的稳定遍历快照，移除停止连接后不再向其发送，保留来源排除和下一帧接收新连接的行为；未修改主链协议、任务或算法。构建 g1-t04-build-20260920-024628-136806 passed，JAR SHA256 为 1ef4a6da82717cf070e63818cafdc94895c021b359a1f682840003b5bfd74237；同一 19 项实验 g4-t07-tcp-20260920-024649-572960 全部通过，31 项统计 g4-t07-statistics-20260920-024650-599928 和原线四组分析回归 g4-t07-line-replay-20260920-024717-454867 通过。必要源码修复现共四文件（原三项统计与 TcpServer）。

GUI 交接：旧候选窗口 g1-t04-run-20260920-014335-273348 已通过 request-shutdown 正常关闭，宿主退出 0；未填写人工确认、未发布，旧异步查看请求不再适用于新候选。新窗口 g1-t04-run-20260920-024650-070740 已运行并暂停，11 组自动验收 g1-t04-automatic-20260920-024736-508525 正在执行。新的实际 GUI 反馈、正常收尾／发布、来源交接及完整正式组合资格仍待完成；T07 未通过，T08～T09 未启动。

新版本自动验收：g1-t04-automatic-20260920-024736-508525 的 11 组全部通过、退出 0，36 项环境和执行策略恢复检查通过；当前 GUI PID 26852 保持暂停，已明确旧窗口不能代替本次确认，新的异步实际查看请求已发送，尚未收到回复。完整候选复验 g4-t07-capture-20260920-025046-760714 开始执行，冻结本次实现来源；继续保持 stageQualified=false，待完整飞行、独立审计及正式发布闭环后再判 T07。

最终候选全程与审计：03:20 后 g4-t07-capture-20260920-025046-760714 四场景均 passed、退出 0，43 项运行实现来源未变；独立 g4-t07-audit-20260920-032048-023253 passed、退出 0。父 result SHA256 为 24548e8fb36a76e23805f725a1b65f8e93778952df46443a3c502e2c58eca97a；核对 131／144／132／175 项案例证据、父／子收据一致、实际类加载及九项审计源码。两份结果保持 stageQualified=false，审计明确 currentFormalPublicationRequired=true，不据候选通过宣告正式卡完成。

实际结果：点／线／区域单例的完成分别为 108410／740709／124410 ms；混合 400 线／500 点／600 区域分别为 740709／133770／125410 ms。规划、任务航段、内部末端到达、同身份可靠完成均通过；各轮 217／1405／249／1405 条 Running SessionStatus 全部倍率 1。独立统计：单点 102671 ms，单线 724／724，单区域 1126／1250；混合线 724／724（400 自身 724，500 相机另见 545），混合点 162671 ms，混合区域 1250／1250（仅 600）。原生计数／百分比／点秒数一致；观察截止于各轮暂停导出时，未把偶然观测记为其他实体执行了该任务，也未调整场景或参数追求跨轮相同统计。

客户端与收尾：每场景三名真实 WebSocket 消费者、两个整网关重启通过，网关发送业务帧均 0。混合普通／刷新／慢消费增量为 6744／6486／4735，慢客户端最终明确重连取快照一次，三者状态摘要一致并与日志暂停边界相同；最后实例客户端队列峰值 47 条／242424 字节，未触发慢客户端阈值关闭。四场景的后端、网关及协议客户端全部正常退出，无强制终止，声明端口释放。当前仅保留独立候选 GUI PID 26852 供用户实际查看，不将其误报为退出泄漏。

当前交接与归档边界：方案、状态、backlog、AGENTS、混合报告及总体计划已同步真实进度。T01～T06 已完成且已归档，HEAD／origin/main 再核对为 e8c6421e972fa5f7848ec86e8136514694eab69f；T07 尚缺当前 GUI 反馈、正常 Finalize／发布、UxAS 的 AMASE handoff 更新、输入来源修订及正式组合资格，修改保留在工作区，未提交／推送。T08～T09 未开展；当前新窗口查看请求仍待答复，不复用旧窗口或旧阶段的确认。后续先完成以上正式资格，再按原九卡顺序推进。

收尾核查：14 个新增 Python 源码语法、7 份变更文档／385 个本地链接、git diff --check 及历史工作日志前缀保持检查通过。最终监听核对仅有独立待确认 GUI 的 5555／9400／9500，均由 PID 26852 持有；8000／5556／9999／19400／19500／19600 无监听。GUI 最后事件仍为 Paused 20410 ms／gui-ready，保留当前窗口等待真实反馈。

## WL-20260920-004｜G4-T07 当前 GUI 确认与正式资格收尾

时间／时区：2026-09-20，Asia/Shanghai，08:07 起。
关联任务与状态：G4-T07；当前 GUI 已确认并正常退出，正式组合资格复核进行中。
背景与范围：用户明确回复“GUI 确认、正常退出，一切正常”。适用于当前 GUI g1-t04-run-20260920-024650-070740，继续既定 T07 收尾，不复用旧窗口确认。
实际执行：以用户原文调用 amase.tests.ps1 Finalize，收据 g1-t04-finalize-20260920-080752-574040 passed，GUI 退出 0、三个端口释放，正式发布当前候选 g1-t04-build-20260920-024628-136806，JAR SHA256 1ef4a6da82717cf070e63818cafdc94895c021b359a1f682840003b5bfd74237，与四场景取证完全相同。
交接：复用未变更的 UxAS 构建 g2-t05-build-20260919-232557-986；完整复验 g2-t07-test-20260920-080804-302 passed，普通／中文路径、连续三次启停、真实超时及来源／发布故障均通过；新发布 g2-t07-publish-20260920-081057-422 passed，更新其 AMASE handoff。旧包和收据保留。
来源修订：config/g3-baseline.json 保存 revision 3 历史，升至 inputRevision=4、45 项冻结输入，登记四项 AMASE 源码变化；新增点／区域文件不伪造旧冻结记录，同时保留旧构建的实际摘要。config/g4-baseline.json 保留 T03 WAL 修订，另增 AMASE 发布和 UxAS 交接链。新输入资格 g3-t01-check-20260920-081222-593 passed。
验证办法：scripts/g4_environment/manage.py 增加显式发布链核验；原文件另存有摘要的前驱副本。新 scripts/g4_qualification/qualify.py 将独立候选审计与同字节正式包绑定，检查原始证据未变、只有资格检查函数发生变化，并验证错误前驱／发布／GUI 等拒绝；不将来源交接复核称为重新飞行，不改写原 capture 或 audit。

最终验证：环境 g4-t01-20260920-081332-601 passed（真实 HTTP／WebSocket）；独立正式资格 g4-t07-qualified-20260920-081353-913195 passed，原始四案例与审计摘要保持、三个正式产物与候选字节一致、运行环境函数未变，10 项错误来源链全部拒绝。首次资格 ...081341-470279 因摘要大小写直接比较错误失败，规范小写后复验通过；保留失败收据，没有重新构建或改写旧运行。
成果与下一步：T07 正式组合合格，状态、backlog、AGENTS、总体计划和专题报告同步 T01～T07 已完成、T08 可执行。按持续授权归档后推进 20 实体两模式稳定性；本次 AMASE 发布确认不替代 T09 最终阶段 GUI 确认。

归档结果：31 个文件、16 个 Python 源码语法、7 份文档／384 个本地链接及历史日志前缀检查通过。提交 d43821b966b705338508ec25d8b7b3da5a3401af，普通推送 origin/main 成功，远程同编号、工作区干净。

## WL-20260920-005｜G4-T08 二十实体扩展与稳定性

时间／时区：2026-09-20，Asia/Shanghai，08:17 起。
关联任务与状态：G4-T08 进行中，T07 正式资格及归档已完成。
目标与修改：固定 400／500／600～617，8 线／6 点／6 矩形，一架一项任务；新增 scripts/g4_scale 与 tests/g4_scale，独立模式预算、增量证据读取、原始记录落盘及五进程 Windows 内存／CPU 采样。候选硬上限为网关私有内存 1 GiB、已有每客户端 256 条／8 MiB 队列；每模式 2700 秒自动预算，真实倍率 1。增加 AMASE 半帧、UxAS 观察断线、两路跨完成断开、整个网关重启及三客户端。Java 显式 -Xmx2g，传感器／飞行／任务参数不变。
实际失败：首个 60 秒诊断 g4-t08-diagnostic-20260920-082105-760256 failed；20 架真实初始化后首项任务即被网关判 XML array exceeds limit。检查本次 UxAS messageLog_1_0.db3 第 203 行，CreateNewService 把全体 20 架配置与状态装入 UXNATIVE 最大 16 项数组；单实体 AutomationRequest 本身符合模型。网关拒绝而未发布完整快照，后端正常关闭，旧记录保留。
原因与处理：TaskManagerService 创建新服务时无条件复制所有实体。TaskServiceBase 对搜索配置本已按 EligibleEntities 筛选。必要修复仅对大于 16 架环境、显式 EligibleEntities 的 CMASI 点／线／区域三种任务初始化装入对应子集；小规模及其他任务初始化保持原逻辑，超模型上限明确拒绝，未放宽网关或修改 MDM／生成库。正在按既有 C++ 来源规则重建和复验，修复合格前不继续正式 30 分钟。

修复发布：新 UxAS 构建 g2-t05-build-20260920-082330-977、候选验收 g2-t05-test-20260920-082558-938、HelloWorld g2-t06-test-20260920-082835-289、发布复验 g2-t07-test-20260920-083019-966、正式发布 g2-t07-publish-20260920-083245-918 全部 passed。AMASE 不变。G3 inputRevision=5 保留 revision 4，45 项冻结输入；G4 另增 uxasScaleRevision，保留此前两次修订。资格 ...083353-364 因摘要大小写的历史精确比较约定失败；统一配置为大写后新资格 g3-t01-check-20260920-083505-519 passed，未改变文件字节。环境复验 g4-t01-20260920-083609-418 与六项来源拒绝 g4-t08-lineage-20260920-083609-845796 passed。
验收工具：controls g4-t08-controls-20260920-083203-248470 确认旧 20 项数组严格拒绝、优化后的独立统计与 T07 四场景逐项相同、真实进程资源采样有效。新 Windows 入口初次单模式调用因 PowerShell 单元素输出退化为字符串、参数被按字符展开而 argparse 拒绝（未启动后端）；改为显式 string[] 后启动新 60 秒诊断 g4-t08-diagnostic-20260920-083628-695397。当前已实际完成 20 项初始化和规划，等待重启／客户端及正常收尾结果。

规模诊断收尾：...083628-695397 failed，仅客户端测试失败。独立读取真实 CreateNewService 确认 20 项全部只有对应实体的一个配置／状态，20 项规划和三客户端最终边界一致；慢客户端只见两个流、三次快照，跳过了短命的中间网关实例，旧断言强制每客户端三流不符合重连“获取当前快照”的语义。T08 只调整慢客户端至少见两流；普通／刷新仍三流，所有客户端仍核对每条增量的连续序号及最终同一快照。后端正常退出，失败记录保留。新诊断 ...083857-868424 已开始，不把失败旧轮改为通过。

短程复验 ...083857-868424 passed：20 架真实初始化、20 项规划、两次网关重启、最终三客户端一致、所属后端正常退出及端口释放。隔离副本 3800 条记录的 13 项压力／重建／损坏拒绝检查通过；夹具初次修改绑定后未重开读取器，被 Journal binding changed 拒绝，修正夹具后通过，没有修改网关防线。
长跑采样纠正：g4-t08-capture-20260920-084111-252471 从 out/tmp 工作目录开始，达到约 159 仿真秒，点／区域已报告 12 项完成，AMASE 半帧断线并恢复；资源记录却显示网关与客户端仅约 750 KiB／CPU 0。只读 Windows 父子进程核对确认 venv/python.exe 为启动器、实际解释器另在子进程。主动写本次网关正常停止标记，使编排明确 failed 并正常关闭所属后端；原记录保留、不计为长期通过。采样改为各所属进程树总量，编排器排除其他已单列树；真实 32 MiB 分配测试 g4-t08-process-checks-20260920-084544-156531 passed，解释器纳入且正常退出。重新开始完整两模式 1800 秒，原失败轮不续算。

恢复边界失败与修复：...084606-755741 在约 265 秒、UxAS 观察恢复时 failed，原保护报告关键事件未进日志，后端正常收尾。独立对照 200 个待关联哈希全部位于已提交数据库，首行为 19227，网关重建捕获边界为 19223；实际原因是长重建结束时按当前时刻检查 10 秒缺失窗，尚未消费的新边界事件被误拒绝。src/sim_bridge/gateway.py 改以完整消费边界的取得时刻判定缺失，重建导致陈旧时保持 recovering；src/sim_bridge/journal.py 增加持久消息哈希索引及摘要／身份复查，解决关键哈希离开 4096 项缓存后的重复来源核对。真实缺失、损坏保护均保留，原失败记录不改写。tests/g4_scale_checks/critical.py 的 g4-t08-critical-checks-20260920-085415-846073 七项 passed；records.py 的 13 项重复、删除、损坏及有界队列检查重跑 passed。规模恢复等待预算显式 120 秒、每模式总预算仍 2700 秒，等待期间持续采样，正在从不同工作目录重启新完整两模式。

修复后回归 g4-t08-regression-20260920-085650-501657 的 32 项状态、9 项恢复拒绝、8 项发布检查全部 passed。新完整取证 g4-t08-capture-20260920-085508-125483 从 out/tmp 调用绝对入口；Headless 于 09:26 正常结束，案例 passed，1800009 ms、356 次资源采样、719 项证据、20 项完成报告，三个客户端最终摘要 255342f0529c6fc3b16bd6fe70a20f58014ed8111878faee1569230ca4f30a51 一致。全部观察恢复与两次整网关重启完成，后端正常退出及端口释放。GUI 已启动独立 30 分钟；执行和统计独立审计尚待全程结束，本卡继续进行中。

完整取证于 09:57 passed，两模式均到 1800009 ms，Gui 357 次资源采样，全部所属进程正常退出／声明端口释放；父 result SHA256 a0a11e9838202278bd2955107c82d90d9e9a593d8cdf64eb700c9b2eb0c778fc。端口冲突补验 g4-t08-port-checks-20260920-095101-363156 passed，第二网关拒绝且原实例不受影响。缺口独立检查 g4-t08-gap-checks-20260920-095810-716470 passed，两轮各 8 项线任务完成均无原观察 TCP 帧，持久库完整唯一、恢复快照逐项匹配。

离线审计夹具纠正：g4-t08-audit-20260920-095806-639763 的无界面 20 项规划／命令／实际航段／完成及统计通过，但整体 failed 于 Source sampling has a gap。逐项追踪发现唯一超限间隔为连接初始 Time=0 快照到首动态状态 2040 ms，不能按稳定运行间隔混算。只修订离线 audit.py／performance.py，按固定旧摘要保存前驱，其他原运行输入必须完全一致；执行／统计函数 AST 未变。新增初始转换统计，动态区间保持递增、2000 ms 上限和整轮跨度要求。八项审计修订检查 ...100536-769672 passed，真实缺口、异常初始采样、运行源码变化和未知前驱均拒绝。新审计 ...100537-000770 按首次 failed 收据固定 SHA256 ca9e7aa14ae1ac7759c3d5134b040feac78de4c46e0dcc0107b6cf5028dc7156 复用其中已通过的无界面执行／统计文件，重新核对性能，并计算 GUI；原运行、旧失败结果和来源均不改写。

最终独立审计 g4-t08-audit-20260920-100537-000770 passed，退出 0：两模式各 20 项真实规划、命令、内部导航、实际航段、末端到达及同身份完成全部通过；40 组统计独立重算一致，每模式 20 个任务服务配置／状态均为对应单实体。八条线分别为 723／723 或 724／724、六矩形均 1250／1250，六点观察秒数按各轮原始记录独立对照，不设覆盖或观察时长门槛。GUI 及无界面各 3385 条 Running 时钟均 1 倍，自动取证墙钟 1881.145／1884.231 秒，源动态频率约 1.8806 Hz、最大动态间隔 541 ms。

性能与边界：网关私有内存采样峰值无界面 73.30 MiB／GUI 72.66 MiB，每客户端队列至多 256 条／8 MiB，普通客户端延迟 P95 为 226.26／217.72 ms；慢消费 P95 约 50.6／49.1 秒，最终重连同快照，无延迟门槛。Java 运行末段约 240／374 MiB，暂停导出时采样峰值 711.58／1483.09 MiB，最大堆 2 GiB；不据有限运行声称无限时长表现。每模式日志约 2.47 GB，900 秒整网关重启恢复约 53.671／48.368 秒，期间明确 recovering／503。完整机器、逐实体结果、延迟分位数、CPU、恢复时长及失败修订均写入 docs/g4-scale-validation.md。

交付状态：G4-T08 已完成，T09 可执行；本卡取证／审计仍 stageQualified=false，未冒充 G4 阶段验收。状态、任务清单、AGENTS、总体计划和 G4 方案同步；当前所属进程及全部声明端口已退出／释放。下一步按持续授权核对文档、来源和提交范围，提交、普通推送及远程核对后，开展 T09 当前正式组合复验、合格网关发布与 G5 交接。


T08 归档补记：提交 e18f8abea036853ad508d034fc6c04af55323ee8 已普通推送，git ls-remote 确认 origin/main 同值；进入 T09 前工作区干净。

## WL-20260920-006｜G4-T09 当前网关包、最终复验及阶段交接

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G4-T09 进行中；T01～T08 已完成。
背景、目标与范围：复用 T08 相同网关业务字节和已验证两模式各 30 分钟稳定性，建立不可变候选与正式入口，当前正式后端重跑原两实体两模式及 20 实体 GUI 完整任务。本轮独立执行／统计审查后才请求新的阶段 GUI 确认；此前用户确认已绑定 T07，不重复使用。
工作过程与决定：读取实际来源、T08 首次离线审计失败与更正链，T09 显式核查最终审计摘要及只允许的两项离线来源更正；业务源码保持。网关包与 workspace 的生成模型／独立环境分开绑定，记录实际导入 sim_bridge 路径和摘要；第二机器部署继续归 G8。独立后端编排、只读网关与正常关闭分离。新增 scripts/g4_stage、config/g4-stage.json、Windows 入口及包／确认反例；审查和发布均须完整来源与收据。
验证：当前来源资格 g3-t01-check-20260920-102910-298 passed；独立 Web 环境 VerifyOnly g4-t01-20260920-103254-705 passed；新增 Python AST 检查通过。后续实际包取证、当前 GUI、正常退出及合格发布尚待进行，不能登记 G4 完成。
问题与遗留：发现 backlog 的 T08／T09 汇总两行仍为历史进行中／待前置，而卡正文已更新，本轮更正为 T08 已完成、T09 进行中。最终运行和新用户确认待完成；失败将保存原始收据并正常回收所属进程。


T09 入口复核与纠正：首轮 g4-t09-stage-20260920-103431-086805 已真实接入并通过候选文件加载和在线端口冲突拒绝（g4-t09-live-package-20260920-103744-923766），复核中补充包对依赖锁、环境指针和 environment.json 的固定摘要，将日常会话改为独立无故障注入入口。通过所属 AMASE request-shutdown 正常中止首轮，原收据 failed（stream-failed:amase:None），normalExit=true、portsReleased=true，无强制终止；没有将短程诊断算作任务通过。

新的普通会话冒烟 g4-t09-session-smoke-20260920-104252-670580 passed：Original／Mixed20 均真实初始化与规划，HTTP live、WS 同连接快照后序号加一增量、正常停止和端口释放，每轮仅一个网关实例。首次冒烟 ...104125-386618 执行中客户端 max_size 夹具从默认改为 8 MiB，故另跑此新轮并冻结检查前后全部来源，不用首次结果证明最终夹具版本。最终 17 项包／确认拒绝 g4-t09-checks-20260920-104408-727139 passed。

最终取证已以 g4-t09-stage-20260920-104409-452486 重新开始，先原两实体两个模式，再 20 实体 GUI 到 785 秒，之后本轮独立审查和人工确认。新包在线端口冲突检查 g4-t09-live-package-20260920-104537-367540 passed，原 live 身份不变。全部业务文件仍与 T08 相同；新增包启动器、编排和会话代码在最终取证开始前固定。

文档检查：5 份当前修改 Markdown／221 个本地链接、UTF-8、git diff --check 和历史日志前缀检查通过；13 个新增 Python AST 通过。其他目录运行时辅助语法命令曾误用相对路径，退出失败后从仓库正确重跑通过；实际最终入口一直为绝对路径。当前阶段仍进行中，最终运行、GUI 和发布结果待续记。


最终自动结果：g4-t09-original-20260920-104409-452486 passed，两模式任务 1000／实体 400 均在 740709 ms 完成、覆盖 724／724；命令／内部导航／实际任务航段、跨观察缺口完成恢复、独立统计复算及两模式分析回放、三客户端、原生页面刷新、正常退出和端口释放均通过。真实提前发布检查 g4-t09-premature-20260920-110135-418378 passed，仅无界面通过、GUI 仍运行时正确拒绝。

最终 20 实体 g4-t09-mixed-20260920-104409-452486：真实 1 倍、各架各一任务，全部 20 项后端完成，到 785569 ms 暂停，三客户端同 sequence=40909／状态摘要 c2142ebf8c6bad81a2485d7dcfa61901926a9a26f2658d601a009920c5909038。关闭前独立审查 passed，20 项逐架规划／命令／实际航段／同身份完成与统计一致；线 724／724 或 723／723，矩形均 1250／1250，六点分别 182.011、182.531、181.471、181.471、181.471、180.971 秒。审查摘要 f35edaae0fe97d6ae2842d8424302f3a8bff2fb2ef1f314789a83cf20919d567，明确尚未正常退出／人工确认。原生 Edge 附加检查 g4-t09-browser-20260920-112051-201733 passed，20 个实体／任务、完成身份及时间、刷新、无 JS 错误、正常退出／调试端口释放通过，真实截图已查看。

当前候选 g4-t09-build-20260920-104409-688458，package.json 摘要 dcc6b6294ff0687ae690f6518780b2ba563339b177ba7c79b22b3aa9ff41689e，七个核心模块实际加载路径均在候选内。20 实体当前 GUI 绑定控制器 PID 18372、Java 27428、UxAS 26200 及各自创建时间，ready／review 摘要与 nonce 一并登记。已按已批准 G4 方案第 5 节发起新的本轮 GUI 确认；旧确认属于 T07。本次人工等待单独计时，进程、主链路和暂停时间继续监测，不能以超时或历史确认代替。

状态：T09 awaiting-gui，当前诊断接口 live。正常退出、关闭后再次独立审计、不可变包发布、正式入口消费验证、最终文档及提交推送待完成；T08 同字节两模式 30 分钟稳定性证据继续引用，不将本轮 785 秒标作新 30 分钟。未提前切换正式网关指针，T09 尚未提交或推送。

等待确认前最后核对：8 份 Markdown／362 个本地链接、UTF-8、历史 worklog 前缀、14 个 Python AST 和 git diff --check 通过；状态仍 awaiting-gui，诊断接口 live／ready，仿真时间保持 785569 ms，正式网关指针尚未创建。


## WL-20260920-007｜G4-T09 本轮 GUI 确认与受控收尾

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G4-T09 已完成；收到用户原文“本轮 20 实体 GUI 正常”，本记录追加完整收尾过程。
背景与事实：准备按该确认收尾时，发现原混合运行 g4-t09-mixed-20260920-104409-452486 已于 11:32:11 结束为 failed，唯一错误 GUI resumed/reset after reviewed pause；两个后端退出码均 0、无强制结束，端口已释放。原 GUI-ready 的时钟为 785569 ms；记录显示 11:32:01～11:32:02 两个 Running SessionStatus 分别为 786039／786569 ms，倍率均 1，最终暂停为 786599 ms。仅观察到 40 个状态更新及状态心跳，无新的任务、命令或删除；不能据此推断具体操作人。原分析 XML、格子和分析事件文件与审查副本完全同摘要。

处理与决定：保留原 failed result（摘要 f0c20224910814c3179a4cd2e66dca5c03a39f0ef76b619e0ff9724547652f25）和 failed case（506f8d04228c81f083163c8ce3067eec18fc95af48610c3ea3d90a1cc64a992f），不改写来源、不放宽正常验收的暂停规则。参照项目既有同版本受控复验的收尾方式，本次用户确认只绑定其实际查看的 GUI／原审查／候选包，另要求同字节完整 20 实体 GUI 受控复验、审查后正常退出和关闭后审计。不会声称用户查看了后续自动复验窗口，也不把原失败运行改判 passed。

修改范围：新增 config/g4-finalization.json 与 scripts/g4_finalize，固定唯一已知原运行／审查摘要和 1030 ms 的具体后续记录；任务、命令、生命周期变化、未知前驱、旧确认、自动确认或跨包确认均拒绝。原 scripts/g4_stage、网关核心、HTTP 服务、AMASE／UxAS 及生成模型字节全部保持，原两实体两模式通过收据继续有效。

本轮确认已保存为 g4-t09-visual-confirm-20260920-113828-487244/confirmation.json，scope=visual-only-same-version，状态 accepted-visual-only，明确 visualRunStatus=failed、controlledRerunRequired=true。新受控运行已启动，真实 1 倍、785 秒、20 项任务和三客户端全程，自动审查后直接正常收尾；人工等待不再次引入。新增 Python AST 通过，后续独立反例、关闭后审计、正式入口消费、文档同步及归档继续执行。

受控复验 g4-t09-controlled-20260920-113903-072650 已 passed，20 项执行／完成／统计关闭前审查通过，三客户端 sequence=40909、状态摘要 f14c14649ba7703faad8ecc2fab30fd12456d37b3684a1b4107ae23c75043aeb；所属后端正常退出、端口释放、全部冻结输入不变。明确 manualGuiAcceptance=false，本次真实视觉确认仍只属于原 GUI。8 项确认绑定反例 g4-t09-finalization-checks-20260920-114055-815004 passed；受控运行尚未结束时发布尝试 g4-t09-publish-20260920-114324-620268 正确拒绝，没有提前创建正式指针。当前执行完整关闭后审计及发布。

日常入口复核发现：原 scripts/g4_stage/session.py 在网关暂时离线时会结束整个后端会话，与运行独立要求不符；此前短程冒烟没有覆盖此情形。冻结的阶段验收源码和包保持不变，新增 scripts/g4_session/runtime.py 与 start-g4-session.ps1 作为日常入口，监控后端与网关分别处理，网关退出自动以同一清单和事件库启动新实例，也允许显式 gateway-offline 留空观察。后端退出／主任务链路故障仍由原有防线拒绝。新的独立正式入口检查将分别停止两实体和 20 实体的网关，核对后端身份不变、仿真时间继续、新流快照恢复及整组正常退出；通过后单独发布 session.json 来源资格，旧入口仅保留历史取证用途。

最终发布 g4-t09-publish-20260920-115418-723744 passed：关闭后审计 20 项完整执行／统计与受控运行自身审查完全一致；T08 同字节两模式 30 分钟证据及原两实体两模式资格继续绑定。本轮用户确认只归原视觉运行，原 failed 保留。正式 package.json SHA256=dcc6b6294ff0687ae690f6518780b2ba563339b177ba7c79b22b3aa9ff41689e，qualification SHA256=245d2fdc1d5699784c088f8f0e762365009e892e2aa19fbf413b45f66db39c51，current.json 已原子切换。

正式日常入口验收 g4-t09-release-check-20260920-115528-388842 passed；从中文空格工作目录调用 start-g4-session.ps1，Original／Mixed20 分别验证真实初始化／规划、正式模块导入、HTTP／WS；停止网关至少 3 秒后自动恢复，后端身份／运行清单不变、仿真时间推进、新流快照后有序增量正确，整组正常退出／端口释放。session.json 单独绑定当前正式包和入口源码；旧阶段脚本未改动。

状态：G4-T09 已完成，G4 九张卡全部验收并发布；已同步状态、待办、AGENTS、总体计划、G3／G4 方案、阶段报告及 G5 交接。G5 待细化，不自动进入实现。最终文档／来源及提交范围检查、提交、普通推送和远程核对继续收尾。

最终普通入口复查：从 out/tmp 调用绝对 start-g4-session.ps1、不带 ValidateEntry，运行 g4-t09-demo-20260920-115944-865791 passed；正式入口资格检查、20 架真实快照／live、正常停止、来源不变均通过。最终只读检查 5 个完整／入口运行全部正常退出，48 个声明端口可独占绑定，正式包、qualification 和 session 来源摘要一致。21 个 Python AST、4 个 PowerShell 解析、9 份 Markdown／本地链接、UTF-8、git diff --check 及原 worklog 历史前缀检查通过；没有提交 out／.tools、原始运行记录、生成物或本机绝对路径。按持续授权提交、普通推送并核对 origin/main；实际提交号及远程结果见本轮交付和 Git 记录。

## WL-20260920-008｜G5 实施方案与十一张任务卡落盘

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5 规划已完成；G5-T01 可执行但尚未启动，T02～T11 待前置。本次为规划文档交付，按用户所确认方案的最后一节，不安装工具、不改动地理数据、不创建前端工程或启动功能实现。

背景、目标与范围：在 G4 正式网关及独立日常入口已发布的基础上细化 G5。用户确认查询／图层／视角操作、本地全球矢量与 DEM、可切换在线卫星影像，以及 Cesium／AMASE 同源真实地形；继续使用现有 DEM 做功能验收，稳定性为硬条件、约 30 FPS 为目标且无硬帧率或 250 ms 门槛。控制／重置、回放及第二机器部署仍分属 G6／G7／G8。

工作过程与核查：先读取状态、任务卡、AGENTS、G4 交接、浏览器契约及最近工作日志；规划期间核查实体／地形／统计源码并参考 Cesium、PMTiles、Terrain Tiles 官方资料及用户指定的 Airports 数据组织方式。用户补充数据位于 tiles；只读检查确认 planet.pmtiles 为 PMTiles v3、z0～15、137370745450 字节，辅助 global-z9 包为 z0～9、1557952591 字节，DEM z0～10 各级数量与全球 XYZ 数量相符、抽样 PNG 为 256×256 RGB。用户确认 DEM 由 Airports 下载器获取。以上仅为目录／头部观察，尚无完整性、实际垂直基准或地形资格。本次落盘起点 Git 为 b29a6b1，跟踪文件干净，tiles/ 与 tiles.zip 为用户现有未跟踪输入，保持原状并排除在文档提交之外；忽略规则由 T01 实施。

重要发现与决定：AMASE 地形参与飞行、传感器和分析，G4 覆盖检查明确限定零高程。因此 G5 新增独立高程资格及真实地形后端资格，既有 G4 零高程结果不改写或直接复用为新地形通过。按验收区域核实垂直基准，再生成同源标准化栅格、DTED Level 1 及 Cesium 高度场；未知基准或缺数据不能静默回退零高程。约 107 米源像素间距、派生间距、20 米统计栅格和 1 米数值转换判据分别解释。保持平面场景与完整水道，地面按 DEM 赋高，必要飞行高度按范围及外围 5 公里最高地面高程统一适配至至少 100 米仿真净空并冻结；越界失败，不寻优覆盖效果。

成果与修改文件：新增 [G5 实施方案](docs/g5-cesium-display-plan.md)，在 [任务清单](docs/backlog.md#9-g5-顺序与任务卡) 增加 T01～T11 的顺序、状态、目标／修改范围、步骤、验收证据和回退／停止条件；同步 [当前状态](docs/status.md)、[总体计划](04.项目总体实施计划与阶段验收.md)、[AGENTS](AGENTS.md) 和 [G4 → G5 交接](docs/g4-g5-handoff.md)。T01 为下一实施任务；本次未新增 G5 可执行脚本、依赖、派生数据或运行验收收据。

验证：从仓库根目录运行 git status --short、git diff --check，以及 Python 标准库文档检查；前六份说明的 UTF-8、代码围栏、279 个本地链接、24 个标题锚点、十一张任务卡的编号／结构／状态和当前 G5 状态一致性检查通过，退出码均为 0。Git 提示部分文件后续可能按既有配置换为 CRLF，未为此格式化全库。文档交付不运行编译、仿真或 GUI 验收；本条追加后继续核对历史日志前缀、完整变更范围和最终文档。

问题与处理：原 G4 交接末段仍仅描述显示范围，已更新为用户本轮确认的同源地形扩展及独立复验责任；原状态中的 G5 待细化已同步为已细化／未实现。没有用规划声明替代工具可用性、地图加载或任务通过。用户数据不移动、不删除、不提交。

遗留事项与下一步：按 G5-T01 重新核查正式来源并准备项目隔离工具、锁定版本和输入，T02 再核验真实地形数据及基准；所有功能和资格均待各卡实施。文档最终核查通过后按持续授权提交并普通推送，远程结果另核对，不自动进入 T01。

最终文档核查：7 份修改 Markdown 的 UTF-8、代码围栏、388 个本地链接、31 个标题锚点、11 张任务卡及状态一致性全部通过；历史 worklog 前缀与 Git 基线一致且本次追加保留原文件字节，git diff --check 通过。未创建前端目录，原始数据保留，提交范围限定上述 7 份文档。按持续授权进入提交、普通推送及远程核对；实际提交号和远程结果见本轮交付及 Git 记录。

归档补记：主文档提交 5ee6471 已生成，git commit 最终退出 0；其自动 maintenance 触发 geometric-repack，扫描到约 19.6 万个已有松散对象，长时间未结束。只读核对父子 PID 与命令后，仅停止本次提交启动的 pack-objects 子进程；维护报告 geometric-repack failed，原提交有效。未删除对象／临时文件，未结束其他既有 Git 进程。后续归档命令单次使用 -c maintenance.auto=false -c gc.auto=0，避免再次触发该维护，不修改持久 Git 配置；仓库整体维护另行处理，不扩展本次文档任务。


## WL-20260920-009｜G5-T01 环境与输入基线

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5-T01 已完成；T02 可执行，尚未启动。本轮用户明确要求执行 T01，范围为隔离工具、最小前端工程、数据登记及资格验证，不进入真实地形转换或仿真显示联调。

背景与输入：起点 Git 7ca739b5c88798edb5398a9b01924423ac9c125c，跟踪文件干净，用户原始 tiles/ 和 tiles.zip 未跟踪。读取根 AGENTS、状态、任务卡、G5 方案、最近日志、G4 环境及正式包／日常入口代码，确认没有子目录说明。保留正式 AMASE／UxAS、七模型和 G4 来源。重新执行只读 G3 资格，g3-t01-check-20260920-143951-894 与 g3-t01-check-20260920-144822-482 均 passed；G4 正式包、环境、日常入口收据和当前文件完整复核通过。

工具选择与准备：从 Node 官方发布／SHASUMS256、npm registry 与 PyPI 元数据固定来源及摘要，Node 24.21.0 LTS／npm 11.19.0、TypeScript 7.0.2／Vite 8.3.0／Cesium 1.145.0，前端 lockfile v3 共 91 个包条目。独立 Python 3.14.7 venv 固定 11 个包，实际 Rasterio 1.5.1／GDAL 3.12.4、PyProj 3.8.0／PROJ 9.8.1、NumPy 2.5.3／Pillow 12.3.0；WebSockets 用于独立浏览器探针。未更改 G4 环境、系统 Python 包或持久 PATH。锁定清单先形成，再通过固定摘要下载／离线 wheel 安装与 npm ci 消费。准备 g5-t01-setup-20260920-143951-653 passed，补齐基础 Python 摘要及发布前复核后，新准备 g5-t01-setup-20260920-144636-058 passed；.tools/g5/current.json 指向后者，4609 个工具文件摘要固定，旧批次保留。

成果与修改文件：新增 apps/cesium_viewer 最小 TypeScript／Vite 工程，生产包包含全部 Cesium Workers／ThirdParty／Assets／Widgets 及许可，397 个候选文件；中文界面明确“环境验证”“尚未连接仿真”，显示椭球、异步几何样片和中文标签。字体用 Windows 本地 Microsoft YaHei，不下载字体。新增 config/g5-* 工具／资源／运行清单、scripts/g5_environment、tests/g5_environment、Windows 准备／构建／运行／验收入口。原始目录及压缩包加入忽略规则；只读登记主／辅助 PMTiles、DEM 样本与压缩包的大小、有限头部和时间，仍为 registered-only，无全量摘要或地形资格。

验证与证据：在仓库根执行 setup-g5.ps1，随后从中文空格工作目录及系统临时目录调用绝对入口。最终完整矩阵 g5-t01-test-20260920-145253-581 passed，14 组覆盖声明端口、独立地理能力、普通／中文空格工程干净构建、两次生产页面、重复启停、中文开发页面、缺依赖／修改来源／非白名单 npm 源／错误下载摘要拒绝、8080／5173 冲突拒绝。GeoTIFF 与 1201×1201 DTED Level 1 合成往返、正负 Terrarium 高程、无效值、网格位置、WGS84→ECEF、缺转换网格拒绝均通过；PROJ 网络关闭。实际 Edge 153.0.4234.32 加载工作线程、四类资源、中文标签及刷新通过；实际渲染器为 NVIDIA RTX 4000 SFF Ada 的 ANGLE／D3D11，未通过显卡型号推断。外部域名解析禁用，截图只作为资源探针旁证。

公开入口：g5-t01-build-20260920-144822-264 从 out/tmp/G5 入口 中文 调用，自动新建后端资格和中文工程，passed；最终来源的 g5-t01-build-20260920-145303-771 从系统临时目录调用，passed。VerifyOnly g5-t01-verify-20260920-144934-494 passed。最终公开运行 g5-t01-serve-20260920-145331-517 从系统临时目录调用，独立 Edge 再次加载／刷新通过，创建本组 request-stop 正常退出；result、entry-result 及 public-entry-edge 收据均 passed、退出 0。各入口参数均显式 PythonExecutable，可选 BaselineRunId 仍重查当前来源；build 可选 ChinesePath，run 必须绑定 BuildRunId。具体命令与证据见 [环境报告](docs/g5-environment-validation.md)。

问题、失败尝试与修复：g5-t01-test-20260920-144246-503 因 Cesium 类型没有 VERSION 导出而编译失败，改从固定 package 元数据注入版本；g5-t01-test-20260920-144359-705 浏览器因 favicon 404 失败，新增本地 SVG；g5-t01-test-20260920-144453-990 的两种生产路径通过，但开发中缺失资源被 Vite SPA 返回 HTML，修复资源中间件明确 404 后 g5-t01-test-20260920-144710-554 首次完整 passed。完善调用目录记录及使用说明后，g5-t01-test-20260920-144934-494 再次 passed。公开运行补测 g5-t01-serve-20260920-145008-280 的服务正常退出，浏览器相对用户目录导致 Edge 在就绪前退出 21；探针统一解析绝对路径，同样相对输出参数在最终运行通过，完整矩阵按最终源码再次通过。所有 failed 收据、候选和日志保留，不改写旧记录或忽略缺资源错误。

重要决定与边界：T01 工具资格和最小页面资格分开，candidate.stageQualified=false，没有正式 Cesium 指针；G4 指针保持。静态 8080 与开发 5173 只承担 T01 资源验证，8000 代理／Range／地图／卫星／真实地形由 T03，模型由 T06，业务状态接入由 T05；没有提前启动这些任务。地理环境可处理 DTED 不代表用户 DEM 完整、垂直基准合格或 AMASE 已加载真实地形。所有合成地理产物仅为探针。没有启动后端仿真，没有修改或转换原始数据，没有宣称 FPS、任务统计或人工 Cesium 验收通过。

文档与收尾：新增 [环境报告](docs/g5-environment-validation.md)，同步 [当前状态](docs/status.md)、[任务清单](docs/backlog.md#9-g5-顺序与任务卡)、[G5 方案](docs/g5-cesium-display-plan.md)、[G4 交接](docs/g4-g5-handoff.md)、[总体计划](04.项目总体实施计划与阶段验收.md) 和 [AGENTS](AGENTS.md)。接下来完成最终语法／链接／来源／端口／Git 范围复查，并按持续授权提交、普通推送和核对远程；归档命令使用单次 -c maintenance.auto=false -c gc.auto=0，避免再次触发 WL-20260920-008 记录的仓库维护问题，不处理历史对象或修改持久 Git 配置。

遗留与下一步：T02 核验 PMTiles 内容和 DEM、固定验收区域、查明实际高程来源及垂直基准、选择并验证转换网格，生成同源标准化栅格和派生物。未知基准／缺覆盖／缺转换资源仍停止资格流程；不自动进入 T02。

最终复查：g5-t01-final-check-20260920 passed；5 个 Python AST、9 份 Markdown／414 个本地链接、历史日志前缀、最终 14 组来源清单与当前字节、4609 个工具文件、原始数据登记及正式后端／网关身份全部一致。另有 5 个 PowerShell 入口和 3 个 JavaScript 模块语法检查通过，git diff --check 通过；8000／8080／5173／9223 均可独占绑定。原始地图、工具、候选、浏览器配置和日志均被忽略，未纳入提交。按持续授权进行本卡提交、普通推送及远程核对，实际提交号和归档结果见本轮交付及 Git 记录。
## WL-20260920-010｜G5-T02 数据资格检查与 DEM 异常复核

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5-T02 部分完成／受阻，未验收通过，T03 不启动。用户要求执行 T02；发现数据问题后用户要求“不会吧？再检查一下”，本轮已独立复核，没有取得修复或更换数据路线的确认，没有擅自补值或切换来源。

背景与范围：起点 Git 0bdbd0f7f83625fabab0142af907a4be64c99573，跟踪文件干净；后续出现用户 models/ 未跟踪文件，保持不动。读取根 AGENTS、当前状态、任务卡、G5 方案、T01 工具与 G4 场景来源。原始 tiles/、tiles.zip、正式后端／网关及已发布收据只读。T01 VerifyOnly g5-t01-verify-20260920-150718-453 passed，绑定 g3-t01-check-20260920-150718-681，独立工具／正式来源核查通过。

实施与调查：新增 scripts/g5_geography、tests/g5_geography、Windows 准备／检查入口及 config/g5-geography-lock.json、config/g5-terrain.json。PMTiles 两文件完成完整 SHA-256 和全部索引检查，主包 177280509 条记录、135393326 个独立内容、2918 个目录，辅助包 179074 条记录、97590 个独立内容、45 个目录；区段／计数／去重引用一致，各层级代表性 MVT 解码通过。全量摘要 g5-t02-full-digest-20260920 和索引 g5-t02-index-investigation 独立留存，正式入口再次核验。索引检查使用磁盘映射，主包约 1.08 GB 临时索引；不声称全部 MVT payload 都已解码或原始下载已获外部签名证明。

区域与基准：根据原 WaterwaySearch 及 G4 原场景生成器构造只读来源副本，912 个位置／任务几何点及 5 km 外围包络包含于经度 [-122,-120]、纬度 [45,46]。包括插值边缘需 z10 的 x=164～170、y=364～368，共 35 个瓦片。本地全文件摘要均与上游一致，锁定 S3 version-id／来源头。GMTED 文档明确保留来源垂直基准，不能直接视为全球 EGM96；查到 USGS 官方 Spatial Metadata ZIP 后完整下载、固定摘要，流式检查区域 12 个完整来源矩形及连续覆盖，全部为 SRTM DTED2 Void Filled、WGS84／EGM96／米。固定 PROJ 发布的 NGA EGM96 15 分钟网格及摘要，关闭 PROJ 网络；基准核实只适用于本区域。

主要问题与复核：首次完整网格派生发现 91 个负高程节点，最低约 -3718.70 m，纠正了此前尚未完成全网格检查便称区域为正值的口头判断。最低原始像素在 tiles/dem/10/170/365.png，第 53 列、第 80 行（从 0 开始），RGB=(112,59,0)，Terrarium 手算为 -4037 m，坐标约经度 -120.16090393066405、纬度 45.75171424271876。用户要求再查后，另写不依赖 Pillow 的 PNG CRC／滤波解码，与 Pillow、GDAL 的全部 65536 个像素逐值一致，PROJ 独立核对像素中心。S3 两种 URL（含固定版本）重新下载仍与本地逐字节一致；原文件 SHA-256 为 95e12d588026d764119078a56a1df4ca95096b24ab008fbd9ba48704332992c6。USGS 同点查询返回约 144.54 m，z11／z12 约 143.66／144.08 m，但来源为 NED、基准与分辨率另需核实，全部仅作旁证，未拿来补值。独立标量插值再次得到 -3718.70297766 m，排除本轮 RGB 解码、XYZ 顺序及重采样算法产生该低点。异常上游成因未确认，不直接断言 NoData 污染。证据在 g5-t02-independent-recheck-20260920、g5-t02-anomaly-investigation、g5-t02-recheck。

实现验证：g5-t02-core-20260920-03 的 25 项实现检查 passed，明确 dataQualified=false。包括格式损坏、缺数据、未知基准、重复改正、经纬度交换、越界与坏转换网格拒绝；真实异常三路径解码及标量复算；中文空格路径中合成完整正高／DTED／Cesium 派生。2883601 个合成节点的 GDAL 往返通过，独立 geoid 数值最大差约 0.000025 m；正式 AMASE JAR 最近邻误差 0、双线性误差约 4.6e-13 m；实际固定 Cesium HeightmapTerrainData 与独立三顶点平面求解五样点误差约 6.6e-12 m。合成数据不构成真实地形资格或后端任务运行。原 AMASE 负高程读取问题实测：标准 DTED -100 m 被解成 -32668 m；间距元数据亦有十倍问题。当前工具明确拒绝负值，不修改正式 JAR；后续必要修复须遵守后端重建／复验规则。

失败尝试与修正：core-01 因 Pillow CRC 错误抛 SyntaxError 而测试异常分类不符，生产解码统一转成明确 ValueError 后修正。core-02 的独立地球水准面检验发现显式 multiplier=1 与 inverse 同用，符号反转；改为正向 h=H+N，依据 PROJ 参数说明并用 GDAL 网格独立复验，core-03 全部通过。原失败目录保留，这两个实现问题均不改变原始像素或正高插值异常。正式准备之前冻结最终源码，后续检查其 15 项来源摘要一致。

真实入口与停止证据：从 C:/Windows/Temp 调用 prepare-g5-geography.ps1，运行 g5-t02-prepare-20260920-154127-213；重新生成 g3-t01-check-20260920-154127-456，后端／网关／工具 passed。全球 DEM 1398101 个期望 XYZ 文件名、类型及非空长度齐全；仅区域及各层级样本做解码，不宣称全球高程合格。35 个区域上游版本再次匹配；完整 PMTiles 摘要、索引、代表性解码及区域来源核查通过。完整真实网格遇异常后明确拒绝，result／worker-result／entry-result 均 failed，退出 1；同名 out/geography/terrain-rejection.json 保存全部 91 个负值节点，automaticFillApplied=false，没有生成合格 manifest。环境和调用目录保持，未启动仿真或修改原始数据。再以失败编号调用 g5-geography.tests.ps1，g5-t02-test-20260920-154638-284 明确 Candidate entry did not pass、退出 1，没有 acceptance 或正式指针。

文档与收尾：新增 docs/g5-geography-validation.md，同步 status、backlog、G5 方案、G4 交接、总体计划与 AGENTS，均标明部分完成／受阻。8 个 Python AST、3 个 PowerShell 解析、1 个 JavaScript 语法、7 份 Markdown／297 个本地链接、冻结来源摘要与 git diff --check 已检查通过；本条追加后继续核对日志历史前缀及最终范围。实际 Java／Node 探针正常退出；无仿真、浏览器或页面服务待关闭。

重要决定与下一步：来源一致、文件完整、垂直基准已知不能代替高程数值可用性。保持 T02 地形资格失败，不把负值归零，不静默改用高 zoom／其他数据源，不削减二十实体场景范围，不发布合格地形。须先明确异常数据处理依据和差异清单，再重做真实标准化栅格／DTED／Cesium 及区域接缝／边界资格。用户本次仅要求重新核查，修复／替换路线尚未确定。按 AGENTS 第 10 节，部分完成不触发“完成后自动提交／推送”；本轮改动留在工作区，原始证据、失败目录和用户 models/ 保留。

## WL-20260920-011｜G5-T02 替代 DEM 调查

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5-T02 受阻期间的补充调查，替代数据候选已找到；任务卡仍未通过。用户确认原 DEM 源有问题，随后询问“有无替代的数据”，本轮范围为候选比较与只读可获取性／局部数值检查，不执行数据替换。

过程与成果：核查 USGS 3DEP 无缝裸地 DEM、Copernicus GLO-30 DSM、NASADEM_HGT 的官方产品文档。明确美国本土 3DEP 为 NAVD88、Copernicus 为 EGM2008、NASADEM merged HGT 为 EGM96；后者的 SRTM-only 浮点产品为椭球高，不可混用。对覆盖场景核心区的 USGS 两块和 Copernicus 两块进行匿名 HTTP HEAD，四个对象均 200、支持 Range；两组总大小分别为 896605235 和 84695229 字节。仅通过 GDAL /vsicurl/ 读取 GeoTIFF 元数据及 3×3 窗口，没有整包下载。USGS 实际水平 CRS 为 EPSG:4269，Copernicus 为 EPSG:4326；问题点原始像素分别约 144.5237274 m 和 142.8855591 m，邻域未复现数千米低点。未作跨基准转换或统一采样，不据单点推断整区资格或绝对精度。

验证与证据：目录 out/runs/g5-t02-alternative-research-20260920，availability.json 保存 URL、文件长度、ETag／Last-Modified，metadata-samples.json 保存布局、CRS 与原始邻域。HEAD 与实际 Range 读取两条 Python 命令均退出 0。官方来源及比较追加至 docs/g5-geography-validation.md 第 6 节。没有修改 scripts、config、原始 tiles、正式指针或 T02 状态。

问题、决定与边界：USGS 更适合当前美国区域的裸地仿真，Copernicus 更适合跨区域／全球统一来源但具有 DSM 语义；更换后均须重新核查垂直转换。Copernicus 公共 AWS 副本登记为 2021 release，区别于 CDSE 当前发布；两幅核心瓦片不等于覆盖派生全部边界，邻接输入需重新计算。NASADEM 本轮仅完成资料核查，未进行实际下载或样点检查。推荐优先评估 USGS 3DEP，但用户尚未选择替换路线，原始数据保持，T02 仍受阻。

收尾与下一步：本次是未完成 T02 内的补充取证，记录调查结论和可获取性，不把资料／单点检查写成数据通过，不触发“任务卡完成后”自动提交推送。下一步在确定来源后完整下载、锁定版本及摘要、核查全区域及边界、完成转换链和同源派生验收。


## WL-20260920-012｜G5-T02 双来源区域高程下载

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：用户明确要求“USGS 3DEP 和 Copernicus 都要，下载吧”；本次区域数据下载及完整性检查已完成，作为尚未完成 G5-T02 的补充输入。T02 仍未取得真实地形资格，不进入 T03。

目标与范围：完整下载两套候选源，保存官方来源、版本线索、元数据和摘要；保留原始 tiles/、压缩包、地形配置、旧失败记录及正式后端／网关。没有安装工具、修改持久环境、运行仿真或进行数据转换。

工作过程：读取当前状态、任务卡、日志和根说明，核对现有未提交 T02 改动，保持用户 models/ 不动。官方 S3 目录列出 USGS 源栅格、XML 与 GPKG；Copernicus 公共 COG 布局明确去掉东／南共享节点。因此按原 [-122,-120]×[45,46] 存储网格下载 USGS n46w122／n46w121 两幅，加各自 XML／GPKG；Copernicus 下载 N45／N44×W122／W121／W120 六幅，其中四幅供东／南外边界取样。磁盘余量足够，HEAD 冻结 12 个源文件共 1146472790 字节，GET 绑定 ETag，两个并发下载流写独立 .part，全部实际完成且没有数据文件重试。

成果与修改文件：数据目录 out/geography/sources/g5-t02-dem-download-20260920-160406；USGS 栅格 896605235 字节，Copernicus 247571242 字节，另有 XML／GPKG 2296313 字节、三份说明／许可证 4200863 字节，总计 1150673653 字节。最终 verified-manifest.json 保存来源、版本、路径和 SHA-256；SHA256SUMS.txt 列出 15 个文件；integrity.json 为完整性通过收据。过程及本次一次性下载／检查脚本位于同编号 out/runs，全部受忽略规则保护。更新 docs/g5-geography-validation.md 第 7 节、docs/status.md、docs/backlog.md、AGENTS.md 和本条日志；没有新增正式下载入口或改动地理实现／配置。

验证：工作目录为仓库根，使用 .tools/g5/current.json 对应的 Python 3.14.7 地理环境运行本次 download.py／verify.py；下载退出 0，最终完整验证退出 0。12 个源文件长度与固定 HEAD 一致，本地 SHA-256 独立重读匹配；USGS 两个大文件使用服务端 base64 MD5 元数据核对，未把 multipart ETag 当 MD5，其余十个文件核对普通单段 S3 MD5 ETag。8 幅 GeoTIFF 全分辨率逐块解码共 311558688 个像素并读取内部概览，无解码错误，基础栅格 NoData／非有限计数均为 0；两份 XML 明确 NAD83／NAVD88／米、发布日期 20260202，两份 GPKG 的 SQLite integrity_check 均 ok。三份文档长度／摘要和 PDF 文件头通过。异常位置本地原生像素仍为 USGS 144.5237274 m、Copernicus 142.8855591 m，未经基准转换，只作诊断。

问题与处理：USGS 规范 PDF 的 Python 默认 HTTP 请求返回 403，首次完整检查在文档步骤退出 1，此时源文件均已通过。PowerShell HTTP 客户端使用常见 User-Agent 访问同一官方 URL，取得 200 和完整 PDF；单独保存 supplemental-documents.json，保留 download-manifest.json 中的原失败及 integrity-attempt-01.json，再完整复验通过。未跳过缺文件、修改源栅格或改写原失败收据。Copernicus 许可证及 README 正常下载。

重要决定与影响：仅下载当前区域及必要边界邻接，不下载全球全集。USGS 来自 2026-02-02 current 对象，Copernicus 来自公共 2021 release COG；不把后者写成 CDSE 最新发布。两组分别为裸地／NAVD88 与 DSM／EGM2008，尚未统一水平或垂直基准，不据无坏像素和单点对照声明地形合格或精度优劣。所有收据保持 terrainQualified=false，旧 config/g5-terrain.json 仍是 Terrarium 配置，不能直接拿新路径代替旧锁。下载文件与旧原始数据、用户模型均不入 Git。

遗留与收尾：下一步继续 T02 的候选来源选择、转换资源核实、同源标准化及 DTED／Cesium 派生和独立验收。本次只是未完成 T02 内的输入补充，按第 10 节保持部分完成状态，不触发任务卡完成后自动提交／推送；已有未提交 T02 改动保持。下载及检查进程正常退出，没有后台服务待关闭。收尾核对文档本地链接、UTF-8、日志历史前缀、下载目录忽略规则和 git diff --check，最终结果保存在本批 final-check.json。

## WL-20260920-013｜G5-T02 新旧高程共用建议

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5-T02 未完成期间的方案说明；用户询问新下载的 USGS／Copernicus 是否能与原有数据共用，本轮只分析用途与边界，不执行替换、拼接或转换。

过程与核查：读取 git status、当前地形配置、状态、双来源 verified-manifest 和最近日志。确认新下载只覆盖当前验收区域及邻接，terrainQualified=false，现有配置仍指向原 Terrarium 方案。查阅 Copernicus 官方产品说明和公共下载登记；USGS 产品网页本轮请求超时，使用已下载逐瓦片 XML 及此前来源记录，不把请求失败写成新验证通过。

结论与建议：原 PMTiles 矢量底图与高程用途互补，继续使用；建议 USGS 3DEP 通过转换／验收后作为当前美国区域主地形，Copernicus 留作交叉对照和后续其他区域候选。原 DEM 在已替换区域不必参与仿真，保留原始归档；目前新数据不是全球全集，不能据区域下载宣布旧全球 DEM 已完全无用。若暂留旧 DEM 做区域外浏览，须隔离已知异常、标明未获仿真资格；后续可逐步扩展其他合格来源。按区域共存或拼接需先统一水平／垂直基准、采样规则和接缝处理；不直接混合或平均不同来源。同一次验收运行应以冻结的标准化地形同时派生 AMASE／Cesium 输入，Copernicus 对照不等于已具备独立测绘真值。

成果、验证与边界：仅追加本条日志，未修改配置、源文件、派生产物、正式指针或任务卡状态；没有新增下载、仿真和运行验收。日志 UTF-8 与历史字节前缀核查、git diff --check 通过。上述为建议，尚未登记用户采用哪种替换方案；T02 继续等待转换及同源派生资格，未触发提交／推送。


## WL-20260920-014｜G5-T02 USGS 主地形与 WGS84 同源派生验收

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：G5-T02 已完成；原始 z10 失败历史保留，区域同源地形通过独立资格，下一张任务为 G5-T03。用户明确采用原矢量底图＋USGS 区域主地形、Copernicus 对照并要求继续；工作中又询问“统一为 GSM84”，按上下文解释为 WGS84，已说明经纬度统一而正高／椭球高需要明确转换，不改 AMASE 既有高度语义。

背景与范围：从 main 的 T01 提交 0bdbd0f7f83625fabab0142af907a4be64c99573 及先前未提交的 T02 工作继续。保留用户 models/、原始 tiles/ 和压缩包，正式 AMASE／UxAS／G4 及旧收据不变。读取说明、状态、任务卡和 WL-010～013，核查已下载 15 个源／附件文件、T01 工具与新数据定义。本轮只完成 T02，不启动仿真、浏览器或 T03 地图服务。

来源与方案：USGS 文件明确为 NAD83／NAVD88，不能直接标作 WGS84 或套用旧 EGM96 网格。检查固定 PROJ／EPSG v12.029 非 ballpark 操作，并按官方摘要清单下载／复核 NADCON5、GEOID99、EGM96、EGM2008 四份网格；网络消费使用固定文件，PROJ 网络关闭。选定 NAD83→HARN（8556）、NAVD88→HARN 椭球高（9160 逆）、HARN→WGS84（1901），再减 EGM96 改正。较早 GEOID99 与框架近似的限制完整保留；未把 NAD83 擅自重标为 NAD83(2011) 以使用 GEOID18。登记复合模型精度 1.1 m、加 EGM96 后 2.1 m，明确与数值一致性≤1 m 判据不同，不宣称源 DEM 绝对精度。

实现与交付：新增 config/g5-regional-sources.json、g5-usgs-operation.json 与 g5-terrarium-legacy.json，更新 g5-terrain.json 为 USGS 主源、Copernicus 对照，保留旧来源锁供矢量和异常回归。新增 scripts/g5_geography/regional.py、tests/g5_geography/regional_checks.py，接入已有 T02 Windows 准备／验收入口。所有经纬度标准化为 WGS84；3 角秒 2401×1201 float32 EGM96 正高是共同依据，DTED 两幅从该网格量化，Cesium 高度场从同一网格加一次 N96。固定西侧 USGS 优先，有界栅格窗口，无混合、补值、旧源或 Copernicus 回退；对照单独使用 H2008+N2008−N96。

问题与解决：初次试派生 g5-t02-usgs-development-01 在严格 1e-10 度迭代收敛条件下失败。诊断确认 NADCON5 双二次插值邻域切换有厘米级跳变，八次迭代仍重复残差；未认定为 USGS 数值异常或重写输入。采用四次实际求值中最小残差，固定保守水平残差上限 0.05 m，超限仍拒绝。第二次试派生及正式全网格最大残差上界 0.0337182 m，29 个节点超过 0.00001 m，全部留证。独立 3×3 Lagrange、标量原生插值、ECEF／Helmert／椭球反算确认结果；开发失败目录及补录失败说明保留。一次只读 rg 命令误用了 PowerShell 的 ErrorAction 参数，未执行有效搜索或更改文件，后续使用正确命令读取配置。

实际验证：开发独立 regional-checks-01、real-numeric-01 及 core-usgs-01 通过后，从 out/tmp/G5 T02 中文 调用 执行 prepare-g5-geography.ps1，正式准备 g5-t02-prepare-20260920-174145-970 passed；从系统临时目录执行 g5-geography.tests.ps1，正式验收 g5-t02-test-20260920-174558-688 passed。两者均显式使用已核查基础 Python，result／worker-result／entry-result 全部 passed、退出 0，环境与调用目录保持。各自新建后端资格 g3-t01-check-20260920-174146-201／g3-t01-check-20260920-174558-917，正式后端、网关、入口、七模型及工具重新复查通过。20 项实现／配置输入严格绑定，未用历史来源状态代替本次检查。

正式结果：两份 PMTiles 完整摘要、全部索引及代表性 MVT 再次通过；原例与 20 实体 912 个输入位置／几何顶点及 5 km 外围仍在既定范围。八组正式验收包含旧 25 项实现／异常回归、新 11 项来源／基准／边界拒绝、全部同源节点、独立坐标链、DTED 共边、AMASE 两类读取和实际 Cesium 插值。正高范围 2.0935845～3418.8381348 m，DTED 量化至多 0.5 m，全部 1201 个共边节点一致；原生 USGS 重叠区最大差约 0.0001044 m。5 个复合坐标样点独立 ECEF 最大差约 3.54e-8 m，10 个标准化节点最大高差约 0.00006455 m。正式 AMASE 最近邻误差 0、双线性约 6.71e-12 m，Cesium 三角插值约 1.63e-10 m，EGM96 网格独立差约 0.00003609 m。这些是数值实现误差，不是测绘精度。

对照与边界：Copernicus 3321 个统一基准样点中位差约 0.404 m、绝对差 P95 25.033 m、最大 58.909 m，原始最大差点保留，DSM 与裸地语义不同，不拿差值直接判定精度或调整任务。正式 AMASE 负 DTED 与间距元数据限制仍保留，本区域非负数据通过两种实际查询，不扩展为全球资格。下载 manifest 保持 terrainQualified=false，其历史下载状态不回写；本次 acceptance 为 regionalTerrainQualified=true，globalSimulationQualified、backendTerrainExecutionQualified、stageQualified 均 false。没有 GUI 人工确认或正式指针切换，真实地形飞行／传感器／20 米统计归 T04。

证据与文档：候选 out/geography/g5-t02-prepare-20260920-174145-970，manifest SHA-256 f0f6372b16cda86811750f83df44560ec345097fbcd695ad6fb6dffa7854a8c0；验收目录 out/runs/g5-t02-test-20260920-174558-688，acceptance SHA-256 3294d503ef172afd3f123a0a67a6393ef3b535f2eda1247b2fd6bdb57ff22c9a。详见 docs/g5-geography-validation.md 第 8～9 节；同步 G5 方案、status、backlog、总体计划、G4 交接和 AGENTS。前七节和 WL-010～013 保留历史。

收尾与下一步：全部所属准备／验证／Java／Node 进程正常退出。最终检查冻结输入及候选摘要、文档本地链接／UTF-8、脚本语法、日志历史前缀、忽略规则和 Git 范围，记录在 out/runs/g5-t02-final-20260920-usgs。任务卡完成后按持续授权提交并普通推送本次 T02 工作，包括前述失败／调查文档，不夹带用户 models/、原始地理数据、工具或 out 产物；归档命令单次禁用自动 maintenance／gc，保留历史对象。下一卡为 T03，按候选和 acceptance 双编号消费，不把区域文件资格当成页面、全球地形或仿真完成。


## WL-20260920-015｜G5 三维实体模型输入登记

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：用户新增 models/ 原始资源的登记已完成；关联 G5-T06，模型转换和显示验收尚未开始，T06 仍待 T05 通过。下一张实施卡仍为 G5-T03。

背景与范围：用户说明在 models 目录中增加了后续三维展示所需实体模型。本轮检查并记录输入，不安装转换工具、不转换或改写原件、不启动浏览器或仿真，不改变已通过的 T01／T02 来源和资格。

过程与成果：核查 Git 和目录说明，读取状态、T06 任务卡及最近日志。枚举确认 16 个 OSGB，共 154070891 字节，另有 .DS_Store 目录元数据。逐文件计算完整 SHA-256、读取前 24 字节并检查读取前后的长度及修改时间；16 个模型的头部一致。按文件名推测包含固定翼、直升机、导弹、雷达和发射车，未进行几何解码或视觉确认。未发现独立纹理文件，尚不能判断纹理是否内嵌或是否缺失。

接口与安排：查阅 Cesium Model 官方接口，确认实体模型接入使用 glTF／GLB，将 OSGB 转换、材质／外部资源、尺寸／坐标轴／原点校准及实体资产映射列入 T06；high／lo／icon 的实际内容需另验，UH-60 与 AH-64D 大文件的转换后开销需重点检查。显示资产只影响可视化，不增加对应仿真能力。来源说明、单位和动画等信息留待实际接入时登记。

修改与证据：新增 docs/g5-model-inputs.md，保存全量文件摘要及接入安排；同步 docs/backlog.md 和 docs/status.md 的输入记录，保留 T03 为下一动作；.gitignore 只增加 models 内 OSGB 与 .DS_Store 规则，未来 README 等说明仍可跟踪。原始清单及检查结果在 out/runs/g5-model-input-registration-20260920-180727/inventory.json、check.json。本记录追加到 worklog.md。

实际验证：工作目录为仓库根目录。PowerShell 枚举、Get-FileHash 及既有 Python 的独立复查均退出 0；17 个输入文件长度和摘要一致，16 个模型合计字节数一致；git check-ignore 确认原始输入被忽略、未来 models/README.md 未被忽略；三份文档 137 个本地链接存在、UTF-8 正常，git diff --check 通过。追加日志前记录原文件长度和 SHA-256，追加后复核历史前缀。未执行模型转换／Cesium 运行测试，本次没有此类实现改动；资源登记不等于显示资格。

问题与决定：没有文件读取或摘要检查失败；格式转换可行性、材质完整性和性能仍待实际验证，不能据文件后缀、头部或大小判定模型已经可用。保留用户原始资源，后续转换候选放入 out，由前端资源发布流程纳入运行包。

收尾与下一步：本次输入登记完成，按持续授权仅提交并普通推送文档、忽略规则和本日志，核对远程；不提交原件或 out。归档结果单独保存在本次证据目录。继续实施时先执行 T03；到 T06 再选择合适样本开展转换与姿态校准。


## WL-20260920-016｜G5-T03 地图与本地资源服务

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：用户要求执行 G5-T03；本卡已完成，普通与中文空格路径各一份候选、各十组正式验收及日常入口正常收尾通过。下一卡为 G5-T04，本轮没有启动真实地形仿真或推进 T04。

背景与范围：从已归档的模型登记 18f40da 继续，工作区起始干净。读取 AGENTS、状态、任务卡、T01 前端入口和 T02 地理收据；保持正式 AMASE／UxAS／G4、原始 tiles／models 及旧资格记录。原矢量＋USGS 区域主地形、Copernicus 对照继续生效；区域外明确按未验收参考椭球浏览，不消费异常 Terrarium 或拼接 Copernicus。

工作过程：复查正式后端基线 g3-t01-check-20260920-181642-990，检查 T02 同批候选／独立验收、来源与派生文件、工具和后端绑定。查阅 Cesium 地形／ArcGIS API、PMTiles v3 规范，读取真实 Protomaps 图层属性。Noto CJK 固定官方提交 f8d157532fbfaeda587e826d4cd5b21a49186f7c 的简体字体与 OFL 许可下载到忽略缓存，固定字节数和 SHA-256；读取 Esri 真实元数据并保留来源。未增加 npm 依赖或改变工具锁。

实现与修改：新增 config/g5-map.json、scripts/g5_map 的候选解析和 Node 资源服务，新增 build-g5-map、run-g5-map 与 g5-map.tests Windows 入口。前端工作线程解码 PMTiles／MVT，用本地字体绘制矢量影像；新增区域地形适配器、地形与资格边界开关、区域／近景／全球／北京定位，按选择加载 Esri 并在失败时恢复本地底图。生产 8080，开发 Vite 中间件 5173；只读代理 G4 HTTP／WS 至 8000。T02 高度场直接按同一三角插值派生 65×65 片，不重复垂直改正。页面未接真实消息，后端时间仍显示未连接。

资源上限：PMTiles GET 单段最多 4 MiB，拒绝整包和多段；工作线程 Range／目录各 16 MiB、128／64 项，任务最多 4，解压及要素数量有界。地形服务缓存最多 64 片、1081600 字节，前端并发 8；日志保留 128 条。字体、Cesium 四类资源本地打包。区域外参考椭球不能用作飞行或统计资格，细分采样不等于提高 DEM 精度。

问题与纠正：首次 TypeScript 构建因 Cesium 声明要求丢弃策略等字段而失败，已提供明确实现。首次 Edge 探针因相对 profile 路径退出 21，改为绝对路径后通过。实际截图暴露 ImageBitmap 上下翻转，已按 Cesium 纹理约定翻转一次；北京中文地名和方位复查正常。development-browser-03 刷新时读取已销毁 Viewer，截图异常又跳过 Browser.close，导致该失败轮次强制收尾；修正旧句柄清理、销毁状态识别和独立 Browser.close，后续开发与全部正式轮次均正常退出。TypeScript 7 没有旧 JS 编译 API，独立测试改用锁定 CLI。补丁格式错误及一次文档旧短语断言失败均未部分写入，随后修正并完成。所有开发失败证据保留，不计入正式通过。

实际验证：普通构建 g5-t03-build-20260920-184439-778、中文构建 g5-t03-build-20260920-184747-244 均 passed，各自完整计算 137370745450 字节 PMTiles，匹配 b4c4674234ae75c8fea8a10c23f1cce49626155f56a437eda89b006f1e02b2b7。中文构建从 out/tmp/G5 T03 中文 调用 启动；两次验收从系统临时目录调用，g5-t03-test-20260920-184747-248、g5-t03-test-20260920-185000-639 均通过十组：独立格式／地形／缓存、HTTP Range、HTTP／WS 代理、断网、真实在线与阻断回退、重启、开发页面、缺字体页面拒绝、坏资源启动拒绝、端口冲突与正常退出。所有正式 PowerShell／Python／Node／Edge 退出 0，来源与环境／调用目录核对通过。

独立证据：八个真实 MVT 瓦片与 Python T02 读取器的层／要素／顶点对照一致，超大整数保持字符串。NumPy 独立重心坐标与服务地形采样最大差 5.5706550483591855e-11 m；z8／10／12／14 两方向共边逐节点一致，HTTP 再查 65 个共边。上述为数值一致性，不是测绘精度或不同重采样网格逐位一致。普通矩阵单次 Range 最大 151338 字节、总 Range 12268908 字节，地形缓存实测峰值恰为 1081600 字节；两矩阵未越界。实际 Edge 153.0.4234.32 报告 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11，记录实际渲染器与 JS 堆快照，不视作 T10 性能验收。协议夹具标为 fixture，保留 HTTP 503、WS 1013 和 uint64 字符串，不是真实业务接入证据。

入口复验与收尾：从系统临时目录执行 run-g5-map.ps1，省略 BaselineRunId 取得新基线 g3-t01-check-20260920-185120-206；日常运行 g5-t03-serve-20260920-185119-976 加载中文候选，经 out/runs/g5-t03-native-entry-browser-20260920 实际浏览器复查后创建本次 request-stop，服务和封装正常退出，环境／位置保持。没有所属正式服务留待关闭；未进行 GUI 人工确认或正式前端指针发布。

文档与交接：新增 docs/g5-map-validation.md，更新前端 README、status、backlog、G5 方案、G4 交接、总体计划和 AGENTS。两候选各绑定 33 项来源、400 个发布文件和同一 T02 地形资格；报告登记 manifest／acceptance 摘要及截图。mapResourcesQualified=true，simulationDisplayQualified=false、stageQualified=false。下一步按 T04 独立场景及固定净空规则验证真实地形飞行、传感器和 20 米统计；模型转换归 T06。

最终归档：收尾核对候选来源／资源摘要、UTF-8／本地链接、Python／JavaScript／PowerShell 语法、日志历史字节、忽略规则和 Git 范围，记录在 out/runs/g5-t03-final-20260920。按持续授权仅提交本卡源码、配置、测试及文档，普通推送并核对远程；不提交原数据、模型、字体缓存、工具或 out，不改变既有发布指针。归档命令单次禁用自动 maintenance／gc，保留历史对象。


## WL-20260920-017｜按数据问题与 T01～T03 结果修订 G5 方案

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：用户要求根据之前发现的问题和讨论更新 G5 实施方案；本次文档修订已完成。G5-T01～T03 仍为已完成，T04 可执行，T05～T11 待前置；未启动 T04 或新增运行资格。

背景与范围：从已推送的 T03 提交 61e77dc4ead87d14889ccc1c66fb7927f929b5ee 继续，起始 Git 工作区干净。核查根级说明、状态、任务卡、T01 环境、T02 区域地理、T03 地图报告及模型登记，对照冻结配置和本机候选／验收收据。本轮仅修改规划和协作说明，不安装工具、不改数据或代码、不启动服务、浏览器、仿真和模型转换。

问题与修订：原方案正文仍含“前端尚未创建”“其余接口为规划”和使用旧 Terrarium 的约定；任务清单总览、状态页下一步及 AGENTS 历史入口混有 T03 可执行的过期表述。按真实证据改为 T01～T03 分别完成环境、区域地理和地图资源资格，T03 代理明确为协议夹具，真实业务／时间与后端地形执行仍待后续。历史验收报告和 WL-014～016 原义不改写。

数据与高度决定：落实原全球矢量＋USGS 区域主地形、Copernicus 仅对照，旧 Terrarium 只读归档和异常回归；区域外只按未验收参考椭球浏览。明确下载 verified-manifest 的 terrainQualified=false 是历史下载状态，当前地形资格取 T02 acceptance。固定 WGS84 经纬度／EGM96 正高共同网格、AMASE 整米 DTED 和 Cesium h=H96+N96，一次改正；列明 NAD83／NAVD88 的实际转换链、网格和旧 GEOID99／框架近似限制。源分辨率、标准化与显示间距、DTED 量化、20 米统计栅格、模型不确定性和 1 米数值判据分别说明，不把换源或加密采样写成测绘精度提升。

后续任务落点：T04 先追踪 DTED 负值误读和十倍间距元数据对飞行、传感器、分析的实际影响，必要时按来源规则修复源码、重建复验；无影响的路径仍需证据和区域限制。独立场景保持完整水道及平面几何，冻结 5 公里外围和 100 米仿真净空适配并检查 DTED 量化影响；缺主源、越界、统计或退出失败拒绝。T05／T08 必须补真实接入和故障隔离；T06 转换选用 OSGB 为 glTF 2.0／GLB，校准单位、轴、原点及资产映射，未选用资产不增加仿真能力或场景要求；T10 记录实际模型组合、浏览器／GPU 与各类缓存，T11 完成本地包及本轮人工确认。

工程回归：将已修复的 ImageBitmap 一次翻转、中文和方位、Edge 绝对 profile、刷新销毁旧 Viewer、截图异常仍执行 Browser.close、TypeScript 7 使用实际 CLI、有界资源等按后续修改影响落实到任务卡。地图 ready、参考椭球、截图、夹具、单次堆快照及强制收尾均不扩展为业务或稳定性资格；awaiting-gui 仅用于 T11 实际待人工环节。

修改文件：docs/g5-cesium-display-plan.md 重整为当前方案；docs/backlog.md 同步十一张卡的状态、证据和停止／回退条件；docs/status.md、04.项目总体实施计划与阶段验收.md、AGENTS.md 同步稳定约定，本记录追加至 worklog.md。未修改属于 T03 冻结输入的前端 README 或运行配置，未改写 T01～T03 报告、下载／失败收据和正式指针。

验证与纠正：工作目录为仓库根，git diff --check 通过；既有 Python 3.14.7 运行 out/tmp/g5-plan-revision-check.py，退出 0，检查六份文档 UTF-8、420 个本地链接、33 个章节锚点、十一张卡顺序／状态，复核 T02 的 20 项来源及 T03 两候选各 33 项冻结输入摘要。首次只读定位误将 candidate.json 写成 manifest.json，一次 Python -c 引号传递失败；检查脚本首次还遗漏中文候选的“中文 空格工程”子目录。按实际 acceptance 路径读取后通过，失败检查保留于 out/runs/g5-plan-revision-20260920/check-failure.json，未改动候选或数据。最终结果保存同目录 result.json。运行测试未重跑，因为本次无实现变化；历史工作日志先登记 332420 字节及摘要，追加后再核查此前字节完全保持。

收尾与下一步：按持续授权仅提交这六份文档并普通推送、核对远程，归档记录另存本次 out 证据目录；单次禁用自动 maintenance／gc，不强推、不提交 out／工具／原件。下一张实施任务保持 G5-T04，先执行方案 3.1 节的来源与 DTED 实际调用检查，再进入真实地形小规模任务及统计。


## WL-20260920-018｜G5-T04 真实地形后端资格

时间／时区：2026-09-20，Asia/Shanghai。
关联任务与状态：用户要求执行 G5-T04；本卡已完成，三份独立场景准备、三实体两模式真实 1 倍任务及独立地形／执行／统计验收通过。下一卡 G5-T05 可执行，本轮未启动前端业务接入、原两实体／20 实体显示联调或阶段发布。

背景与范围：从 4050be5be383e067a4a167eceaf766578ca111e0 继续，起始工作区干净。读取根级说明、修订后的 G5 方案、任务卡和 T02／T03 报告，核查实际正式后端、网关、独立环境和源文件。正式基线 g3-t01-check-20260920-193713-108 passed；沿用原矢量＋USGS 区域主地形、Copernicus 对照以及 WGS84／EGM96 共同网格。没有安装或升级工具，没有改动原始数据／模型、正式后端源码／二进制、G4 零高程场景和旧收据。

实现：新增 config/g5-backend-terrain.json、scripts/g5_backend 的独立准备／运行／资格编排和 TerrainProbe、tests/g5_backend 的独立 DTED／执行／统计检查，新增 prepare-g5-backend、run-g5-backend 和 g5-backend.tests PowerShell 入口。复用已有受控初始化、规划、ExecutionProbe 与 CompletionProbe；新的缓存包装器把有效查询委托正式 DTEDCache／DTEDTile，只增加范围拒绝、调用计数和有界证据，实际类来源与正式 JAR 摘要核对。准备候选绑定 37 项输入和 113 个文件；场景、运行配置／DTED 和运行后证据逐项检查，捕获与独立资格分开登记。

场景与高度：保留原两实体、small 三实体与 mixed20 三份副本；完整 90 点水道、实体初始经纬度、矩形几何、20 实体 8 线／6 点／6 区域及固定分配不变。共同包络计入矩形半对角线和外围 5 公里，最高标准化正高 989.7364501953125 米、量化 DTED 990 米；原最低飞行高度 700 米，统一加 390 米，最低配置高度 1090 米、净空 100 米。914 个地面任务点按共同网格双线性赋高并明确 MSL／EGM96，飞行正高、任务高度和椭球改正不重复叠加，逐项原值／新值记录在 adaptation.json。性能上下限、覆盖栅格和任务条件不调整。

源码与实际调用结论：正式 DTED 负值补码误读和十分之一角秒解析扩大十倍的缺陷仍存在；当前非负区域的最近邻／双线性按节点数定位，中心射线步进用固定 3 角秒，不消费错误 dlat／dlon 字段。因此本轮不改正式源码，保留负值拒绝和区域限制。KinematicFlight、CameraControl 与 SearchTaskAnalysis 都在本次运行调用非零地形；分析在导出时重放本轮事件。相机中心射线采样 DTED，四角仍按中心高度的局部平面投影，角点 Altitude 为零，Centerpoint 也不能作为精确地面高程；不扩展为逐角点三维求交或遮挡能力。统计独立重算飞机位置最近邻地面高度，再以正高减地形求 AGL／GSD。

实际执行与验收：准备 g5-t04-prepare-20260920-195013-339、两模式运行 g5-t04-run-20260920-195027-645 和独立验收 g5-t04-test-20260920-201626-019 均 passed。准备、运行从仓库根执行；最终 tests/windows/g5-backend.tests.ps1 从 out/tmp/G5 T04 中文 验收调用 执行，省略 BaselineRunId 取得新基线 g3-t01-check-20260920-201626-239。全部正式 PowerShell／Python 入口退出 0，进程环境及调用目录保持；三类运行各一架，共 72／15／5 个规划航点，正确任务请求、规划分配、实际命令、内部目标航点及移动均独立关联。两模式均在 120410／131640／752969 ms 分别完成区域／点／线任务，继续至 756169 ms 后暂停导出，实体完成后保留终端飞行。

独立数值与统计：全量 DTED 节点与共同 float32 网格量化误差最大 0.5 米，共边 1201 点一致；六项准备边界／负值拒绝通过。两模式实际采样最低净空均 340 米，规划与飞行均在合格区域；每模式独立核对 124 个最近邻样本和 40 个中心射线，最大重建数值差 7.909761734481435e-10 米，不解释为测绘或算法几何精度。Headless／Gui 分别记录 103058／103005 次地形查询，中心射线各 4279 次，飞行、相机、分析调用均有计数。20 米线覆盖均 724／724、区域均 1250／1250，点观察分别 203.202／204.811 秒，与原生 203.20／204.81 秒报告及逐格明细一致。每模式按自身真实采样窗口复算；联合覆盖含非指定实体附带观察，指定执行实体贡献另列，不把联合观察当作该实体执行证据，也不设效果门槛。

负向检查与问题：out/runs/g5-t04-negative-20260920-02 的独立 Java 驱动直接调用本次已编译包装器，八项越界／NaN／双线性边界／缺图／负高程拒绝通过；只在隔离副本中减少一个 SeenCells，独立统计明确拒绝。首次独立驱动漏建 SimTimer 导致空指针，补齐正常计时器初始化后通过；原失败输出保留在不带 -02 的目录，正式运行和冻结源码未修改。早期一次只读定位使用了错误的生成 Java 路径，按实际布局重读；正式候选准备、两模式运行及最终验收没有失败。无界面结束后先独立检查其执行和统计，再由完整入口同时核对两模式，预检不替代 acceptance。

退出与交接：每模式 AMASE／UxAS 都退出 0、无强制终止，TCP 尾部为空、端口释放；没有遗留本卡正式服务。候选 SHA256=b93d4bd70f269521987e7df7915f178d8ec3f414d2df8e92c7c7aac26e6f91af，acceptance SHA256=7db8a7c587606cac8353acc49b3bdd20126e860c81915e3329d1a4a4d4f1d60a。backendTerrainExecutionQualified=true 仅由独立验收登记，stageQualified=false；T09／T10 完整显示联调、T11 本轮 Cesium 人工确认和正式前端指针仍待后续。

文档与收尾：新增 docs/g5-backend-terrain-validation.md，同步状态、任务清单、G5 方案、G4→G5 交接、总体计划和 AGENTS。核对八份文档 UTF-8／本地链接、六份 Python 和四份 PowerShell 语法、运行来源／编译类摘要、T02／T03 冻结输入和 Git 差异；结果保存 out/runs/g5-t04-final-20260920。追加前记录工作日志 337127 字节及 SHA256=10f69b2800f8e669ba0a64c6ab2125d9717d08cde99bdc1ba7842e94ac60990e，追加后复核此前字节完整保持。按持续授权仅提交本卡源码、配置、测试及文档，普通推送并核对远程；不提交工具、原件或 out，归档命令单次禁用自动 maintenance／gc。下一步执行 G5-T05，使用本卡合格场景、地形和正式后端接入真实 G4 协议。


## WL-20260921-001｜G5-T05 前端状态与真实接入

时间／时区：2026-09-21，Asia/Shanghai。
关联任务与状态：用户要求执行 G5-T05；本卡已完成，32 项协议／缓存检查及两模式真实首次接入、晚加入、刷新、独立状态对照和正常退出通过。下一卡为 G5-T06；本轮未实现实体模型、姿态、任务几何或仿真控制，未进行 G5 最终发布。

背景与范围：从已推送的 1c9adec383b2127e52b6b869cdfe37a9163d2a6c 继续，起始工作区干净。读取根级约定、状态、任务卡、G4 浏览器契约及 T03／T04 实际源码与收据，重新运行正式基线 g3-t01-check-20260921-090551-401 通过。保留原矢量、USGS 主源、Copernicus 对照和同源高度链，使用 T04 small 三实体地形副本与正式 AMASE／UxAS／G4；没有安装工具、升级依赖、转换模型、修改原始数据或后端源码。

实现与来源决定：新增 apps/cesium_state 的 TypeScript 类型、归约器、只读连接和中文状态面板，config/g5-state.json 固定协议与预算，scripts/g5_state 提供独立构建／页面服务／真实验收／联合会话，tests/g5_state 保存 Node 反例和独立浏览器检查；增加四个 scripts/windows 入口与 tests/windows/g5-state.tests.ps1。T03 来源递归绑定 apps/cesium_viewer，T04 又绑定 T03，因此新模块独立保存，复制合格地图生产资源进入新的 T05 候选，用锁定 TypeScript 7 CLI 编译并加入模块入口，不改写旧候选或收据。T05 的 22 项输入及新候选同时绑定 T03、T04、地形和正式后端／网关；原正式指针保持。

状态与时间：每个 WS 必须先快照再连续增量，运行／流隔离，ID／时间／序号保持字符串并用 BigInt 比较；完整 upsert、显式 delete，包内变更检查后统一提交。未知版本、旧流、重复／乱序／缺口、非法结构或超限清空业务和样本，重连重新积累；最后时间只作为过期值显示。地图就绪和业务同步独立，health 的 ready、身份与 freshness 都参与判断；不通过 HTTP snapshot 拼接 WS，不发送业务或控制包。时间来自后端且不外推。命令收到、观察到执行与后端完成分开保留，完成不删除实体、任务或完整规划。

缓存边界：单包 8 MiB，各集合 4096 项，业务 JSON 总计 32 MiB；这不是 JavaScript 堆上限。轨迹至多 128 实体，每实体 2048 点／10 分钟，只保留固定位置／姿态字段，从增量积累并按权威时钟清除过期样本，包括停止更新的实体。完整规划独立保存，不随分段命令缩短。只读 inspect 接口返回副本，供独立验收和后续显示接入。

开发失败与处理：首轮构建 g5-t05-build-20260921-091704-065 因 reducer.mjs 测试脚本缺少结束括号失败，修复后重新构建。091732-681 候选的 091740-289 业务验收通过，但真实截图暴露后台页刷新后地图尚未绘制，不能据此登记完整页面证据。加入地图就绪条件后，092050-181 候选的 092114-113 验收在刷新等待失败，业务仍有连续消息；正常收尾并保存完整 timeout-state。将受检页 Page.bringToFront 后再核查地图／业务，092418-643／092438-070 候选的 092440-382 运行中无界面检查通过，但共享 Node 服务因未处理客户端 Socket ECONNRESET 崩溃，GUI 接入失败。T05 新增 server.mjs 包装器，对客户端错误隔离、有界计数，并在两模式之间核查服务存活、主动注入六次 HTTP 重置；未改冻结的 T03 源码。期间页面服务 092519-243 曾正常启动／退出、092552-875 端口冲突明确拒绝，均保留原记录，不作为最终实现的正式资格。所有失败批次保持，最终新候选重新完成全矩阵。

最终构建与命令：工作目录为仓库根，使用明确的 Python 3.14.7 路径调用 build-g5-state.ps1，中文候选 g5-t05-build-20260921-092811-412、普通候选 g5-t05-build-20260921-092813-768 均退出 0。各绑定 22 项输入、412 个候选文件，405 个页面资源跨两类路径逐文件摘要一致；两次独立 Node 32 项检查通过。candidate SHA256 分别为 2bcbca43797c20c919d48299700024e5f28d3119cff0bcce3127711e3be0ffca、2e3bd530a7b20d5009e0d569d522a1cab3d057b855897145d81558d74d5fd471。最终 tests/windows/g5-state.tests.ps1 使用中文候选，从 out/tmp/G5 T05 中文 调用 执行，编号 g5-t05-test-20260921-092825-217；result／entry-result／runtime-result／acceptance 全部 passed，acceptance SHA256=61742cefe87d42c7d17e92044eea68e6cecc86f71b9899e4f720cf1a451fc0b1。

真实证据与结果：两模式均以正式 G4 接收真实后端，浏览器先经历未就绪 HTTP 503，再首次接入、晚加入第二页、刷新第一页。独立 Python 以 CDP 捕获帧重放，在同序号边界逐集合核对页面；Headless 首次／晚加入／刷新重放 10／9／27 帧，Gui 为 14／9／32 帧。后端实际暂停后两个页面均与独立 HTTP 快照全量一致：Headless 序号 90／9259 ms、业务 JSON 69877 字节；Gui 序号 82／8199 ms、69878 字节，刷新后各实体轨迹两点。独立 HTTP 查询属于验收进程，页面没有 snapshot 请求或 WS 业务发送。网关关闭后浏览器清空对象／样本并标记最后时间过期，暂停后端仍存活；真实 1013／观察恢复／网关重启全矩阵未在本卡执行，1013 仅由独立连接反例验证。

地图、地形与退出：Edge 153.0.4234.32 报告 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11，本地地图／地形／字体实际加载并保留截图，Gui real-paused.png 已查看。页面只有真实状态与计数，尚无实体绘制；没有 FPS、全堆内存或长时稳定性结论。两模式地形查询分别 1617／1417 次，飞行、相机、分析仍消费非零合格 DTED；本轮为短程状态接入，不冒充 T04 全任务复验。最终共享服务隔离 8 次 ECONNRESET，含主动六次重置，持续提供页面并正常关闭。两模式 AMASE／UxAS、G4、Edge 和 Node 均正常退出，无强制终止，端口释放。

联合入口：普通候选使用 start-g5-state-session.ps1 -Mode Headless 从中文空格工作目录启动，省略 BaselineRunId 自动取得 g3-t01-check-20260921-092942-549；会话 g5-t05-session-20260921-092942-332 的真实 health／snapshot 为 ready，三实体／三任务齐全，实体 400 正高 1090 米。运行中再启动页面服务 g5-t05-serve-20260921-093122-031，EADDRINUSE 明确失败，已有后端及页面仍可用。根目录 request-stop 正常结束本组；本轮验证的是启动和提前结束，自动等三任务完成后三秒暂停保留的分支未另跑一轮全程。联合入口没有自动观察恢复，完整生命周期归 T08；页面独立入口不自动授予外部后端地形资格。

最终核对与文档：新增 docs/g5-state-validation.md，同步 AGENTS、状态、任务清单、G5 方案、G4 交接和总体计划。out/runs/g5-t05-final-20260921 保存最终审计：复查当前正式资格、两候选所有来源和文件，T02 的 20 项、T03 两候选各 33 项、T04 的 37 项冻结输入一致；两模式各 503 项及联合入口 119 项证据、实际编译插件摘要一致。四份 Python、两份 JavaScript、五份 PowerShell 语法及九份文档 UTF-8／本地链接通过。审计辅助脚本首次将历史大写哈希交给大小写敏感比较而误拒绝，改为独立规范化比较；又将 service.stderr 误写为带 .log 的文件名，按实际目录更正后通过，候选和证据未改写。早期少量只读路径定位错误已按实际文件纠正。收尾没有本轮所属进程或端口监听；git diff --check 通过。日志追加前为 343819 字节、SHA256=ad88fc19e4779e4e3edf9ba103f1fecf200f370dea9bc232be77f4c56d64128f，追加后复查该历史字节前缀保持。

收尾与下一步：只登记 realStateConnectionQualified=true，simulationDisplayQualified=false、stageQualified=false；T06 模型／姿态／轨迹，T07 几何，T08 恢复矩阵，T09／T10 完整显示联调和 T11 人工确认／正式发布继续按顺序推进。按持续授权仅提交本卡源码、配置、测试及文档，普通推送并核对远程；单次禁用自动 maintenance／gc，归档结果另存本次 out 目录，不提交工具、原件和运行输出。下一张实施任务为 G5-T06。

## WL-20260921-002｜G5-T06 实体、姿态与时间及模型尺寸快捷键

时间／时区：2026-09-21，Asia/Shanghai；关联任务与状态：G5-T06 已完成，entityDisplayQualified=true、stageQualified=false。背景与范围：继续用户已授权的实体模型／姿态／轨迹／视角及后端时间工作；用户说明 models 原件来自 AFSIM，确认 1 源单位＝1 显示米，并要求快捷键调整大小。中途用户释放 E 盘空间后恢复当前工作，不进入 T07。起点 HEAD=106da371f7d81910707bad0bba9c23195d5d4176，工作区原为干净；未删除旧包、模型、数据或历史 Git 对象。

环境与模型过程：未找到可直接使用的 osgconv／assimp／Blender。下载官方 OpenSceneGraph 3.6.5 源码，SHA256=aea196550f02974d6d09291c5d83b51ca6a03b3767e234a8c0e21322927d1e12，SHA512 与固定 vcpkg osg 配方核对；解包后逐文件对比原归档。以现有合格 MSVC v143／CMake／Ninja 在项目目录构建受限静态 OSGB 读取器，原上游源不改写，输出目录及工具均忽略，过程环境恢复。工具记录原 primitive 模式并使用官方 Tessellator 处理多边形，绑定构建前后输入一致；当前工具 g5-t06-model-tools-20260921-100741-808，manifest SHA256=f70548a479a10d1d7259ffe1bd65f248a82c7b128be0c3a68247e416351b75c1，后续 VerifyOnly g5-t06-model-tools-20260921-102849-073 passed。

故障与处理：首轮 PowerShell 将原生 CMake stderr 警告当成异常，修正为检查原生命令实际退出码；第二轮原生编译在第 35/413 步因 E 盘空间耗尽失败。用户询问原因时只读检查，曾只剩约 0.9 MB；用户随后自行释放空间，恢复后可用约 1.41 TB。清理后目录扫描见 tiles 约 247.9 GB、tiles.zip 136.8 GB、.git 128.5 GB（含约 10.5 GB tmp_pack）、out 54.0 GB、.tools 6.4 GB；扫描不含回收站／系统目录，不能据事后大小确定用户删除了什么或清理前增长归因。只对本轮 .tools/g5-models 做了保字节压缩，没有删旧数据或运行 git gc，也未迁移到 F 盘。恢复后完整 413 步原生构建通过，失败批次保留。

选用资产：实际解码 models/ucav_lo.osgb，原件 SHA256=ffb83ce6da482060a65a2eff5ef508e29398c34a1b2c9fc7e7507393cf2676c7；三视图检查 nose=−Y、up=+Z，原点保留且不声称重心。1 网格、1776 三角形、5328 展开顶点、128×128 RGBA 内嵌纹理；透明度保留，无外部资源或动画。源尺寸 18.961639×11.781900×1.911730 个单位，按用户决定显示为米，不认定为真实机型精度或修改仿真性能。资产采用 (x,y,z)→(x,z,−y) 正旋转，结合实际 Cesium 轴修正；独立 GLB 检查几何、法线、UV、PNG 原点／CRC／像素及材质。生成 188488 字节 ucav.glb，SHA256=4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51；只选这一资产，其余 15 个维持登记。来源声明不是再分发许可，后续打包按 T11 核查。

实现及修改文件：新增 apps/cesium_entities、scripts/g5_entities、scripts/g5_models、tests/g5_entities、config/g5-entities.json／g5-model-toolchain.json 与五个 Windows 入口。T06 把未经改动的 T03 地图源码、T05 状态类及实体模块复制到独立候选，锁定依赖重新构建为单一 Cesium 包，不修改前置来源或收据。中文实体列表／详情、模型／标签／实际轨迹、定位／跟随／复位和图层开关落地。默认模型 1×、minimumPixelSize=0，+ 或 = 放大、- 缩小、0 复位，步长 √2、范围 0.25～32×；倍率仅改变显示，刷新复原，输入控件不触发。实体 MSL／EGM96 通过同一冻结改正网格转换一次到 WGS84 椭球高；未知基准／越界／非有限值拒绝。后端时间减 1 秒在已收样本内插值，暂停保持、不墙钟外推；2048 个源样本／10 分钟，加至多一个显示端点，快照不虚构历史，完成保留实体。

构建调试：g5-t06-build-20260921-101726-401 因字体目标目录已存在失败；102100-627 因 Cesium pack 缺输出数组失败；102131-481 因 TypeScript 7 显式文件编译需 ignoreConfig 失败；102455-390 独立 NodeNext 编译因类型导入缺 .js 失败。各问题按实际输出修正，后续使用新批次，不覆盖旧失败。初次真实 g5-t06-test-20260921-102213-967 已通过加载／移动／暂停／交互，却在刷新时发现 T03 beforeunload 先销毁 Viewer、实体层仍访问 trackedEntity。改为实体捕获阶段 beforeunload 先清理并撤销检查入口，pagehide 保留幂等保护，已销毁 Viewer 防御检查；未更改旧地图来源。另修正独立回放探针对刷新同一运行／同序号时选取旧连接的问题，只检查当前最新 WS 请求。

最终构建与数值：中文 g5-t06-build-20260921-102511-629（candidate SHA256=ebc0a41e05a60c05d56b9915139e23609d9b6393cabad9ed418bf07a29253715），普通 g5-t06-build-20260921-102716-695（e14d201adbde6f3963eb22c3d828bbfeb6dd14590acc770756b8cb1fcf4adc25）；各绑定 20 项 T06 输入／455 个文件，405 个生产文件逐项同字节。85 个 PROJ 独立 ECEF 点最大差 2.134e-9 米，EGM96 差 5.330e-14 米；正式 AMASE JAR Euler 的 10 组四向／俯仰／滚转／混合姿态最大差 1.208e-6 度。五类高度／位置拒绝、六类插值边界和超大时间保留通过；九项隔离生命周期检查验证删除清理选中／跟随、完成保留、恢复清空、未知高度拒绝和暂停冻结。数值误差不代表源 DEM 绝对精度。

真实两模式及地图回归：根目录 tests/windows/g5-entities.tests.ps1 -PythonExecutable <3.14.7> -BaselineRunId g3-t01-check-20260921-094741-945 -BuildRunId g5-t06-build-20260921-102511-629，运行 g5-t06-test-20260921-102534-000 passed、入口退出 0；acceptance SHA256=1f8fd81b8df17e41fbbc91953a6c955a07ed706d43ad93638f004e8b2fcbf0b0。Headless／Gui 均使用真实 T04 三实体地形和正式 G4，实际加载模型／纹理并移动，运行／暂停／刷新与真实收到的同连接帧独立对照，位置最大差 1.614e-9 米。暂停分别 10609／10039 ms，各实体 10 样本，时间／姿态／位置冻结；快捷键实际 primitive.scale、0.25／32 上下界、复位与位置姿态不变通过。刷新从新快照重建且零历史，断网关清空对象与跟随，没有浏览器 WS 业务发送。六个暂停模型的渲染矩阵列与四元数差最多 3.331e-16，复核收据 matrix-review.json。定位、跟随、复位、实体／标签／轨迹开关通过，真实暂停截图已查看。

实际浏览器 Edge 153.0.4234.48，renderer 报告 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11；每模型引擎纹理统计 87381 字节含 mipmap，不等于总显存。未做完整 FPS／内存或长时资格。重新打包后的原 T03 离线浏览器探针通过中文标签、地形、卫星失败回退、全球浏览和刷新；HTTP Range 四类、24 并发、六类范围拒绝、65 个公共边界节点、有界缓存、8080 冲突、客户端不完整请求重置及正常退出通过。两模式 AMASE／UxAS、网关、Edge 与 Node 均正常退出，无强制终止，端口释放。

日常入口与拒绝：start-g5-entities-session.ps1 使用普通候选，从 out/tmp/G5 T06 中文入口 启动 Headless，省略 BaselineRunId 自动取得 g3-t01-check-20260921-102742-708；会话 g5-t06-session-20260921-102742-495 的真实代理 health ready、三实体快照和 GLB 字节摘要正确，创建该运行 request-stop 后整组正常退出。仅验证启动及提前收尾，未把自动等待全部完成后暂停分支声明为本轮全程显示资格。隔离反例 g5-t06-model-rejection-20260921-102900 验证无效 OSGB 返回 1、候选使用的文件校验函数拒绝缺失／损坏 GLB，原件和合格包保持。

文档与收尾：新增 docs/g5-entities-validation.md，同步状态、任务卡、G5 方案、AGENTS、总体计划和 G4→G5 交接，下一卡为 T07。最终审计 final-audit.json 重新核对前置、候选输入／产物、405 个生产文件一致和所属端口释放，Python／PowerShell 语法、UTF-8／本地文档链接及 git diff --check 通过。只读定位少量路径名与日志名按实际目录纠正；文档补充脚本一次因 PowerShell 管道将中文变为问号而在断言处停止，无文件修改，改为 UTF-8 文件执行。日志追加前 352322 字节、SHA256=b613ec81a638f43b039d88b11e1ee7f2003f4f4a6c2413faef92d1f570f1e6c8，追加后保留该完整字节前缀。

重要决定及下一步：默认模型尺寸严格按用户约定，按键缩放只影响显示；AMASE／G4 协议、原件、地理数据、正式指针和全部旧资格保持。T07 完整航线／任务／区域，T08 恢复矩阵，T09／T10 全程及稳定性，T11 人工确认／正常退出／正式发布仍按顺序推进。本次完成后按持续授权提交本卡源代码、配置、测试和文档，普通推送并核对远程；单次禁用自动 maintenance／gc，Git 归档结果保存在本轮 out 收据，不提交原件、工具或运行输出。

## WL-20260921-003｜启动 G5 当前成果预览

时间／时区：2026-09-21，Asia/Shanghai；关联任务与状态：G5-T06 已合格成果的日常展示，预览启动完成，仿真保留运行供用户查看。用户询问当前可见成果；本次只启动已验收普通候选，不修改业务源码、不进入 T07，也不登记 T11 人工确认或新全程资格。起点 abda34c479c3ebfdef83e2f844cdc14a4f15a249，git status 干净。

过程与验证：先核对入口、报告与端口，使用 start-g5-entities-session.ps1 -PythonExecutable <已核查 Python 3.14.7> -BuildRunId g5-t06-build-20260921-102716-695 -Mode Headless，从根目录以隐藏 PowerShell 启动。入口自动复查正式来源。启动器记录 out/runs/g5-preview-launch-20260921-104249-617/launch.json，PID 27724；真实会话 g5-t06-session-20260921-104249-844。通过 8080 同源代理实际查询 health ready=true、status=live；snapshot 三实体齐全，核查时 simulation_time_ms=18910、state=Running。入口与仿真仍存活，未写 request-stop，未取得退出结果。

问题与处理：尝试启动带本机调试端口的可见 Edge 预览窗口时，工具自动审批在执行前拒绝，返回 blocked by policy，未给出更具体原因；本次没有打开该浏览器窗口，也没有绕过拒绝重试。已明确告知用户，改为提供 http://127.0.0.1:8080 供直接点击打开，保留已正常运行的后端服务。没有把 API 就绪写成此次浏览器实际渲染验收，页面渲染资格仍引用 T06 的真实双模式记录。

可查看内容与交接：当前本地矢量／USGS 地形、三实体模型、姿态、标签、实际轨迹、详情、后端时间及连接状态可用；选实体 400 后点定位／跟随查看近景，+／= 放大、- 缩小、0 恢复原尺寸。完整航线／任务／区域图层仍归 T07。页面关闭不会自动停止后端；结束本次会话应创建 out/runs/g5-t06-session-20260921-104249-844/request-stop 并核查正常退出。模型、地理数据和正式指针未改动，只追加本条日志；按持续授权提交／普通推送文档记录，归档回执保留于本次预览 out 目录。


## WL-20260921-004｜G5-T06 阵营荧光色、模型描边及控制需求交接

时间／时区：2026-09-21，Asia/Shanghai。关联任务与状态：G5-T06 显示体验修正已完成，两模式真实浏览器及地图回归通过；新预览保留运行，尚未结束。用户反馈模型发黑，要求不同阵营使用不同荧光色并勾边；用户明确 400／500 蓝方、600 红方。本轮起点 8d94d7f0b67886b5dcb822dfc1d159e523da9e82，初始工作区干净；不进入 T07 或控制实现。

工作过程：先正常结束 WL-20260921-003 留下的 g5-t06-session-20260921-104249-844，result／entry-result／runtime-result 均 passed，端口释放后才修改来源。核查实际 G4 快照，三个实体的 EntityConfiguration.Affiliation 都为 Unknown；因此采用用户指定的显示缺省映射，保持原消息／场景及 G4 v1。查看锁定 Cesium 实现与 ModelGraphics 接口，用 ColorBlendMode.REPLACE 替换受深色材质／光照影响的最终颜色，配合 silhouetteColor／silhouetteSize；不修改原 OSGB、GLB、资产尺寸或姿态转换。

成果与文件：新增 apps/cesium_entities/affiliation.ts，修改 layer.ts、main.ts、style.css、README.md、config/g5-entities.json、scripts/g5_entities/manage.py 及两项测试。蓝方 #00e5ff、红方 #ff4265、中立 #66ff5e、未知 #ffe94a；普通轮廓深色 2 像素，选中近白色 3 像素。模型、点、标签、轨迹及实体列表同色，图例包含文字，详情区说明阵营来源。明确的后端阵营优先；仅 Unknown／缺失时使用用户指定，未配置的其他名称保留并提示，不推断友敌。新增 docs/g5-affiliation-display-validation.md，同步状态、任务卡、G5 方案、原 T06 报告交接、总体计划、AGENTS 与 G4→G5 交接。

构建与验证：工作目录仓库根；使用已核查 Python 3.14.7、固定 Node 24.21.0／Cesium 1.145.0／模型工具。入口为 build-g5-entities.ps1 -PythonExecutable <已核查解释器> -BaselineRunId g3-t01-check-20260921-111528-272，分别普通路径和 -ChinesePath；独立入口 tests/windows/g5-entities.tests.ps1 使用中文 BuildRunId 和同一基线。最终中文候选 g5-t06-build-20260921-111807-770、普通候选 g5-t06-build-20260921-112011-442；两模式／地图验收 g5-t06-test-20260921-111838-512，result／entry-result／runtime-result／acceptance 均 passed，退出码 0。acceptance SHA256=e787689f2dee7c49b3214616851bb1e70f6223e91610be0bda2f09c84005656b。

实际检查：原 85 个位置、10 组正式 AMASE 姿态、五类拒绝、六类插值及九项生命周期继续通过；新增五组阵营检查覆盖用户缺省、后端别名、未知／未配置名称、明确后端值优先、动态变色及选择轮廓。Headless／Gui 真实三实体的 Cesium primitive 均核实颜色、REPLACE、独立 silhouette ID 和 2／3 像素轮廓；选中 600 后 400 恢复普通边缘，快照仍为 Unknown。已查看蓝方／红方暂停近景截图，实际模型鲜亮且白色边缘可见。两模式暂停时间 11239／10239 ms；定位、跟随、复位、大小快捷键、图层、刷新零历史、断流清理及所属进程正常退出通过。Edge 153.0.4234.48，实际 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11。离线地图、Range、缓存、接缝、端口冲突及客户端重置回归通过；不扩展为全程任务或长时性能资格。

问题与纠正：首候选 g5-t06-build-20260921-111528-054 构建通过，首验收 g5-t06-test-20260921-111646-220 在切换选择后立即读取 primitive，仍读到上一帧 2 像素；实体属性已为 3，实际普通轮廓正常。测试改为等待引擎应用所需帧后严格核对，再新建候选完整复验通过；不放宽判据，失败收据和正常清理记录保留。最终临时审计脚本首次导入产生 11 个未跟踪 pyc，导致文本扫描拒绝；只清理这次生成的明确文件，并开启 -B／dont_write_bytecode 后复核。脚本还曾错误把预览 result.json 的存在当成已结束，核查其 status=running 和存活 PID 后改为检查状态及尚无 entry-result；这些是审计脚本问题，不是业务程序故障。最终 UTF-8、351 个本地文档链接、Python 语法和 git diff --check 通过。

来源与预览：final-audit.json 复核前置、21 项 T06 输入和候选全部文件，普通／中文候选 405 个生产文件摘要完全一致。保持正式后端、网关、T02～T05 输入／收据、原始模型和地理数据。隐藏入口 start-g5-entities-session.ps1 使用新普通候选及上述基线，启动器 out/runs/g5-color-preview-20260921-112138-326/launch.json、PID 14248，新会话 g5-t06-session-20260921-112138-562；8080 代理 health ready=true，运行配置含用户阵营映射，保留供用户刷新查看。没有再次尝试此前被拒绝的可见 Edge 调试启动。新预览仍运行，不将启动就绪写成正常退出或 T11 人工确认；关闭应创建该会话 request-stop 并复查收尾。

范围决定与下一步：工作中用户指出缺少控制按钮、时间进度条及倍速，随后明确“先完成配色，控制按后续任务实施”，进度条只需“显示当前仿真时间和进度”。已将开始／暂停、倍速及只读时间进度条记入 G6；进度依据真实后端时间和已确认场景时长，总时长无效时不伪造百分比。历史拖动回放继续归 G7，不纳入该进度条需求。只读源码确认 SimTimer.go／pause／setRealtimeMultiple 存在，未新增远程控制接口、不声明控制已通过。下一张实施卡仍为 G5-T07，正式前端发布仍归 T11。

归档：追加前 worklog 为 363668 字节、SHA256=c3bd669be8d96844016d0d8d008ca93ad5803b594d39e25a941b16815f94dc72，保留完整历史字节前缀。本轮按持续授权提交源码、配置、测试和文档并普通推送，单次关闭自动 maintenance／gc，归档回执保存在本轮 out；不提交模型、工具、原始运行记录或临时审计脚本。


## WL-20260921-005｜G5-T06 固定屏幕尺寸与立体明暗

时间／时区：2026-09-21，Asia/Shanghai；关联任务与状态：G5-T06 显示修正完成，两模式及地图回归通过，新预览保留运行。用户要求模型固定显示尺寸、可人为缩放、远距保持轮廓而不缩成点，并指出荧光纯色缺少光影和立体感。起点 787ccdb85533ef9e6392aa42094170501b65c727，工作区干净；继续 T06 显示改进，不进入 T07 或 G6。

调查与决定：源码确认原模型 minimumPixelSize=0，按米制随距离缩小，1200 米以外还有 7 像素点；前轮 ColorBlendMode.REPLACE 在光照阶段后覆盖最终颜色，解释纯色平面感。查锁定 Cesium 1.145.0 投影、ModelGraphics、CustomShader 与着色阶段源码，并核对官方文档。选择真正的固定屏幕尺寸，而不只加 minimumPixelSize 下限：用 CSS 视口、相机深度和投影像素尺度生成 CallbackProperty 显示倍率；默认包围直径 96 CSS px，+／= 与 - 按 √2 步长调节 48～384 px，0 复位，刷新默认。旧 0.25～32× 米制显示范围由本次用户需要替换；资产换算仍为 1 单位＝1 米，不改变位置／姿态／仿真性能。移除点替代，屏幕符号不能用于真实占地、净空或碰撞判断。

实现与文件：新增 apps/cesium_entities/display.ts，用真实模型法线、观察方向和固定视角主光／补光／高光计算明暗，并保留有亮度下限的原面板／舱盖纹理细节；HIGHLIGHT 叠加阵营色，蓝／红／中立／未知、用户 400／500 蓝方和 600 红方、描边及来源优先级保持。UNLIT 仅防止 Cesium 对自算光照再次叠加太阳照明，不是平面纯色；不声称真实太阳或地面投影。原 OSGB／GLB 字节未变。共享着色器随实体层释放，恢复不按实体积累资源。修改 layer.ts、main.ts、README、config/g5-entities.json、scripts/g5_entities/manage.py；新增 tests/g5_entities/screen.py，更新真实浏览器和生命周期探针。

运行前收尾：在源码变化前向 g5-t06-session-20260921-112138-562 写 request-stop，等待所属启动器 PID 14248 结束，result／entry-result／runtime-result 均 passed；不修改旧收据。之后从仓库根运行 build-g5-entities.ps1 -PythonExecutable <已核查 Python 3.14.7> -ChinesePath，自动取得基线 g3-t01-check-20260921-121235-215；继续构建／测试显式复用此编号，使用固定 Node／Cesium／OSG，不安装新工具。

失败与修正：首中文候选 g5-t06-build-20260921-121235-001 构建 passed，首验收 g5-t06-test-20260921-121341-139 在截图亮度跨度检查失败，进程正常收尾。三个距离的尺寸和缩放已通过，但放大模型像素第 90 与第 10 百分位强度差仅 7，截图仍偏平。将正面主光改为侧上方斜射，降低环境补光并保留有下限的原纹理明暗，未降低本轮独立检查的 18 阈值；该阈值只是本次视觉回归，不新增 G5 全局性能／光学精度门槛。修改后新建候选和完整复验，失败证据保留。一次批量补丁因 README 原文不匹配被整体拒绝，读取实际段落后重新应用，没有产生部分业务修改。

最终候选及资格：中文 g5-t06-build-20260921-121522-226、普通 g5-t06-build-20260921-121650-265，入口退出 0；tests/windows/g5-entities.tests.ps1 -PythonExecutable <已核查解释器> -BuildRunId g5-t06-build-20260921-121522-226 -BaselineRunId g3-t01-check-20260921-121235-215 对应 g5-t06-test-20260921-121533-381，result／entry-result／runtime-result／acceptance 均 passed、退出 0。acceptance SHA256=d713e71d2ec803c68523e24097c1331f7fbfa3652a43078e1fb984091cffa469；当前来源 23 项，普通／中文候选 405 个生产文件摘要完全一致，原 GLB SHA256 仍为 4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51。

实际浏览器证据：关闭标签／轨迹后由标准库 PNG 独立解码、过滤模型阵营色并取场景中心最大连续像素区；约 160 米／2 公里／50 公里三个观察距离，Headless 均 72×38，Gui 均 69×41 像素。两次 + 后分别为 144×77／136×81，符合两倍；两个运行姿态不同造成尺寸差异，实际可见轮廓不等于 96 像素包围直径。放大样本强度跨度 33／37、92／87 个强度等级；已查看蓝方放大和红方默认截图，舱盖、面板及机身明暗可见。全部模型均无 point fallback，真实 HIGHLIGHT、自定义光照及 2／3 像素轮廓生效。该测量不等于物理光学精度，也不单独证明所有设备／DPI 或所有视角。

回归：原 85 个位置、10 组姿态、拒绝／插值、九项生命周期及五组阵营行为通过；新增共享着色器正常释放检查。缩放不改变后端位置／姿态，暂停后相机变化仍按显示投影更新，暂停时间 Headless 11309／Gui 10309 ms。定位、跟随、复位、图层、快捷键上下限、刷新零历史、断流清理正常。离线地图、Range、缓存、接缝、端口冲突和客户端重置通过；全部测试所属 AMASE／UxAS、网关、Node、Edge 正常退出，浏览器无强制终止。实际 Edge 153.0.4234.48，renderer 为 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11；不扩展长时性能或完整任务资格。

文档与新预览：新增 docs/g5-model-display-validation.md，同步状态、任务卡、G5 方案、AGENTS、总体计划和 G4 交接；原 T06 与前轮配色报告增加当前版本入口，历史数据保持。旧预览已正常关闭；新普通候选通过隐藏入口 start-g5-entities-session.ps1 -Mode Headless 启动，启动器 out/runs/g5-model-display-preview-20260921-121834-745/launch.json、PID 5736，会话 g5-t06-session-20260921-121834-998。实际 8080 代理 ready=true，运行配置为 96 px／0.5～4×，保留供用户刷新查看；尚未结束，不登记其正常退出或本轮人工确认。正常结束仍使用该会话 request-stop。

收尾与下一步：final-audit.json 复核当前前置、候选输入／产物、405 文件一致、原件／GLB、UTF-8、Python 语法及文档链接；git diff --check 通过。日志追加前 369746 字节、SHA256=d27681225e2981fa972b2a24624fda2898bdcb85c74c885ced83070b239c3cd9，保留完整历史字节前缀。按持续授权提交并普通推送本轮改动，关闭自动 maintenance／gc，归档收据保存在 out。正式指针不变；下一卡仍为 G5-T07，控制按钮／倍速／只读时间进度条按已明确决定归 G6，历史拖动归 G7。


## WL-20260921-006｜G5-T06 实体逐帧平滑移动

时间／时区：2026-09-21，Asia/Shanghai；关联任务与状态：T06 移动显示修正完成，普通／中文构建、两模式真实浏览器及地图回归通过，新预览保留运行。用户反馈实体移动一顿一顿；起点 382374418d030753b64aaa7a8252ad2e7d557328，工作区干净。本轮不进入 T07 或控制实现。

原因取证：真实 G4 原始帧显示 400／500／600 的位置样本间隔为 530／540 仿真毫秒，1 倍运行约每半秒一条；原层虽调用 interpolate，但只在收消息或操作时更新 ConstantPositionProperty，两包之间模型保持不动。检查固定 Cesium 1.145.0 CustomDataSource、DataSourceDisplay 实现和官方公开接口后，采用本实体层拥有的 CustomDataSource.update，在可视对象与跟随相机前逐帧更新；不新增永久 RAF 或修改 G4 推送率。

实现：新增 apps/cesium_entities/playback.ts，以后端时间减 1 秒初始化 BigInt 显示游标，本地单调时间仅按后端 real_time_multiple 安排已收样本播放，最多 ±10% 的有限速率修正消息抖动。收包不重置游标；最新后端时间和各实体实际样本分别限制，绝不外推。暂停立即冻结、缓冲耗尽停止、恢复或新身份清空；长帧只消费最多 100 毫秒本地间隔，避免切回页面瞬间追赶。位置及四元数姿态逐帧插值，详情和轨迹重建约每 250 毫秒一次，保留消息／操作更新及已有缓存上限。layer.ts 释放更新入口，README 说明本地时间与后端权威时间区别；底部仍显示真实后端时间。原模型、屏幕尺寸、快捷键、阵营色和光影保持。

测试及入口：新增 tests/g5_entities/playback.mjs 与 motion.js，扩展生命周期和真实浏览器探针，manage.py 将播放游标检查纳入构建。独立检查覆盖 250 个消息间中间帧、超大整数、重复 health、暂停／恢复、2 倍速、长帧上限、缓冲耗尽、无效倍率及身份重置。生命周期直接调用实体层帧入口确认位置和姿态变化、暂停及销毁稳定。真实浏览器 postRender 记录实际模型矩阵，验证同一接收区间内多个不同中间位置、单调且不按包跳动，静止和跟随视角分别实测。

运行与资格：修改前向原 g5-t06-session-20260921-121834-998 写 request-stop，所属 PID 5736 正常结束，result／entry-result／runtime-result 均 passed。固定环境沿用已核查 Python 3.14.7、Node 24.21.0、Cesium 1.145.0，不安装工具。仓库根运行 build-g5-entities.ps1 -PythonExecutable <已核查解释器> -BaselineRunId g3-t01-check-20260921-121235-215，中文路径增加 ChinesePath；对应中文 g5-t06-build-20260921-123401-097、普通 g5-t06-build-20260921-123530-656。独立 tests/windows/g5-entities.tests.ps1 绑定中文候选及同一基线，验收 g5-t06-test-20260921-123413-494，四层收据均 passed，命令退出 0；acceptance SHA256=b2b5485c30643c76a78c94122ccf073d5f2d85f63bb16692a95515979bf6a365。

实际结果：四段各约 3 秒的真实呈现帧，Headless 静止／跟随为 97／95 帧、同接收区间中间移动 90／88 次；Gui 为 94／95 帧、87／88 次，每段 7 个接收区间。观察帧率分别 32.01／31.67／32.63／32.61，最大显示步长 38／94／110／97 ms；实际渲染矩阵与当帧插值位置差 0 米。source-cadence.json 核实两模式每实体 17 个唯一源样本，16 个间隔中 530 ms 十一个、540 ms 五个。结果证明已脱离原半秒位置跳动，不声称所有设备无长帧或 T10 性能已合格。

回归：Headless／Gui 后端暂停时间 15249／14119 ms，显示与后端时间随后稳定；原坐标／姿态／高度拒绝、真实消息重放、定位／跟随／复位、刷新零历史、断流清理通过。默认固定尺寸的近／中／远截图分别保持 72×28／74×32 像素；2 倍后 144×57／148×65，明暗跨度 22／21，阵营色／描边保持。离线地图、Range、缓存、接缝、端口冲突及客户端重置通过，全部测试所属进程正常退出。Edge 153.0.4234.48，实际 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11；没有测试全部倍速档、20 实体或长时稳定性。

过程问题：首次批量补丁因生命周期输出行上下文不匹配被拒绝，读取并去掉错误上下文后重新应用，最终 diff 核对没有重复测试入口。一次临时结果读取使用 Python 本机默认 GBK，遇 UTF-8 中文而失败，显式 -X utf8 后正常；资格入口与正式记录始终 UTF-8，未修改运行收据。业务构建和本轮完整验收一次通过，没有放宽原判据。

文档与预览：新增 docs/g5-motion-validation.md，同步 status、backlog、G5 方案、AGENTS、总体计划、G4 交接及旧报告入口，历史批次数据保留。新普通候选由隐藏 start-g5-entities-session.ps1 -Mode Headless 启动，启动器 out/runs/g5-motion-preview-20260921-123611-657/launch.json、PID 23152，会话 g5-t06-session-20260921-123611-912；8080 代理 ready=true、status=live。保留供用户刷新，未登记新预览正常退出或 T11 人工确认；正常关闭创建该会话 request-stop。未重试此前被拒绝的可见调试浏览器启动。

来源与收尾：final-audit.json 复核 26 项当前 T06 来源、冻结前置、候选全文件及普通／中文 405 个生产文件摘要一致；原 GLB SHA256 仍为 4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51。UTF-8、Python 语法、本地文档链接和 git diff --check 通过。工作日志追加前 376354 字节、SHA256=e3def04531a22ca78abd69a8434081295c5fa6c43c5dcd37f6ffe817938a72a2，完整保留此前字节。按持续授权提交并普通推送本次源码／测试／文档，关闭自动 maintenance／gc，归档回执保留在 out；不提交运行记录、模型、工具或地理数据。正式指针保持，下一卡仍为 G5-T07；控制、倍速及只读时间进度条归 G6。


## WL-20260921-007｜任务完成暂停后的预览重启

时间／时区：2026-09-21，Asia/Shanghai；状态：日常预览重新启动并实际运行，未新增产品控制。用户反馈页面显示暂停、实体不动，要求跑起来。起点 119d57dbb3608c09ee1eac23c48fb337842591a2，git status 干净。

原因：8080 代理 health ready=true、freshness.paused=true，snapshot 为 state=2、simulation_time_ms=756309。原会话 g5-t06-session-20260921-123611-912/session-paused.json 记录三个任务分别于 120910／133240／752969 ms 完成；源码 scripts/g5_state/runtime.py 的日常 Session 在三个任务完成后三秒请求暂停并保留现场。这是实际后端暂停，不是本轮逐帧显示失效。现有 StartupProbe 只有首次 request-start、一次 request-pause 和正常关闭，没有继续接口；SimTimer 收到 SessionStatus 也不等同于调用 go，未伪造 Running 消息或修改合格来源。

操作与结果：先向原会话根目录创建 request-stop，等待所属启动器 PID 23152 退出，result／entry-result／runtime-result 均 passed。随后以隐藏 PowerShell 调用 start-g5-entities-session.ps1，BuildRunId=g5-t06-build-20260921-123530-656、BaselineRunId=g3-t01-check-20260921-121235-215、Mode=Headless、PythonExecutable 为已核查 Python 3.14.7。新启动器 out/runs/g5-restart-preview-20260921-132131-908/launch.json，PID 32624；新会话 g5-t06-session-20260921-132132-134。实际代理 ready=true、state=1、real_time_multiple=1.0，两次独立快照时间 14309→16409 ms，400／500／600 坐标均变化；证据见该启动器 running-check.json 和两份原始快照。

交接：新会话运行供用户刷新 http://127.0.0.1:8080 查看，属于从头启动新一轮，不声称原运行恢复／重置接口已实现。任务完成仍会自动暂停；开始／暂停、倍速及只读时间进度条继续按用户决定归 G6，未提前实现。补充当前状态和运行报告中的自动暂停说明，保留原记录；浏览器刷新本身不能解除后端暂停。本次新会话尚未退出，不登记其正常收尾或新人工确认；正常结束创建 out/runs/g5-t06-session-20260921-132132-134/request-stop。阶段仍为 T06 已完成、T07 可执行。

归档：只追加运维记录和交接说明，不改源码、合格候选或正式指针；日志追加前 382421 字节、SHA256=f197cf36980bc565a8b5dd55f3b248047e7dabb44f85f2905773b2d4342ee9b7，保持历史字节前缀。检查文档 UTF-8、git diff --check 和实际运行证据后，按持续授权普通提交／推送，关闭自动 maintenance／gc；归档回执保留在本次启动器目录。


## WL-20260921-008｜银灰金属机体与阵营细描边预览

时间／时区：2026-09-21，Asia/Shanghai；状态：用户要求的外观预览已实现，两模式真实显示／地图回归通过，新预览运行中。用户希望机体有金属质感，阵营颜色用于更细的勾边，先看效果。起点 f320bc42936be4f995181ca502f15d2b6875ead4，工作区干净；本轮不推进 T07、不新增控制，自动检查不代替用户外观反馈或 T11 人工确认。

运行前收尾：向 g5-t06-session-20260921-132132-134 写 request-stop，等待所属启动器 PID 32624 正常退出，result／entry-result／runtime-result 均 passed，再修改来源。固定 Python 3.14.7、Node 24.21.0、Cesium 1.145.0／模型读取工具保持；未安装依赖。

方案及实现：核对锁定 Cesium 的 czm_modelMaterial、czm_pbrLighting、CustomShader 和颜色管线源码，参考官方 CustomShader／ModelGraphics 文档。修改 display.ts：银灰主体、金属度 0.92／粗糙度 0.30、GGX 直接高光及 Fresnel 环境反射；解析展示环境包含冷色背景与柔光反射面，保留原面板／舱盖纹理细节，不依赖外部贴图。UNLIT 只防止引擎再次叠加场景光，自算结果经过色调映射和线性／sRGB 管线；属于展示材质，不声称真实太阳／地面反射或光学仿真。原 GLB、几何、法线、纹理与姿态转换不变。

配色与边缘：layer.ts 不再将阵营色乘到机身，HIGHLIGHT 使用白色保持银灰材质；轮廓直接读取当前阵营颜色，普通 1 像素、选中 1.5 像素，选中不换白边。用户指定 400／500 青蓝边、600 红边，后端明确 Affiliation 优先、未知／未配置语义保持，标签／列表／轨迹继续同色。affiliation.ts 和 config/g5-entities.json 移除旧深色／白色固定边缘字段；README 同步新规则。固定 96 CSS px、48～384 px 快捷键和逐帧缓冲播放保持。

检查调整：更新生命周期中的材质／动态阵营／选择宽度检查；browser.py 实际核查引擎颜色与轮廓，保留原运动、暂停、刷新及断流矩阵，新增红蓝实际裁剪截图。screen.py 使用相机静止时模型隐藏／显示成对截图作差，排除背景、标签和轨迹；分别检查中性机体与阵营边缘像素，机体明暗单独测量，避免亮边代替主体立体感。未为简单参数另加实现镜像测试，沿用必要的真实渲染回归。

失败与调整：首中文候选 g5-t06-build-20260921-133137-581 构建通过，首验收 g5-t06-test-20260921-133149-268 在暂停对象全字段相等检查失败，进程正常关闭。由于旧探针未保存第二次对象明细，具体差异字段无法从该收据确定，不归因于材质也不声称已定位修复。加入 frozen 原始诊断记录，保持原严格判据；g5-t06-build-20260921-133312-180／g5-t06-test-20260921-133324-991 两模式及地图回归通过，失败记录保留。该中间版截图主体偏暗，随后增强解析环境和柔光反射；同时把亮度检查限定为中性机体像素，仍要求跨度至少 18、色阶至少 25。最终新候选重新完整复验通过，没有降低判据。一次临时 PowerShell 通配路径读取未匹配到文件而报参数错误，改用实际运行编号后正常；不影响业务记录。

最终资格：仓库根执行 build-g5-entities.ps1 -PythonExecutable <已核查解释器> -BaselineRunId g3-t01-check-20260921-121235-215，中文增加 ChinesePath；最终中文候选 g5-t06-build-20260921-133544-913、普通候选 g5-t06-build-20260921-133636-599。tests/windows/g5-entities.tests.ps1 绑定中文候选和同一基线，验收 g5-t06-test-20260921-133556-243，result／entry-result／runtime-result／acceptance 均 passed、退出 0。acceptance SHA256=162b3bb9349d9bbe850d6866a43be680192abebf8d0ac7bab31675902ec87827。来源仍为 26 项，原 GLB SHA256 4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51。

实测效果：Headless 蓝方放大截图 2816 个模型像素，其中中性主体 2432、青蓝边 383；机体独立亮度跨度 61。Gui 分别为 3189、2790、399，跨度 49。红方放大图 Headless／Gui 中性主体 3361／3288、红边 374／379。已直接查看最终红蓝截图，银灰反射、暗面和纹理可见，边缘细且保留阵营色。近／中／远尺寸、手动缩放、原 85 位置／10 姿态、播放游标、真实消息间逐帧移动与跟随、暂停、刷新、断流和资源释放通过。离线地图、Range、缓存、接缝、端口冲突和客户端重置通过，测试所属进程均正常退出。Edge 153.0.4234.48，实际 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11；不扩展到 T09／T10 全程或长时性能资格。

文档和预览：新增 docs/g5-metal-display-preview.md，同步 AGENTS、状态、任务卡、G5 方案、总体计划、G4 交接及旧报告的当前候选入口，保留历史批次。新普通候选由隐藏 start-g5-entities-session.ps1 -Mode Headless 启动，启动器 out/runs/g5-metal-preview-20260921-133957-915/launch.json、PID 32436，会话 g5-t06-session-20260921-133958-135。8080 代理 ready=true、state=1、rate=1.0，两次快照时间 3639→5239 ms，三实体位置变化；证据在启动器 running-check.json。新预览保留供用户刷新查看，未结束，不登记正常退出；结束仍创建本次会话 request-stop。三任务完成后的自动暂停规则保持，页面控制继续归 G6。

收尾：final-audit.json 复核当前来源、冻结前置、候选文件和普通／中文 405 个生产文件摘要一致；UTF-8、Python 语法、文档链接和 git diff --check 通过。日志追加前 385125 字节、SHA256=bf349a5afacd67ce024756cbb5b4c746676a197eb2e45d74875e2f0c49e91f96，完整保留此前字节。按持续授权提交并普通推送本次源码／配置／测试／文档，关闭自动 maintenance／gc，归档回执保存在 out；不提交模型、数据、工具或原始运行文件。正式指针保持，用户可先评审此预览，下一实施卡仍为 T07。


## WL-20260921-009｜重新启动金属模型预览服务

时间／时区：2026-09-21，Asia/Shanghai；状态：运维启动完成，新会话保留运行。用户要求再次启动服务查看视角和材质。起点 205111134df522368d587d395cddedd1876b9a6f，git status 干净；只使用当前合格候选，不改源码或阶段范围。

检查和操作：原 g5-t06-session-20260921-133958-135 仍存活，后端 state=2、time=756289 ms，属于任务完成后自动暂停。创建该会话 request-stop，等待所属 PID 32436 正常退出；result／entry-result／runtime-result 均 passed。仓库根以隐藏 PowerShell 运行 start-g5-entities-session.ps1，PythonExecutable 为已核查 Python 3.14.7，BuildRunId=g5-t06-build-20260921-133636-599、BaselineRunId=g3-t01-check-20260921-121235-215、Mode=Headless；入口复查来源，没有端口冲突、下载或额外业务修改。

实际结果：启动器 out/runs/g5-view-preview-20260921-142331-795/launch.json、PID 5148，新会话 g5-t06-session-20260921-142332-017。8080 代理 ready=true、state=1、rate=1.0，连续快照时间 2040→3639 ms，400／500／600 坐标均变化，原始快照和 running-check.json 保存在启动器目录。保留运行供用户打开 http://127.0.0.1:8080 查看；浏览器仍由用户打开，不把接口就绪写成本轮新的人工／浏览器渲染验收。材质与视角资格继续引用上一轮，未扩大测试。任务结束仍会自动暂停，正常关闭创建本次会话 request-stop；本次会话尚未退出。

交接与归档：同步当前状态中的最近预览编号，下一卡仍为 G5-T07，控制按已定 G6 范围实施。没有发现新的实现问题；无需新增测试或重新构建。日志追加前 391273 字节、SHA256=c56e17b71418c955e77d9d4843a3d59c050e91068225b224fedde5c2cf4513fb，历史字节保持。检查 UTF-8、git diff --check 及运行证据后按持续授权提交／普通推送运维记录，关闭自动 maintenance／gc，归档回执保存在启动器目录。


## WL-20260921-010｜修复放大近景的跟随顿挫与前后抖动

时间／时区：2026-09-21，Asia/Shanghai；关联 G5-T06，状态：修复及两模式自动回归完成，新预览保留运行。用户反馈“放大后还是顿挫，而且不时会前后抖”；起点 b3d2deba4ce7dbb8534529638b26682dc55ccee4，git status 干净。只修复当前显示，不进入 T07／G6，不改变金属机体、阵营细描边及大小约定。

定位过程：正常关闭 g5-t06-session-20260921-142332-017，再以旧合格候选 133636-599 启动复现会话 g5-t06-session-20260921-143941-251（隐藏启动器 g5-motion-debug-20260921-143941-023，PID 14112）。两个独立无头 Edge 探针只读真实 G4 数据，记录最终屏幕投影、模型矩阵／scale 和相机。g5-close-motion-before-20260921-1440 的 384 px、约 43 米近景最大锚点跳动 108.13 px、尺寸偏差 9.41%；g5-close-motion-isolate-20260921-1445 关闭轨迹后锚点跳动消失到舍入量级，但尺寸偏差仍达 1.24%。两浏览器正常退出，原始帧与事件保留。复现会话 request-stop 后三层退出 passed，再改源码。一个临时 Python -c 因 shell 引号失败，未改文件；一个文档辅助脚本 apply_patch 因补丁行格式拒绝，未写入；均改为明确补丁后正常执行，不影响业务数据／收据。

原因及处理：Cesium 原生跟随依赖 DataSourceDisplay 包围球 DONE；轨迹每约 250 ms 替换 ConstantProperty，异步静态几何 pending 时镜头停住、随后追赶。ModelVisualizer 又先于跟随相机计算 scale，旧相机与新位置不一致。旧逐帧修复只验证 ECEF 矩阵，没有识别屏幕回拉。layer.ts 现于 source.update 中先插值，再用公开 lookAtTransform 同步 ENU 相机原点，保留用户局部视角／距离，随后计算尺寸；显式 ENU，定位／复位／快速重跟随清理目标。轨迹 CallbackProperty 使用有界缓存及动态折线，避免重复静态几何重建。browser.py／motion.js 检查最终屏幕锚点、实际模型尺寸、相机局部偏移，覆盖轨迹开启、384 px、50 米内、旋转及同帧复位重跟随。README 同步；后端时间、播放游标、坐标姿态、GLB、G4 和 T02～T05 保持。

验证：仓库根使用固定 Python 3.14.7、Node 24.21.0、Cesium 1.145.0；build-g5-entities.ps1（中文增加 ChinesePath），基线 g3-t01-check-20260921-121235-215；tests/windows/g5-entities.tests.ps1 绑定中文候选。首中文 g5-t06-build-20260921-144510-919／验收 g5-t06-test-20260921-144525-795 完整通过；补齐快速重跟随后最终中文 g5-t06-build-20260921-144802-049、普通 g5-t06-build-20260921-144826-020；最终验收 g5-t06-test-20260921-144815-830，四层结果 passed、退出 0，acceptance SHA256=5117260e031e112a86c63dc25e240fc8a856cbfa7e24786a163bcb23c62ddf6e。未覆盖历史记录或放宽判据。

效果：Headless／Gui 在 384 px、43.3275 米近景各 96 呈现帧，最大锚点跳动 1.59e-7／1.40e-7 px；旋转和快速重跟随通过，尺寸误差为浮点舍入量级，相机不再停顿追赶。帧率仍约 32 FPS，只是短程实测，长时高规模／不同设备仍需后续验证。Edge 153.0.4234.48，实际 ANGLE／NVIDIA RTX 4000 SFF Ada／D3D11。原坐标／姿态／游标／生命周期、消息重放、暂停、刷新、断流、尺寸与金属截图、离线地图／Range／缓存／接缝／端口及正常退出回归通过。GLB SHA256 保持 4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51。

交接：新增 docs/g5-close-motion-validation.md，同步 AGENTS、状态、任务卡、G5 方案、总体计划和报告当前入口。新预览 g5-t06-session-20260921-145042-078，启动器 g5-close-motion-preview-20260921-145041-845，PID 31700，新普通候选／Headless，ready=true、1 倍速、12709→14839 ms 及三实体推进通过。Ctrl+F5 刷新 8080 查看；结束创建本次会话 request-stop。新预览未结束，不登记人工确认；任务结束仍自动暂停，控制保持 G6，下一卡 T07。

归档：final-audit.json 复核来源、普通／中文生产文件一致、原资产、UTF-8／语法／文档链接及 diff。日志追加前 393332 字节、SHA256=a9b56e0ac8b1d0d734b73de026057444bfd6fd9d190bef15dfae5ce22f45d71c，保留完整历史字节。按持续授权普通提交／推送并核对 origin/main，关闭自动 maintenance／gc，归档回执保存在本次验收目录；不提交工具、数据、原始运行或候选。正式前端指针保持，T09／T10 长时稳定性与 T11 人工确认未提前完成。


## WL-20260921-011｜重新启动修复后的 Cesium 预览

时间／时区：2026-09-21，Asia/Shanghai；关联 G5-T06，状态：运行启动完成，新会话保留运行。用户要求再次启动服务查看；起点 30b2212ef97f6216b4e8e966670902555de195c6，git status 干净。复用近景跟随修复后的合格候选，不改源码、资产或阶段范围。

检查与操作：旧会话 g5-t06-session-20260921-145042-078 的所属启动器 PID 31700 仍存活，代理 state=2、time=756199 ms，属于任务完成后的自动暂停。写入该会话 request-stop，等待正常退出；result／entry-result／runtime-result 均 passed。仓库根以隐藏 PowerShell 调用 scripts/windows/start-g5-entities-session.ps1，PythonExecutable 为已核查 Python 3.14.7，BuildRunId=g5-t06-build-20260921-144826-020、BaselineRunId=g3-t01-check-20260921-121235-215、Mode=Headless。入口自行复查来源，未发生冲突或失败，无需重新构建或新增测试。

实际结果：启动器 out/runs/g5-view-restart-20260921-151035-692/launch.json，PID 27748，新会话 g5-t06-session-20260921-151035-926。8080 ready=true，state=1、rate=1.0；两次独立快照时间 2569→4709 ms，400／500／600 坐标均变化；原始快照和 running-check.json 保存在启动器目录。用户可打开 http://127.0.0.1:8080 并 Ctrl+F5 查看；不将接口启动检查计作新的人工或浏览器渲染验收。

交接与归档：同步 docs/status.md 当前运行记录，新会话保留运行；任务结束仍会自动暂停，正常结束创建 out/runs/g5-t06-session-20260921-151035-926/request-stop。本次尚未正常退出，不登记退出或人工确认；下一卡 T07，控制按 G6 实施。日志追加前 397986 字节、SHA256=960193ee831ed6273a25dc482892d9480c558e4cc279c857f40f3f2651d40641，保留历史字节前缀；UTF-8、git diff --check 及运行证据通过后，按持续授权普通提交／推送并核对远程，关闭自动 maintenance／gc，回执保留在本次启动器目录。


## WL-20260921-012｜完成 G5-T07 航线、任务与区域显示

时间／时区：2026-09-21，Asia/Shanghai。关联 G5-T07，状态：实施及自动验收完成；预览保留运行。用户要求执行本卡，起点 02fc40654c188ccd87fb5a56e491df1954a55891，git status 干净。范围为完整规划、当前命令、执行证据、三类任务及区域／高度／关联查询，未提前进入 G6 控制、T08 完整恢复、T09／T10 全程或 T11 发布。

实施：先复查 AGENTS、状态／任务卡、T06 近景修正、真实 G4 状态和 CMASI／AMASE 几何语义；正常关闭旧预览 g5-t06-session-20260921-151035-926，三层结果 passed。新增 apps/cesium_missions、scripts/g5_missions、tests/g5_missions、config/g5-missions.json 与 Windows 构建／验收／联合入口，共 19 项独立输入；冻结 T02～T06 和正式后端／网关、原始数据、UCAV 保持。构建在 T06 合格副本生成连接、初始化和销毁接点，共用一个 Viewer 与只读连接。按字符串 Number／NextWaypoint 绘制完整规划和当前分段；命令、当前目标及时间匹配且目标存在才确认执行，绿色航段还要求同命令内容、严格前进的相邻目标变化。快照不恢复旧推进，后端完成不删除任务、规划或停止飞机。

地理与界面：点／线按 Location3D 基准处理；二维矩形等范围忽略中心高度，地面区域使用 T04 同源正高栅格；AGL 加一次地面正高，再加一次 N96。KeepIn／KeepOut 的矩形、圆、多边形绘制上下界和侧壁，OperatingRegion 聚合引用；时间基准和 Padding 不擅自解释。新增五组开关、航点查询、关联实体筛选／选择、详情和定位，业务顶点预算 50000，异常隔离及删除／新流清理。任务查询还显示坐标／原高度、折线顶点数或矩形宽高／旋转。

问题及修正：g5-t07-test-20260921-203238-801 的刷新清理访问了已销毁 Viewer，保留原异常并修复监听与引用释放；204013-985 的 Headless 通过、GUI 在命令已到而目标未就绪的过渡中失败，现明确待确认。独立探针发现同一时间的新目标会遗留旧航段起点，现重新验证时间和相邻关系并加入构建反例。尝试在修改前正常关闭诊断浏览器时，其端点已随 GUI 失败正常关闭，未发送关闭命令，另有补充记录；没有强制清理或改写原结果。探索中的不存在路径／通配读取错误未修改输入。

渲染兼容：204709-578 的两模式任务检查通过，原 T06 像素回归发现放大后缩小的细描边消失。实际渲染器是 Intel／ANGLE D3D11；独立重放在旧 T06 包同样复现，开关业务图层不消除。放大可见而缩回旧轮廓内不可见的现象符合多重采样轮廓遮罩残留，未声称定位驱动内部根因。组合页面采用公开 msaaSamples=1 加 FXAA，保留原资产、材质、1／1.5 像素描边及像素判据，不改引擎或系统。对照诊断在 out/runs/g5-t07-outline-*；修正后大／小／默认轮廓均可见，真实两模式原探针完整通过。普通构建 210352-613 的前置 LMCP 复查与另一个操作争锁，底层 g2-t03-resolve-20260921-210410-129 为非阻塞锁 PermissionError；等资格复查结束后串行重试成功，未放宽锁或来源规则。

验证：仓库根／C:/Users/zhiyu，固定 Python 3.14.7 和项目 Node；build-g5-missions.ps1（中文加 ChinesePath），最终普通 g5-t07-build-20260921-210702-807、中文 g5-t07-build-20260921-210342-447 均退出 0。每包 459 文件，407 个生产文件逐项一致；UCAV SHA256 仍为 4e7e7646fcb24656f1d944bdf9fdc4767af38d26c4f23a617dbab642c17ada51。1004 个独立坐标／高度样点最大数值误差 2.833e-9 米，矩形 0／90／33 度、非顺序／超大编号、区域上下界、时间边界、非法几何、预算和清理通过。tests/windows/g5-missions.tests.ps1 绑定中文候选，最终 g5-t07-test-20260921-210534-383 的四层结果 passed，acceptance SHA256=18fa2b24c27390820068bd50b2999c58534753b137d84c60611c22dc2ceda831。两模式真实三实体／完整 90 点水道、规划／分段／执行证据、点／区域完成后保留并继续运动、暂停刷新与断流清理通过；独立收到帧与 Cesium 实际 Entity 几何对照通过。区域原场景无消息，另以明确标识夹具验证四类区域引用、超大航点查询及删除，不冒充真实后端区域证据。

回归与退出：T06 原浏览器探针在新包的 Headless／Gui 均通过；384 px、约 43.33 米近景锚点跳动／帧率为 Headless 1.621e-07 px / 55.46 FPS；Gui 1.340e-07 px / 55.05 FPS。此为本机短时记录，非长时稳定性资格。金属机体、阵营细描边、近中远固定尺寸、缩放上限循环、姿态／暂停／跟随／旋转／重跟随及地图资源均通过；离线、Range、缓存上限、地形接缝和端口冲突通过。所有受检浏览器、后端及资源服务正常退出，无强制结束；正式前端指针保持。

交接：同步 docs/g5-missions-validation.md、状态、任务卡、G5 方案、总体计划和 AGENTS；下一卡 T08。联合入口正常运行及退出会话 g5-t07-session-20260921-211420-838 的三层结果 passed。新预览 g5-t07-session-20260921-211814-236，启动器 out/runs/g5-t07-preview-20260921-211814-011/，普通候选、Headless，ready=true、1 倍时间 94410→96410 ms、三实体坐标推进通过；访问 http://127.0.0.1:8080。创建本次会话 request-stop 正常结束，当前预览未退出、不登记人工确认；三任务完成后仍自动暂停，控制／倍速／只读进度继续归 G6。

入口探针补充：临时探针曾等待重定向 stdout 的就绪标记，但 PowerShell 子进程未及时刷新输出，实际 session-ready.json 已生成。后改为核对本次就绪文件及会话上下文；当前预览的 HTTP、两次时间和三实体位移检查通过。已关闭的独立会话只登记正常启动／退出，不把失败的临时探针写成位移检查通过。

归档：日志追加前 400042 字节，SHA256=beda888dc695cd54dd9c739e4b4eeb1e9905544b2b006d631aad3d71df43743a，保留历史字节前缀；检查当前候选、19 项来源、UTF-8／语法／文档链接及 Git diff 后按持续授权提交并普通推送，关闭自动 maintenance／gc，回执保存在本次验收目录。完整线任务显示全程、恢复全矩阵、规模／长时及本轮人工确认分别由 T08～T11 继续完成。


## WL-20260922-001｜新增当前传感器与累计侦察覆盖及独立开关

时间／时区：2026-09-22，Asia/Shanghai。关联 G5-T07 显示补充，状态：实现和自动验收完成，新预览保留运行。用户报告没有侦察覆盖显示，明确需要“当前传感器覆盖＋累计已侦察区域”，随后要求两种显示各有开关。起点 705787535eda47a8d890801f4ee5cd6b26dc0e77，git status 干净。下一主要任务仍为 G5-T08，未提前实施 G6 控制、覆盖效果优化或 T11 人工确认／正式发布。

调查与取证：复查 AGENTS、状态／任务卡、T07 来源及原生 Camera／SearchTaskAnalysis、真实网关事件。用户重新启动的 g5-t07-session-20260922-103218-267 当时已完成三任务并自动暂停；通过原有取证入口导出本次原生分析，只读重放 4279 个 AirVehicleState，724 个线单元、1250 个区域格的坐标和已见标记一致，点有效观察 203742 ms 对应原生 203.74 秒。核对所属 PID 后创建该会话 request-stop，三层结果 passed，未强制结束。初步证据 coverage-replay.json 已复制到本次最终验收 review-evidence。

实现与修改文件：新增 apps/cesium_coverage 四文件、scripts/g5_coverage 六文件、tests/g5_coverage 三文件、config/g5-coverage.json 及四个 Windows 包装入口，共 18 项独立来源；新增 docs/g5-coverage-display-validation.md，同步状态、任务卡、G5 方案、总体计划和 AGENTS。本补充严格组合已合格 T07 项目副本，不修改旧源码、候选、收据、正式 AMASE／UxAS／G4、原始 DEM／PMTiles 或 GLB；无工具安装或依赖升级。

显示语义：当前覆盖直接用同一 G4 连接的 CameraState.Footprint，按阵营色显示并贴同源地形，原角点零高程不是海平面。局部平面足迹的地形外部分仅裁剪显示并提示，保留原始足迹用于任务单元分类，不扩大地形资格或虚构地形遮挡。累计层绿色显示任务定义内真实已见线段、20 米区域格和点观察秒数；采用正式波段／GSD、飞机位置 DTED 最近邻、0.5 秒分析节拍及同相机有效间隔并集，包括其他相机附带贡献。目前资格范围为零 DwellTime 点／线／矩形，其他输入明确拒绝。没有把轨迹或 TaskComplete 当作累计覆盖。

开关与恢复：新增“当前传感器覆盖”“累计侦察覆盖”两个默认开启开关，独立控制 CustomDataSource 并保存用户选择；隐藏不停止后台计算，刷新／晚加入恢复本运行结果。只读线程分批查询网关持久事件，mode=ro／query_only，检查摘要、来源、运行和顺序，不发业务消息；通过同端口 /api/coverage/v1/snapshot 提供有界快照，不改 G4 v1。断流／新运行清理旧显示，匹配 runId 后恢复；累计故障不终止仿真。最多 50000 单元、128 任务／实体、8 MiB 单响应和 16 并发响应；已见区域只合并相邻格，线段不跨未见样本连线。

问题与纠正：初次将所有足迹角点也限制到合格地形，被相机过渡姿态真实外延角点拒绝，核查后区分显示裁剪、合法平面分类和地形查询位置。构建 g5-coverage-build-20260922-110305-316 因合并数组缺类型触发 TypeScript 7034／7005，补明确类型后重建。首次真实验收 g5-coverage-test-20260922-110636-708 的原实体浏览器通过，但网关数据库未建立即被只读线程打开，明确失败并正常收尾；改为首次消费前最多 30 秒等待库／表／元数据与第一条记录，增加空库反例，已开始消费后的错误仍立即拒绝。旧候选和失败记录保留，不改验收判据。

审查过程：修复后的 g5-coverage-test-20260922-111122-465 四组真实运行全部通过，但截图显示新增面板使 Flex 侧栏压缩实体列表；继续修正面板禁止收缩、将覆盖开关移到航线面板前，并新增侧栏高度与实际 Cesium 矩形／折线对应单元的检查。诊断中提前关浏览器的临时方法因 CDP 缺命令行字段失败，后续 PID 守卫又因用例已切换拒绝操作，两次均未实际发送关闭命令，测试正常执行至通过。一次临时 Python -c 因 shell 引号 SyntaxError 未执行操作，改为脚本文件；初步 rg 路径猜测错误也未改动输入。最终另增加累计响应并发上限后重新构建，不将此前候选写成最终资格。

最终验证：从仓库根及不同工作目录使用固定 Python 3.14.7、项目 Node 24.21.0；build-g5-coverage.ps1 -PythonExecutable <已核查解释器>（中文增加 ChinesePath），普通 g5-coverage-build-20260922-112335-163、中文 g5-coverage-build-20260922-112213-396 均退出 0；每包 462 文件，407 个生产文件逐项一致。17 项计算／只读／坏数据／就绪检查和 10 项前端几何／超大 ID／运行隔离检查通过。tests/windows/g5-coverage.tests.ps1 -PythonExecutable <已核查解释器> -BuildRunId g5-coverage-build-20260922-112213-396 的最终验收 g5-coverage-test-20260922-112501-163 四层结果 passed、退出 0。原 T06 实体 Headless／Gui 和新增覆盖 Headless／Gui 四组真实运行全部 passed，累计线程及所属后端、资源服务、浏览器正常退出，无强制结束。

实际效果与对照：两模式各对照原生 1975 个统计单元坐标和已见标记；Headless 线 603/724、区域 1062/1250，Gui 线 599/724、区域 1062/1250；两者点观察 125.871 秒均与原生两位小数报告一致。直接复算实际绘制矩形、线段及点所对应的已见单元分别为 1666、1662，逐项一致。这是各自约 135 秒的真实窗口，差异未抹平，也不作为完整水道最终覆盖率。当前 footprint 与同连接消息、同源坐标转换对照通过。独立开关／关闭显示继续累计／恢复开关、暂停、刷新累计恢复、断流清理通过；实体列表高度和最终截图人工检查正常。实际渲染器、近景帧率及锚点误差保存在报告和原始 browser/result.json；原金属材质、阵营细描边、固定尺寸、缩放循环、姿态／时间／近景跟随、完整规划／命令／任务／完成保留、离线地图／Range／有界缓存／接缝／端口冲突回归通过，不扩大为 T09／T10 规模或长时资格。

日常入口与交接：新 start-g5-coverage-session.ps1 使用 PythonExecutable、BuildRunId 和 Mode，访问 8080；旧 T07 入口保留历史功能，新覆盖需使用新入口。独立正常启停会话 g5-coverage-session-20260922-113321-401，真实 1 倍时间／三实体位移、三任务与累计运行绑定及 POST 405 拒绝通过，创建所属 request-stop 后三层结果 passed。随后启动预览 g5-coverage-session-20260922-113600-734，启动器 g5-coverage-preview-20260922-113600-501，同样运行检查 passed，保留供用户 Ctrl+F5 查看。当前预览尚未退出，不登记正常退出或人工确认；三任务完成后自动暂停。正常结束创建本次会话 request-stop。T08 后续需涵盖两个覆盖层和累计线程恢复／释放；控制、完整两实体／20 实体联调和阶段发布仍按既定顺序。

预览探针补充：首次检查新预览时 session-ready.json 尚未创建，临时探针明确 FileNotFoundError，没有把启动进程等同于就绪；随后等待同一所属启动器的真实就绪文件再重跑，通过后才登记上述预览运行结果。

收尾与归档：本条追加前 406604 字节、SHA256=8c18c58e7004e7df6812ca548bc84af158874fda72670fc429b6639166440b9f，历史字节完整保留。final-audit.json 复查 18 项来源、前置绑定、普通／中文全部候选文件、生产一致性、UTF-8／语法／链接和 Git diff。按 AGENTS 第 7 节持续授权完成后提交并普通推送，关闭自动 maintenance／gc，核对 origin/main；实际提交及远程结果另存本次验收 archive.json。只提交 25 项源码／配置／测试／说明，不提交原始日志、工具、地理或模型资产。正式前端指针保持，下一实施卡 G5-T08。


## WL-20260922-002｜重新启动侦察覆盖预览服务

时间／时区：2026-09-22，Asia/Shanghai。关联 G5 显示补充，状态：服务重启完成，新预览保留运行。用户要求重新启动服务再次查看；起点 ad25e62bba4fe6cca883627c2941fba8e04efee7，git status 干净。复用当前合格覆盖候选，不改实现、资产或任务范围。

操作与原因：原会话 g5-coverage-session-20260922-113600-734 仍存活，后端 state=2、time=756389 ms，为任务完成后自动暂停。核对所属 PID 27900 的命令行及快照运行身份后，创建该会话 request-stop；正常退出后三层 result／entry-result／runtime-result 均 passed，没有强制结束。仓库根通过隐藏 PowerShell 调用 scripts/windows/start-g5-coverage-session.ps1，PythonExecutable 为已核查 Python 3.14.7，BuildRunId=g5-coverage-build-20260922-112335-163、BaselineRunId=g3-t01-check-20260922-112501-380、Mode=Headless；入口自行复查来源。未发生端口冲突或启动失败，无需重新构建或新增测试。

实际验证：启动器 out/runs/g5-coverage-restart-20260922-121323-648/launch.json，PID 11916，新会话 g5-coverage-session-20260922-121323-878。等待本次 session-ready.json 后检查 8080 ready=true、state=1、rate=1.0；两次独立快照时间 12709→14839 ms，400／500／600 坐标均推进；三条规划、三任务及累计覆盖运行身份一致，覆盖接口 POST 返回 405。原始快照及 running-check.json 保存在启动器目录，检查命令退出 0。预览可通过 http://127.0.0.1:8080 并 Ctrl+F5 查看，两个覆盖开关继续可用；不把接口就绪记作新的浏览器渲染或人工验收。

交接与归档：同步 docs/status.md 当前运行；新预览保留运行，尚未正常退出，三任务完成仍自动暂停。正常结束创建 out/runs/g5-coverage-session-20260922-121323-878/request-stop。未发现新问题，下一卡保持 G5-T08，控制继续归 G6。日志追加前 414776 字节、SHA256=41dd8a020d2d129010335d32317e0a976337b78272bb30da2c342f3f98cffc49，历史字节前缀完整保留；UTF-8、运行证据与 git diff --check 通过后按持续授权提交并普通推送本次两份运维说明，关闭自动 maintenance／gc；实际提交及远程核对结果保存在本次启动器 archive.json。


## WL-20260922-003｜交换地图左右键拖动操作

时间／时区：2026-09-22，Asia/Shanghai。关联 G5 界面操作，状态：调整、专项验证及新预览启动完成。用户要求左右键功能互换，澄清为“只交换左右键拖动，保留左键选中目标”。起点 6f99ebc756d312c120709dba7a65e3ff6e059245，工作区干净；不改变系统鼠标设置或仿真控制。

实施：复查本地锁定 Cesium 源码，原普通左键为平移／环绕，右键为缩放；在当前组合页面设置公开 ScreenSpaceCameraController 的 translate／rotate 为 RIGHT_DRAG，zoom 为 LEFT_DRAG、WHEEL、PINCH。Viewer 点击事件、Ctrl／Shift、滚轮、中键及触控保持。只改 apps/cesium_coverage/ui.ts 和 README，实体面板增加操作提示，销毁时释放该提示。修改前正常关闭旧会话 g5-coverage-session-20260922-121323-878（所属 PID 11916，原 state=2、time=756239 ms），三层结果 passed，避免运行过程中改变来源。

验证与结果：仓库根使用固定 Python 3.14.7、项目 Node 和 build-g5-coverage.ps1（中文增加 ChinesePath），基线 g3-t01-check-20260922-112501-380；普通 g5-coverage-build-20260922-134929-900、中文 g5-coverage-build-20260922-134901-892 均退出 0，18 项来源中仅两文件变化，407 个生产文件逐项一致，17 项计算和 10 项几何检查通过。新普通包真实 Headless 会话 g5-coverage-session-20260922-134949-576 的独立 Edge 鼠标专项 g5-coverage-mouse-check-20260922-134949-352/browser passed：实际左拖缩放、右拖平移，中键／滚轮保持；跟随时缩放／环绕生效；点击实际模型像素仍由左键选择，右键点击不选择；覆盖两个开关与刷新后的映射、累计运行身份保持。浏览器退出 0，无强制终止，无页面异常或业务消息回送。仅做与本次改动相关的专项，第 5 节父版本全量覆盖证据保持，不改写旧收据或声称本次重跑完整矩阵。

运行交接：正常关闭检查会话 g5-coverage-session-20260922-134949-576，三层结果 passed；隐藏启动新预览 g5-coverage-session-20260922-135252-148，启动器 g5-coverage-mouse-preview-20260922-135251-922，当前普通候选／Headless，真实 1 倍时间 117910→119910 ms、三实体移动、三任务及累计运行身份检查通过。用户 Ctrl+F5 刷新 8080 查看；新预览保留运行、尚未退出，三任务完成仍自动暂停。正常结束创建本次 request-stop，下一卡仍为 G5-T08。没有新的实现失败或未解决问题；Edge 隔离网络下后台同步的 DNS 诊断不属于页面异常，不放宽页面检查。

文档与归档：同步覆盖报告第 6 节、当前状态、G5 方案及 AGENTS；历史工作日志保留，本条追加前 417137 字节、SHA256=eca1731cc6d8d7f9ea7500b623792790a4795b9cffba9a930a5ab8469149ed54。候选来源、正常退出、UTF-8、相对链接及 git diff --check 复核通过后按持续授权提交并普通推送，关闭自动 maintenance／gc；本次七项改动不含原始日志或工具，final-audit.json 与实际 Git 归档回执保存在鼠标专项目录。正式前端指针和 T11 人工确认边界保持。


## WL-20260922-004｜按最新要求设置左键平移、右键环绕

时间／时区：2026-09-22，Asia/Shanghai。关联 G5 操作调整，状态：实现及专项通过，新预览保留运行。用户进一步明确“右键环绕，左键平移”，本次覆盖 WL-20260922-003 的旧操作方式，历史记录保持。起点 04b1607f9c4b06192097c91b620ffa9ccc6775e1，git status 干净。滚轮负责缩放，左键点击仍选中目标，不改变仿真控制或系统鼠标设置。

实现与原因：源码核实 Cesium 自由视角 spin3D 为地理平移，但相机处于跟随坐标系时转为绕实体旋转，而 tilt3D 在该坐标系直接返回。因此新增 apps/cesium_coverage/navigation.ts，按自由／跟随状态分别映射左／右拖动；跟随时左拖通过公开相机 moveRight／moveUp 和屏幕像素比例平移，保留方向及跟随，右拖仍绕实体转动。松键、失焦、切换跟随结束拖动，销毁移除自有监听、恢复映射；不替换 Viewer 点击事件。ui.ts 接入与释放模块，README 和界面提示同步。修改前正常关闭 g5-coverage-session-20260922-135252-148（所属 PID 23652，state=2、time=756369 ms），三层结果 passed。

构建与验证：仓库根使用固定工具和 build-g5-coverage.ps1，基线 g3-t01-check-20260922-112501-380，普通 g5-coverage-build-20260922-140926-685、中文 g5-coverage-build-20260922-140910-656 均退出 0；19 项来源、各 463 文件、407 个生产文件一致。两类构建的 17 项计算和 10 项几何检查通过。真实 Headless 会话 g5-coverage-session-20260922-140940-929 的独立 Edge 输入专项 g5-coverage-navigation-check-20260922-140940-708/browser passed；实际左拖平移、右拖环绕、滚轮缩放、中键保持；跟随平移的前后相机方向不变、位移垂直于视线且保持跟随，右拖转动有效。松键／失焦后无拖动残留、退出跟随恢复自由平移、真实模型左击选择／右击不选择、覆盖开关及刷新通过。无页面异常／业务回送，浏览器正常退出 0，没有强制终止。截图实际已检查；没有发现本次实现失败，不新增持久测试或重跑无关的完整两模式矩阵，父版本全量覆盖证据仍为历史资格。

交接：检查会话 g5-coverage-session-20260922-140940-929 正常退出，三层结果 passed。新预览 g5-coverage-session-20260922-141126-456、启动器 g5-coverage-navigation-preview-20260922-141126-220，当前普通候选／Headless，1 倍时间 135370→137510 ms、三实体位置和累计身份通过。访问 8080 并 Ctrl+F5，正常关闭创建本次会话 request-stop；当前预览尚未退出，三任务完成仍自动暂停，不登记 T11 人工确认或发布。下一卡仍 G5-T08。

文档与归档：覆盖报告新增第 7 节，上一轮第 6 节标为历史；同步当前状态、G5 方案、AGENTS 和来源 README。本条追加前 420381 字节、SHA256=a48643cb200fc7dccbce96bc47df2960753831a983da2f7dfc01ea3e1db805d2，历史字节前缀完整保留。复查新候选来源／文件、前置绑定、普通中文一致性、UTF-8／链接和 diff 后按持续授权提交并普通推送本次八项改动，关闭自动 maintenance／gc，回执保存在本次专项 archive.json；不提交工具、候选或原始运行记录。


## WL-20260922-005｜重新启动当前导航预览服务

时间／时区：2026-09-22，Asia/Shanghai。关联 G5，状态：重启完成，新会话保留运行。用户要求重启再次查看；起点 110e54328e4e15db249b9b282d0fd760b5d8a291，git status 干净。复用已核查“左拖平移、右拖环绕、滚轮缩放”候选，仅运行操作，不改源码或扩大任务范围。

操作与原因：原会话 g5-coverage-session-20260922-141126-456 的所属 PID 16812 存活，state=2、time=756189 ms，为完成三任务后的自动暂停。复核命令行与快照身份，创建本次 request-stop；正常退出后三层 result／entry-result／runtime-result 均 passed。仓库根以隐藏 PowerShell 调用 scripts/windows/start-g5-coverage-session.ps1，PythonExecutable 为已核查 Python 3.14.7，BuildRunId=g5-coverage-build-20260922-140926-685、BaselineRunId=g3-t01-check-20260922-112501-380、Mode=Headless；入口复查来源，无冲突或失败，不重新构建、不增加测试。

实际验证：启动器 out/runs/g5-coverage-navigation-restart-20260922-162523-025/launch.json、PID 30912，新会话 g5-coverage-session-20260922-162523-266。等待 session-ready.json 后 8080 ready=true、state=1、rate=1.0；两次快照时间 8439→10039 ms，400／500／600 坐标均推进，三条规划／三任务、累计覆盖运行身份与只读 POST 405 检查通过，退出 0。快照及 running-check.json 保存在启动器目录；不将接口检查登记为新的浏览器渲染或人工确认。

交接与归档：用户可 Ctrl+F5 刷新 http://127.0.0.1:8080。新预览保留运行，尚未退出，完成三任务仍自动暂停；正常结束创建 out/runs/g5-coverage-session-20260922-162523-266/request-stop。当前状态同步，下一卡仍 G5-T08；未发现新问题。日志追加前 423750 字节、SHA256=7e912dd8641433365b8e77b70bbd4ec3cfcab0adf3634b831cfa61f90c5cb08a，历史字节前缀保持。UTF-8、运行证据及 git diff --check 通过后按持续授权提交并普通推送两份运维说明，关闭自动 maintenance／gc；实际归档结果写入本次启动器 archive.json。


## WL-20260922-006｜排查累计覆盖显示反馈（待补充现象）

时间／时区：2026-09-22，Asia/Shanghai。关联 G5 侦察覆盖，状态：现场诊断已完成，用户所见问题尚未复现，修复范围待明确。用户反馈“累计覆盖并没有生效”；起点 eed20ec63a7bcdffe43f2b01f84b5ec0c24c7a57，git status 干净。当前服务 g5-coverage-session-20260922-162523-266 保持运行，未重启或修改源码／候选。

核查与证据：8080 累计接口 status=live，runId 与真实后端一致；线任务 724/724、区域任务 1250/1250，点观察时间持续增长。独立 Edge/CDP 连接同一真实运行，关闭当前足迹层并定位任务 3002；24 次采样，后端时间 664439→677769 ms、点观察 190941→197341 ms，绿色累计区域在 live-03.png／live-23.png 中可见。原始证据在 out/runs/g5-coverage-live-diagnose-20260922-1637，浏览器退出 0，无页面异常，后端未受影响。初步脚本对 PowerShell 快照 tasks.Keys 返回 null，仅为 PSObject 非字典读取方式；随后以累计接口任务数组及页面真实显示核查，没有据此判为任务丢失。

发现与判断：当前已实现累计范围仅为任务定义内的统计单元，任务外相机扫过范围未累计。另发现前端 signature 包含点观察时长，变化会重建整个累计数据源；13 秒内首个实体实例序号 1→469，而线／区域覆盖集合未变化。反复重建是明确的实现现象，但本机本次截图没有复现绿色区域消失，不能将它直接登记为用户问题的根因。

待确认与下一步：已询问用户是“看不到已累计图层”“希望累计相机扫过全部范围”还是“闪烁／消失／不增长”，这会决定修复显示还是扩展累计语义；不能把任务外几何足迹直接冒充满足 GSD 的任务统计。保留当前实现及服务，不猜测更换统计口径。后续沿本条和现场证据继续，未登记已修复、未推进 T08／G6。只同步状态及本日志，本次处于问题待确认，不触发完成后的自动提交／推送。追加前 425922 字节、SHA256=fefd50031a1d1d072db5e3e4dd99d61106fb5a633cc5029b31f552a61326eea6，历史字节保持。


## WL-20260922-007｜说明当前场景的任务名称与显示类别

时间／时区：2026-09-22，Asia/Shanghai。关联 G5 说明，状态：名称与语义核查完成；累计覆盖反馈仍待明确现象。用户在排查期间要求解释场景中不同任务名称的差异；起点 eed20ec63a7bcdffe43f2b01f84b5ec0c24c7a57，工作区已有上一轮未提交的 docs/status.md 和工作日志诊断记录，全部保留，不将此问题认定为取消原反馈。

依据及结论：只读当前快照、scripts/g4_mixed/scene.py、scripts/g5_backend/prepare.py 与前端 MissionModel／MissionPanel。任务 3000 / G4_line_400 为实体 400 的水道线搜索（90 个地面折线顶点）；3001 / G4_point_500 为实体 500 的点搜索；3002 / G4_area_600 为实体 600 的 1000×500 米矩形区域搜索。G4 是沿用场景生成器的命名前缀，line／point／area 表示搜索类型，末尾编号在当前固定一机一任务场景对应实体，不是任务 ID。真实任务与分配摘录在本次诊断目录 task-explanation.json。

说明范围：区分搜索目标几何、完整规划、当前命令、实际执行目标／航段、允许／禁入区域；线／区域累计按已观察的任务单元，点按有效观察时间，任务完成事件与统计覆盖率分开。相机当前视场、实体实际轨迹和任务累计覆盖不能混用。未修改服务、场景、源码或统计口径，未重启或安装工具，不新增测试。

过程边界：首次快照输出包含完整 90 点折线而过长，改为只提取名称、类型、分配和几何尺寸；一次误查不存在的 scenarios.py，随后通过 rg --files 定位实际 scene.py。均为只读操作，无输入变更。原累计反馈仍按 WL-20260922-006 等待明确，未登记修复或结束；保留本次说明记录，待该问题处理后统一归档，不将分析说明当成功能修复。追加前 428187 字节、SHA256=b286f9054ee9016c0e2738aa6e962854c89545d25afed4949706cf927d6436f6，历史字节完整保留。


## WL-20260922-008????????????????

??????2026-09-22?Asia/Shanghai?????????????????????????????????????????????????? Markdown??? eed20ec63a7bcdffe43f2b01f84b5ec0c24c7a57?????? WL-20260922-006?007 ? docs/status.md ????????????????????????????????????????????????????

????? [?????????????](docs/project-overview-and-user-guide.md)??????????? AMASE ???UxAS ?????LMCP ???G4 ????? Cesium ??????????????????????????????????????????????????????????????????????????????????????????????????docs/status.md ? AGENTS.md ???????backlog ? T08 ????????????????

????????? README?? WaterwaySearch ????????????????????????????? AFSIM ??????????? TorchRL?BenchMARL ???????????? 8080 ????????? g5-coverage-session-20260922-162523-266/session-headless ???? 756229 ms?????????? 3000?3001?3002????? 400?500?600??? 90 ???? 1000?500 ????????????? 756169 ms?? 724/724??? 1250/1250???? 204271 ms?????????????????????????????? out/runs/project-guide-review-20260922-172502/live-scene.json ? live-coverage.json?

????????????? Python ?????UTF-8????????46 ?????????????????PowerShell 5.1 Parser ???????????????????????git diff --check ????????????? review.json???????????????????????????? BOM ?????? PowerShell ?????????????? UTF-8 ???????????? session.py???? rg ???? scripts/g5_state/runtime.py ?????????????????????????????????????????????????????

???????????????? WL-20260922-006 ????????????????????????????????????????????????????????????????????????????????????????????????? WL-006?007 ????????????????????????????????????????????????????????? archive.json???? 430251 ???SHA256=955995042ed40207afc1f041108b7d8b7122ea6870ab73ef3fb8f2635eb02981????????????


## WL-20260923-001｜说明原有规划能力与操作流程，补记上一条日志

时间／时区：2026-09-23，Asia/Shanghai。关联项目使用指南，状态：源码分析与文档补充完成。用户询问原项目是否具备规划能力、能力范围及操作流程；起点 c054c2bcce520499b647fdc5ae58e8d0de032990，工作区干净。只读取仓库资料并补充说明，不改动规划代码、场景、配置或服务，不启动仿真或进入后续实施卡。

结论与依据：复核原 WaterwaySearch README、cfg_WaterwaySearch.xml、AutomationRequest、原消息流程 TeX、路线与分支定界分配源码、CMASI 字段及当前初始化／混合任务编排。UxAS 原有任务执行选项、路线代价、多机任务分配与顺序、转场与搜索航点生成、完整计划组装、分段命令及执行中传感器指向能力。原例请求允许 400／500 竞争任务 1000，配置由 SendMessagesService 在 300 ms 发任务、5000 ms 发请求；当前移植按真实数据和 TaskInitialized 屏障触发，当前三实体场景固定一架一任务。公开分配代码的 MINMAX 比较累计任务与转场时间的最大值，不直接优化 AMASE 实飞覆盖率；平面区域路线能力不等于本轮三维地形避障或动态防碰撞资格。

交付：在 [整体使用指南](docs/project-overview-and-user-guide.md) 新增 4.3～4.6 节，给出能力输入输出、原水道选择示例、规划流程图、原 GUI／消息配置操作、Windows 联合入口触发方式及改任务／重规划的边界。注明 9 月 23 日仅补充规划说明，其余实现状态沿用 9 月 22 日核对结果。当前 Cesium 没有画任务、手动分配和在线重规划入口；G6 仿真控制不自动包含这些能力。累计覆盖反馈保持未关闭，T08 下一卡及现有资格不变，无需改写状态／任务卡。

补记 WL-20260922-008：本次读取发现该已提交条目的中文被替换为问号。已核实 PowerShell 5.1 的管道 OutputEncoding 为 us-ascii；上一轮中文 here-string 经管道传给 Python 时发生不可逆替换，Python 的 UTF-8 选项无法恢复已经丢失的字符。此前验证仅检查 UTF-8 可解码和字节哈希，未识别语义上的中文丢失。保留该条原始字节和已发布历史，本条补记：9 月 22 日已新增 572 行整体使用指南，同步 AGENTS／status 阅读入口；按上游说明、源码与当时真实三实体快照核查，46 个本地链接、两段 PowerShell 语法和 diff 检查通过，服务与场景未改。该次只读快照为 g5-coverage-session-20260922-162523-266/session-headless，暂停 756229 ms、三任务完成；累计线 724/724、区域 1250/1250、点观察 204271 ms，只是当时运行实例。四份文档已提交 c054c2b 并推送核对，累计覆盖问题没有被认定修复；审计保存在 out/runs/project-guide-review-20260922-172502。指南正文经 apply_patch 写入，中文内容正常，损坏限于上一条日志的写入内容。

验证与处理：本次中文先以 UTF-8 文件写入，再由只含 ASCII 的脚本读文件并以二进制追加，禁止经默认 ASCII 管道传递中文正文；核查新增段包含预期中文标题、无连续问号／替换字符，历史前缀保持。文档链接／锚点、Markdown 代码块、UTF-8 中文内容及 git diff --check 检查；不运行业务测试或将静态阅读登记为本轮规划实测。初次按猜测检索不存在的 g3_startup／runtime.py 等路径及 PowerShell 未展开的目录通配符，随后用 rg --files 和实际 g3_integration、g4_mixed/planning.py 定位复核；均只读。证据与归档回执保存在 out/runs/planning-overview-review-20260923-094925。

归档：本轮只提交整体使用指南与本条工作日志，按持续授权普通推送并核对远程，不改变候选或正式指针。追加前 431900 字节、SHA256=400ea21277d234e2c4653785e83d8455e58915094f00473b56b11e4297345c43；原日志前缀完整保留。既有累计反馈及未交付的在线任务编辑／重规划流程继续保持原状态。
